{
  "activityTrail": {
    "lineShape": {
      "example": "2026-08-16 15:28:55 · COMP/1 · the assistant was ready after 1.8s",
      "why": "Local time to the second, then the course and section when the line is about one, then a sentence in the words a teacher would use. Not a function name and not a level: the trail is read by a person deciding what went wrong, months later, and 'started preview.sh COMP 1' beats 'DEBUG runScript(preview.sh)'."
    },
    "mustRecord": [
      {
        "carries": "app version, build number, process id, bundle path",
        "event": "app opened",
        "why": "The bundle path and process id are not decoration. Two copies of the app can run at once, and when they do they take turns rewriting the same working folder; two process ids in one trail is the fastest way to see it. The bundle path answers 'is this build the one I think it is' without asking."
      },
      {
        "carries": "OS version, OS build, architecture, core count, memory",
        "event": "machine described",
        "why": "The assistant runs natively on one architecture and not the other, and the whole toolchain emulates on the wrong one. Memory decides which model tier a teacher gets. OS build number and kernel release isolate virtualization, Metal, and WinUI/WSL2 platform issues."
      },
      {
        "carries": "bundled assistant engine and toolchain helper software versions",
        "event": "helpers described",
        "why": "The assistant flags and chat template handling depend on the engine build. Static tool versions (Colima, Lima, Docker CLI, Buildx on macOS; WSL2 and container tools on Windows) isolate whether a machine is running on expected helper versions."
      },
      {
        "carries": "the folder path, redacted",
        "event": "working folder opened",
        "why": "Almost every question about a course begins with which folder it is in - and a folder outside the home directory bind-mounts as an EMPTY folder inside the container, which is a whole class of report explained by this line alone."
      },
      {
        "carries": "the course code",
        "event": "settings saved",
        "why": "'It broke after I changed something' is the commonest report there is, and this is the line that dates the change."
      },
      {
        "carries": "the course code and the reason",
        "event": "settings could not be saved",
        "why": "A save that failed leaves the teacher believing it succeeded. Recording only successes would hide exactly the case worth seeing."
      },
      {
        "carries": "the launcher and its arguments, redacted",
        "event": "task started",
        "why": "What was actually asked for, as against what the teacher believes they asked for."
      },
      {
        "carries": "the launcher, the outcome, and how long it took",
        "event": "task finished",
        "why": "The outcome distinguishes a failure from a task stopped on purpose and from one backed out of at a question - all three exit non-zero, and only one is a fault."
      },
      {
        "carries": "which credential was asked for, never the answer",
        "event": "asked for a publishing credential",
        "why": "The first publish of a section stops behind a dialog asking for a Netlify or Cloudflare token, and a teacher who does not notice it reports a publish that 'never finished'. This line is the difference between reading that as a hang and reading it as a question waiting. The answer is a live credential and is never written down - only the name of the thing asked for."
      },
      {
        "carries": "course, section, and which assistant",
        "event": "assistant opened",
        "why": "Which tier is running decides what routing accuracy to expect, and it is the first thing to establish about any report of the assistant misunderstanding something."
      },
      {
        "carries": "course, section, and how long the engine took",
        "event": "assistant ready",
        "why": "A slow start and a failed start read identically to a teacher ('it just sat there'). The seconds tell them apart."
      },
      {
        "carries": "course, section, and the reason",
        "event": "assistant would not start",
        "why": "The one assistant failure that leaves nothing else behind - no turn, no tool, no output."
      },
      {
        "carries": "course, section, and one line the engine itself printed - shortened, and redacted like every other line on the trail",
        "event": "assistant engine said",
        "why": "Until this existed a report from a teacher whose assistant was misbehaving could carry nothing the engine had said, because both its streams went to the null device. That was not laziness: a redirected pipe nobody drains fills up and blocks the engine mid-request, which is exactly what wedged the Windows server, so the output must never be read on the engine's timetable. It goes to a FILE, and a bounded tail of it is sampled onto the trail - never the firehose. What is sampled is deliberately narrow, and the narrowness was measured against a real engine: a healthy start prints six warnings that are pure noise here (a CORS block that cannot matter on a loopback-only server, and a token-type quirk in the weights), so warnings are NOT recorded. Errors are, along with any line naming an error or an exception - which is the shape of the two lines worth having, 'request (20030 tokens) exceeds the available context size' and 'got exception'. When the engine never becomes ready at all, the tail is taken whatever it says, because then every line is the diagnosis."
      },
      {
        "carries": "course, section, and the sentence the teacher typed, on its own marked line",
        "event": "assistant asked",
        "why": "Recorded the moment the teacher's words are ACCEPTED, above every branch below it - not when the message goes to the model, and not when an answer comes back. Two different bugs came from placing it lower: a sentence matching a fixed shape returns early and never reaches the model, so a whole class of input left no trace at all; and an engine that failed, or one still thinking when the teacher gave up, lost the sentence that caused the trouble. It is also what the report's checkbox keys off, so recording it late made the checkbox vanish for somebody who had plainly just used the assistant. The general rule: record what the teacher did where they did it, above every branch, never at a later point that can be skipped."
      },
      {
        "carries": "course, section, and the tool it ran",
        "event": "assistant matched a fixed phrase",
        "why": "Some phrasings are matched in code and never reach the model at all. Without this line, 'why did it not think about what I said?' is unanswerable - the trail would show a tool running with nothing between it and the teacher's sentence."
      },
      {
        "carries": "course, section, the tool name, the ARGUMENT NAMES, the seconds, the completion-token count, and whether it waited for the approval button",
        "event": "assistant chose a tool",
        "why": "Routing has no automated gate anywhere in this product, so the field is the only place a routing regression shows. Argument NAMES rather than values: which tool with which arguments filled in answers the routing question completely, while the values are the teacher's page titles. The token count is the honest check on thinking having been switched back on - the engine parses the thinking OUT of the content, so a bad reply looks clean and is merely slow."
      },
      {
        "carries": "course, section, and the reason",
        "event": "assistant could not answer",
        "why": "The failure that leaves nothing else behind: no tool, no turn, no output. Without this line 'I asked it something and nothing happened' is unfalsifiable."
      },
      {
        "carries": "which assistant the teacher is set to use",
        "event": "app settings opened",
        "why": "The settings panel is where a teacher changes how much of their Mac the assistant may take, and 'it got slow last week' is a report about a change nobody else can date. Recording the state on the way IN also means a trail shows what the setting was even when the teacher looked and changed nothing."
      },
      {
        "carries": "which assistant was chosen, and which one that lands on for this Mac",
        "event": "assistant model chosen",
        "why": "The choice is the single biggest lever a teacher has over the assistant's accuracy AND over the machine's memory, so almost every later report about either is explained by this line. Both halves are recorded because 'chose automatic' does not answer the question anybody is asking months later - the resolved answer depends on the Mac and on a rule that can change."
      },
      {
        "carries": "which assistant, and how big the download is",
        "event": "assistant model download started",
        "why": "Gigabytes over a school connection, and the commonest failure is that it never finished. A start with no matching finish is the whole diagnosis, and it cannot be seen without recording the start."
      },
      {
        "carries": "which assistant, and how big the download was",
        "event": "assistant model downloaded",
        "why": "The other half of the pair above. It also dates when a Mac gained several gigabytes, which is the answer to a disk-space question that otherwise looks like it has nothing to do with Plantoir."
      },
      {
        "carries": "which assistant, and the reason it failed",
        "event": "assistant model download failed",
        "why": "Same reasoning as 'settings could not be saved': recording only the successes hides exactly the case worth seeing. A captive portal answers 200 with something that is not a model, so the failure here is a wrong-size file rather than a network error, and that sentence is worth having verbatim."
      },
      {
        "carries": "which assistant, and how much space it freed",
        "event": "assistant model removed",
        "why": "Removing one is allowed even when it is the one in use - the next time the assistant is opened it offers to download it again. Without this line that re-download looks like the app losing a file on its own, which is a report about a bug that is not there."
      },
      {
        "carries": "which assistant",
        "event": "assistant model download stopped",
        "why": "A start with no ending line reads as a stall, and a stall is what somebody would go looking for. Recording the stop is what makes the absence of a stop mean something - it turns 'started, nothing after' from ambiguous into a genuine finding."
      },
      {
        "carries": "whether the assistant now asks before changing anything",
        "event": "assistant confirmation changed",
        "why": "Turning off the confirmation step changes what every later request DOES - it goes straight through instead of stopping for a button. A report that says 'it just did it without asking' is explained entirely by this line, and without it the behaviour looks like a bug in the gate rather than a setting somebody chose."
      },
      {
        "carries": "which course, the section when the repair is about one, and what was put back — \"put the front page back\"",
        "event": "folder problem repaired",
        "why": "A different event from finding the problem, deliberately: one records that something is wrong, the other that somebody acted on it, and a trail that could not tell them apart leaves \"did they ever fix it?\" unanswerable. Only the two genuinely repairable cases can produce it — a missing Media folder and a missing section front page."
      },
      {
        "carries": "which course and section, and a sentence naming what happened with the stable check NAME in brackets — 'found a problem with this course’s folders (curriculumCoverageFoundNothing)'. Never the product wording, which changes, and never anything written on a page",
        "event": "folder problem found",
        "why": "A teacher who reports 'it stopped showing my curriculum map last month' leaves nothing to look at otherwise: the finding is printed into a build console that is gone by then, and the condition itself is invisible on disk — a renamed folder looks exactly like a folder that was always called that. Recording the check NAME rather than the sentence is deliberate: the sentence is product wording and will be reworded, while the name is what a person searching the trail six months later can match against the contract."
      },
      {
        "carries": "which section, and which destinations it was published to",
        "event": "section content marked published",
        "why": "Why a section stopped saying \" — Edited\". The marker is DERIVED - its presence and its absence look identical on disk - so a teacher reporting 'it still says Edited after I published' leaves nothing to look at without this line. It is also the only record that a publish succeeded at EVERY destination rather than merely at one."
      },
      {
        "carries": "the course code, the scope ('shared' or 'per-section'), and the item name",
        "event": "item excluded",
        "why": "A teacher removed a folder or file in Course Settings, excluding it from previews and deploys. Without this line, 'why is my folder not showing on the site' cannot be answered from the trail alone."
      },
      {
        "carries": "the course code, the scope ('shared' or 'per-section'), and the item name",
        "event": "item re-included",
        "why": "A teacher added back a previously excluded folder or file in Course Settings, returning it to previews and deploys."
      },
      {
        "carries": "the item name, which list it is in, and the reason sentence the teacher was shown (a specialNames sentence, never page content)",
        "event": "removal blocked",
        "why": "A teacher tried to remove or untick a folder or file a feature depends on (Course Settings or the New Course Wizard) and was shown why it cannot go. 'I could not remove the folder' is a report support will receive; the line says which rule refused."
      },
      {
        "carries": "the course code, the old folder name, the new one, which list it was in, and how many folders on disk were moved",
        "event": "folder renamed",
        "why": "A rename leaves nothing behind to look at: a folder called 'Class Pages' is indistinguishable from one that was always called that, and every key that named the old one has been rewritten. Without this line, 'when did this course stop having a Tasks folder?' — the first question asked when a curriculum map goes wrong — has no answer at all. The folder NAME is recorded; nothing from inside the folder ever is."
      },
      {
        "carries": "the course code, the folder name, and which list it was added to",
        "event": "folder created",
        "why": "Adding a name in Course Settings now creates the folder on disk, so a folder appears in the teacher's vault that they did not make in Obsidian. Separate from the rename because it answers a different question, and unexplained new folders are exactly what a teacher reports as 'Plantoir did something to my notes'."
      },
      {
        "carries": "the service's name and the folder path, redacted — recorded by a WINDOW only, never by the assistant's or the MCP server's own reading of a folder, since those show nothing",
        "event": "synced folder noticed",
        "why": "The working folder just opened is kept in sync by a cloud service and the teacher had not yet been told about THIS folder. The effects of a synced folder — a slow build, a rename that takes minutes, a move that failed once — arrive weeks later as unrelated reports, and this one line is what connects them. See cloudSyncedFolders."
      },
      {
        "carries": "whether it was the picker's choice or the window's notice, and the service's name",
        "event": "synced folder accepted",
        "why": "The teacher read the note and went ahead. Separate from 'synced folder noticed' because it is a different fact: one says Plantoir saw it, the other says the teacher did — and 'nobody warned me' is answered by the second, not the first."
      },
      {
        "carries": "the course code",
        "event": "built site moved out of the working folder",
        "line": "moved {course}'s built website out of the working folder, so it is no longer copied, synced or backed up with the course",
        "lineWhy": "The one line in this list whose WORDS are pinned, because it has two authors: the app writes it when a folder is opened, and the launchers write it when the move is done by a build at the command line or by a publish launchd ran overnight. A sentence typed in two places is a sentence that stops matching, and a trail whose lines differ by who wrote them is one nobody can search.",
        "why": "A one-off change a teacher can SEE. Their working folder suddenly weighs much less, a folder they may have looked at in Finder is a shortcut afterwards, and a folder they sync stops uploading builds. Each of those arrives separately — 'where has my website gone?', 'did something delete my files?', 'why did my folder shrink?' — and this line, dated and per course, answers all three. Recorded ONCE per course, at the moment the move happens, and never again: a line on every launch would say nothing and bury the one that does.",
        "appliesOn": [
          "mac"
        ],
        "appliesOnWhy": "Windows has always built into %LOCALAPPDATA%\\Plantoir\\builds\\<id> (PLANTOIR_BUILD_ROOT, row 290), so there is nothing to move there and no moment to record. If Windows ever migrates a folder that predates that, the event is here to use."
      },
      {
        "carries": "which course and section, and HOW MANY website-builder processes were ended",
        "event": "section processes reclaimed",
        "why": "The count is the point. Stopping a preview, closing a window or cancelling a publish asks the launcher to end whatever that section still has running, and nothing else on the trail can tell “there was nothing left to stop” from “a build was still going and was ended” — which are the two competing explanations when a teacher reports that their publish stopped halfway. Recorded from 2026-09-05, when the sweep began ending a mid-flight BUILD and its driver rather than only a preview server: that is a change in what a teacher can lose, and it had no line describing it. The launcher prints the number and the app used to discard it."
      }
    ],
    "note": "The breadcrumb trail: what the teacher was doing, in the order they did it, in ONE file. Task records answer 'what did that publish print'; they do not answer the question support opens with, which is 'what were you doing when it went wrong'. Two files each holding half a story force whoever reads them to interleave by timestamp in their head, and they will get it wrong - so the assistant's turns go in here too. The list below is the MINIMUM: an app may record more, but a missing event is a gap in both apps at once. Deliberately coarse - every line is something a teacher would recognise as an action they took, never a view redraw or a state change, because the failure mode of logging everything is that the one line that mattered is on page forty.",
    "promptMarker": {
      "prefix": "  asked: ",
      "why": "The teacher's own words go on their own line behind a fixed prefix, so leaving them out of a report is dropping lines by prefix rather than parsing anything. They are kept locally because a routing problem cannot be looked into without the sentence that caused it, and withheld from the report unless the teacher ticks the box, because their words are the one thing here unmistakably theirs to hand over."
    },
    "rule": "EVERY new feature, and every CHANGED behaviour, that a teacher can see leaves a line on this trail. Adding one means naming the event here and in both apps — not writing free text at a call site, and not deciding it is too small to bother with. The failure this prevents is the one already met in the field: a teacher builds a preview, opens the assistant, asks for a report, and is told there is nothing to report, because the two things they just did were not among the things anybody had thought to record. A feature that leaves no trace is a feature that can only be diagnosed by asking the teacher to do it again, which is the thing they came to you because they could not do."
  },
  "assistantConfirmation": {
    "note": "Whether the assistant describes what it is about to do and waits for a button, before anything that changes the built website.",
    "defaultsToOn": true,
    "why": "The local model is a router and routers are wrong sometimes. Showing what it understood before it acts turns a misroute from a thing that HAPPENS into a thing a teacher declines.",
    "sameOnBothAssistants": {
      "value": true,
      "why": "The smaller assistant used to refuse to be turned off and was never told the setting existed, on the measured reasoning that one request in five going wrong is not a rate at which anybody should stop reading. That withheld a setting from exactly the machine where knowing about it matters most. The measured number is put in front of the teacher as a caution instead, which respects the measurement AND the person."
    },
    "deployingAlwaysAsks": {
      "value": true,
      "why": "Deploying puts work in front of students and cannot be taken back by us, so it is gated separately and the setting does not reach it."
    },
    "mentionedAfter": {
      "plansAccepted": 15,
      "appWide": true,
      "onlyOnce": true,
      "why": "For DISCOVERABILITY, not to ask for trust: a switch nobody knows about might as well not exist. App-wide because the count used to reset with every conversation, so a teacher working in short bursts could accept a hundred plans and never be told. Once ever, because a suggestion declined is an answer and asking again is how a helpful mention becomes nagging."
    },
    "aCancelDoesNotResetTheCount": {
      "value": true,
      "why": "The number measures how much of the assistant's work this teacher has READ, and a cancel is evidence of reading rather than evidence against it."
    },
    "oneStoredAnswer": {
      "value": true,
      "why": "The switch in Settings and the gate in the conversation are the same value, read fresh each turn - the two windows are open at the same time, and a teacher who turns it off expects the next thing they ask to go straight through."
    }
  },
  "assistantModelChoice": {
    "note": "Which assistant runs on a teacher's machine, and what they are allowed to do about it. Shared because these are rules with inputs and outputs, not pictures: the panel that holds them looks nothing alike on the two platforms and must BEHAVE identically. What is deliberately not here is the layout, the keyboard shortcut, and where the settings window lives - see WINDOWS-HANDOFF.md, 'Letting a teacher choose which assistant runs'.",
    "defaultChoice": "automatic",
    "choices": [
      {
        "key": "automatic",
        "label": "Choose for me",
        "namesATier": false
      },
      {
        "key": "smaller",
        "label": "The smaller assistant",
        "namesATier": true
      },
      {
        "key": "larger",
        "label": "The larger assistant",
        "namesATier": true
      }
    ],
    "automaticResolvesAtPointOfUse": {
      "value": true,
      "why": "The automatic choice is stored as an INTENT and resolved every time it is used, never resolved once and written into preferences. Storing the answer would mean a later change to the tier ladder reached every machine EXCEPT the ones whose teacher had opened Settings - so the people who engaged with the setting would get the stalest behaviour."
    },
    "comfortFraction": {
      "denominator": 3,
      "why": "A model is comfortable on a machine when its resident size is at most a third of physical memory. Not a new number: it is the line the automatic ladder has always been held to, which is why the automatic choice can never produce a caution and a test asserts that."
    },
    "caution": {
      "appearsWhen": "a hand-picked choice is not comfortable on this machine",
      "neverAppearsFor": "automatic",
      "mustName": [
        "this machine's memory",
        "the chosen assistant's memory while working"
      ],
      "blocksTheChoice": false,
      "why": "A caution rather than a disabled row. A teacher who has just quit everything else knows something the operating system does not, and greying the option out with the reason beside it gives them no way to act on what they know. What they are owed is both numbers in one sentence, so the choice is informed rather than prevented."
    },
    "removal": {
      "refusedWhileAnyAssistantWindowIsOpen": true,
      "messageNamesTheSectionToClose": true,
      "theCurrentlyChosenOneMayBeRemoved": true,
      "why": "The refusal is about ANY open assistant, not about whether that window happens to use that rung: working out which file is genuinely mapped means tracking state neither app keeps, and getting it wrong deletes weights out from under a running engine. Removing the CHOSEN one is allowed on purpose - it is not a broken state, it is the state every machine is in before its first download, and refusing would trap a teacher who wants their disk space back behind a choice they did not want to change."
    },
    "guidance": {
      "mustNameDownloadSize": true,
      "mustNameMemoryWhileWorking": true,
      "why": "Two different decisions, and neither is guessable from the other: disk is what runs out on a small laptop, memory is what makes the machine feel slow while a class is being prepared and never appears as a number anywhere. On the mac the larger download is 2.2x the smaller but 2.9x the memory, because most of the difference is the conversation being held rather than the file being read. Derive both from the tier table; typing them into the interface guarantees they drift the first time a quant changes."
    },
    "namesNoModel": {
      "value": true,
      "jargon": [
        "qwen",
        "llama",
        "gguf",
        "instruct",
        "1.5b",
        "4b",
        "7b",
        "3b",
        "quant",
        "token",
        "context window",
        "parameter",
        "docker",
        "container",
        "toolchain",
        "gpu",
        "metal",
        "inference"
      ],
      "why": "Rule one, applied where it is hardest: the settings panel is the one screen genuinely ABOUT the model, so a name leaks in most naturally here. Sweep every label, detail, caution and summary the panel can produce against this list rather than spot-checking."
    }
  },
  "curriculumRules": {
    "expectationWording": {
      "cases": [
        {
          "body": "Describe how data is stored. ^b21",
          "expect": "Describe how data is stored.",
          "why": "The anchor is machinery and is cut."
        },
        {
          "body": "Describe how data is stored.",
          "expect": "Describe how data is stored."
        },
        {
          "body": "Compare two designs ^ and contrast them",
          "expect": "Compare two designs ^ and contrast them",
          "why": "Only a BARE word can be an anchor. Anything with a space after it is the teacher's own prose, and cutting there would lose wording."
        }
      ],
      "note": "What an expectation page SAYS: the body after the frontmatter, without the `^anchor` Obsidian puts on the end of a block."
    },
    "isCurriculumPage": {
      "cases": [
        {
          "expect": true,
          "path": "Curriculum/B2.1.md"
        },
        {
          "expect": true,
          "path": "Ontario Curriculum/B2.1.md",
          "why": "The normal case outside the example content — folders are rarely called plain 'Curriculum'."
        },
        {
          "expect": true,
          "path": "College Board Curriculum/Unit 3/A1.2.md"
        },
        {
          "expect": true,
          "path": "curriculum/B2.1.md",
          "why": "Case-insensitive."
        },
        {
          "expect": false,
          "path": "All Classes/Curriculum notes.md",
          "why": "The FILE name is ignored — a lesson that mentions the word is not curriculum material."
        },
        {
          "expect": false,
          "path": "All Classes/Unit 1, Day 1.md"
        }
      ],
      "note": "Matches `build_site.py`'s own rule exactly, and that is the point — the Python decides what ships, so an app that disagreed would report coverage for pages the site does not treat as curriculum. Any FOLDER segment containing 'curriculum', case-insensitive; the FILE name is ignored."
    },
    "isExpectationCode": {
      "cases": [
        {
          "code": "A1.1",
          "expect": true
        },
        {
          "code": "B2.11",
          "expect": true
        },
        {
          "code": "b2.3",
          "expect": true,
          "why": "Either case."
        },
        {
          "code": "D1.1",
          "expect": true,
          "why": "BC Applied Design competency code."
        },
        {
          "code": "K2.3",
          "expect": true,
          "why": "BC Content knowledge standard code."
        },
        {
          "code": "B2",
          "expect": false,
          "why": "A strand heading, not an expectation."
        },
        {
          "code": "Overall Expectations",
          "expect": false
        },
        {
          "code": "B2.1 Introduction",
          "expect": false,
          "why": "Whole-name match: anything after the code makes it not a code."
        },
        {
          "code": "12.3",
          "expect": false,
          "why": "Must start with a letter."
        }
      ],
      "note": "A leaf expectation is coded like A1.1 or B2.3: one letter, digits, a dot, digits — the WHOLE name, not a prefix. Index and strand-heading pages sit in the same folder and must not be counted."
    },
    "note": "What counts as a curriculum expectation. Written after checking the code rather than from memory: an expectation is not a code found in a page's TEXT, it is a PAGE named by a code inside a curriculum folder. A first draft of this contract asserted text-scanning and was wrong before it was ever run."
  },
  "followingLinks": {
    "note": "What publishing and unpublishing do to the pages a named page LINKS TO. The two verbs are not mirror images and each rule is written once. Shared because both apps sweep the same folders and a difference here silently changes what students can see.",
    "publishing": {
      "takesLinkedPages": true,
      "transitive": true,
      "disclosedInThePlan": true,
      "why": "Publishing a page whose links lead somewhere students cannot see is the one thing publishing must never do, so it always takes what it links to, and what those link to in turn. The plan names every page that will become visible, so a teacher agreeing to publish one class can see the four pages going with it."
    },
    "unpublishing": {
      "takesALinkedPageOnlyWhenNothingElseNeedsIt": true,
      "aReferrerCountsOnlyWhenVisible": true,
      "why": "'X still links to it' is a reason to keep a page published only when students can SEE X. Counting hidden referrers left pages visible and reachable from nothing, held up by drafts nobody had published - kept alive by a page that is not there. Safe in one direction because of the other: publishing is transitive, so a page taken down here comes back the moment anything visible needs it again."
    },
    "neverTakenDownByFollowingLinks": [
      {
        "kind": "a page this section's Key Links points at, and the Key Links page itself",
        "why": "A section cannot do without them; they are the way in to everything else."
      },
      {
        "kind": "a folder's landing page - index.md - which includes All Classes and every sidebar folder",
        "why": "It is the way IN to a folder rather than a page anything links to, so following links must never take it down."
      },
      {
        "kind": "a curriculum page: any page with a folder segment containing 'curriculum'",
        "why": "build_site.py's own rule, so a course whose folder is called 'Ontario Curriculum' is covered exactly as a plain one is. In a real course this is every expectation page, the folder's own index, and the page explaining them."
      }
    ],
    "theOrderIsLoadBearing": {
      "value": true,
      "why": "The exclusions are checked BEFORE the referrer test, so narrowing what counts as a referrer cannot reach them. Narrowing a sweep rule makes every exclusion above it matter more than it did the day before, which is why the exclusions are the thing to re-test after any change to what gets swept."
    }
  },
  "note": "Eight rule sets that both apps need and neither platform owns. The MECHANISMS behind several of them differ completely - launchd against Task Scheduler, AppKit against WinUI, ~/Library/Logs against %LOCALAPPDATA% - which is exactly why the rules are written here instead: what must be refused, matched, stripped, or taken out of a problem report does not change with the machinery that does it.",
  "pageNaming": {
    "note": "What a page is CALLED when the assistant talks about it, which is not what its file is called. Shared because it is a rule with inputs and outputs and because both apps read the same folders: a teacher told that 'index' still links to a page has been told nothing, since every folder in a course has an index.md. Reported from a real course (ADA1O Section 1).",
    "theRule": [
      "the frontmatter title, unless it is literally 'index'",
      "for a file called index.md, the name of its FOLDER",
      "otherwise the file name without .md"
    ],
    "why": "Copied from Quartz's own fileTrie.ts displayName rather than invented, so what the assistant says and what the site's sidebar shows cannot drift: `const nonIndexTitle = data?.title === 'index' ? undefined : data?.title; return displayNameOverride ?? nonIndexTitle ?? fileSegmentHint ?? slugSegment`. If Quartz's rule changes, follow it.",
    "neverShown": "index",
    "identityIsSeparateFromLabel": {
      "value": true,
      "why": "A page's FILE NAME stays the identity, because wikilinks resolve by file name and every lookup in the page graph goes through it. Only the label changes. The trap is what happens in between: a referrer remembered by NAME and looked back up resolves 'index' to whichever folder's landing page came first in the walk, which was invisible while the answer printed as 'index' and becomes a confidently WRONG folder name the moment it does not. Carry the page, not its name."
    },
    "cases": [
      {
        "file": "Portfolios/index.md",
        "frontmatterTitle": "Portfolios",
        "shown": "Portfolios"
      },
      {
        "file": "Warm-Ups/index.md",
        "frontmatterTitle": null,
        "shown": "Warm-Ups"
      },
      {
        "file": "Style/index.md",
        "frontmatterTitle": "index",
        "shown": "Style"
      },
      {
        "file": "Portfolios/Journal Checklist.md",
        "frontmatterTitle": "Journal Checklist",
        "shown": "Journal Checklist"
      },
      {
        "file": "Concepts/Stage Directions.md",
        "frontmatterTitle": null,
        "shown": "Stage Directions"
      }
    ]
  },
  "gradedFolders": {
    "note": "Which folders hold work that COUNTS FOR MARKS. An expectation is \"assessed\" \u2014 the ring on a cell in the Curriculum Coverage map, and Ontario's requirement that every overall expectation be evaluated at least once \u2014 when a page addressing it lives in one of these. It used to be hardcoded: any folder whose name CONTAINED \"task\". A teacher who called theirs \"Tests\", or renamed \"Tasks\", silently lost every assessed mark on the map with nothing said.",
    "absentIsNotEmpty": {
      "rule": "An ABSENT `graded_folders` key means the teacher has never been asked, and the HISTORICAL rule applies: any folder segment containing \"task\", case-insensitive. An EMPTY list means they were asked and cleared it, which is a real answer and is honoured.",
      "why": "This is the whole migration, and conflating the two breaks courses both ways. The exact-name rule is NARROWER than the substring one \u2014 `support/skeletons` ships a family whose folder is \"Thinking Tasks\", which the old rule counted and a pool of [\"Tasks\"] does not \u2014 so seeding every existing course with [\"Tasks\"] would have quietly taken the assessed marks off that course's map. Nothing is written back from a build either: the apps serialise course_config.json wholesale from their own in-memory copy, so a key a build wrote is dropped the next time a teacher saves anything in Settings."
    },
    "matching": {
      "rule": "A FOLDER segment \u2014 never the file name \u2014 equals a pooled name, case-insensitively, at ANY depth.",
      "why": "At any depth so a teacher who files by unit inside `Tasks` does not lose credit. Never the file name, because a page is not assessed work on account of what it is called."
    },
    "cases": [
      { "name": "not configured: the historical substring rule", "configured": false, "graded": [], "path": "Tasks/Culminating.md", "expect": true },
      { "name": "not configured: a folder that merely contains the word", "configured": false, "graded": [], "path": "Thinking Tasks/Problem 3.md", "expect": true,
        "why": "Ships in a skeleton family. The old rule counted it, so an existing course must keep counting it." },
      { "name": "not configured: nothing task-ish", "configured": false, "graded": [], "path": "Concepts/Loops.md", "expect": false },
      { "name": "configured: an exact pooled name", "configured": true, "graded": ["Tasks", "Tests"], "path": "Tests/Unit 1 Quiz.md", "expect": true },
      { "name": "configured: nested inside a pooled folder", "configured": true, "graded": ["Tasks"], "path": "Tasks/Unit 1/Quiz.md", "expect": true },
      { "name": "configured: a near miss is NOT a member", "configured": true, "graded": ["Tasks"], "path": "Thinking Tasks/Problem 3.md", "expect": false,
        "why": "Once a teacher has chosen the list, it means what it says. This is the case that makes seeding an existing course with [\"Tasks\"] unsafe." },
      { "name": "configured: the teacher removed Tasks", "configured": true, "graded": ["Tests"], "path": "Tasks/Old Assignment.md", "expect": false,
        "why": "Removing a folder from the pool is the point of the feature." },
      { "name": "configured: cleared deliberately", "configured": true, "graded": [], "path": "Tasks/Culminating.md", "expect": false },
      { "name": "the file name never counts", "configured": true, "graded": ["Tasks"], "path": "Concepts/Tasks.md", "expect": false },
      { "name": "an explicit null is a CLEARED list, not an unset one", "configured": true, "graded": [], "path": "Tasks/Culminating.md", "expect": false,
        "why": "`{\"graded_folders\": null}` reads as configured-and-empty on both platforms rather than as absent: the key is PRESENT, and the absent case is reserved for a course that has never been asked. Pinned because the two implementations agreed by accident and nothing said which answer was intended." }
    ]
  },
  "problemReportDialog": {
    "askAboutPromptsWhen": [
      {
        "expect": "ask",
        "trailHasPromptLines": true,
        "why": "There is something to answer the question about, so the teacher gets to decide whether their own sentences travel."
      },
      {
        "expect": "do not ask",
        "trailHasPromptLines": false,
        "why": "A teacher who has never TYPED to the local AI assistant should not be shown a question implying they might have - the box governs their sentences, and with none recorded, ticking it would change nothing. An unanswerable choice reads as the app knowing something about them they do not recognise. This is why 'assistant asked' is recorded the moment a message is SENT: tie it to the reply instead and the box vanishes for somebody who plainly did use the assistant, which is exactly how this was first reported."
      }
    ],
    "includePromptsLabel": "Include what I typed to the local AI assistant",
    "labelWhy": "The LOCAL AI assistant, said in full. Plantoir will grow ways to connect a teacher's own account with a hosted assistant, and from that day an unqualified 'the assistant' is a question about the wrong thing - answered wrongly, about where their words have been. Naming it now costs three characters and saves a rewording that would have to reach both apps at once.",
    "note": "The dialog that asks a teacher what to send. The rule worth sharing is not the layout - that is per-platform - but WHEN the question about the local AI assistant appears and what the report's own note then says. The condition is the presence of PROMPT lines on the trail, not of assistant events: opening the assistant and closing it again leaves turns but nothing the teacher typed, and the box controls only the typing.",
    "promptStates": [
      {
        "noteSays": "nothing about the local AI assistant at all",
        "state": "none",
        "why": "Never used it, so neither an IN line nor a NOT IN line. Promising that something was left out invites the teacher to wonder what else the app thinks they did."
      },
      {
        "noteSays": "listed under NOT IN THIS REPORT",
        "state": "excluded",
        "why": "They used it and left the box unticked, which is the default. Saying so is the point: it is the promise they can check by opening the file."
      },
      {
        "noteSays": "listed under IN THIS REPORT, saying they asked for it",
        "state": "included",
        "why": "They ticked the box. The note records that it was their choice, so nobody reading it later assumes the app took the words on its own."
      }
    ],
    "reportName": {
      "example": "Plantoir problem report 2026-08-16 at 15.50.22.zip",
      "format": "<name> yyyy-MM-dd 'at' HH.mm.ss",
      "why": "Timed to the SECOND, not just dated. Somebody chasing a problem sends two or three reports in one afternoon - before a change and after it - and a date alone leaves a pile of files that must be opened to be told apart, or that silently overwrite one another. Dots rather than colons in the time because a colon is not legal in a file name on either platform. The folder INSIDE the archive carries the SAME stamp, or two reports unzipped side by side both land in a folder of the same name and collide on the machine of whoever is trying to help."
    },
    "supportEmail": "support@plantoir.app",
    "supportEmailWhy": "Taken from here rather than from a screenshot of the mac dialog, and shown as something a teacher can CLICK rather than retype. One constant feeds the dialog and the note inside the report, so the two cannot drift."
  },
  "problemReportRedaction": {
    "cases": [
      {
        "expect": "Working folder /Users/person/Documents/Teaching/ICS3U",
        "input": "Working folder /Users/russellgordon/Documents/Teaching/ICS3U",
        "why": "The account name is the single most common personal detail in this output, it appears in almost every line that names a path, and it is very often the teacher's real name. The REST of the path survives, because where a course folder sits is half of most diagnoses."
      },
      {
        "expect": "Reading C:\\Users\\person\\Documents\\Teaching",
        "input": "Reading C:\\Users\\Russell\\Documents\\Teaching",
        "why": "The same rule in the Windows shape. BOTH apps carry BOTH forms, deliberately: a report then reads identically wherever it was made, and neither side is left with a rule the other side's tests cannot reach."
      },
      {
        "expect": "Logged in as [removed: email address]",
        "input": "Logged in as teacher@school.board.on.ca",
        "why": "A deploy tool announces the account it is signed in as. A board email address names the teacher and often their school."
      },
      {
        "expect": "CLOUDFLARE_API_TOKEN=[removed: token]",
        "input": "CLOUDFLARE_API_TOKEN=abc123XYZ_super-secret-value",
        "why": "The LABEL is kept and only the value goes. A report that says a token was present and empty is far easier to read than one with a hole in it, and the label is what tells you which credential the run was using."
      },
      {
        "expect": "Paste Cloudflare token: [removed: token]",
        "input": "Paste Cloudflare token: qQb7Z_kL8mN3pR5tV9wX1yA2cD4eF6gH8jK0lM2n",
        "why": "Both token prompts use a silent read, so a pasted token does not echo into the output today. This rule is what stands behind that: the day a prompt is written without the silent flag, or a tool prints the token back in an error, the report is still safe."
      },
      {
        "expect": "authorization: Bearer [removed: token]",
        "input": "authorization: Bearer eyJhbGciOiJIUzI1NiJ9.abc.def",
        "why": "A dumped HTTP header. The word Bearer survives so the line still reads as an authorization header."
      },
      {
        "expect": "using [removed: token]",
        "input": "using nfp_9aBcDeFgHiJkLmNoPqRsTuVwXyZ012345",
        "why": "Netlify personal access tokens carry a fixed prefix, so this one is recognisable with no label beside it at all."
      },
      {
        "expect": "deploy.sh ICS3U 1 --target cloudflare --account [removed: account id]",
        "input": "deploy.sh ICS3U 1 --target cloudflare --account 9b2c4d6e8f0a1b3c5d7e9f0a1b2c3d4e",
        "why": "An account identifier is not a credential, but it names the teacher's Cloudflare account and nothing can be diagnosed with it. The flag is kept so the report still shows that an account WAS passed, which is a real failure mode of its own."
      },
      {
        "expect": "Paste Cloudflare Account ID: [removed: account id]",
        "input": "Paste Cloudflare Account ID: 9b2c4d6e8f0a1b3c5d7e9f0a1b2c3d4e",
        "why": "This prompt is NOT silent, so whatever the teacher types echoes into the output. It is the one place an identifier reaches the transcript by being typed."
      },
      {
        "expect": "token [removed: token] rejected",
        "input": "token qQb7Z_kL8mN3pR5tV9wX1yA2cD4eF6gH8jK0lM2n rejected",
        "why": "A run of exactly 40 token characters is the shape of a Cloudflare API token, and it goes wherever it appears, with or without a label. Exactly 40 - neither 39 nor 41 - so ordinary long words and identifiers are left alone."
      },
      {
        "expect": "commit da39a3ee5e6b4b0d3255bfef95601890afd80709 built the image",
        "input": "commit da39a3ee5e6b4b0d3255bfef95601890afd80709 built the image",
        "why": "THE ONE EXCEPTION, and it is worth having. A 40-character run that is entirely lowercase hexadecimal is a git commit or a SHA-1 digest, not a token, and those are exactly what a report about a build is read for. A real token being all-hex by chance is about a 1-in-10^24 event, so nothing is given up for it."
      },
      {
        "expect": "image teaching-quartz:src-8f3a1c9d container teaching-quartz-4d5e6f70",
        "input": "image teaching-quartz:src-8f3a1c9d container teaching-quartz-4d5e6f70",
        "why": "KEPT, and this is the field the report exists to carry: the image tag says which recipe the folder actually built from, and the container name says which working folder it belongs to. Redacting these would leave a report that cannot answer the first question anybody asks of it."
      },
      {
        "expect": "uploaded 12 of 48 files to https://ics3u-1.pages.dev",
        "input": "uploaded 12 of 48 files to https://ics3u-1.pages.dev",
        "why": "KEPT. A class site address is public by design - it is the thing the teacher hands to students - and it is how a report is matched to the site that went wrong."
      },
      {
        "expect": "Netlify token missing",
        "input": "Netlify token missing",
        "why": "KEPT. A sentence that merely says the word 'token' is not a secret, and turning the product's own error messages into holes would make failures unreadable. The rule needs a value after a separator before it removes anything."
      }
    ],
    "note": "What is taken OUT of a problem report before it is written to disk, and - just as important - what is KEPT. Redaction happens as the file is written, never as it is sent: a report that is only safe when it leaves by the right button leaks the first time somebody opens the folder it sits in, and writing it safe is what lets a teacher open the file and see for themselves what they are about to send. The rules lean towards removing too much, because removing a build hash by mistake costs a moment of puzzlement while a surviving token costs the teacher their account - but the four KEPT cases below are load-bearing, and a redactor that swallows them produces reports nobody can diagnose from. There is no way to switch this off in a development build: a redactor that is off while it is being worked on is a redactor nobody has run.",
    "placeholders": {
      "account": "[removed: account id]",
      "email": "[removed: email address]",
      "person": "person",
      "token": "[removed: token]"
    },
    "secretLength": 40
  },
  "scheduledDeployRefusals": {
    "alsoSaid": {
      "note": "Not a refusal - a teacher may be deploying deliberately without them - but the plan NAMES the section's unpublished class pages before the teacher agrees. A deploy that runs perfectly at half six and puts up a site missing tomorrow's class is the failure worth catching while somebody is awake.",
      "rule": "list the class pages students cannot see yet, by name"
    },
    "cases": [
      {
        "expectRefusal": "hasAlreadyPassed",
        "given": {
          "whenIsInThePast": true
        },
        "name": "a time that has already passed",
        "why": "First, because it is the one the teacher can fix without leaving the sheet."
      },
      {
        "expectRefusal": "deployFolderNeedsAttention",
        "given": {
          "folderProblem": true,
          "target": "local_folder"
        },
        "name": "a folder deploy whose folder is not usable"
      },
      {
        "expectRefusal": "cloudflareAccountMissing",
        "given": {
          "cloudflareAccountID": "",
          "target": "cloudflare_pages"
        },
        "name": "Cloudflare with no Account ID",
        "why": "A deliberate divergence, and the reason is worth keeping. The mac's plist carries --account, so the question is asked here and answered once. On Windows the scheduled task could not be handed one, which would make Cloudflare unschedulable - if that is still true, refuse it with a sentence that says so rather than scheduling something that will fail at 06:30."
      },
      {
        "expectRefusal": "additionalDeployFolderNeedsAttention",
        "given": {
          "additionalFolderProblem": true,
          "additionalTarget": "local_folder",
          "hasDeployedBefore": true
        },
        "name": "an ADDITIONAL folder destination whose folder is not usable",
        "why": "Checked after the primary's own two checks, in the same order additional destinations are deployed to (primary first, then each additional one) - a redundancy target with no valid folder would otherwise sit silently broken until the scheduled moment, exactly what asking everything up front exists to prevent. hasDeployedBefore is true here so the PRIMARY's own checks pass first and this is genuinely the additional check firing."
      },
      {
        "expectRefusal": "additionalCloudflareAccountMissing",
        "given": {
          "additionalTarget": "cloudflare_pages",
          "cloudflareAccountID": "",
          "hasDeployedBefore": true
        },
        "name": "an ADDITIONAL Cloudflare destination with no Account ID",
        "why": "cloudflareAccountID is the SAME key the primary's own Cloudflare check uses - it is one credential per teacher, not one per destination, so a missing Account ID refuses whichever destination (primary or additional) actually needs it."
      },
      {
        "expectRefusal": "neverDeployed",
        "given": {
          "hasDeployedBefore": false
        },
        "name": "a section that has never been deployed",
        "why": "The FIRST deploy of a section asks what to call the website. Nobody is there to answer at the scheduled time and it would simply wait. Deploy once by hand, then it can be scheduled - which is what the refusal says."
      },
      {
        "expectRefusal": "additionalDestinationNeverDeployed",
        "given": {
          "additionalTarget": "cloudflare_pages",
          "additionalTargetHasDeployedBefore": false,
          "cloudflareAccountID": "0123456789abcdef0123456789abcdef",
          "hasDeployedBefore": true
        },
        "name": "the PRIMARY has deployed before, but an additional destination has not",
        "why": "The same reasoning as the primary's own check, for whichever additional destination is new - a brand-new Netlify or Cloudflare destination also asks what to call the site; local_folder never does, so it can never trigger this one. A valid cloudflareAccountID is given so THAT check passes first and this is genuinely the never-deployed check firing."
      },
      {
        "expectRefusal": null,
        "given": {
          "hasDeployedBefore": true
        },
        "name": "everything in order"
      }
    ],
    "note": "Everything a deploy running at 06:30 would ASK is asked NOW instead, while somebody is awake to answer. A scheduled deploy that stops at a prompt in the dark is the worst outcome available: it reports nothing, changes nothing, and is discovered by a class looking at yesterday's site. Checked in this ORDER - the first match is what the teacher is told, so a time in the past is answered before a missing account."
  },
  "sidebarFilter": {
    "cases": [
      {
        "expect": [
          "ICS3U",
          "SPH4U",
          "MCV4U"
        ],
        "filter": "",
        "why": "An empty filter shows everything."
      },
      {
        "expect": [
          "ICS3U",
          "SPH4U",
          "MCV4U"
        ],
        "filter": "   ",
        "why": "Trimmed first."
      },
      {
        "expect": [
          "ICS3U",
          "SPH4U"
        ],
        "filter": "ics",
        "why": "Case-insensitive, and mid-word against the NAME as well as the code — so 'ics' finds ICS3U by its code AND SPH4U through 'Phys-ics'. That looks like a bug and is the rule working: a teacher hunting for a course types more letters, and one who typed 'ics' meaning Physics is served too. This case was written expecting ICS3U alone, and running it said otherwise."
      },
      {
        "expect": [
          "ICS3U",
          "SPH4U"
        ],
        "filter": "ICS",
        "why": "Case-insensitive, and mid-word against the NAME as well as the code — so 'ics' finds ICS3U by its code AND SPH4U through 'Phys-ics'. That looks like a bug and is the rule working: a teacher hunting for a course types more letters, and one who typed 'ics' meaning Physics is served too. This case was written expecting ICS3U alone, and running it said otherwise."
      },
      {
        "expect": [
          "ICS3U"
        ],
        "filter": "comp",
        "why": "Mid-word, and against the NAME rather than the code."
      },
      {
        "expect": [
          "ICS3U",
          "SPH4U",
          "MCV4U"
        ],
        "filter": "u",
        "why": "One letter matches all three codes."
      },
      {
        "expect": [
          "MCV4U"
        ],
        "filter": "vectors"
      },
      {
        "expect": [],
        "filter": "biology",
        "why": "No match shows nothing - and the sidebar says so in words rather than showing an empty space."
      }
    ],
    "courses": [
      {
        "code": "ICS3U",
        "name": "Introduction to Computer Science"
      },
      {
        "code": "SPH4U",
        "name": "Physics"
      },
      {
        "code": "MCV4U",
        "name": "Calculus and Vectors"
      }
    ],
    "note": "What the sidebar's filter field shows. Case-insensitive, matched against the course CODE and the course NAME, and mid-word - a teacher typing 'comp' should find Introduction to Computer Science without knowing it starts with ICS. Matching is CONTAINS rather than prefix, deliberately: a teacher who knows their course as 'Calculus' should not have to know it is filed under MCV4U."
  },
  "siteHealth": {
    "note": "What the toolchain checks about a course's folders before it builds, and the WORDS a teacher reads when one of them is wrong. Certain folder and file names carry behaviour — the curriculum folder, the folder holding class pages, Media, a section's index — and nothing stopped a teacher deleting or renaming one in Obsidian. The features then failed SILENTLY: the Curriculum Coverage map in particular still rendered, still looked healthy, and was wrong.",
    "whereItRuns": "Inside build_site.py, after the shared and per-section folders have been merged into content/ and BEFORE Quartz builds or anything is uploaded. It has to run there rather than in the apps: every check is defined over the MERGED tree, which does not exist until the build makes it, and a check that only guarded the GUI button would be bypassed by the assistant, by `Plantoir --mcp-stdio`, by the launchers, and by the scheduled deploy — which runs from launchd or Task Scheduler with the app CLOSED, and is the case that matters most.",
    "checksTheFeatureNotTheFolder": "Every check asks whether the FEATURE produced anything, never whether a folder exists. Recreating an empty `Ontario Curriculum` folder does not restore a teacher's expectation pages, and `_find_curriculum_folder` requires a page named for an expectation code — so an existence check plus a Fix button would have silenced the warning AND left the map missing. A check that can be satisfied without fixing the problem is worse than no check.",
    "scheduledDeployPublishesAnyway": "A scheduled deploy NEVER refuses on a health finding. A slightly inaccurate curriculum map is a paper cut; a site update a teacher was counting on that silently did not happen is not. The finding is recorded and surfaced afterwards instead.",
    "marker": {
      "prefix": "PLANTOIR_HEALTH:",
      "examples": ["PLANTOIR_HEALTH: {\"name\": \"curriculumCoverageFoundNothing\", \"sentence\": \"The curriculum map for ICS3U Section 1 could not be built.\", \"detail\": \"Plantoir looks for a folder whose name mentions the curriculum, holding one page per expectation. It could not find one, so the Curriculum Coverage page has been left out of this build. If you renamed or moved that folder, the map will come back as soon as it can find it again.\", \"fixable\": false, \"course\": \"ICS3U\", \"section\": 1}", "PLANTOIR_HEALTH: {\"name\": \"mediaFolderMissing\", \"sentence\": \"The Media folder for ICS3U is not there.\", \"detail\": \"Images and files you have added to pages live in a folder called Media, beside your course. Plantoir could not find it, so anything on a page that points into it will be missing from the built site.\", \"fixable\": true, \"course\": \"ICS3U\", \"section\": 1}"],
      "rule": "One line per finding on stdout, the prefix followed by a single-line JSON object carrying name, sentence, detail, fixable, course and section. The apps parse these lines out of the console transcript they are already reading; the teacher-facing SENTENCE travels in the line rather than being re-authored on each platform, so the two apps cannot word the same problem differently.",
      "why": "A progress marker is matched loosely and getting one wrong is silent (see app-rules.json markerOrigins). A prefixed JSON line is unambiguous, survives being interleaved with ordinary build output, and carries structure a sentence cannot."
    },
    "repair": {
      "note": "What Plantoir offers to put right, and what it says afterwards. Only two of the checks are repairable, and the line is the point: a fix must restore the FEATURE, not merely satisfy the check.",
      "neverOffered": {
        "checks": ["curriculumCoverageFoundNothing", "courseTeachesNothing", "handWrittenCoveragePage", "noGradedFolders"],
        "why": "Recreating an empty curriculum folder would silence \"the curriculum map could not be built\" and leave the map missing, because the folder only counts once it holds a page named for an expectation code. A button that makes a warning go away without fixing anything is worse than no button: the teacher then believes it is dealt with. Decide from the check's NAME, not from the `fixable` flag alone — the flag means \"this kind of thing is repairable\", while what has to be true is that THIS app has a repair for it."
      },
      "offered": {
        "checks": ["mediaFolderMissing", "sectionIndexMissing"],
        "rule": "Create an empty Media folder, or a section index carrying only a title. Never overwrite: check first, so pressing twice — or pressing after fixing it in Obsidian — changes nothing.",
        "why": "An empty folder and a bare front page are the correct starting state rather than a pretence. The pictures themselves are the teacher's and cannot be conjured back, and inventing content for a front page would put words in their mouth."
      },
      "afterwards": {
        "rule": "Say what was put back, offer to preview it again, and say what the preview does NOT cover. After a repair reported while PUBLISHING, the sentence must name who this does not reach yet (students) and what changes that (publishing again) — the preview is still offered, because that is how a teacher checks the repair worked. The preview is NOT offered in three cases: nothing needed repairing, the repair failed, and a publish is running just now. And the publish sentence must not assert a past publish: it is shown after a FAILED deploy too, and for a first publish nothing has ever gone out.",
        "why": "The button is called \"Preview Again\", not \"Build Again\": the old label never said WHAT would be built, and the thing on offer already has a name the teacher knows. The preview was at first WITHHELD after a publish, reasoning that it does not change what students see — true, and beside the point: the teacher has just put a folder back and wants to SEE that it worked. Withholding it removed something useful to prevent a misunderstanding the words already prevent, so the words carry it instead. A repair whose outcome is invisible is one nobody trusts a second time, and the Media folder is somewhere a teacher cannot see from the app at all. A repair that FAILED must be reported too: both restores can fail on a read-only volume or a file sitting where the folder should be, and reporting only success made the two indistinguishable — silence on the failure path, in the feature written to end silence."
      },
      "oneAlertAtATime": {
        "rule": "The outcome is shown AFTER the dialog it was requested from has gone, never raised from inside that dialog's own button action.",
        "why": "A view presents one alert at a time; asking for a second while the first is dismissing loses one of them, and the one lost would be the report the teacher just asked for."
      }
    },
    "checks": [
      {
        "name": "curriculumCoverageFoundNothing",
        "sentence": "The curriculum map for {course} Section {section} could not be built.",
        "detail": "Plantoir looks for a folder whose name mentions the curriculum, holding one page per expectation. It could not find one, so the Curriculum Coverage page has been left out of this build. If you renamed or moved that folder, the map will come back as soon as it can find it again.",
        "fixable": false,
        "why": "The headline case. The map used to vanish with nothing said, and the page it would have linked from says nothing either — a teacher discovers it in May."
      },
      {
        "name": "courseTeachesNothing",
        "sentence": "Plantoir could not find any class pages in {course} Section {section}.",
        "detail": "The curriculum map counts an expectation as addressed when a page that a class links to mentions it. With no class pages to start from, it has counted every published page instead, so the map is more generous than it should be.",
        "fixable": false,
        "why": "This is the fallback that made a wrong map look healthy. Saying so is the entire fix: the fallback itself is deliberate, because a map that hid everything would be worse."
      },
      {
        "name": "mediaFolderMissing",
        "sentence": "The Media folder for {course} is not there.",
        "detail": "Images and files you have added to pages live in a folder called Media, beside your course. Plantoir could not find it, so anything on a page that points into it will be missing from the built site.",
        "fixable": true,
        "why": "Checks the TARGET, not the link: content/Media is recreated on every build, so checking the link itself always passes. What breaks is the course-level folder it points AT."
      },
      {
        "name": "sectionIndexMissing",
        "sentence": "{course} Section {section} has no front page.",
        "detail": "Every section has a page called index.md that students land on first. This section's is missing. A preview will open on whatever page happens to come first, and publishing cannot go ahead at all until the front page is back.",
        "fixable": true,
        "why": "Restorable safely, unlike the curriculum folder — which is why this one may offer to fix itself and that one may not. The detail used to stop at \"whatever page happens to come first\", which is true of a PREVIEW and understates publishing by a mile: Quartz emits no root index.html without an index.md, so the build produces no site to publish and the deploy has nothing to upload. Naming both outcomes is what tells a teacher whether they can carry on for now or must fix it before they publish."
      },
      {
        "name": "handWrittenCoveragePage",
        "sentence": "A page called Curriculum Coverage will be overwritten each time you build.",
        "detail": "Plantoir writes the Curriculum Coverage page itself, every build. There is a page by that name in your own notes, and it is not the one being published — rename yours to keep it.",
        "fixable": false,
        "why": "The inverse of the other checks: not something missing, something PRESENT that gets clobbered. A name refusal in Settings cannot see this, because a teacher makes this page in Obsidian."
      },
      {
        "name": "noGradedFolders",
        "sentence": "Nothing in {course} Section {section} currently counts for marks.",
        "detail": "The curriculum map marks an expectation as evaluated when a page in your graded folders addresses it. None of those folders were found, so every expectation is shown as not yet evaluated. You can choose which folders count in Course Settings.",
        "fixable": false,
        "why": "When no folders count for marks or the configured folders are missing, the curriculum map shows zero evaluated expectations. Telling the teacher lets them choose which folders count in Course Settings rather than wondering why no expectations show as evaluated."
      }
    ]
  },
  "transcriptStripping": {
    "cases": [
      {
        "expect": "Deploy complete",
        "input": "\u001b[32mDeploy complete\u001b[0m",
        "why": "CSI colour codes: ESC [ ... final letter."
      },
      {
        "expect": "Building",
        "input": "\u001b]0;Plantoir\u0007Building",
        "why": "An OSC window-title sequence ends at BEL, not at a letter - a stripper that only knows CSI leaves the title text in the console."
      },
      {
        "expect": "one\ntwo\tthree\r\n",
        "input": "one\ntwo\tthree\r\n",
        "why": "Newline, tab and carriage return are KEPT. Stripping the carriage return breaks the progress lines the launchers redraw in place."
      },
      {
        "expect": "before after",
        "input": "before\u0001 after",
        "why": "Other control characters go, and nothing else moves."
      },
      {
        "expect": "plain text",
        "input": "plain text"
      }
    ],
    "note": "The launchers' output is written for a terminal: colour codes, cursor moves, window-title sequences. Both apps show that output to a teacher, so both must strip the same things - and KEEP the same things. Newlines, carriage returns and tabs survive; every other control character does not. Inputs below use \\u001b for ESC and \\u0007 for BEL."
  },
  "workingFolderPathBar": {
    "actions": [
      {
        "action": "reveal",
        "gesture": "context menu",
        "macLabel": "Show in Finder",
        "means": "Open the folder's PARENT with the folder selected.",
        "why": "The same words and the same meaning this action has everywhere else in the app.",
        "windowsLabel": "Show in File Explorer"
      },
      {
        "action": "open",
        "gesture": "double-click, and a second context-menu item",
        "macLabel": "Open Folder",
        "means": "Open the folder ITSELF in the file manager.",
        "why": "Two different things, and a teacher wants both: revealing shows where a folder sits, opening shows what is in it. Finder's own path bar makes the same distinction, which is why the gestures match it — double-click opens, right-click reveals.",
        "windowsLabel": "Open Folder"
      },
      {
        "action": "tooltip",
        "gesture": "hover",
        "means": "The full path of that crumb.",
        "why": "The crumbs show display names, which can be localised or truncated; the tooltip is how a teacher checks they are looking at the folder they think they are."
      }
    ],
    "ancestorPaths": {
      "cases": [
        {
          "expect": [
            "/",
            "/Users",
            "/Users/teacher",
            "/Users/teacher/Desktop",
            "/Users/teacher/Desktop/Courses"
          ],
          "path": "/Users/teacher/Desktop/Courses"
        },
        {
          "expect": [
            "/",
            "/Users",
            "/Users/teacher"
          ],
          "path": "/Users/teacher"
        }
      ],
      "note": "Every ancestor from the volume root DOWN TO and including the folder itself. The root is included — a teacher scanning for where their folder lives reads the whole line.",
      "windowsEquivalent": "Start at the drive rather than at '/': C:\\\\ , C:\\\\Users, C:\\\\Users\\\\teacher, … The RULE is 'every ancestor, root first, folder last'; the spelling of a root is the platform's."
    },
    "everyCrumbIsLive": {
      "rule": "EVERY ancestor is clickable, not just the last one.",
      "why": "It is how a teacher gets to the folder ABOVE their working folder — to make a sibling, or to see where things sit — without leaving the app to find it."
    },
    "tooLongForTheSpace": {
      "rule": "A path too long for the space it is given shows its END — the folder itself and its nearest ancestors — never its start.",
      "why": "The folder's own name is the part a teacher is looking for; 'Macintosh HD › Users › …' is the same for every folder they own. Found 2026-09-05 with an iCloud Drive folder, whose real path runs through ~/Library/Mobile Documents/com~apple~CloudDocs and was cut off in the folder picker before reaching the folder's name. Wherever the bar appears — the window's own bar and the picker's — the same rule."
    },
    "icons": {
      "rule": "Each crumb shows the folder's real icon from the host file manager.",
      "why": "Not decoration: a teacher recognises their Desktop or their iCloud Drive by its icon before reading the word. Optional if the platform makes it expensive — say so rather than dropping it silently."
    },
    "note": "The 'Working folder:' bar under the sidebar. Modelled on the host file manager's own path bar — Finder's on the mac — because a teacher who has seen one knows what it does without being told. Reported missing on Windows 2026-08-16, in use: the context menu is not there. The LABELS are per platform ('Finder' against 'File Explorer'); the actions, the gestures and the crumb list are not."
  },
  "buildOutputLocation": {
    "note": "Where a section's BUILT WEBSITE is kept, and why it is not inside the working folder. Decided 2026-09-05; Windows had already done its half in row 290. The rule is implemented twice on the mac on purpose — in the app (BuildOutputLocation.swift, when a working folder's courses are read) and in the launchers (preview.sh and deploy.sh before they build, setup.sh for every course once the wizard has finished) — because a teacher at the command line and a publish scheduled with launchd have no app to do it for them, and if the two built into different places every build would read as stale.",
    "whyItMoves": "A built site is DERIVED: every file in it comes from the teacher's notes and can be made again, which is why Plantoir's own backups already skipped it by name (CourseArchiver.excludedFromArchives, scripts/setup_course.py). Nothing OUTSIDE Plantoir knew that. A cloud-synced folder uploads every build and charges it against the teacher's quota; Time Machine backs it up; a zip, a Finder copy or a folder handed to a colleague carries it; Get Info counts it. Measured on this Mac on 2026-09-05, on the small EXC2O course the verification suite builds: 331 files and 9.8 MB for ONE section, and every build rewrites all of them (Quartz emits its output fresh, so rsync's mtime comparison copies every file), so the cost is paid again on every build rather than once. A real course with media is many times that.",
    "appliesToEveryFolder": {
      "rule": "The built site is kept outside the working folder for EVERY working folder, not only the ones Plantoir believes are synced.",
      "why": "The benefit is not confined to syncing — a backup, a copy, a zip and a folder size are all smaller for it — and one code path is one code path: a rule that ran only for folders detected as synced would be a rule tested in one case and running in another, and cloud detection is deliberately not certain enough to hang behaviour on (see cloudSyncedFolders.neverRefused)."
    },
    "macLocation": {
      "buildsRoot": "{home}/Library/Application Support/Plantoir/builds/{folder id}",
      "perCourse": "{home}/Library/Application Support/Plantoir/builds/{folder id}/{COURSE}",
      "folderId": "The first 8 hex characters of SHA-256 over the working folder's PHYSICAL path plus a trailing newline — exactly `pwd -P | shasum -a 256 | cut -c1-8`, the same value the container name already uses, so a folder's container and its builds folder can never disagree about which folder they belong to. Resolved with POSIX realpath, never Foundation's resolvingSymlinksInPath(), which strips the /private prefix that pwd -P keeps.",
      "underHomeWhy": "The container VM (Colima) mounts only $HOME, so a builds folder anywhere else would appear EMPTY inside the container and every build would seem to vanish.",
      "workingFolderMarker": "Each builds folder holds working-folder.txt naming the working folder it serves. The id is a hash and cannot be read backwards, so without it a builds folder left behind by a deleted working folder could never be recognised as abandoned."
    },
    "windowsLocation": {
      "buildsRoot": "%LOCALAPPDATA%\\Plantoir\\builds\\{folder id}",
      "how": "PLANTOIR_BUILD_ROOT, honoured by scripts/toolchain_paths.py -> merged_output_root(), which then does NOT nest a .merged_output level. Shipped in row 290 and unchanged by this piece.",
      "doNotChange": "Windows keeps its environment variable and its flat layout. The symlink below is a MAC answer to a mac problem (a container that mounts only courses/, and three launchers plus three Swift readers that name the path), not a better idea Windows should copy."
    },
    "theLink": {
      "rule": "On the mac, courses/{COURSE}/.merged_output is a SYMLINK to the per-course builds folder, and the launchers bind-mount the builds root into the container at the SAME absolute path, unconditionally.",
      "why": "Every reader keeps working unchanged and they cannot disagree about where the build went: deploy.sh (MERGED_DIR_HOST, SECTION_DIR_IN_CONTAINER), preview.sh (OUTPUT_PATH, and stop mode), the Swift readers (BuildFreshness, ScheduledDeploy, SectionDetailView), the shell BuildFreshness that a launchd deploy writes out, verify.sh, verify-deploy.sh and the screenshot harness. Mounting the builds root anywhere ELSE in the container would leave the link dangling inside it, and every build would fail on a path a teacher can plainly see working in Finder.",
      "rejected": [
        "One environment variable, as on Windows. The mac build runs in a container that mounts only courses/, so a build root outside the working folder is invisible in there until a second mount exists — and a launchd deploy and a teacher at the command line have no app to set a variable for them, so the app and the command line would build into different places and every build would read as stale.",
        "Computing the path at every reader. Three launchers, three Swift readers, a generated launchd script and two verification scripts name it today; each one is a chance for two of them to disagree, and the failure mode of disagreeing is a publish that ships the wrong bytes.",
        "A .nosync suffix. iCloud-only — Dropbox and OneDrive ignore it — and it moves the path just as much as this does."
      ]
    },
    "aBuildWithNoLinkIsNotAdopted": {
      "rule": "A per-course builds folder found with no link pointing at it is CLEARED, never reused.",
      "why": "Archiving a course, restoring one from a backup, and replacing a course's contents all remove the link along with everything else in the course folder — and each of them leaves content whose timestamps may be OLDER than the built site standing outside. Adopting that build would let a restored course publish last month's pages while every freshness check said it was up to date, which is the silent wrong answer this codebase refuses. Archiving a course or a section discards its build outright as well, so the clearing is a safety net rather than the only line of defence.",
      "aSecondMacClearsRatherThanAdopts": "A link left by a SECOND MAC sharing the folder clears this machine's build too, rather than adopting it. ADOPTING was proposed by one adversarial review and rejected by the next, and the reasoning is the point. Adopting looks strictly better: a teacher switching between two Macs would keep each machine's build instead of rebuilding after every switch. But the second Mac cannot tell 'the folder came back unchanged' from 'the folder was archived and restored while I was shut' — and in the second case the pages it would adopt a build for are OLDER than that build, so the freshness check says up to date and the teacher publishes what they undid. Clearing costs one rebuild, which is cheap and visible; adopting risks a wrong site nobody is told about. A generation stamp carried in the course folder (a UUID written beside the link and copied into the build, adopted only when the two match) would buy both, and was judged not worth a new synced file inside a teacher's vault for a case this rare. If two-Mac working ever becomes common, that is the design to build.",
      "renameIsTheException": "Renaming a course CARRIES its build across to the new code and re-points the link, because the built site used to travel with the folder and a rename should not silently cost a teacher their website.",
      "restoringOneSection": "Putting ONE section's pages back — from a backup, or by the assistant undoing what it just did — discards that section's built website too. The link is at COURSE level and survives, so the clearing rule above does not reach it. This staleness is OLDER than the move: it was equally true when .merged_output sat inside the course folder, because a section restore replaces section<N> and never touched the built tree beside it. Found and fixed on 2026-09-05 while moving the output; recorded as pre-existing so the fix is not read as repairing damage this piece did."
    },
    "whatATeacherSees": {
      "inFinder": ".merged_output is hidden, so it is invisible in Finder unless hidden files are shown — where it now appears as a shortcut rather than a folder. Their working folder gets markedly smaller, which is the point.",
      "atTheCommandLine": "preview.sh --help says where built websites are kept and that the old path still works.",
      "onTheTrail": "One line per course, the first time its built site moves — activityTrail 'built site moved out of the working folder'. Written by whichever of the two implementations does the move: the app when a folder is opened, and the LAUNCHERS when the move is done by a build at the command line or by a publish launchd ran at six in the morning. The launchers append it to the same file in the same shape, because a trail that recorded only the moves the GUI happened to make would be missing exactly the ones a teacher never watched happen.",
      "notesAreNeverMoved": "Only the built site moves. Nothing a teacher wrote is touched, and the course folder keeps every one of its own files."
    },
    "syncedFoldersFollowTheLink": {
      "measured": "Tested on this Mac on 2026-09-05 against a real iCloud Drive folder and a real ~/Library/CloudStorage/Dropbox, each given a throwaway course folder containing a real note and a .merged_output symlink to an outside folder holding a 3 MB file. BOTH kept it as a symlink and neither followed it: iCloud reported the link itself as uploaded at 82 bytes — the length of the target path — with isUbiquitousItem true and the 3 MB nowhere; Dropbox tagged the LINK with its own extended attributes and left the target folder outside Dropbox with none at all, so it had not reached through it. `du` of the synced folder counted 4 KB. OneDrive is not installed on this Mac and was not tested.",
      "consequence": "The link is synced, which means a teacher with two Macs receives a link naming a path that may not exist there. On a Mac running THIS version that is handled before anything builds: a link naming another machine's builds folder is replaced, and this machine's own build under that course code is CLEARED with it — see aBuildWithNoLinkIsNotAdopted.aSecondMacClearsRatherThanAdopts for why the tempting alternative was rejected. scripts/build_site.py replaces a link it still finds dangling with a real folder and builds into the old place — deliberately NOT creating the target, which would answer the second-Mac case and silently ruin the other one it meets (a container made before the builds folder was mounted into it, where the folder would be created inside the container and the host would see an empty site with no error).",
      "aSecondMacMustBeUPDATED": "The mac's OLD build_site.py fails outright on a link whose target is missing — mkdir raises 'File exists' — and no code shipped before 2026-09-05 can repair it. So a working folder synced between two Macs needs BOTH of them on this version or later. It is worse than one stale machine failing on its own: the launchers and .toolchain/ live inside the synced folder, so an older app refreshes them BACK to its own copies, and a scheduled publish on the up-to-date Mac then runs whatever deploy.sh is in the folder that morning. Before this change a version mismatch between two Macs was harmless. This belongs in the release notes, not only here."
    },
    "upgradingAFolderThatAlreadyHasBuiltSites": {
      "rule": "Everything an existing teacher already has survives the change, and nothing about it is one-way: the built site is MOVED (never rebuilt from scratch and never deleted), the site markers are untouched because they were never inside it, and if any step cannot be done the course is left exactly as it was and the build writes a real .merged_output folder — which is what it did before.",
      "whatIsAlreadyThere": [
        "A real .merged_output folder holding built sites. Moved out on the first folder-open, or on the first build from the command line, and the same path still reads it.",
        ".netlify_sites/ and .cloudflare_sites/ — the markers that say which published site a section belongs to. They live beside .merged_output in the course folder, NOT inside it, so nothing here touches them. Losing one silently creates a brand-new site on the next publish, which has happened on Windows before, so it is checked rather than assumed.",
        "A container created without the builds mount. Recreated once, by whichever launcher runs first. A container is recreated on a toolchain change anyway, so this is not a new kind of interruption.",
        "Scheduled publishes already written out to launchd. Their script names courses/<CODE>/.merged_output/section<N>/public/index.html and keeps working through the link, unchanged and unregenerated.",
        "A course folder synced to a second Mac. The link is synced with it and names a home folder that is not there; it is replaced on that machine before anything builds — PROVIDED that Mac is also on this version. It must be: see syncedFoldersFollowTheLink.aSecondMacMustBeUPDATED."
      ],
      "neverWhileABuildIsRunning": "A course being previewed IN THIS APP is left alone and picked up at the next reload. Moving the output from under a running build would break it on a path the teacher can see working. The qualifier is honest rather than cautious: preview leases are per-process, so the app cannot see a preview started at the command line, and the MCP server cannot see the app's. The window is small — a course is linked the first time its folder is opened, long before anything can be previewing it — and the cost if it is ever hit is a failed build, not a lost note.",
      "whenItCannotBeDone": "A read-only folder, a full disk or a permission the teacher has changed must never turn 'the built website could not be moved' into 'publishing is broken'. Every step is allowed to fail without stopping the run, and the fallback is the OLD behaviour rather than a broken one. The launchers run under `set -e`, which is what makes this a rule rather than a nicety. If the move succeeds and the link then fails, the move is put BACK — a course with its website in the old place still builds and still publishes, while a course with neither has lost it for nothing. The one case not covered is a DOUBLE fault, where putting it back fails too: then the built site stands outside with nothing pointing at it, and the next run clears it. That costs a rebuild and nothing else, and is not worth more machinery than saying so here."
    },
    "containerRecreate": {
      "rule": "A container that exists without the builds mount is recreated. All three launchers check, and all three create the container with the same mount set.",
      "why": "A mount cannot be added to a container that already exists, so every teacher's existing container predates this and has to be replaced once. And if only SOME launchers created the mount, two of them would recreate the container away from each other on alternate runs."
    }
  },
  "cloudSyncedFolders": {
    "note": "A working folder that a cloud service keeps in sync — iCloud Drive, Dropbox, OneDrive, Google Drive, Box. Teachers keep their notes in such folders ON PURPOSE (it is how the notes reach an iPad and a second machine), so Plantoir never refuses one. It recognises the folder from markers the system exposes, says once and in plain words what a synced folder costs, lets the teacher decide, and leaves their content exactly where they put it. Decided 2026-09-05; both sides of the argument are in TODO.md under 'Should Plantoir refuse to work in a cloud-synced folder?'.",
    "neverRefused": {
      "rule": "A synced folder is never blocked, and detection is never treated as certain enough to block on.",
      "why": "A hard block is the one answer a teacher cannot opt out of, and it would mean telling them to give up reaching their own notes from other devices. Detection is also unreliable in both directions — a false refusal is unrecoverable for them, and a synced folder Plantoir cannot see still works. Refusing was rejected once already, on Windows, where the same problem was met and answered by moving the build output out of the folder rather than turning the folder away."
    },
    "detection": {
      "rule": "From markers the platform exposes, never from a folder's NAME. A teacher can have a folder literally called 'Dropbox' that is not one, and a wrong answer here is shown to them as fact.",
      "macMarkers": [
        "Under ~/Library/Mobile Documents/ — iCloud Drive's real location; '~/iCloud Drive' in Finder is a view of it.",
        "Under ~/Library/CloudStorage/<Service>-<Account>/ — where macOS 12.3+ keeps every File Provider service's folders: OneDrive-Personal, GoogleDrive-name@gmail.com, Dropbox, Box-Box. The service is the part of that folder's name before the first hyphen.",
        "The item's own 'ubiquitous' flag (isUbiquitousItem), trusted ONLY under ~/Desktop and ~/Documents — the two folders 'Desktop & Documents Folders' syncs in place, which match no path rule. The flag is NOT iCloud-specific: it is set for any File Provider item, a Dropbox or OneDrive folder included (checked on a real ~/Library/CloudStorage/Dropbox), so trusting it anywhere else would name the wrong service.",
        "Symlinks are resolved before any rule runs. Dropbox and OneDrive both leave a link at the old place (~/Dropbox → ~/Library/CloudStorage/Dropbox); a path arriving through it matches no rule while the folder behind it does. The resolved path is also the key the acknowledgement is remembered under, so one folder is one folder whichever spelling it arrives by."
      ],
      "windowsMarkers": [
        "The OneDrive roots the client publishes in the environment: %OneDrive%, %OneDriveConsumer%, %OneDriveCommercial%. A Desktop or Documents moved into OneDrive by 'Known Folder Move' physically lives under one of these, so the prefix rule catches it.",
        "Dropbox: the 'path' entries in %APPDATA%\\Dropbox\\info.json (or %LOCALAPPDATA%\\Dropbox\\info.json).",
        "iCloud for Windows: %USERPROFILE%\\iCloudDrive by default; the client's own setting if it can be read.",
        "Google Drive for desktop and Box: their mount points, when the client publishes them; otherwise unrecognised, which is allowed — see neverRefused."
      ],
      "unknownServiceName": "your cloud service",
      "cases": [
        {
          "path": "{home}/Library/Mobile Documents/com~apple~CloudDocs/Course Notes",
          "expectService": "iCloud Drive",
          "why": "The ordinary iCloud Drive folder."
        },
        {
          "path": "{home}/Library/Mobile Documents",
          "expectService": "iCloud Drive",
          "why": "The root itself counts."
        },
        {
          "path": "{home}/Library/CloudStorage/OneDrive-Personal/Course Notes",
          "expectService": "OneDrive"
        },
        {
          "path": "{home}/Library/CloudStorage/GoogleDrive-teacher@gmail.com/My Drive/Course Notes",
          "expectService": "Google Drive",
          "why": "The account is part of the folder name; the service is the part before the first hyphen."
        },
        {
          "path": "{home}/Library/CloudStorage/Dropbox/Course Notes",
          "expectService": "Dropbox",
          "why": "No hyphen at all — the whole name is the service."
        },
        {
          "path": "{home}/Library/CloudStorage/Box-Box/Course Notes",
          "expectService": "Box"
        },
        {
          "path": "{home}/Library/CloudStorage/pCloud-Drive/Course Notes",
          "expectService": "your cloud service",
          "why": "Recognisably synced — it is in the reserved location — but not a service Plantoir names. Say so honestly rather than guessing."
        },
        {
          "path": "{home}/Library/CloudStorage",
          "expectService": null,
          "why": "The reserved folder itself is nobody's."
        },
        {
          "path": "{home}/Desktop/Dropbox/Course Notes",
          "expectService": null,
          "why": "A folder CALLED Dropbox proves nothing. The name rule that would catch it is the one that would also be wrong."
        },
        {
          "path": "{home}/Desktop/Course Notes",
          "expectService": null,
          "why": "From the path alone, nothing — on a Mac with Desktop & Documents in iCloud, the live iCloud flag is what answers, and this rule must not pretend to."
        },
        {
          "path": "/Volumes/Backup/Users/teacher/Library/CloudStorage/Dropbox/Course Notes",
          "expectService": null,
          "why": "Only the teacher's own home folder carries the markers; the same shape on another volume is a copy."
        }
      ]
    },
    "whenShown": {
      "chosen": {
        "rule": "A synced folder the teacher just CHOSE, or an empty synced folder about to be set up, stops at the folder picker: the folder's path bar FIRST, then the headline, the full explanation, and two buttons — go ahead, or choose a different folder. Setting up the empty folder IS going ahead. Neither button is the default action while the note is showing — including the set-up button, which is the default at every other time.",
        "why": "This is the one moment the choice is free — nothing has been written into the folder yet. A Return pressed out of habit must not decide it. The path bar comes first because the sentences say 'this folder', and a teacher reads that and looks for which folder."
      },
      "notAskedAboutAFolderThatCannotBeUsed": {
        "rule": "A synced folder the picker will not take anyway — neither a working folder nor empty — gets no note about syncing.",
        "why": "The teacher is about to choose again, and a note about a folder they cannot use is noise beside the guidance that says what to choose."
      },
      "restored": {
        "rule": "A synced folder the window RESTORED — opened on launch, or inherited by a new window — gets a quiet notice inside the window, above the working-folder bar: the headline, the one-line summary, a way to open the full explanation in place, and a button to dismiss it. Never a dialog, never a sheet.",
        "why": "A folder can become synced AFTER it was set up (moved into iCloud, Desktop & Documents turned on) or have been set up before Plantoir could tell. But a folder that opens on every launch must not interrupt every launch, and the teacher did not choose anything just now."
      },
      "rememberedPerFolder": {
        "rule": "Going ahead — from either button — is remembered for THAT folder, keyed by its resolved path, and neither the choice nor the notice is shown for it again, in this window or any other window showing the same folder. A second synced folder gets its own note. The list is never pruned: a folder that is renamed or moved is a new key and is asked about again, and a deleted folder's key stays harmlessly.",
        "why": "Once is information; twice is nagging. And a teacher with two synced folders was told about one of them. Asking again after a rename is accepted: the folder may have moved INTO a synced location, which is exactly the case worth a note."
      },
      "reChoosingTheOpenFolder": {
        "rule": "Choosing, through the picker, the folder a window already shows counts as a restore, not a new choice: the notice form, never the picker's.",
        "why": "A teacher who re-picks their open folder while the notice is showing would otherwise find their courses hidden behind the picker, and read it as the app losing the folder."
      },
      "checkedEveryOpen": {
        "rule": "The check runs every time a working folder is adopted, not only the first time.",
        "why": "Folders move into cloud services after they are made, and the check costs nothing."
      }
    },
    "wording": {
      "rule": "The sentences below, word for word, with the service's name where {service} is. They name EFFECTS a teacher can recognise — building can be slower, renaming a folder can take a while — and never machinery. No 'sync client', no 'file provider', no 'dataless file', no 'build root'.",
      "headline": "This folder is kept in sync with {service}.",
      "summary": "Plantoir works here, but building can be slower and renaming folders can take a while. Your notes stay where they are.",
      "notesStayPut": "Your course notes stay exactly where they are. Plantoir never moves them.",
      "buildFilesAreCopiedRetired": "RETIRED 2026-09-05, on BOTH platforms — do not show it, and do not add it back. It said: 'Building or previewing a website creates thousands of small files inside this folder, and {service} will copy every one of them to the cloud. That can make building slower and take up storage space.' It was true on the mac only, and only until the mac moved its build output out of the working folder as Windows had already done (see buildOutputLocation). Kept here rather than deleted because a sentence that once shipped gets re-proposed by whoever remembers it, and because the contract's own note said this sentence would leave when the mac moved its output — this is that.",
      "offloadedPagesAreSlow": "If {service} has moved some of your pages off this computer to save space, Plantoir has to download each one before it can read it. Renaming a folder reads every page, so it can take a while.",
      "syncingCanInterruptAMove": "If {service} is busy copying a file at the moment Plantoir needs to move or rename it, that step can fail. Plantoir checks before it moves anything, and you can try again once syncing has settled.",
      "whatToDo": "To avoid all of this, keep your working folder somewhere that is not synced — a folder inside your home folder, for example. If reaching your notes from other devices matters more, carry on: Plantoir will work, just more slowly at times.",
      "whatToDoWhy": "'Your home folder', not 'your Desktop': on a Mac with Desktop & Documents in iCloud, the Desktop IS synced, and the advice would send them to the problem. The second sentence states the trade honestly and pushes neither way.",
      "explanationOrder": ["notesStayPut", "offloadedPagesAreSlow", "syncingCanInterruptAMove", "whatToDo"],
      "explanationOrderWhy": "Reassurance first — the words 'kept in sync' beside a warning read as 'your notes are at risk', and they are not. The choice last, once the reasons have been given.",
      "useAnywayButton": "Use This Folder Anyway",
      "chooseDifferentFolderButton": "Choose a Different Folder…",
      "dismissNoticeButton": "Got It",
      "showDetailsButton": "Show Details",
      "hideDetailsButton": "Hide Details"
    }
  },
  "specialFoldersHelp": {
    "note": "The in-app answer to \"What else does Plantoir use my folders for?\" - a sheet reached from Course Settings that lists the folders and files THIS course has which the build treats specially. Shared because it is a rule with inputs and outputs wrapped around a set of sentences a teacher reads, and the two apps must give the same answers about the same course. Deliberately NOT here: the layout, and the MECHANISM of dismissal - a sheet on the mac, a dialog on Windows. The two button LABELS are here, because a label is a sentence a teacher reads and both apps already say the same words; only the thing the button is attached to differs.",
    "openedBy": "What else does Plantoir use my folders for?",
    "dismissedBy": "Done",
    "title": "Folders Plantoir uses",
    "intro": "Renaming or deleting one of these changes what appears on your site. Everything else in your course is yours to arrange however you like.",
    "namesTheCoursesOwnFolders": {
      "rule": "Every row that CAN be named from the course is named from the course, never from the rule that finds it. A row names a real folder, or it names nothing.",
      "why": "Saying \"any folder whose name mentions the curriculum\" invites a teacher to get creative with it, and turns an implementation detail into a promise the product then has to keep. Saying \"your expectations live in Ontario Curriculum\" tells them the thing they can act on. It is also why the sheet is per-COURSE rather than one static help page: the answers genuinely differ. One course grades \"Tasks\", another \"Tests\" and \"Thinking Tasks\"; one calls its class folder \"All Classes\", another \"Lessons\"."
    },
    "rows": [
      {
        "key": "lessons",
        "namedFrom": "course",
        "source": "every per-section folder the build counts as a class folder",
        "what": "Your lessons",
        "why": "Each day's class page lives here. Plantoir puts new classes in this folder, keeps them in date order, and uses them to work out which pages your course actually teaches."
      },
      {
        "key": "curriculum",
        "namedFrom": "course",
        "source": "the curriculum folder the build resolves BY NAME for this course - the same first-the-recorded-name-then-the-scan order the build uses. Only the name half: the build also wants an expectation page inside the folder, which neither app can see from configuration, so the sheet can name a folder the build would go on to skip. Naming it is still righter than naming one that is not there.",
        "placeholderWhenNone": "Your curriculum folder",
        "what": "Your curriculum expectations",
        "why": "One page per expectation. The curriculum map is built from these — without them there is nothing to measure your lessons against, and the map is left out.",
        "whyPlaceholder": "The NAME is the only thing that changes: \"Your curriculum folder\" is the single piece of text in the sheet allowed to describe rather than name, and only for a course that has no such folder at all. The explanation beside it does not change - a course with no expectations needs to hear the same thing about what they are for. The mac has a SECOND sentence here today (\"One page per expectation, in a folder whose name mentions the curriculum\") and it is deliberately NOT adopted: it publishes the matching rule in plain words, which is what saysNoMachinery exists to stop. Retiring it is one of the two things MAC-HANDOFF.md asks of the mac."
      },
      {
        "key": "graded",
        "namedFrom": "course",
        "source": "the folders whose work counts for marks, including for a course never asked",
        "what": "Work that counts for marks",
        "why": "The curriculum map shows an expectation as evaluated when a page in one of these addresses it. You choose these above."
      },
      {
        "key": "media",
        "namedFrom": "fixed",
        "name": "Media",
        "what": "Images and files you add to pages",
        "why": "Plantoir looks after this one itself and keeps it out of your sidebar. If it goes missing, pictures stop appearing on your site."
      },
      {
        "key": "folderIndex",
        "namedFrom": "fixed",
        "name": "index.md",
        "what": "The page a folder opens on",
        "why": "Every section has one, and so does each folder. It is the way in, not a lesson."
      },
      {
        "key": "keyLinks",
        "namedFrom": "fixed",
        "name": "Key Links.md",
        "what": "The shortcuts in your sidebar",
        "why": "Plantoir adds the curriculum map to this list when it builds your site. Your own copy is left exactly as you wrote it."
      },
      {
        "key": "coverage",
        "namedFrom": "fixed",
        "name": "Curriculum Coverage",
        "what": "Written for you, every time you build",
        "why": "Do not write your own page with this name — it is replaced each time, so anything you put there would be lost."
      }
    ],
    "rowsAreOrdered": {
      "rule": "The rows appear in the order above, and no row is ever omitted - the curriculum row uses its placeholder rather than disappearing.",
      "why": "A row that vanishes when it has nothing to say is the case a teacher most needs to see: a course with no curriculum folder is exactly the course whose map is missing."
    },
    "listing": {
      "rule": "Several names are joined the way a person says them: \"A\", \"A and B\", \"A, B and C\". An empty list reads \"None chosen\".",
      "why": "These lists are read aloud in a teacher's head. A bracketed, comma-separated list is a data structure showing through.",
      "cases": [
        {
          "names": [
            "Tasks"
          ],
          "expect": "Tasks"
        },
        {
          "names": [
            "Tasks",
            "Tests"
          ],
          "expect": "Tasks and Tests"
        },
        {
          "names": [
            "Tasks",
            "Tests",
            "Quizzes"
          ],
          "expect": "Tasks, Tests and Quizzes"
        },
        {
          "names": [],
          "expect": "None chosen"
        },
        {
          "names": [
            "Tasks",
            ""
          ],
          "expect": "Tasks",
          "why": "An empty name is dropped before the joining, not rendered as a gap."
        }
      ]
    },
    "saysNoMachinery": {
      "rule": "No text the PRODUCT writes - the title, the intro, and every row's what and why - may name the machinery or the matching rule. A folder's NAME is excluded: it is the teacher's own word shown back to them as they typed it, and a computer studies course with a folder called \"Scripts\" is not a wording bug.",
      "why": "Rule one, plus the reason this sheet names configured folders in the first place. \"Substring\", \"segment\" and \"case-insensitive\" are banned alongside \"container\" and \"script\" because publishing the matching rule in words is the same mistake as printing it in a row, taken by another route.",
      "jargon": [
        "toolchain",
        "script",
        "docker",
        "container",
        "wsl",
        "python",
        "json",
        "quartz",
        "config",
        "repository",
        "substring",
        "regex",
        "case-insensitive",
        "segment"
      ]
    },
    "cases": [
      {
        "name": "a course names its own folders, not the defaults",
        "perSectionFolders": [
          "Lessons"
        ],
        "sharedFolders": [
          "Concepts",
          "Tests",
          "Expectations"
        ],
        "gradedFolders": [
          "Tests"
        ],
        "curriculumFolder": "Expectations",
        "expectNames": {
          "lessons": "Lessons",
          "curriculum": "Expectations",
          "graded": "Tests"
        },
        "mustNotAppear": [
          "All Classes"
        ],
        "why": "The whole point. A course that renamed everything is told what ITS folders are called."
      },
      {
        "name": "never asked about marks: shown what the build counts today",
        "perSectionFolders": [
          "All Classes"
        ],
        "sharedFolders": [
          "Concepts",
          "Tasks",
          "Ontario Curriculum"
        ],
        "gradedFolders": null,
        "curriculumFolder": "Ontario Curriculum",
        "expectNames": {
          "lessons": "All Classes",
          "curriculum": "Ontario Curriculum",
          "graded": "Tasks"
        },
        "why": "An absent pool is not an empty one - see gradedFolders.absentIsNotEmpty. The sheet shows what counts today rather than a blank list or a guess."
      },
      {
        "name": "asked and cleared: an empty pool is a real answer",
        "perSectionFolders": [
          "All Classes"
        ],
        "sharedFolders": [
          "Concepts",
          "Tasks"
        ],
        "gradedFolders": [],
        "curriculumFolder": null,
        "expectNames": {
          "lessons": "All Classes",
          "curriculum": "Your curriculum folder",
          "graded": "None chosen"
        },
        "why": "A teacher who cleared the pool is not shown \"Tasks\" as though they had never been asked, and a course with no curriculum folder gets the placeholder rather than losing the row."
      },
      {
        "name": "the curriculum folder is named even when the course never recorded one",
        "perSectionFolders": [
          "All Classes"
        ],
        "sharedFolders": [
          "Concepts",
          "Ontario Curriculum"
        ],
        "gradedFolders": [
          "Tasks"
        ],
        "curriculumFolder": null,
        "expectNames": {
          "curriculum": "Ontario Curriculum"
        },
        "why": "PROPOSED FROM WINDOWS, 2026-09-06 - see MAC-HANDOFF.md. The build finds this course's curriculum folder perfectly well without the key being set, so the sheet must name it. Showing the placeholder here would tell a teacher to go and create a folder they already have, which is the one failure a sheet about folder names cannot afford."
      },
      {
        "name": "a recorded name the course no longer has is not shown",
        "perSectionFolders": [
          "All Classes"
        ],
        "sharedFolders": [
          "Concepts",
          "Ontario Curriculum"
        ],
        "gradedFolders": [
          "Tasks"
        ],
        "curriculumFolder": "Expectations",
        "expectNames": {
          "curriculum": "Ontario Curriculum"
        },
        "mustNotAppear": [
          "Expectations"
        ],
        "why": "PROPOSED FROM WINDOWS, 2026-09-06 - the second half of the case above and the sharper one. The course renamed its folder and the recorded name went stale; the build resolves past it to the folder that is really there. A sheet reading the raw name shows \"Expectations\", which the teacher does not have and cannot find - worse than the placeholder, because it looks like an answer."
      },
      {
        "name": "more than one class folder is listed, not just the first",
        "perSectionFolders": [
          "All Classes",
          "Class Notes"
        ],
        "sharedFolders": [
          "Concepts"
        ],
        "gradedFolders": [
          "Tasks"
        ],
        "curriculumFolder": null,
        "expectNames": {
          "lessons": "All Classes and Class Notes"
        },
        "why": "The build counts every class folder when working out what a course teaches, so the sheet names every one."
      }
    ]
  },
  "specialNames": {
    "excludedFolderIndexNote": {
      "noteBody": "> [!NOTE]\n> This folder was removed in Course Settings and is excluded from your website. Its pages will not appear in previews or on your published site. To include it again, add it back in Course Settings.",
      "sentinelEnd": "<!-- plantoir:excluded-folder-note:end -->",
      "sentinelStart": "<!-- plantoir:excluded-folder-note:start -->",
      "why": "Explains why the folder is still present in Obsidian after being removed in Course Settings, and that it is ignored during build. Written into existing index.md files by preflight; stripped during merge so it never ships."
    },
    "curriculumFolderBlockedByCoverageSetting": {
      "reason": "The curriculum coverage map needs this folder to show your expectations. To remove it, turn off “Publish the curriculum coverage map” in Settings first.",
      "why": "Explains why the curriculum folder cannot be removed while the coverage map is enabled in Course Settings, naming the switch to turn off first."
    },
    "curriculumFolderBlockedByCurriculumPages": {
      "reason": "This folder holds your curriculum expectations. To remove it, turn off “Include {jurisdiction} curriculum pages” first.",
      "why": "Explains why the curriculum folder cannot be removed in the New Course Wizard while curriculum pages are enabled, naming the switch to turn off first."
    },
    "curriculumFolderBlockedByCoverageMap": {
      "reason": "This folder holds your curriculum expectations for the coverage map. To remove it, turn off “Include the curriculum coverage map” first.",
      "why": "Explains why the curriculum folder cannot be removed in the New Course Wizard while the coverage map is enabled, naming the switch to turn off first."
    },
    "lastGradedFolderBlocked": {
      "reason": "At least one folder must count for marks while the curriculum coverage map is enabled. To remove or uncheck this folder, choose another graded folder under Marks first, or turn off “Publish the curriculum coverage map”.",
      "why": "Explains why the last graded folder cannot be removed or un-ticked in Course Settings while the coverage map is enabled, preventing an empty marks pool."
    },
    "lastGradedFolderBlockedWizard": {
      "reason": "At least one folder must count for marks while the curriculum coverage map is enabled. To remove or uncheck this folder, choose another graded folder under Marks first, or turn off “Include the curriculum coverage map”.",
      "why": "Explains why the last graded folder cannot be removed or un-ticked in the New Course Wizard while the coverage map is enabled, preventing an empty marks pool."
    },
    "classFolderBlocked": {
      "reason": "“All Classes” holds your class pages and lessons — the pages the next-class button and the schedule write to. It cannot be removed; other per-section folders can.",
      "why": "Explains why the folder named All Classes can never be removed, in Course Settings or the New Course Wizard. Russell's decision, 2026-08-24: exactly that folder is never removable; every other per-section folder can be added or removed as before. The name is compared case-insensitively and is ClassFolder.fallbackName."
    },
    "lastPerSectionFolderBlocked": {
      "reason": "Each section needs at least one folder for its class pages and lessons. Add another per-section folder first before removing this one.",
      "why": "Explains why the last per-section folder cannot be removed, ensuring each section has a folder for its lessons."
    },
    "sectionIndexFileBlocked": {
      "reason": "Every section needs an index.md page for its home page. Without it, the section cannot be published.",
      "why": "Explains why index.md cannot be removed from per-section files."
    },
    "removeGradedFolderConfirmation": {
      "title": "Remove “{name}”?",
      "message": "This folder holds work that counts for marks. Removing it will take it out of your course’s marks pool.",
      "why": "Prompts for confirmation when removing a graded folder when other graded folders exist."
    },
    "removeCurriculumFolderConfirmation": {
      "title": "Remove “{name}”?",
      "message": "This folder holds your curriculum expectations. Removing it means expectations will not be available if you later enable curriculum coverage.",
      "why": "Prompts for confirmation when removing a curriculum folder when curriculum coverage is off."
    },
    "renameFolder": {
      "sheetTitle": "Rename “{name}”",
      "explanation": "This renames the folder on your Mac — in every section that has one — and points your pages’ links at the new name. It happens straight away, so Cancel in Settings will not undo it.",
      "problems": {
        "empty": "Type the folder’s new name.",
        "unchanged": "That is already this folder’s name.",
        "hasSeparator": "A folder’s name cannot contain “/” or “:”.",
        "isHidden": "A name starting with a dot makes the folder hidden, and Plantoir would stop finding it.",
        "isMedia": "Plantoir looks after the Media folder itself, so nothing else can be called Media.",
        "alreadyUsed": "This course already has a folder called “{name}”.",
        "looksLikeASection": "“{name}” is what Plantoir calls a section’s own folder, so it cannot be used here.",
        "destinationExists": "There is already something called “{name}” beside it. Move or rename that first."
      },
      "done": "“{old}” is now “{new}”.",
      "doneRelinkedOne": "One page had links pointing into it, and they now point at the new name.",
      "doneRelinkedMany": "{count} pages had links pointing into it, and they now point at the new name.",
      "doneRelinkedNone": "No page linked into it by name, so nothing else needed changing.",
      "doneNothingWasThere": "There was no folder by that name on your Mac, so only this course’s settings changed. Make it in Obsidian when you need it.",
      "carriesAcross": [
        "shared_folders",
        "per_section_folders",
        "graded_folders",
        "curriculum_folder",
        "class_folder",
        "hidden",
        "expandable",
        "excluded_items"
      ],
      "hiddenIsTheDangerousOne": "`hidden` holds the folders and files kept OUT of the built site. A rename that does not carry it across silently UN-HIDES the folder, and the next publish puts pages the teacher deliberately hid in front of students. Missed until the real app was driven on 2026-09-04, on a course whose `hidden` list named its Curriculum folder. `expandable` beside it is only cosmetic — the sidebar's expansion state — but is carried for the same reason: a key that names a folder and is not rewritten leaves the configuration describing a folder that no longer exists.",
      "materialisesOnRename": {
        "keys": ["class_folder", "curriculum_folder"],
        "rule": "Renaming the folder a course currently RESOLVES to as its class folder or its curriculum folder writes that key, even on a course that never had one.",
        "why": "Both are found by guessing at the name when the key is absent — 'class' in it, or 'curriculum' in it — and a course made from scratch has `curriculum_folder: null` and no `class_folder` at all. Rename `Curriculum` to `Expectations` without writing the key and the guess stops finding it: the map is built from nothing, and NOBODY IS TOLD, because from the coverage check's point of view the folder was never there. Carrying an existing key across is not enough; the key has to be created. A rename is the one moment Plantoir witnesses the change, which is the whole reason renaming belongs in the app rather than in Obsidian."
      },
      "why": "Renaming was the gap behind the whole special-names family: the list editors changed course_config.json and never touched disk, so Add wrote an entry pointing at no folder and Remove left a folder full of the teacher’s work unreferenced — and a rename was only possible in Obsidian, after which preflight discovered the new name and APPENDED it, leaving the config naming both. Renaming from inside Plantoir is the one place a rename can be WITNESSED, which is what lets every key that names the folder be carried across in the same breath. One constraint is not arbitrary and must not be relaxed without replacing what it protects: it commits to disk immediately rather than waiting for Save, because the folder really has moved and a Cancel that appeared to undo it would be a lie; only the keys in carriesAcross are written, so a teacher’s other unsaved edits stay unsaved. **A SECOND constraint was here for a day and is gone — do not put it back.** The class folder had to keep a name containing “class”, because ClassFolder found it by that substring. Russell, 2026-09-01: that is Plantoir’s vocabulary imposed on a teacher’s, and somebody whose units are Threads and whose classes are Days calls the folder “All Days”. The lookup was what was at fault. A recorded `class_folder` replaced the substring guess, and this rename MATERIALISES it — see materialisesOnRename. A rule enforced because a lookup was weak is a rule to delete once the lookup is fixed."
    },
    "addCreatesTheFolder": {
      "message": "Plantoir made the folder “{name}” for you. Open it in Obsidian to put pages in it.",
      "why": "Adding a name used to write a config entry pointing at nothing: the folder had to be created in Obsidian afterwards or the entry named a folder that did not exist. Creating it is what makes the list mean what it appears to mean. Nothing is put INSIDE it — an empty folder is the honest starting state, and inventing a page would put words in the teacher’s mouth."
    },
    "removeLeavesTheFolderOnDisk": {
      "message": "“{name}” and everything in it stays on your Mac — this only takes it off your website. Add it back here to include it again.",
      "why": "Removal excludes; it has never deleted anything, and teachers could not tell. Saying so is what makes the button safe to press, and it is also the sentence that explains why a folder they “removed” is still sitting in Obsidian. Deleting the folder was rejected outright: it holds the teacher’s work, and no button in Settings should be able to destroy it."
    },
    "curriculumFolderResolution": {
      "cases": [
        {
          "configured": "Ontario Curriculum",
          "folders": [
            "Concepts",
            "Ontario Curriculum",
            "Tasks"
          ],
          "resolved": "Ontario Curriculum",
          "why": "Configured name wins."
        },
        {
          "configured": "Expectations",
          "folders": [
            "Concepts",
            "Expectations",
            "Tasks"
          ],
          "resolved": "Expectations",
          "why": "Configured name wins even when it does not mention the word curriculum."
        },
        {
          "configured": null,
          "folders": [
            "College Board Curriculum",
            "Ontario Curriculum",
            "Tasks"
          ],
          "resolved": "College Board Curriculum",
          "why": "Unconfigured: alphabetically first folder containing 'curriculum' wins."
        },
        {
          "configured": null,
          "folders": [
            "Concepts",
            "Curriculum",
            "Tasks"
          ],
          "resolved": "Curriculum",
          "why": "Unconfigured single curriculum folder."
        },
        {
          "configured": null,
          "folders": [
            "Concepts",
            "Tasks"
          ],
          "resolved": null,
          "why": "No curriculum folder present."
        }
      ],
      "note": "Which folder the GUI protects as the curriculum folder, computed from the config's shared_folders list: if `curriculum_folder` is configured and in the list, it is used; otherwise the alphabetically first name containing 'curriculum' (case-insensitive). This is the NAME half of build_site.py:_find_curriculum_folder, which additionally scans the vault on disk and accepts a folder only if it holds a page whose title is an expectation code — a check the apps cannot make from the config alone. So the GUI may protect a folder the build would skip (one with no expectation pages yet); it never protects a different folder than the build would pick among those it can see."
    }
  },
  "stopPreview": {
    "note": "Which processes belong to a section's preview, and must therefore be stopped. This was answered THREE times before it was written down here - `preview.sh --stop` swept working directories inside the container, `preview.ps1 --stop` matched command lines natively, and `build_site.py` matched command lines again from inside the container because it cannot call either host script (both are HOST scripts; it is not). The reason for the third copy is sound and has not gone away. What went wrong is what always goes wrong: they drifted, and by the time anyone looked, only one of the three walked the process tree and one had a prefix bug that stopped the wrong section.",
    "whatWasNotObvious": "The three were not three copies of one rule - they were three PARTIAL rules, and picking any one of them as 'the' implementation would have shipped that one's blind spot everywhere. A working directory catches what a command line cannot (a child launched by a relative path carries no directory: `npm install` runs exactly that way). A command line catches what a working directory cannot: the Python driver itself, which never calls os.chdir and so sits in the container's own folder for the whole build. So the shared rule is a DISJUNCTION of evidences plus a walk down the process tree, and it stops strictly more than any of the three did on its own.",
    "modes": {
      "everything": "The launcher's `--stop`. The teacher, or the app, has ended a preview and the container-side work must be reclaimed: the server, the build, the driver, and every child of them. A mid-flight build is most of the point - ending the host-side script leaves it burning CPU for a preview nobody is waiting for any more.",
      "servingOnly": "`build_site.py --build-only`. A build for PUBLISHING is about to start and the only thing that must go is the preview SERVER, which would otherwise overwrite what was just built about a second later through its own host mirror. A build must never be stopped here, because the build being protected is itself a build of this section. Implemented today by `build_site.py` inside the container, and there ONLY: run natively on Windows there is no `/proc`, the process list comes back empty and this does nothing, so the overwrite race is still live on that platform. That is a gap, not a decision — see WINDOWS-HANDOFF.md item 20."
    },
    "identity": {
      "evidences": [
        {
          "evidence": "workingDirectory",
          "rule": "The process's working directory IS the section's build directory, or is inside it.",
          "catches": "Children launched with cwd= and no directory on their command line - `npm install`, and the esbuild workers under it.",
          "modes": [
            "everything"
          ]
        },
        {
          "evidence": "commandLineNamesTheDirectory",
          "rule": "The command line contains the section's build directory, ending at a path separator, a quote, whitespace, or the end of the line - NEVER as a bare substring.",
          "catches": "The serving node, which the launcher runs by absolute path precisely so that this works.",
          "modes": [
            "everything",
            "servingOnly"
          ]
        },
        {
          "evidence": "commandLineIsTheDriver",
          "rule": "The command line names `build_site.py` and carries `--course` and `--section` whose VALUES, read as arguments rather than as substrings, are this course and this section.",
          "catches": "The build driver itself, during every phase it does in its own process - which is the only thing running for much of a build.",
          "modes": [
            "everything"
          ]
        }
      ],
      "descendants": "Every descendant of a matched process is matched too, repeatedly until no new child appears - the chain is driver -> npm -> node -> esbuild, so one pass is not enough. This is the half Windows had and the mac did not, and it is what catches a child spawned by a relative path that no evidence above can see.",
      "boundaries": "Every path and argument comparison ends at a boundary. `.../section1` is a prefix of `.../section10` and `--section=1` is a prefix of `--section=10`; both have already stopped the wrong section on one platform. A boundary is a path separator, a quote, whitespace, or the end of the string - and allowing the end of the string is safe precisely because what follows `section1` in `section10` is `0`, which is not one.",
      "separators": "Both `/` and `\\` count, on both platforms. The container never sees a backslash and a native Windows run passes POSIX spellings around in places; accepting either costs nothing.",
      "caseInsensitive": "Every comparison is case-insensitive, on BOTH platforms. Windows filesystems are case-insensitive but case-preserving, so a folder stored as `Ada1o` against a command line saying `ADA1O` is an ordinary Windows state. Making the rule case-sensitive on POSIX only would mean a case here could have two different right answers depending on which suite ran it, and then it is not one rule.",
      "spellings": "A caller passes EVERY spelling of the directory it knows, because `courses/<CODE>/.merged_output/section<N>` is a symlink to the builds folder outside the working folder and a working directory is always the resolved path. When the built site moved, a sweep that knew only the symlinked spelling matched nothing and reported success while the build ran on.",
      "neverStop": "Never the calling process, and never pid 1 - in this image pid 1's working directory can be inside a section's folder, and stopping it takes the container down with every other section's work in it.",
      "evidenceAPlatformMayNotHave": "A case carries `needsEvidence` when it can only be decided by evidence some platform cannot see — today that means `workingDirectory`, which `Win32_Process` does not expose. A runner that has the evidence MUST run the case; a runner that does not MUST skip it and say which evidence was missing, rather than recording a pass. Cases without the key are every platform's to answer.",
      "repeatedFlags": "A repeated `--section` or `--course` is read as the FIRST occurrence, while `build_site.py`'s own argparse would take the LAST. Nothing produces a repeated flag today — every caller builds the argument list itself — so this is recorded rather than fixed, and is the thing to change if a caller ever starts appending."
    },
    "notShared": [
      {
        "thing": "How the process list is obtained",
        "mac": "`/proc`, read inside the container, which is where both of the mac's paths run.",
        "windows": "`Get-CimInstance Win32_Process`, natively. It exposes no working directory at all. Where a process can be reached another way — its command line, or the descendant walk from a parent that WAS matched — a platform without that evidence still reaches the same verdict, and proving that is most of what these cases are for. Where it cannot, the case says so in `needsEvidence` and a runner without that evidence SKIPS it, naming the reason. A skip is honest; a case quietly passing because nothing ran it is not."
      },
      {
        "thing": "How a process is ended",
        "mac": "SIGTERM, then SIGKILL a second later - ask, then insist. A node server closes its sockets and a Python driver runs its own cleanup.",
        "windows": "`Stop-Process -Force`. Windows has no SIGTERM to send, so there is nothing to ask with. Deliberately not shared rather than papered over: the rule is WHICH processes, not how they end."
      },
      {
        "thing": "Which processes are even considered",
        "mac": "Every process in the list. The evidences below are the whole filter.",
        "windows": "`node.exe` and `python.exe` only, for DIRECT evidence — a pre-existing narrowing in `preview.ps1`, recorded here rather than left to be discovered. It is a real difference: a `cmd.exe` running `npm.cmd` with the section's folder on its command line is direct evidence on the mac and is reached on Windows only if some matched process happens to be its ancestor. Worth revisiting there; not worth the mac pretending to have the same filter."
      },
      {
        "thing": "How hard a publish build insists",
        "mac": "`servingOnly` sends SIGKILL at once rather than asking first. The preview server has to be gone BEFORE the build writes anything, and a second spent waiting politely is a second in which its host mirror can overwrite the build — which is the whole failure being prevented. `everything` asks first, because there a build's own cleanup is worth having.",
        "windows": "`Stop-Process -Force` throughout; there is nothing to ask with."
      }
    ],
    "cases": [
      {
        "name": "the preview serving this section is stopped",
        "why": "The ordinary case, and the one both older rules already agreed on.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 101,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8081 --wsPort 9081",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": [
          101
        ]
      },
      {
        "name": "a build for publishing stops the preview that would overwrite it",
        "why": "Serve mode keeps mirroring its own build to the host every second, so a publish build that leaves the server running has its output replaced by the preview - live-reload client and all - about a second after it lands.",
        "mode": "servingOnly",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 101,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8081 --wsPort 9081",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": [
          101
        ]
      },
      {
        "name": "another section's preview is left alone",
        "why": "The regression that caused this rule to be written down. Stopping by PORT killed whatever held the default 8081, which is the first section to have previewed in the folder rather than the one being published: previewing section 1 while publishing section 2 took section 1 down.",
        "mode": "servingOnly",
        "section": {
          "course": "ADA1O",
          "number": 2,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section2",
            "/tmp/quartz-builds/ADA1O/section2"
          ]
        },
        "snapshot": [
          {
            "pid": 101,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8081 --wsPort 9081",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": []
      },
      {
        "name": "section 1 is not section 10",
        "why": "A LIVE BUG when this was written, on the platform that matched with a bare substring test: `.../section1` is a prefix of `.../section10`, so stopping section 1 stopped section 10's preview. Needs ten sections in one course to bite, which nothing refuses.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 110,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section10/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8082 --wsPort 9082",
            "cwd": "/tmp/quartz-builds/ADA1O/section10"
          }
        ],
        "stops": []
      },
      {
        "name": "section 10 is stopped when section 10 is what was asked about",
        "why": "The other half of the boundary: refusing the prefix must not refuse the real thing.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 10,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section10",
            "/tmp/quartz-builds/ADA1O/section10"
          ]
        },
        "snapshot": [
          {
            "pid": 110,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section10/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8082 --wsPort 9082",
            "cwd": "/tmp/quartz-builds/ADA1O/section10"
          }
        ],
        "stops": [
          110
        ]
      },
      {
        "name": "a publish build is never mistaken for a preview server",
        "why": "`--serve` is the only honest separator between the two. Without it a build for publishing would stop ITSELF, since the build it is protecting is also a build of this section.",
        "mode": "servingOnly",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 120,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": []
      },
      {
        "name": "the launcher's stop DOES end a build",
        "why": "The two questions differ here and this is the case that shows it. A mid-flight build is most of what `--stop` exists to reclaim: ending the host-side script leaves it running, burning real CPU for nothing.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 120,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": [
          120
        ]
      },
      {
        "name": "the build driver is found although it sits in the container's own folder",
        "why": "The mac's blind spot for as long as `--stop` existed. `build_site.py` never calls os.chdir - it passes cwd= to its CHILDREN - so the driver stays in /teaching. During every in-process phase (copying the scaffold, copying content, social cards, the rsync mirror) it is the ONLY process there is to find, and a sweep by working directory found nothing and said so.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 200,
            "ppid": 99,
            "name": "python3",
            "commandLine": "python3 -u /opt/scripts/build_site.py --host-os mac --course=ADA1O --section=1 --port 8081",
            "cwd": "/teaching"
          }
        ],
        "stops": [
          200
        ]
      },
      {
        "name": "--section=1 is not --section=10",
        "why": "The same prefix bug by a second route, and live on the same platform: the driver is recognised by its arguments, and `--section=1` is a prefix of `--section=10`. Read the value as an ARGUMENT, never as a substring.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 210,
            "ppid": 99,
            "name": "python3",
            "commandLine": "python3 -u /opt/scripts/build_site.py --host-os mac --course=ADA1O --section=10 --port 8082",
            "cwd": "/teaching"
          }
        ],
        "stops": []
      },
      {
        "name": "another course's build is left alone",
        "why": "Two courses can build at once in one working folder; a section number on its own means nothing without the code beside it.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 220,
            "ppid": 99,
            "name": "python3",
            "commandLine": "python3 -u /opt/scripts/build_site.py --host-os mac --course=ICS3U --section=1 --port 8081",
            "cwd": "/teaching"
          }
        ],
        "stops": []
      },
      {
        "name": "a child launched by a relative path goes with its parent",
        "why": "The reason the rule is a walk down the process tree rather than a question about one process. `npm` and the esbuild workers under it carry no directory on their command lines at all, and esbuild does not even sit in the section's folder - it is reachable only through its parent. Windows found this first and had walked descendants for months while both of the mac's copies did not.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 200,
            "ppid": 99,
            "name": "python3",
            "commandLine": "python3 -u /opt/scripts/build_site.py --host-os mac --course=ADA1O --section=1 --port 8081",
            "cwd": "/teaching"
          },
          {
            "pid": 201,
            "ppid": 200,
            "name": "npm",
            "commandLine": "npm install --no-audit --silent",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          },
          {
            "pid": 202,
            "ppid": 201,
            "name": "esbuild",
            "commandLine": "esbuild --service=0.19.9 --ping",
            "cwd": ""
          }
        ],
        "stops": [
          200,
          201,
          202
        ]
      },
      {
        "name": "a child of a process that does not belong is left alone",
        "why": "The walk must start from a real match. A descendant rule that runs from an unmatched root reaches every process on the machine.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 300,
            "ppid": 1,
            "name": "node",
            "commandLine": "node /opt/other-tool/server.mjs",
            "cwd": "/opt/other-tool"
          },
          {
            "pid": 301,
            "ppid": 300,
            "name": "esbuild",
            "commandLine": "esbuild --service=0.19.9",
            "cwd": ""
          }
        ],
        "stops": []
      },
      {
        "name": "a process is caught by its working directory alone",
        "why": "What a command line cannot see. `npm install` runs with cwd= the section's folder and names no directory at all, so a rule that reads only command lines leaves it running.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 201,
            "ppid": 999,
            "name": "npm",
            "commandLine": "npm install --no-audit --silent",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": [
          201
        ],
        "needsEvidence": [
          "workingDirectory"
        ]
      },
      {
        "name": "a build for publishing does not stop a build by working directory",
        "why": "The same process, the other question. `servingOnly` must ignore the working directory entirely: the build being protected is itself sitting in that folder, and so is every child it has already started. Answerable on every platform despite the working directory in it: the verdict is 'nothing', which a runner that cannot see a working directory also reaches — checked, rather than assumed, by the test that blinds each case and compares.",
        "mode": "servingOnly",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 201,
            "ppid": 999,
            "name": "npm",
            "commandLine": "npm install --no-audit --silent",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": []
      },
      {
        "name": "a publish build does not stop itself",
        "why": "`build_site.py --build-only` calls this rule from inside the very process the driver evidence describes. Driver evidence is not consulted in `servingOnly` for exactly this reason - and the caller excludes its own process id besides, because one guarantee that matters this much is worth having twice.",
        "mode": "servingOnly",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 200,
            "ppid": 99,
            "name": "python3",
            "commandLine": "python3 -u /opt/scripts/build_site.py --host-os mac --course=ADA1O --section=1 --port 8081",
            "cwd": "/teaching"
          },
          {
            "pid": 101,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8081 --wsPort 9081",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": [
          101
        ]
      },
      {
        "name": "the resolved spelling and the symlinked spelling are one section",
        "why": "`courses/<CODE>/.merged_output/section<N>` is a SYMLINK to the builds folder outside the working folder, and a process's working directory is always the resolved path - never the spelling used to get there. When the built site moved out of the working folder, a sweep that knew only the symlinked spelling matched nothing at all and reported success while the build kept running. Every caller passes every spelling it knows.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 130,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /teaching/courses/ADA1O/.merged_output/section1/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8081",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": [
          130
        ]
      },
      {
        "name": "the init process is never stopped",
        "why": "A container's pid 1 is its own entry point, and in this image its working directory is /teaching. Killing it takes the container down with every other section's work inside it.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 1,
            "ppid": 0,
            "name": "sh",
            "commandLine": "sleep infinity",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": []
      },
      {
        "name": "a blank build directory is not a match for everything",
        "why": "An empty string is a prefix of every path, so a caller that loses track of which section it is asking about would sweep the whole container rather than fail. Found by review, and it is the prefix bug one level up: the same mistake about where a path ENDS, made about where it begins.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            ""
          ]
        },
        "snapshot": [
          {
            "pid": 400,
            "ppid": 1,
            "name": "node",
            "commandLine": "node /opt/something-else/server.mjs --serve",
            "cwd": "/opt/something-else"
          }
        ],
        "stops": []
      },
      {
        "name": "a Windows path is matched with backslashes, quotes and any casing",
        "why": "The port sees only this shape, and until this case existed nothing in the contract did — the separators-and-casing rules were claimed in prose and pinned only by the mac's own unit tests. The space in the folder name is deliberate: it is why the path is quoted, and a rule that reads a command line as whitespace-separated tokens would get this wrong.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "C:\\Users\\Russell Gordon\\AppData\\Local\\Plantoir\\builds\\a1b2c3d4\\work\\ADA1O\\section1"
          ]
        },
        "snapshot": [
          {
            "pid": 500,
            "ppid": 400,
            "name": "node.exe",
            "commandLine": "\"C:\\Users\\Russell Gordon\\AppData\\Local\\Plantoir\\builds\\a1b2c3d4\\work\\ADA1O\\SECTION1\\quartz\\bootstrap-cli.mjs\" build --serve --port 8081",
            "cwd": ""
          }
        ],
        "stops": [
          500
        ]
      },
      {
        "name": "a Windows path for section 1 is not section 10",
        "why": "The live bug, in the shape the platform that had it actually sees.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "C:\\Users\\Russell Gordon\\AppData\\Local\\Plantoir\\builds\\a1b2c3d4\\work\\ADA1O\\section1"
          ]
        },
        "snapshot": [
          {
            "pid": 510,
            "ppid": 400,
            "name": "node.exe",
            "commandLine": "\"C:\\Users\\Russell Gordon\\AppData\\Local\\Plantoir\\builds\\a1b2c3d4\\work\\ADA1O\\section10\\quartz\\bootstrap-cli.mjs\" build --serve --port 8082",
            "cwd": ""
          }
        ],
        "stops": []
      },
      {
        "name": "the space-separated spelling of an argument is read too",
        "why": "`build_site.py`'s argparse accepts `--section 1` as well as `--section=1`. The launchers use the `=` form today, so a rule that only understood that one would look correct indefinitely and fail the day a caller changed.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 600,
            "ppid": 99,
            "name": "python3",
            "commandLine": "python3 -u /opt/scripts/build_site.py --host-os mac --course ADA1O --section 1 --build-only",
            "cwd": "/teaching"
          }
        ],
        "stops": [
          600
        ]
      },
      {
        "name": "cancelling a publish stops the build it started",
        "why": "A deploy runs `build_site.py --build-only` as a child, and Cancel calls the launcher's stop. Before the driver became evidence, the mac's sweep could not see that process at all — so Cancel ended the host script and left the build running inside the container. Pinned because the fix is easy to lose: it is a consequence of the driver evidence rather than anything named after cancelling.",
        "mode": "everything",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 700,
            "ppid": 99,
            "name": "python3",
            "commandLine": "python3 /opt/scripts/deploy.py --course=ADA1O --section=1",
            "cwd": "/teaching"
          },
          {
            "pid": 701,
            "ppid": 700,
            "name": "python3",
            "commandLine": "python3 /opt/scripts/build_site.py --course=ADA1O --section=1 --build-only --host-os=mac",
            "cwd": "/teaching"
          },
          {
            "pid": 702,
            "ppid": 701,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          }
        ],
        "stops": [
          701,
          702
        ]
      },
      {
        "name": "a publish build takes the server's own children with it",
        "why": "The descendant walk applies in `servingOnly` too, and nothing said so. A serving node's esbuild workers are not stopped by anything else — the node is what is matched, and the workers hang off it.",
        "mode": "servingOnly",
        "section": {
          "course": "ADA1O",
          "number": 1,
          "directories": [
            "/teaching/courses/ADA1O/.merged_output/section1",
            "/tmp/quartz-builds/ADA1O/section1"
          ]
        },
        "snapshot": [
          {
            "pid": 101,
            "ppid": 100,
            "name": "node",
            "commandLine": "node /tmp/quartz-builds/ADA1O/section1/quartz/bootstrap-cli.mjs build --concurrency 1 --serve --port 8081",
            "cwd": "/tmp/quartz-builds/ADA1O/section1"
          },
          {
            "pid": 102,
            "ppid": 101,
            "name": "esbuild",
            "commandLine": "esbuild --service=0.19.9",
            "cwd": ""
          }
        ],
        "stops": [
          101,
          102
        ]
      }
    ]
  }
}
