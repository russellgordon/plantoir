# GUI Improvement Log

A running log of improvement instructions for the graphical interfaces to
this toolchain. Two purposes:

1. Track what has been asked for and where it stands in the **macOS app**
   (`mac-app/`).
2. Serve as the **specification feed for the Windows app** (`windows-app/`,
   built and shipping since 2026-08-11) — every entry here describes
   behaviour the Windows app must also have, unless marked macOS-only, and
   keeping the two in step is what this feed is now for.

**This log is HISTORY, not a specification.** A row records what was true on
the day it was written and is never edited when the behaviour changes again —
so a sentence quoted in row 183 may have been replaced by row 184, and both
rows are correct about their own day. What is true NOW lives in
[`contracts/`](contracts/README.md): `assist-wording.json` for what the
assistant says, `assist-cases.json` for what must happen. Write tests against
those; read this for why things are the way they are. See "Where the truth
lives" in [`CLAUDE.md`](CLAUDE.md).

**The Windows column is required.** An entry is not finished until "Notes
for Windows port" says something a Windows implementer can use: what to do
differently, what is inherited unchanged, or the trap that would pass review.
"Shared Python, nothing to mirror" is a fine note when it is true; an empty
cell never is. And when a change makes existing Windows guidance wrong,
correcting it — here or in [`WINDOWS-HANDOFF.md`](WINDOWS-HANDOFF.md) — is
part of the change, because stale guidance is followed.

Baseline: both GUIs are thin wrappers. They collect input and present
output; all real work happens in the command-line toolchain (scripts on
macOS; the WSL2-backed equivalents on Windows). See
[`documentation/09-mac-app.md`](documentation/09-mac-app.md) for the design
principle and [`mac-app/README.md`](mac-app/README.md) for architecture.

| # | Date | Instruction | macOS status | Notes for Windows port |
|---|------|-------------|--------------|------------------------|
| 1 | 2026-08-09 | When a course code is entered in the New Course wizard, suggest the default course name the way the command-line wizard does (lookup in `support/ontario_secondary_courses.json`; offer formal and short names). | ✅ Implemented — auto-fills the formal name for known codes (never overwriting a teacher-typed name), with formal/short suggestion buttons. `CourseNameCatalog` + wizard `autoFillCourseName()`; unit + UI tested. | Bundle the same JSON; same rules: uppercase/trim the code before lookup, auto-fill only over emptiness or a previous auto-fill, offer both name variants. |
| 2 | 2026-08-09 | Show live previews of the chosen header and code fonts (fonts bundled with the app, registered at launch — pattern from the Canopy app's `CustomFontList`). | ✅ Implemented — all 18 wizard font families bundled (from google/fonts, OFL/UFL licences included) and registered via `CTFontManagerRegisterFontsForURL` (`BundledFontList`); `FontChoiceEditorView` renders header, body, and code sample lines in the actual typefaces, falling back to the system font if unavailable. Unit test asserts all 18 families register. | Ship the same 18 font files + licences; register per-process at app start (Windows: `AddFontResourceEx`/`PrivateFontCollection` or framework equivalent); render header/body/code sample lines; "Helvetica, Arial" previews as a system sans. |
| 3 | 2026-08-09 | Distinguish previews from settings: font samples inside a rounded rect with a slightly darker background and horizontal padding; no divider between "Header & body fonts" and its sample, between "Code font" and its sample, or between "Colour scheme" and its swatch preview. Add a short prompt explaining the footer box (that you can type in it, and what belongs there). | ✅ Implemented — `SampleBox` (rounded rect, `.quaternary` background, inset); each picker + its preview share one form row so no divider appears between them; `FooterEditorView` adds an explanatory prompt above the box and a placeholder example inside it when empty (used in both settings and the wizard). | Same grouping rule: a setting and its preview belong to one visual row with no separator; previews get a recessed rounded background; the footer editor needs a prompt + in-box placeholder. |
| 4 | 2026-08-09 | Establish the standard: ALL example/preview content goes inside the inset `SampleBox` — including the colour scheme swatches. Short "e.g. …" hints use the "Preview:" text style (callout size, secondary colour) and sit UN-inset directly under their setting: applied to Course code, Course name, and Timetable section numbers in the wizard, and to the "Show section marker" toggle (wizard and per-section settings), moving the examples out of the control labels. | ✅ Implemented — colour swatches moved inside `SampleBox`; new `ExampleCaption` view encodes the hint style; field/toggle labels shortened with captions beneath, sharing the control's form row. | Two-tier standard: rendered previews → inset recessed box; textual "e.g." hints → un-inset caption (small/secondary) directly under the control, never embedded in the control's label. |

| 5 | 2026-08-09 | Bump form sub-headings up a bit (Basics, Appearance); move the "(applied to every section — fine-tune later in Settings)" note out of the Appearance heading to sit beneath it, in the same style as the "e.g." hints. | ✅ Implemented — new `FormSectionHeader` view (title3 semibold title + optional callout/secondary caption) applied to every form section in the wizard, course settings, and per-section settings; the Appearance caption uses the header's caption slot. | Section headers one step larger than body, semibold; explanatory notes about a whole section belong in a caption line under the heading (hint style), never inside the heading text. |

| 6 | 2026-08-09 | Put the wizard's content-structure lists under a "Structure" `FormSectionHeader`, with "(defaults are fine for most courses)" moved below the heading as a hint. | ✅ Implemented — a Structure section with caption "Defaults are fine for most courses"; the four folder/file list editors stay collapsed inside a "Folders and files" disclosure to keep the wizard compact. | Same: Structure section with the caption under the heading; keep the long lists collapsed by default. |

| 7 | 2026-08-09 | The Add (+) button in the four folder/file lists was disabled, blocking additions. Also drop ".md" from the UI (files vs. folders is clear enough) while keeping the extension in the stored config. | ✅ Implemented — the + button is now always enabled (empty input is simply ignored, rather than presenting a dead-looking control); file lists hide ".md" in display and append it automatically on add (typed ".md" is not doubled); the Sidebar Visibility toggles hide it too. `normalizedItemName`/`displayName` helpers are unit-tested; a UI test covers the real type-and-add flow. | Same rules: add buttons always enabled with empty input ignored; ".md" never shown, always stored; appended on add, never doubled; reserved name "Media" rejected. |

| 8 | 2026-08-09 | Text inputs were invisible in macOS's flat form style — use bordered text fields for every text input. | ✅ Implemented — `.roundedBorder` on all TextFields (wizard's code/name/short-label/sections fields, settings' name/short-label, list editors' Add fields, console input) and a rounded stroke around the footer TextEditor. | Every free-text input must look visibly editable at rest (bordered/recessed), including multi-line editors — never rely on the platform's borderless form style. |

| 9 | 2026-08-09 | Add a light grey prompt inside the Structure lists' empty Add fields ("Type new folder name here" / "Type new file name here"), and label the add controls "Add new folder" / "Add new file" instead of a generic "Add". | ✅ Implemented — `TextField(prompt:)` and the add button's label both choose their wording from the list's kind (folder vs. file). | Same placeholder and label wording, keyed to whether the list holds folders or files. |

| 10 | 2026-08-09 | The web preview needs Back/Forward buttons with the usual keyboard shortcuts (Chromium acceptable as a fallback, but not needed), and a toolbar button that opens the preview in the default browser. | ✅ Implemented with WebKit — `WebPreviewController` wraps the web view, mirrors canGoBack/canGoForward for button enablement, and adds Back (⌘[), Forward (⌘]), and Reload (⌘R) toolbar buttons plus trackpad swipe navigation. The existing Open-in-Browser button now opens the page currently shown, not just the site root. | Use the platform web view's own history (WebView2 on Windows has GoBack/GoForward/CanGoBack events); shortcuts Alt+Left/Right per Windows convention; Open-in-Browser opens the current page. |

| 11 | 2026-08-09 | Three preview bugs: ⌘R did not reload; Open in Browser failed in Safari ("server dropped the connection"); previewing a different section/course showed the previous site. | ✅ Fixed — (a) preview shortcuts moved to a Preview menu (`PreviewCommands` + focused value): a focused web view swallows toolbar shortcuts, and ⌘[/⌘] had only "worked" via WebKit's native handling; (b) browser hand-off rewrites localhost → 127.0.0.1, because Safari tries IPv6 ::1 first and the container publishes IPv4 only; (c) the server probe waited on port 8081 immediately, so a STALE server from the previous preview answered first — it now waits for this run's "Launching Quartz preview" line before trusting a response. | WebView2 hand-off should also use 127.0.0.1; put shortcuts on real menu/accelerator entries, not embedded-browser-focused controls; gate the server probe on the new run's own launch announcement, never trust an immediately responding port. |

| 12 | 2026-08-09 | Context menu (right-click / control-click) on each course and section in the sidebar: "Show in Finder" (opens a Finder window at the folder) and "New Terminal at Folder" (opens Terminal with that working directory). | ✅ Implemented — `.contextMenu` on both row types sharing one menu builder; `FolderActions` opens the folder via NSWorkspace and launches Terminal.app with the folder (the drop-on-Dock-icon behaviour). Course rows use the course folder, section rows the `section<N>` folder. | Same two context-menu items per row: "Show in File Explorer" and "Open in Terminal" (Windows Terminal / `wt -d`), using course and section folders respectively. |

| 13 | 2026-08-09 | Choosing an empty folder should offer to initialize it as a new working folder (copying the launcher scripts in), instead of showing the missing-scripts error. | ✅ Implemented — the launcher scripts are bundled as app resources; an empty folder (ignoring .DS_Store) shows "Set Up This Folder", which copies the scripts (executable), creates `courses/`, and opens the New Course wizard. Non-empty folders without scripts still get the error, so existing content is never taken over. The error text now also suggests choosing an empty folder to start fresh. | Bundle the platform's launcher scripts (.bat/.ps1, CRLF); same empty-folder detection and take-over guard; initialization flows straight into course creation. |

| 14 | 2026-08-09 | Above the "This folder is empty…" offer, show the chosen folder's path styled like Finder's Path Bar. | ✅ Implemented — `FinderPathBarView`: each ancestor from the volume root down, with its real system icon (`NSWorkspace.icon(forFile:)`) and localized display name, chevron-separated, horizontally scrollable; accessible as a single "Folder location" element. | Windows analogue: breadcrumb like File Explorer's address bar (drive → folders) with shell icons (`SHGetFileInfo`/framework equivalent). |

| 15 | 2026-08-09 | Messaging rule: the GUI must not use "toolchain", "script", or otherwise hint at command-line machinery — it should feel complete on its own to non-technical users. | ✅ Implemented — reworded the workspace picker (intro, empty-folder offer), workspace validation errors, the content-structure tip, the console's input prompt, the wizard's progress title ("Creating your course…"), and launch-failure messages. File names appear only inside genuine error diagnostics (e.g. a missing app resource). "Netlify" stays: teachers knowingly create that account. | Same rule for all Windows GUI copy: no "script"/"toolchain"/"PowerShell"/"WSL" in normal flows; implementation nouns permitted only inside error diagnostics; keep "Netlify". |

| 16 | 2026-08-09 | Hide the raw output behind a disclosure triangle; show a progress bar with a friendly status while actions run; when the task appears to be waiting for input (which shouldn't happen), show a notice pointing at the disclosure. | ✅ Implemented — `TaskProgressView`: indeterminate bar + plain-language phase text derived from output markers ("Downloading components…", "Building your site…", "Publishing your site…"); console behind a collapsed "Show details" disclosure; a stalled-prompt heuristic (prompt-shaped current line + ≥4 s of silence) drives an orange "A question needs your attention" notice; details auto-expand on failure. Used by preview, deploy, and course creation. | Same layered design: progress + phase label by default, output collapsed, prompt-stall notice, auto-expand on failure. Phase markers may differ per platform launcher output. |

| 17 | 2026-08-09 | The red "this folder isn't set up for class websites" message is redundant — the picker's guidance already covers both cases (existing folder or empty). Never show it; behaviour otherwise unchanged. | ✅ Implemented — choosing a non-empty, non-workspace folder now keeps the picker up silently (a `workspaceIsUnrecognized` state) with no red text; genuine failures (unreadable folder, copy errors) still show. Unit test asserts the no-error behaviour. | Same principle: an unfinished choice is not an error. Red text is reserved for genuine failures. |

| 18 | 2026-08-09 | Expanding "Show details" grew the wizard sheet beyond the screen, pushing the header (and the disclosure itself) out of reach. The top of the sheet must stay fixed; details scroll within the remaining space. | ✅ Fixed — the sheet now has a fixed size (was minimum-only, so it grew to the console's full content height); the console fills the space below the pinned progress header and scrolls internally (`maxHeight: .infinity` within the fixed sheet). | Dialogs hosting collapsible output must be fixed-size (or max-capped); the output pane scrolls internally and can never grow the window. |

| 19 | 2026-08-09 | Brief visual glitch when a preview starts: the progress header collided with the window's title/toolbar area. Also: catch this class of problem with automated UI tests rather than manual back-and-forth. | ✅ Fixed — the toolbar is now STABLE: no conditionally inserted items (all always present, disabled when inapplicable) and Preview/Stop is one item that changes face, since inserting/removing toolbar items makes macOS rebuild the toolbar and briefly mis-lay-out the title area. Guarded by a new XCUITest that clicks Preview against a stub launcher and samples element frames through the transition, asserting the progress header stays below the toolbar. | Keep command-bar/toolbar item sets stable; change state via enable/disable, never insert/remove. Encode layout invariants (content below chrome) as frame-sampling UI tests. |
| 20 | 2026-08-09 | The Done button after course creation must sit bottom-RIGHT (it was bottom-left); apply to all sheets of this type. | ✅ Implemented — the wizard footer follows macOS convention: Cancel bottom-left while cancellable; the affirmative action (Create Course, then Done, both prominent, Done = default key) bottom-right. The wizard is currently the only such sheet; the rule stands for future ones. | Windows convention places the affirmative action bottom-right as well (before/left of Cancel per platform guidelines — follow Windows HIG for exact order). |

| 21 | 2026-08-09 | Make the progress bar deterministic with meaningful milestone text instead of a spinner and "Working…": advance 0 → 1/6 → … → 6/6 with brief labels. Also rename the sheet's closing button to "Close": disabled until the work finishes, then enabled as the default (Return) button. | ✅ Implemented — `TaskMilestones` defines six ordered steps each for course creation, preview, and deploy, matched against markers the scripts really print (all 18 verified against the sources); the bar animates through real fractions, showing "Step 2 of 6" beside the title and the current step's label beneath ("Gathering your content", "Building your site", "Uploading your pages"…). A later marker implies earlier steps, so varying output never stalls or reverses the bar. The wizard's button is now "Close", present throughout, disabled while working, and the default action once finished. | Same milestone model; markers must be re-derived from the Windows launchers' output. Keep the closing button present-but-disabled so the footer never reflows. |

| 22 | 2026-08-09 | Every milestone label should end with "…". | ✅ Implemented — all 18 labels; the shared invariant test asserts the suffix. | Same. |
| 23 | 2026-08-09 | The toolbar's top-trailing + isn't idiomatic macOS. Give the "Courses & Clubs" list Xcode-style +/− buttons along its bottom edge (+ opens the New Course wizard; − removes the selected section, or the whole course when a course is selected — archiving rather than deleting, like the scripts do), plus a Filter field. | ✅ Implemented — the toolbar + is gone; the sidebar has a footer bar with +, − (disabled without a selection), and a Filter field. Removal always confirms first and states plainly that a copy is archived, then zips the folder into `courses/_backups/<CODE>/` (the setup wizard's own convention) before removing it; a section removal also updates `section_numbers`/`num_sections`. Removing a course's only section removes the course, and the dialog says so. Filter matches course code and name, case-insensitively. | Same footer bar (WinUI list commands); archive to the same `_backups/<CODE>/` location; confirm-then-archive, never delete. |

| 24 | 2026-08-09 | The preview layout glitch persisted: starting a preview after one had already been shown pulled the progress header up under the window's toolbar. | ⚠️ Structural fix applied, needs visual confirmation — the detail area is now a ZStack whose base layer (progress/console) stays mounted and laid out in the normal safe-area-respecting flow, with the site preview overlaying it. Previously the two swapped places in one container, so the console inherited the departing full-bleed web view's geometry. Reproduced first with a stub launcher that really serves a page (the web view must have appeared for the glitch to occur). NOTE: XCUITest reports frames for this SwiftUI content that place it outside its own window, so geometry assertions proved untrustworthy; the regression test now checks behaviour (restarting a preview returns to the progress view) and the visual result needs a human eye. | Keep long-lived content mounted and overlay transient full-bleed content, rather than swapping siblings in one container. |

| 25 | 2026-08-09 | Deploy could publish without a preview ever being built. It should check whether a build exists that is newer than the course content: if so publish; if not, build first (silently, without showing a preview) and then publish. | ✅ Implemented — `BuildFreshness` compares the built site's date against the newest change anywhere in the course's content (hidden entries skipped, which excludes the generated output, Netlify markers, and Obsidian settings). Deploy runs `preview.sh --build-only` first when needed, then publishes, presented as ONE task with a combined six-step progress bar; a failed build stops before publishing. | Same rule and the same combined progress presentation; skip hidden/generated paths in the freshness scan. |

| 26 | 2026-08-09 | The whole UI went blank during the "publishing to Netlify" step. | ✅ Fixed — a main-thread stall, not a layout or crash problem (no crash report; the window survived but stopped drawing). The progress header refreshes every second and each refresh rebuilt the ENTIRE transcript into one string several times over, then scanned it for six markers — fine early on, ruinous once a publish had produced tens of thousands of lines. Output now has a single entry point that folds new text into the milestone count as it ARRIVES; the joined transcript is cached and capped at 4,000 lines; the fallback phase scan reads only the tail. Measured: five refreshes over a 5,000-line transcript now take microseconds, guarded by a performance test. | Same rule: never re-scan accumulated output on a UI timer — track progress incrementally as output arrives, cache any joined text, and cap what is retained. |

| 27 | 2026-08-09 | The whole interface went blank while publishing (round two — the earlier performance work did not fix it). | ⚠️ Partial (one real contributor) — the console's `maxHeight: .infinity` resolved to its IDEAL height whenever the height proposal was unbounded, and a scroll view's ideal height is its entire content. With a long publish transcript the view claimed 48,231 points (measured in a unit test); the window's SwiftUI content grew to 1940 points inside a 720-point window and slid 584 points out of view — a fully responsive app drawing a blank window. Fixed by declaring a small `idealHeight` alongside the min/max, so the console still fills available space but never claims content height. Guarded by a test that measures the view's claimed height with a 3,000-line transcript, both collapsed and expanded. | Any scrollable output pane must declare a bounded ideal size, not just a max — otherwise an unbounded proposal makes it claim its whole content. |

| 28 | 2026-08-09 | Blank interface while publishing — root cause identified by making the app name the oversized views. | ✅ Fixed at the root — the app's own logging showed the NavigationSplitView's AppKit scaffolding (NSSplitView, both item wrappers, the sidebar's glass views) sized 762.5, then 746.5, then 1940.5 in a 720-point window: the whole SwiftUI root was being sized by content ideal height rather than clamped to the window, so ANY overgrown descendant dragged the entire interface — sidebar included — off-screen. The root now declares a bounded ideal size alongside its minimum and a `.infinity` maximum, and the progress header's `fixedSize(vertical:)` (an ideal-height amplifier) is gone. | Give the window's root content a bounded ideal size, not just a minimum. Layout faults then stay local instead of moving the whole interface. |

| 29 | 2026-08-09 | Typing into the console froze the interface. | ✅ Fixed — my transcript caching had made `displayText` a MUTATING getter, and the console reads it during view body evaluation: writing observed state there invalidates the view, which re-reads, which writes again. The cache now lives in a reference box so reads mutate nothing (the compiler enforces it — `displayText` is non-mutating). Same root pattern as the earlier stall-detector loop. | Never let a view body write to observed state, including indirectly through a mutating getter or a cached property. |
| 30 | 2026-08-09 | Don't make the teacher open the console and type into a box at the bottom; ask for input in an alert and pass it to the script. | ✅ Implemented — the runner itself notices when a script sits at a question (prompt-shaped final line, no output for three seconds), publishes the question, and the app raises an alert with the question and a text field; Send passes the answer straight to the script. Detection lives in the runner, not a view, so nothing decides it mid-body. | Same: detect the waiting prompt in the process layer and ask with a native dialog; never require opening the log to answer. |

| 31 | 2026-08-09 | "One thing is needed to continue" is a false promise — a task can ask more than one question. | ✅ Fixed — the question alert is titled "A question needs your answer", which stays true whether one question comes or five. | Same: never let a prompt's title claim how many more are coming. |

| 32 | 2026-08-09 | When a publish finishes, give the teacher a clickable link to the live site (or a finishing message of that kind). | ✅ Implemented — the completed task now reads "Your website is live." above a clickable link to the site's address, picked out of the publisher's output ("Live URL:" on first publish, "Site URL:" afterwards). Netlify's own admin pages are ignored so the link is always the teacher's site. Covered by tests for both the found and not-found cases. | Same: parse the published address from the launcher's output and end the task with a link, not just a checkmark. |

| 33 | 2026-08-09 | A prompt's square-bracket default ("Enter Netlify site name [ics3u-s3-2026-gordon]:", and the same when a name is taken: "Choose a different Netlify site name (or 'q' to cancel) [ics3u-s3-2026-gordon-01]:") is command-line shorthand. Take it out of the question and put it in the answer field instead. Title the alerts "Input required". | ✅ Implemented — a bracketed default is lifted out of the wording and pre-filled into the text field, so agreeing is one keystroke and changing it is still easy; "[Default: x]" is understood too, while "[Y/n]"-style choice lists stay in the wording (they name options, not a value). Five tests cover the shapes. | Same: a GUI shows a default as a filled-in field, never as text the user must retype. |

| 34 | 2026-08-09 | "(or 'q' to cancel)" shouldn't appear in a GUI alert. | ✅ Implemented — a parenthetical that names a key to type (cancel/quit/skip) is dropped from the question and the spacing closed up, so the retry prompt reads "Choose a different Netlify site name:". Informative parentheticals such as "(y/n)" are kept, since they carry meaning rather than typing instructions. | Same: a dialog's buttons are the escape hatch; never repeat a keystroke instruction in the wording. |

| 35 | 2026-08-09 | With "(or 'q' to cancel)" hidden, Cancel must still cancel — and should do it through the script's own logic rather than by killing anything. Show a suitable closing message (orange) when it does. | ✅ Implemented — the key the script offered is captured before the aside is hidden, and Cancel sends it, so `deploy.py`'s own cancellation path runs (verified at `scripts/deploy.py:303`, which treats q/quit/exit as cancelling). The header then shows an orange "Cancelling…" while the task winds down and an orange "Cancelled" with "<task> was cancelled." once it stops — not the red failure state, even though the task exits non-zero, and the log no longer springs open. A question the script gives no way out of (the Netlify access token, for one) is cancelled by stopping the task itself, so Cancel always means what it says. | Same: capture the launcher's cancel token, send it on Cancel, and treat the resulting non-zero exit as cancellation rather than failure. |

| 36 | 2026-08-09 | Hide the guts when a task fails: a real publish hit `Netlify API error 429: API Deploy rate limit surpassed`, and the app answered by springing the raw output open. | ✅ Implemented — `FailureExplainer` reads the output and says what happened in a sentence: the rate limit ("Netlify is limiting how often websites can be published right now. Try publishing again in about a minute." — the wait read from the script's own reset line), a missing or refused Netlify token, no internet, and publishing before anything was built. The log stays one click away but no longer opens by itself. When nothing is recognised the app says nothing and shows the output, since an honest fallback beats a confident guess; a test locks that behaviour in. | Same explanations from the same markers; keep the recognise-or-stay-quiet rule. |

| 37 | 2026-08-09 | After a publish finished, starting a preview of the same section left the publish's panel on screen — the preview appeared but the interface never said it was being prepared. | ✅ Fixed — the console area chose its task by asking "has the publish any output?", and a transcript is never emptied, so a finished publish held the space forever. It now shows whichever task is running, or — once both have finished — the one that started most recently, so the publish's summary and live link stay until something else begins. Four tests cover the combinations. | Same rule: pick the panel by which task is current, never by whether a task has left output behind. |

| 38 | 2026-08-09 | The live-site link appeared only after a section's FIRST publish; publishing again to an existing site showed no link. | ✅ Fixed — a new site reports its `ssl_url` ("Live URL: https://…"), while a repeat publish quotes the saved site record, whose `url` is plain **http** ("Site:" / "Site URL:"). The link was found by looking for `https://`, so repeat publishes never matched. It now reads the address from the labelled line whatever its scheme, ignores the "Admin:" dashboard line, and offers the https form of a netlify.app address since Netlify redirects there anyway. A site on its own domain is now recognised too. Four tests cover first publish, repeat publish, custom domain, and dashboard-only output. | Same: find the address by the label the publisher prints, never by guessing at the scheme or hostname. |

| 39 | 2026-08-09 | Stopping a preview reported that something had gone wrong, though the output showed a clean run. | ✅ Fixed — ending a task on purpose kills the process, which exits non-zero, and any non-zero exit was read as failure. A task stopped by the teacher now says "Stopped" with "You stopped this.", shows no failure explanation, and no longer springs the log open. Same principle as cancellation: an ending you asked for is not a fault. | Same: distinguish "ended on purpose" from "failed" before judging by exit code. |

| 40 | 2026-08-09 | After a preview was stopped the panel still read "Preparing the preview of…", which was no longer true; the "You stopped this." line is redundant beside the "Stopped" status. | ✅ Implemented — the panel is named for what it is at the moment: "Preparing the preview of ICS3U-S5" while it is being made, "Preview of ICS3U-S5" once it has finished or been stopped. The extra sentence is gone; the "Stopped" status says it already. | Same: titles that describe an action in progress must be re-read when it ends. |

| 41 | 2026-08-09 | Surface the example course in the app: the command-line setup offers one, but a GUI user never sees it. | ✅ Implemented — the New Course sheet opens with a "New to this?" panel offering **Add Example Course**, which installs the full example without asking anything further, shows its own three-step progress, and — on Close — selects the new course in the sidebar so the teacher lands inside it. The install runs the real setup with a new `--install-example` flag; the code it was given (EXC2O, or an alternate when taken) is read back from the output rather than assumed. | Same panel at the top of the Windows wizard; the launcher flag is shared, so only the button and the code-parsing need porting. |

| 42 | 2026-08-09 | The example course itself was thin — it showed the folder structure but taught nothing and demonstrated little of what pages can do. | ✅ Rebuilt — roughly 225 pages of Grade 9 science (SNC1W), modelled on the structure and pacing of a real course: curriculum expectations for all five strands, 35 concept pages, 14 investigations, exercises with answers, rubric-backed tasks, tutorials, discussions, portfolio prompts, and two sections of class pages a few days apart. Mermaid, KaTeX, callouts, transclusion, tags, checklists, footnotes, and per-section drafts all appear in real use, with `Style/What This Site Can Do` as a source-included reference. Verified by building both sections: 0 broken links, drafts correctly withheld. | The content is platform-independent; nothing to port beyond the button in entry 41. |

| 43 | 2026-08-10 | Clicking **Add Example Course** immediately raised "Input required — Install it now? (y/n):" with no context. Someone who clicked the button has already said what they want. | ✅ Fixed three ways. (1) The example install now passes `--no-update-check`, so nothing asks about updating in the middle of it. (2) The alert shows the line that preceded the question, but **only when the question cannot stand on its own** — a bare yes/no or a very short prompt. See entry 45. (3) The underlying misfire: the check compared the local image's digest against the registry's and treated *different* as *newer*. A locally built image has a digest the registry never issued, so it offered an "update" that was really a downgrade — and answering yes replaced a working build with an older one, which is exactly what broke the button. It now confirms the installed digest actually exists in the registry before offering anything. | Same flag, same context rule, same digest check (mirrored in the .ps1 peers). |

| 44 | 2026-08-10 | With no courses at all, the window said "Select a Course or Section" — instructions for choosing from an empty list. | ✅ Fixed — with an empty workspace the window now reads "No Courses Yet", explains the choice, and offers an **Add a Course…** button that opens the same sheet as the sidebar's +. The old wording returns as soon as there is something to select. | Same: an empty state must offer the action, never instruct the user to pick from nothing. |

| 45 | 2026-08-10 | The new question context made deploy prompts worse: "what is your last name?" arrived buried under three lines of container progress. | ✅ Corrected — context is added only when a question cannot be understood alone (a bare yes/no, or fewer than 25 characters), never to a question that names its own subject. Progress lines are excluded outright: anything ending in an ellipsis or opening with a status mark (✅ 🚀 📦 …) explains nothing about what is being asked, and at most one line is ever shown. Four tests pin the cases: the update prompt keeps its explanation, the last-name prompt gets none, pure chatter yields none, and context never reaches past an earlier question. | Same rule — a GUI prompt should read like a dialog, not like a terminal scrollback. |

| 46 | 2026-08-10 | Context was still intruding: "Enter Netlify site name:" is 24 characters, so the length rule fired and pulled in a stray timestamp line. The rule itself was wrong. | ✅ Removed entirely — no heuristic decides what a question needs. Instead the one question that genuinely could not stand alone was reworded at its source: the launchers now ask "Update the website builder now? (y/n)" rather than "Install it now?". Prompts show exactly what the task asked, nothing more. | Same: fix an unclear prompt where it is written, never by guessing context in the interface. |

| 47 | 2026-08-10 | The app appeared to forget its working folder between launches. | ✅ Fixed, and the cause was the test suite. The folder was always saved (`workspacePath`), but tests called `chooseWorkspace(at:)` with temporary fixture folders and those writes went into the app's **real** preferences — so after a test run the app launched pointing at a folder that had just been deleted. Preferences are now injected, tests use a throwaway store, and one test asserts the teacher's own setting is left untouched. A stored folder that no longer exists is also cleared on launch rather than presented as the working folder. | Same isolation rule for the Windows app's settings store. |

| 48 | 2026-08-10 | The working folder belonged to the app, so two windows could not point at different folders. | ✅ Implemented — each window now owns its `WorkspaceModel` and remembers its folder in `@SceneStorage`, which macOS restores per window. A brand new window opens at the last folder chosen anywhere; a restored window returns to its own. File-menu commands act on the focused window through a `FocusedValues` entry rather than a shared singleton. | WinUI has no direct `@SceneStorage`; persist per-window state keyed by window identity, with the same "new window inherits the last folder" rule. |

| 49 | 2026-08-10 | The example course should quote the Ontario curriculum expectations as published, not paraphrase them. | ✅ Done — all 50 specific and 10 overall expectations are now reproduced word for word from the Ministry's SNC1W document, parsed from the published PDF rather than retyped. Every mention of paraphrasing is gone from the pages, the site footer, and the docs; `About These Expectations` now records the source and the copyright line instead. Codes and tags are unchanged, so every existing link still resolves. | No effect on the Windows port; the content is shared. |

| 50 | 2026-08-10 | On the exercise pages the answers sat in one block at the bottom, staring students in the face the moment they landed. | ✅ Rewritten — all ten pages now pose a question and fold its answer underneath it (`> [!success]- Answer`), so a page of questions stays a page of questions until the student has had a go. 69 foldable answers in all; open-ended questions get a "check yours" fold describing what a good answer contains rather than a number. Many answers now explain the reasoning rather than just stating the result. | Same content; nothing platform-specific. |

| 51 | 2026-08-10 | On a slow connection "Uploading your pages…" sits still for minutes with no sign of progress, though the task itself reports counts. | ✅ Implemented — the step label now reads "Uploading your pages… 125 of 234", taken from the counts the publisher already prints, and the total appears as "0 of 234" as soon as it is known. The count belongs to its step: it clears when the step changes. Two faults surfaced while doing it — the "Uploading your pages" step was matched against a marker that never occurred, so the bar jumped straight from step 4 to step 6; and the in-app interface test had been asserting against the teacher's real workspace since windows got their own model, passing only by luck. Windows now register their model so the test drives the interface that is actually on screen, and nothing under test can write the real preference. | Same: read the counts the launcher prints rather than inventing a spinner. |

| 52 | 2026-08-10 | The sidebar's + and − needed pixel-perfect aim — the minus is a few pixels of thin line. `.contentShape(Rectangle())` had been added but changed nothing. | ✅ Fixed — `contentShape` was applied to the *button*, after `.buttonStyle`, so it reshaped bounds that were already just the glyph. It has to wrap the **label**, and the label needs a `.frame` to have any area to claim: each button is now a 24 × 22 target with the whole rectangle clickable, plus a tooltip. Guarded by a test that reads the buttons' real frames from the accessibility tree — and verified to fail (9 × 4) when the frame is removed, so it is not a test that only ever passes. | Same trap exists in WinUI with icon-only buttons: set a minimum target size rather than relying on the glyph's bounds. |

| 53 | 2026-08-10 | The +/- glyphs were drawn smaller than the same buttons elsewhere on the system, and a fixed point size would ignore the accessibility text-size setting. | ✅ Implemented — the glyphs are 15pt at standard text size, and both the glyph and its target are declared with `@ScaledMetric(relativeTo: .body)`, so enlarging text in System Settings ▸ Accessibility ▸ Display enlarges the button along with it. Fixed sizes would have left someone who enlarges text aiming at the same small target as before. | WinUI scales with the system text-scale factor; use scalable units for both the icon and its minimum target rather than fixed pixels. |

| 54 | 2026-08-10 | Removing a course or section talked about working folders and a `_backups` folder — complexity the GUI is meant to hide — and once removed, the thing vanished with no sign of where it went. | ✅ Implemented — **Archived** is a collapsible section header below Courses & Clubs — the same treatment "Courses & Clubs" itself gets, with the chevron appearing on hover as Finder's "Locations" does — collapsed by default. Its rows read like ordinary sidebar rows ("IZN2O — Section 1", with the course or section icon), newest first, the archive date in a tooltip, and Show in Finder on each. The confirmation still appears and now names the place a teacher can see: "Nothing is deleted. This section moves to Archived, at the bottom of the sidebar, where you can get it back." Nothing changed behind the scenes. One subtlety: the setup wizard writes its own automatic backups into the same place, named by timestamp alone, so only archives named after what was removed are listed — a routine settings change must not look like an archived course. | Same group in the Windows sidebar, and the same filename rule for telling a teacher's archive from a routine backup. |

| 55 | 2026-08-10 | Archived courses and sections could be seen but not brought back. | ✅ Implemented — **Restore…** on an archived row's context menu, in the File menu (enabled when an archived item is selected), and as a button when one is selected in the sidebar; each confirms first and says what will happen. A section can only be restored into a course that is present, because restoring also puts its number back into the course's settings — archiving took it out, and a section on disk that the course ignores is worse than no section at all. Nothing is ever written over: restoring onto an existing course or section refuses and explains, so newer work survives. The archive is removed only on success, since a restored item is no longer archived. Five tests cover both round trips and all three refusals. | Same three entry points and the same refusals; the section-number bookkeeping is the part easy to miss. |

| 56 | 2026-08-10 | Nothing in the window said which folder it was working in — awkward now that each window can be on a different one. | ✅ Implemented — a Finder-style **path bar** runs along the bottom of the detail column, showing the working folder as Macintosh HD › Users › … › folder with each ancestor's real icon and localised name, on a `.bar` background under a divider, exactly where Finder puts its own. It reuses the same view the empty-folder message already used. Verified from a window capture; a test asserts it is on screen and names the folder, but deliberately not where it is — the accessibility tree reports a container's frame for SwiftUI content, so positional assertions measure the wrong rectangle. | Same footer beneath the content pane; build the path from the folder's ancestors rather than printing a raw path string. |

| 57 | 2026-08-10 | The path bar showed the working folder but did nothing when clicked. | ✅ Implemented — double-clicking any folder in the bar opens it in Finder, and right-clicking one offers **Show in Finder** and **Open Folder** for that folder. Right-clicking the strip anywhere else offers the same pair for the working folder itself, so the menu is never a dead click. Each component's hit area covers the gap between its icon and its name, and its full path shows as a tooltip. | Same two gestures; a path bar that only displays is a missed affordance. |

| 58 | 2026-08-10 | Typing a filter that matches nothing emptied the sidebar with no explanation — it read as though the courses had gone. | ✅ Implemented — the sidebar shows "No Matches / No course or club matches “MPM2D”." while the filter hides everything. It appears only when there are courses to hide: with an empty workspace the window's own empty state already explains that, and two messages about the same emptiness would contradict each other. Whitespace alone is not treated as a search. Four tests cover the combinations. | Same message and the same restraint about when not to show it. |

| 59 | 2026-08-10 | Windows did not come back the way they were left: two windows on two folders returned as two windows on one folder, and later as one window. macOS convention is precise restoration — count, position, and (here) working folder. | ✅ Rebuilt on the mechanism SwiftUI actually provides: `WindowGroup(for: WindowFolder.self)` presents each window FOR its folder, so macOS encodes the folder into the window's restoration state and restores each window with its own — `@SceneStorage` was the wrong tool, sharing one value across every window of the group. The app delegate opts into secure restorable state (without which modern macOS quietly declines to restore) and snapshots the open folders at `applicationShouldTerminate`, while the windows still exist — a list rewritten as windows close during quit shrinks to nothing, which was yesterday's second bug. When the system restores nothing (rebuild, Xcode kill, closed-windows-on-quit), a fallback reopens a window per remembered folder, in order. | WinUI has no system restoration; the fallback list IS the mechanism there. |

| 60 | 2026-08-10 | Both windows and both folders came back — swapped between the windows. Positions restored correctly, so the system was restoring frames while the folders were being handed out in appearance order, and restoration order is not creation order. | ✅ Fixed — the remembered list now pairs each folder with its window's **frame**, and a blank window at launch claims the folder recorded for its own frame — the one property the system demonstrably restores per window. Order remains only as the fallback when frames were not restored either. Guarded by a test in which the second window appears first and still gets its own folder. | The frame-matching idea carries over; WinUI restores nothing, so record and reapply both frame and folder. |

| 61 | 2026-08-10 | Cooperating with system restoration kept failing differently each run: values restored empty, folders swapped, then three windows from two. | ✅ The app now restores its own windows and system restoration is disabled (`.restorationBehavior(.disabled)`). At quit it records each window's folder and frame, in order, while the windows still exist; at launch the single window takes the first entry and opens a window for each remaining one, each carrying its folder and frame in its presented value. Deterministic: same count, same folders, same positions, same order, regardless of the close-windows-on-quit setting or how the app last exited. Stored frames are sanity-checked so a corrupt value cannot produce an unusable window. | This IS the Windows design already — no system restoration exists there, so the same list drives both platforms. |

| 62 | 2026-08-10 | The app's own restoration should respect the OS-level choice: System Settings ▸ Desktop & Dock ▸ "Close windows when quitting an application". | ✅ Implemented — restoration replays the remembered windows only when the system setting asks for them back (`NSQuitAlwaysKeepsWindows`; the toggle being ON reads as false, and an absent key matches the toggle's default of ON). The list is still recorded either way, so flipping the setting later restores the most recent session. | Windows has no equivalent OS setting; make it an app preference there, defaulting to restore. |

| 63 | 2026-08-10 | With restoration on, windows and folders both came back but paired wrong — macOS reopens the windows itself (`.restorationBehavior(.disabled)` notwithstanding), restoring frames but not values, in an order of its own choosing. | ✅ Fixed — a reopened window finds its folder by matching its own frame against the remembered pairs, retrying briefly while the frame settles (claiming at first sight of the window was what sank frame-matching before). Order decides only when frames never match — the no-restoration path, where there is nothing to mismatch. The spawn pass runs after claiming has had time to finish and opens only what macOS did not bring back. The claim log line records whether frame or order decided, so any future report answers itself. | Moot on Windows — nothing reopens windows but the app, so value-carried spawning covers everything. |

| 64 | 2026-08-10 | Four windows came back from two: macOS restored both windows WITH their values (the optional-value fix made that work), and the app's spawner opened the remembered list again beside them. Each failure in this saga was being found by hand. | ✅ macOS now owns restoration outright — the spawner and the restoration override are gone, and the app only backfills: a window that comes back without its value finds its folder by settled frame, order as last resort, claims closing shortly after launch so a mid-session window inherits nothing. And the week's failures are now a **scenario test suite** — the swap, the unsettled frame, the four windows, the clobbered list, the stale inheritance, the killed app, and the system setting each replay as a named test, so none needs finding by hand again. What remains manual is only macOS's own quit/relaunch, and the claim log line says which rule decided. | The scenario suite is the porting spec for Windows restoration behaviour. |

| 65 | 2026-08-10 | Presented values restored as ONE shared value: both windows relaunched on the most recently opened folder — the same last-writer-wins flaw @SceneStorage showed, one level down. | ✅ SwiftUI per-window persistence is now out of the folder business entirely: plain `WindowGroup`, and every reopened window gets its folder from the app's frame-keyed list, which is per-window by construction. macOS still restores the windows and their frames and still honours the close-windows setting. The claimant claims at most once, claims close shortly after launch, and under tests the claims never read the teacher's real list — the hosted suite caught the app's own window adopting a remembered folder mid-test. | The lesson for Windows: keep per-window state in the app's own store, keyed by something the platform restores faithfully. |

| 66 | 2026-08-10 | Two windows, two previews: starting the second killed the first — both web views ended up showing whichever preview started last, because everything shared port 8081 and the toolchain kills that port before serving. Comparing two sections side by side is a real teacher need. | ✅ Implemented — each preview runs on its own port. The container publishes 8081–8084 plus 9081–9084 (Quartz's dev server opens a second, live-reload websocket port — the hidden collision even distinct site ports would not fix), older containers are recreated once to gain the range, and `preview.sh --port N` reaches `build_site.py`, which now kills only its own ports. The app leases ports across windows (`PreviewLeases`), refuses a fifth preview and a duplicate of an already-live section politely — the same course/section in a *different* folder is allowed, since that is the compare-two-years case — and releases the lease on stop, failure, or window close. Proven live: sections 1 and 2 of EXC2O served simultaneously on 8081/8082 with distinct content, four listeners (two sites, two websockets), both still up after continued polling. Six lease tests. | Same lease model; the port range is a config constant on both platforms. |

| 67 | 2026-08-10 | Phase 2: two windows on two working folders fought over the single shared container, and containers ran forever whether or not anything used them. | ✅ Implemented — one container per working folder, named `teaching-quartz-<hash of pwd -P>`; the app derives the identical name (via POSIX `realpath` — Foundation's symlink resolution strips `/private` where `pwd -P` keeps it, a mismatch the unit test caught before it stomped a live container). Each container gets its own probed HOST port block mapped to fixed container ports, `preview.sh` announces the resolved address and the app polls what was announced, and port leases contend per folder. Resources are freed: the last window on a folder closing — or a window switching folders, or the app quitting — stops that folder's container (`docker stop -t 2`; it restarts in about a second). The legacy shared container is retired automatically. Proven live: the same section of the same course served from two folders at once (host 8081 and 8091, two containers), and the app-style stop verified. | This design ports directly: name-by-folder-hash, probed port blocks, stop-on-last-window-closed. |

| 68 | 2026-08-10 | First run after the port work failed with "Unknown option: --port" — the working folder's own copy of `preview.sh` was months stale. Folders take a snapshot of the launchers at setup and never see an update again. | ✅ Implemented — whenever the app works in a folder, any launcher that differs from the app's bundled (current) copy is replaced, executable bit preserved. Only files that already exist are touched — a folder with no launchers has never been initialized, which is the picker's business — and test fixtures with stub launchers are left alone. This closes the last of the staleness family: image (offered), container (recreated), launchers (refreshed). | Same refresh on the Windows app, from its bundled .ps1 copies. |

| 69 | 2026-08-10 | With the app closed and nothing else in Colima, the VM's reserved memory kept being spent for nobody. | ✅ Implemented — quitting runs one detached script: stop this app's containers, then, only if `docker ps -q` comes back EMPTY, `colima stop`. The emptiness check is the safety — Colima is shared, and another project's stack must never lose its VM — and the sequencing matters: our containers stop before the check, or the check could never pass. Machines without colima (Docker Desktop) are untouched, and the launchers already restart Colima on the next preview, so recovery is automatic (~20–30 s on that first preview). Tests pin the safety check, the ordering, and the no-colima case. | Windows peer: stop the WSL2 distro (`wsl --terminate`) under the same nothing-else-running check. |

| 70 | 2026-08-10 | The Timetable sections field accepted anything and said nothing: "1 3 5" and "1a,3 4" only surfaced as a generic error at Create — and "1,3 5" was worse, silently parsing as just section 1. | ✅ Implemented — the field validates as you type, in the same orange every other warning wears, with a message per mistake: spaces get the comma hint with THEIR numbers ("Use commas between section numbers — e.g. 1,3,5."), a stray letter is quoted ("“1a” isn’t a section number…"), plus empty-between-commas, zero (worded carefully — a teacher may teach only sections 2, 4, and 5, so the list need not include 1 or be contiguous, and a test pins that), and duplicates. When nothing is wrong the usual example caption returns. Create reuses the same specific message. The silent-drop case has its own regression test. | Same validator and the same warning colour; port the message strings verbatim. |

| 71 | 2026-08-10 | Docker Hub existed only as an update vehicle for the container's scripts — a second updater that could disagree with the app's own (Sparkle, when it lands). Remove the dependency: build the image locally from the recipe. | ✅ Implemented — the app bundles the full build recipe (Dockerfile, patches, scripts, support, launchers) and mirrors it into each working folder's `.toolchain/`; the launchers tag the image `teaching-quartz:src-<hash of the recipe>` and build it locally with BuildKit when the tag is missing. A changed recipe → new tag → rebuild → recreated container, all keyed off the app's version. Deleted outright: `publish.sh`, the digest update checks, the update prompt, `--update-image`/`--tag`/`--local-dev`. First-run messaging: a "Building your website builder…" milestone with live "step 7 of 15" counts parsed from the build's own output. Proven: a recipe-only folder built `src-8605863c` and produced the site; a rerun said "ready"; editing one script changed the tag. The host needs no npm/node/python — everything cooks inside the container. | WinSparkle is the Windows update sibling (same appcast format); the .ps1 launchers already mirror the build-from-recipe logic. |

| 72 | 2026-08-10 | "Normies" cannot be asked to install Homebrew. A first run must need nothing pre-installed at all. | ✅ Implemented — the launchers install their own tools: Colima, Lima, the Docker CLI, and the BuildKit plugin download as pinned static binaries into `~/Library/Application Support/Plantoir/tools` (per-user, no admin rights, ~130 MB), used only when the tool is not already on the machine — Homebrew installs keep winning if present. A fresh VM is created with macOS's own virtualization (`--vm-type vz`), removing the qemu dependency the brew path carried silently. Proven by simulation: from `PATH=/usr/bin:/bin` and an empty home, all four tools self-installed and ran. The image lives inside the Colima VM's disk (`~/.colima`, unmoved — it is shared with other Colima toolchains). The app shows "Getting this Mac ready…" as the first milestone. | Windows peer: winget/MSI silent-install of the WSL2 pieces is the analogue; same "use what exists, fetch what is missing" rule. |

| 73 | 2026-08-10 | Formally name the app **Plantoir**. The user-facing identity — window title, menu bar, Dock, bundle — must all say Plantoir. | ✅ Implemented — the rename is skin-deep by design: `PRODUCT_NAME: Plantoir` with `PRODUCT_MODULE_NAME: QuartzTeachers` renames the app bundle, binary, display name, window title, and log subsystem while the Swift module, source folders, and every test's `@testable import` stay untouched (renaming the module broke the link — the product/module split fixed it cleanly). The bundle identifier becomes `ca.russellgordon.Plantoir`, and on first launch under the new identity the app migrates the teacher's preferences (working folder, remembered windows) from the old `ca.russellgordon.QuartzTeachers` domain so nothing is forgotten. The project file and scheme are `Plantoir`; tools install under `~/Library/Application Support/Plantoir`. 166 tests pass; launch check confirmed the running app is Plantoir. | Windows peer: same split — display/installer name Plantoir, internal assembly/namespace unchanged; migrate registry/AppData settings from the old name on first run. |

| 74 | 2026-08-10 | Sections can be removed but not added after the fact. Context-clicking a course should offer "Add Section…", with a stepper-and-number-field dialog, integer entry only, and a warning (with a chance to correct) when the section already exists. | ✅ Implemented — "Add Section…" (`doc.badge.plus`) sits atop the course's context menu and opens a small sheet (an alert cannot host a stepper): a number field that silently drops any non-digit as it is typed, a stepper that walks the value and never goes below 1, and a sentence saying which sections the course already has. The field pre-fills with the smallest free number, validation runs as you type in the same orange every warning wears ("Section 2 of ICS3U already exists."), and Add stays disabled until the entry names a section that can actually be added — a failure from the add itself lands in the same warning slot with the sheet still open, so there is always a chance to correct. The scaffolding creates `sectionN/index.md` plus the per-section folders and files the course's settings call for — and each file copies its frontmatter from the matching file in an existing section (only `created:` freshened, the index title's section number swapped), so a page the siblings keep as an unpublished draft starts as a draft here too, and flags like `enableToc` carry over; the first live try published Private Notes and Scratch Page and doubled the grade ("Grade 9 Grade 9 Science") by rebuilding titles from parts, both now pinned by tests. Only a course with no sibling to imitate falls back to the wizard's plain template, grade prefix included (skipped when the course name already leads with it, and no grade for clubs). Nothing is ever written over — a stray `sectionN` folder on disk stops the add, since it may hold real work. Afterward the sidebar reloads and selects the new section. Seven tests cover the scaffolding, the refusals, the suggestion, and the wording. | Same sheet and validator on Windows; the scaffolding logic is pure file writing, portable as-is. |

| 75 | 2026-08-10 | The File-menu command "Change Working Folder…" should read "Open Working Folder…" and answer to ⌘O. | ✅ Implemented — renamed and given the standard Open shortcut, matching the macOS convention that ⌘O opens the document a window works on (here, the working folder). The path bar's context-menu "Open Working Folder" (which reveals the folder in Finder) is a different action in a different place and keeps its name. | Ctrl+O on the Windows peer's File menu. |

| 76 | 2026-08-10 | The path bar's context menu should not offer "Open Working Folder" — and then, sharper: ONLY the folders in the path bar should have context menus at all. Right-clicking a chevron and being offered the working folder is just confusing. | ✅ Implemented — the strip-wide context menu is gone entirely. Each folder in the path carries its own "Show in Finder" and "Open Folder" (as it already did), a chevron or the blank strip offers nothing, and ⌘O (Open Working Folder…) is the File-menu way to switch folders. | Same rule on the Windows path bar: menus on the folder crumbs only. |

| 77 | 2026-08-10 | "Show in Finder" on a course or section in the sidebar OPENED the folder, while the same words in the path bar reveal the folder highlighted in its parent. One phrase, one behaviour. | ✅ Implemented — the sidebar's action now reveals the folder selected in its parent window, exactly as the path bar (and Finder itself) does. Opening a folder's contents remains available where it is labelled as such. | One "reveal in Explorer" helper on Windows (`explorer /select,path`), used by every Show-in-Explorer item. |

| 78 | 2026-08-10 | The command-line wizard lets a teacher type a CUSTOM emoji for each section's header; the GUI only offered twelve presets. Both the New Course wizard and per-section settings need custom entry — ideally through the macOS emoji picker itself. | ✅ Implemented — one `EmojiChoiceField` serves both places: a Presets menu (the wizard's twelve), a small text field holding the current emoji, and a smiley button that focuses the field and summons the system emoji palette (`orderFrontCharacterPalette`), so whatever is chosen there lands in the field. The field is hard-limited to one emoji: it empties itself when the palette opens (so the choice arrives into a clean field), any edit settles instantly on the emoji that DIFFERS from the current choice (an insertion can land on either side of what was there, so "most recent in the string" is not enough), a field left empty restores the current choice when focus leaves, letters and digits are ignored (Unicode calls digits emoji, so the check insists on emoji presentation for lone scalars), and the modern families all count: skin tones, ZWJ sequences like 🧑‍🚀, flags, keycaps. Five tests pin the judgement, including one that every preset passes its own field's check. | Windows peer: Win+. summons the system emoji panel into the focused field — same component shape, same one-emoji settling logic. |

| 79 | 2026-08-10 | Tooltips for the window's top-trailing toolbar buttons, matching the hover labels controls elsewhere in the app already show. | ✅ Implemented — all six section-view toolbar buttons carry `.help()` tooltips in plain words: Back/Forward/Reload on the page, "Preview this section's website" / "Stop previewing this section" (the label follows the button's current face), "Publish this section's website", and "Open this preview in your web browser". No machinery words, per the standing rule. | Same strings as ToolTip text on the Windows toolbar. |

| 80 | 2026-08-10 | Context-clicking a course or section should offer "Open in Obsidian" — the vault the folder belongs to (a vault being just a folder of text files) — and the same action belongs in the window's top-trailing toolbar, tooltip included. First try errored: "Vault not found." | ✅ Implemented, in two rounds. Open in Obsidian sits in its own menu section — a course's menu reads Add Section… ⁄ divider ⁄ Open in Obsidian ⁄ divider ⁄ the folder commands, and a section's menu leads with it. The action lives in three places — the course/section context menu ("Open in Obsidian", `square.and.pencil` — the classic macOS compose glyph (`long.text.page.and.pencil` was tried and judged too busy); the button leads the section toolbar's trailing group, ahead of Preview/Deploy/Open in Browser), the section view's toolbar, and a new toolbar on the course settings view — each disabled when nothing answers `obsidian://` links, with the link's path percent-encoded (test-pinned). The hard-won part: `obsidian://open?path=…` only works for folders inside a vault Obsidian has REGISTERED in `~/Library/Application Support/obsidian/obsidian.json` — a `.obsidian` folder alone proves nothing (verified live), and a running Obsidian keeps its vault list in memory, ignoring registry edits until relaunch. So an unknown course is handled the long way: quit Obsidian if running (it restores its windows), seed the toolchain's bundled `.obsidian` defaults if the course has none, write the registry entry exactly as Obsidian's own "Open folder as vault" would (quitting BEFORE writing so the entry cannot be clobbered on exit), then fire the link. For a section, the vault registered is the COURSE folder, and because Obsidian opens files rather than folders (a bare folder path gets "File not found"), the link points at the section's landing page (`index.md`), falling back to the vault root when there is none. And so the teacher can SEE where they landed, the seeded defaults now include a `workspace.json` (authored by Obsidian itself, with the File Explorer's "auto-reveal current file" turned on — a layout-state flag, not an app.json setting, found by reading Obsidian's own code) so the landing page's folder highlights in Obsidian's sidebar. Round two, after a live test: Obsidian REBUILDS the layout on a vault's first open, discarding a pre-seeded one — but patching the layout Obsidian itself wrote survives a relaunch (verified). So whenever the app opens a vault while Obsidian is closed, it flips `autoReveal` on in the saved layout (everything else untouched — test-pinned); by the second open every vault reveals. The toolchain's seed stays for the day Obsidian honours it. Tests pin the containment check (a sibling folder whose name merely starts the same is not "inside"), the entry format, and that a teacher's existing vaults survive the write. Separately: the example-course installer never copied `.obsidian` in (the wizard did) — fixed in `setup_course.py`, verified locally. | Windows: same URI, same registry file under %APPDATA%/obsidian/obsidian.json — port the register-then-relaunch dance whole. |

| 81 | 2026-08-10 | The sidebar's − button stopped working: a highlighted course or section, a click, and no confirmation appeared. | ✅ Fixed — the Add Section sheet, attached after the sidebar's stack of alerts, silently broke the two that still used SwiftUI's DEPRECATED `.alert(item:)` API; the button's logic ran perfectly while its alert never presented. Both legacy alerts (removal and restore) now use the modern `.alert(_:isPresented:presenting:)` form. The real lesson is the regression test: the accessibility inspector gained a same-process `press(identifier:)`, and a hosted test now selects a course in the REAL window, presses the actual − button through the accessibility tree, and requires the confirmation alert to appear — presentation bugs like this one can no longer hide behind correct logic. | Presentation APIs age differently per platform; the Windows peer needs the same press-and-look regression test, whatever its dialog API. |

| 82 | 2026-08-10 | Polish: the Icon Composer icon becomes the app icon, and the stock About box is replaced with the custom panel designed in a companion chat. Round two: include the description text from the companion notes (tagline, the plantoir/dibber namesake, credits), scale the whole panel down — it felt big — and make it respect accessibility text sizing. | ✅ Implemented — `Plantoir.icon` lives in the repo and compiles straight into the bundle (`Assets.car` + `.icns`, `ASSETCATALOG_COMPILER_APPICON_NAME`); XcodeGen 2.46 types Icon Composer bundles natively (`wrapper.icon`), no folder-reference tricks. The About panel follows the companion notes with two deliberate deviations: it shows `NSApp.applicationIconImage` — the REAL compiled icon, shaped and margined by macOS — instead of a separate flattened `AboutIcon` asset (never drifts, nothing extra to maintain, no double-mask halo), and the support details are the notes' own — `plantoir.app/support` and `support@plantoir.app` (the domain is not yet registered; fine pre-release, must exist before shipping) — with the email row an optional so it can be hidden by setting nil. The panel now tells its story: the one-line tagline, the namesake paragraph (a plantoir is a dibber — the tool that opens a hole at the right depth so a seedling can take root), the support link, then quiet credit lines (Quartz by Jacky Zhao — a live link — Russell Gordon, Claude; the MIT sentence was later dropped) above the copyright, with `focusEffectDisabled()` so the panel does not open with a focus ring haloing the support link. Everything scaled down (128 pt icon, 26 pt name, 12 pt body, 540 pt panel) and EVERY measurement is a `@ScaledMetric(relativeTo: .body)` — icon, type, and panel width grow together under Accessibility ▸ Display ▸ Text size, the same pattern as the sidebar footer. Also fixed per the notes' warning: Info.plist carried HARDCODED version literals — now `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)` from build settings, so bumps actually reach the About box. | Windows peer: same panel layout; version from the assembly info, icon from the executable's own resources. |

| 83 | 2026-08-10 | Regression: creating a course failed with exit 127 — "run_container_with_mount: command not found". And the progress dialog said "Creating your course" twice when details were showing. | ✅ Fixed, three layers deep. (1) `setup.sh` lost the whole container-helper trio (`run_container_with_mount`, `find_free_port_block`, `retire_legacy_container`) in the per-folder-container refactor — preview and deploy got them, setup did not, and only course creation exercises setup's create path (the PowerShell mirror had them all along). Restored. (2) The gate that should have caught it, `verify.sh`, never actually runs `setup.sh` — it now cross-checks that every helper a launcher calls is defined in that same file (comments and echoed prose excluded; proven to flag the broken file and pass the fixed ones). That check immediately caught bug three: `preview.sh`'s image pre-parser stopped at the first non-flag word, so `--image` after the course and section was never recognized — verify.sh's own preview run had been failing. Both parser halves fixed; full verify.sh green. (3) The dialog: the details console no longer repeats the task title — the header names the task once, and the detail row keeps only the precise outcome (spinner, Finished, or the exit code). | The .ps1 launchers were already correct; port the verify cross-check idea to any future Windows verify script. |

| 84 | 2026-08-10 | A new window opened while no other window exists should have NO working folder (the picker); a new window opened while others exist should adopt the folder of the window that was key when the command ran. | ✅ Implemented — the model no longer silently adopts the last-used preference at all (that lone-new-window surprise is gone; the choice stays recorded only for preference migration). Each window notes its folder whenever it becomes key; a new window whose restoration claims come up empty inherits that noted folder — falling back to any open window's folder if the memory is stale, and to nothing when it truly is the only window. Mid-session windows also skip the claim retries once claims have closed. Round two, after a janky flash of the picker: the mid-session folder decision moved to the window's `onAppear`, before its first frame commits. (A first attempt decided it in the model's initializer — WORSE: the window-group closure runs on every render, so the decision ran at the wrong moments and the flash grew longer. Initializers in a window group's content are the wrong place for once-per-window work.) Launch-time windows still wait for frame-matched restoration; pinned by test. Window RESTORATION is untouched: restored windows still get their own folders from the frame-keyed list. Policy pinned by tests; the superseded remembered-folder tests rewritten to assert the new behaviour. | Same policy on Windows: new window inherits the active window's folder, or opens the picker when first. |

| 85 | 2026-08-10 | After picking an empty folder, the picker still says "Choose Your Working Folder" above the confirmation — but a folder was just chosen. Show only the path bar and everything beneath it once a folder is selected. Round two: the path bar was not horizontally centred with the rest. | ✅ Implemented — the icon, headline, and intro paragraph appear only while a folder is still being chosen; once a candidate is awaiting confirmation, the view is just the path bar, the set-it-up explanation, and the two buttons. The bar itself centres via `ViewThatFits`: its scroll view greedily fills any width it is given (pinning a short path left), so a path that fits renders at natural size — centred by the stack — and only a path too long for the 520 pt cap gets the full-width scrolling form. An invalid choice (a problem message) keeps the header, since the teacher is back to choosing. | Same conditional header on the Windows picker. |

| 86 | 2026-08-10 | Small but important: Private Notes.md and Scratch Page.md must be created with `draft: true` — that is what keeps them from being published. | ✅ Implemented in both creation paths. The setup wizard (`setup_course.py`) now writes the two teacher-eyes-only pages as drafts, with body text saying so ("never published — a private place for your own notes"); every other per-section file still starts published. The app's Add Section keeps its sibling-mirroring (an existing section's draft flags already carry over) and now applies the same rule in its no-sibling fallback, pinned by test. Key Links.md deliberately stays published. | The rule lives in the shared Python and in `SectionAdder.unpublishedFileNames` — keep the two lists in step on Windows too. |

| 87 | 2026-08-10 | A new ADVANCED per-section setting: a custom domain. When set, every link to the published site shows the teacher's domain instead of the Netlify address. Post-creation settings only — the new-course wizard stays untouched. | ✅ Implemented — each section's settings gain a collapsed "Advanced" group holding a Custom domain field: entries are cleaned on the way in (a pasted `https://ics3u.school.ca/` stores as `ics3u.school.ca`), a gentle orange note appears when the entry does not read as a domain, and the caption says plainly that the site must already answer there (set up in Netlify first). Stored per section under `custom_domains.sections` in `course_config.json`. When a deploy finishes, the live-site link swaps its host for the domain — path preserved, promoted to https — leaving the Netlify address everywhere the domain is empty. Four tests: config round-trip, normalization, the link swap, and the runner end to end. | Same field and swap on Windows; the config key is shared. |

| 88 | 2026-08-10 | Every course section should get a social sharing card (the image iMessage and friends show for a shared link), in the design language of the plantoir.app card: section colour scheme, the chosen header and body fonts, course name large, course code as subtitle with the course emoji beside it, the S-marker setting respected, no logo — and no domain, since Apple's share sheet overlays the domain itself. | ✅ Implemented in the toolchain, so both the app and command-line users get it: `scripts/social_card.py` (Pillow) draws a 1200×630 card — scheme's light background, course name in the header font and scheme's secondary colour (auto-sized, wrapping to at most two lines), then the emoji (Noto Color Emoji, rendered at its bitmap strike and rescaled) beside "CODE S1" in the body font and darkgray. `build_site.py` draws it fresh on EVERY build over the Quartz scaffold's `static/og-image.png` — which the site's head already links — so name/scheme/font/emoji/marker changes always reach the card, and a failed draw never fails a build. The font files moved from the app bundle to `support/fonts/` (single source: the container draws with the same TTFs the app previews; the "Helvetica, Arial" system choice falls back to Inter). En route, two toolchain repairs: `toolchain_hash` now prunes non-recipe paths (from the repo root it was checksumming courses/, node_modules, and the app for many minutes, with a tag that changed every build), and `preview.sh`'s shebang moved to line 1 (it sat mid-file below a detection block, so the kernel could not exec the script directly). Cards verified visually: local renders for two schemes/font pairings/marker states, and the in-container render with the colour emoji. | The renderer is shared Python — Windows gets cards with no extra work; only the emoji font path is Debian-specific and already probed. |

| 89 | 2026-08-10 | Two course-title issues: (1) the toolchain always prepends "Grade x" to the chosen course title — it should be a switch, like the section-number setting; (2) renaming a course is not reflected when the site next builds. A test course showed a third: "Grade 12 Computer Science, Grade 12, U, Section 1" — the grade doubled onto a catalog name that already carried it. | ✅ Fixed at the root: the landing page's title was BAKED into `index.md` frontmatter at scaffold time and never touched again. It is now COMPUTED on every build from the current settings — course name, the new course-level "Show the grade in the site title" toggle (default on, in Course Settings beside the reading-time toggle), and the existing section-marker setting (which now governs the ", Section N" suffix by computation rather than by stripping) — and written only into the MERGED copy, so the teacher's source `index.md` is never modified. The grade never doubles: a name already carrying its grade label anywhere gets no prefix. The same rule now guides the app's Add Section scaffold titles and the wizard's (as sensible starting values; the build recomputes regardless), pinned by tests. Verified live in the reporting course: renamed to "Computer Science", built as "Grade 12 Computer Science" — rename flowed, single grade, marker-off honoured. | The toggle is one config key (`show_grade_in_title`); the compute lives in shared Python — Windows needs only the settings checkbox. Round two: the wizard's Appearance section carries the same toggle, right under the section-marker one, so a new course can choose from the start. Round three, from a live report: a course named with the Ministry's citation form ("Computer Science, Grade 12, U") contains its grade, so a no-doubling guard suppressed the prefix and the toggle appeared dead. A first fix stripped the catalog suffix automatically — REJECTED: the behaviour must not outguess the user. Final design: the switch is fully LITERAL (on prepends, off does not, no exceptions; every stripping/dedup cleverness reverted, the wizard suggests the Ministry's formal name untouched), and when the switch is on while the name already carries the grade label, a quiet orange warning under the toggle — in the wizard and in settings — says the title would repeat it and leaves the fix to the teacher: edit the name, or turn the switch off. Round four: the toggle is PER SECTION, like the marker — it lives in each section's settings block (the wizard's single Appearance toggle writes every section's value, marker-style), stored as `show_grade_in_title.sections.sectionN` with an older course-wide boolean still honoured. |

| 90 | 2026-08-10 | The course settings window's "Cancel" should read "Revert" — Cancel belongs to dialog boxes. | ✅ Implemented — the button now says Revert (still disabled until there is something to revert), because a settings form is not a dialog: nothing closes, the values simply go back to the last save. | Same label on the Windows settings form. |

| 91 | 2026-08-11 | The Preview and Deploy toolbar buttons should show text labels beside their icons — a play triangle and a paper plane are not self-explanatory. | ✅ Implemented — both buttons wear their titles via `.labelStyle(.titleAndIcon)`; while a preview runs, the first reads "Stop Preview" with the stop icon — the clearer wording won over keeping the button's width steady, since the width changes with the title anyway. The neighbouring buttons (Open in Obsidian, Open in Browser) stay icon-only: their symbols are the familiar app icons, and labelling everything would crowd the toolbar. | Give the Windows toolbar's Preview and Deploy buttons the same visible text labels; keep the others icon-only with tooltips. |

| 92 | 2026-08-11 | When creating a course, an option (on by default) should pre-populate it with ready-made example content for that specific course code — real pages in the style of the example course, using whatever Obsidian features suit the subject — and a second option should include the official Ontario curriculum, every expectation as a linkable page. Plantoir will eventually carry content for every Ontario course code; ADA1O (Drama, Grade 9, Open) is the first. | ✅ Implemented in the toolchain plus two wizard toggles. Content lives in `support/example_content/<CODE>/` — a `manifest.json` naming the folders/files it provides plus `shared/` and `per_section/` payload trees — so adding a course code needs no code changes anywhere. The setup wizard offers the two questions only when a payload exists for the typed code; the payload's folders join the structure prompts' DEFAULTS (still vetoable), content installs before scaffolding so nothing generic overwrites it, re-runs never touch an existing file, and both answers are saved in `course_config.json` (`prepopulate_example_content`, `include_curriculum_pages`). With curriculum pages declined, delimited blocks (`%%curriculum-start/end%%` comment markers) vanish from every page, whole-line `![[A2.2]]` transclusions are dropped, and inline links to expectation pages unlink into their visible words — no dangling links either way. The app's wizard shows a "Starting Content" section: the pre-populate toggle (on by default), the curriculum toggle beneath it (disabled when the first is off), or a quiet note when no content exists for the code yet. ADA1O ships 81 pages: all 26 specific + 9 overall expectations verbatim from the Ministry's 2010 Arts document (teacher prompts in folded callouts, source cited on About These Expectations), and drama-specific Concepts, Conventions, Tasks, Discussions, Setup, and Style folders — including the drama edition of "What This Site Can Do", where features drama has no use for (equations, code) are still demonstrated but framed as such. | The installer is shared Python, the payload ships in `support/` — the Windows app only needs the same two checkboxes writing the same two config keys, plus the bundled-manifest existence check that decides whether to show them. Round two: pre-populating LOCKS the structure — the Windows wizard must hide its folders/files editor when the toggle is on (a caption explains the example content chooses the structure) and show it again when the toggle is off. | 

| 93 | 2026-08-11 | Choosing to pre-populate scattered empty default folders (Examples, Exercises, Portfolios, Tutorials, the two Curriculum folders) around the example pages, All Classes held only four days, the section landing page lacked the example course's transclusions, and reference pages like Help Sessions and a populated Key Links were missing. The example content should decide the course's structure outright — the folders/files/hidden/expandable choices are not asked when pre-populating — and ADA1O should match the shipped example course's depth, with a semestered September–January arc. | ✅ Implemented. The manifest is now the WHOLE structure authority: when pre-populating, the setup wizard skips every structure question and prints the layout it is using; the app wizard hides its Structure editor behind a caption. No stray empty folders, and the LCS-specific default files can no longer leak in. ADA1O grew to 132 pages: a 26-class semester in four units (Building the Ensemble → Saying Yes → Telling Stories → Production and Performance, each closing with a performance task; the final class page is the payload's one `draft: true` example), a Warm-Ups folder (drama's practice library — 8 games, each naming what it trains), Tutorials (Learning Lines, How to Rehearse, Giving and Receiving Notes…), Portfolios (the Drama Journal hub moved here with first-entry, strong-entry, checklist, growth, and final-reflection pages), four new Concepts (Blocking and Stage Areas, Subtext, Ensemble, The Fourth Wall), a Culminating Performance task, Help Sessions, and a per-section landing page that transcludes the most recent class, Help Sessions, and Key Links — with a teacher-facing comment explaining how to advance it. The installer replaces a `__SECTION_NUMBER__` sentinel in per-section pages and always accepts a payload's `index.md`. | Same rule on Windows: pre-populate on → structure editor hidden, config lists come from the manifest via the shared Python. The payload format is unchanged — only content grew. |

| 94 | 2026-08-11 | The default empty course used LCS-specific terms. The factory defaults should be school-neutral ("Extra Help" rather than "Grove Time"; no "SIC Drop-In Sessions", no "College Board Curriculum" folder), with a "Use LCS-specific terminology" switch restoring the school's own set-up. | ✅ Implemented — both wizards. Factory defaults are now neutral: shared files "Extra Help.md" + "Learning Goals.md", no College Board Curriculum folder. The app's Structure section gains the toggle (off by default, hidden when example content drives the structure); flipping it swaps the FACTORY portion of the lists wholesale while file names the teacher typed themselves survive. The CLI wizard asks the same question before the structure prompts; saved lists always win over either factory set. The choice is stored as `use_lcs_terminology` in `course_config.json`. The hidden-by-default list covers both terminologies, so whichever names a course ends up with are hidden. | The Windows wizard needs the same toggle in its structure area, writing the same config key; the two factory sets live in `scripts/setup_course.py` (`DEFAULT_*` vs `LCS_*`) and must stay mirrored in the Windows equivalent of `WizardDefaults`. |

| 95 | 2026-08-11 | Dates matter: with every pre-populated class page carrying the same creation date, the All Classes listing cannot sort — the most recent class never floats to the top, which is the whole point of that page. Class pages need real, distinct, semester-shaped dates like a genuine course's. | ✅ Implemented — a second sentinel. Regular payload pages keep `created: __CREATED__` (install time); class pages carry `created: __CREATED_CLASS_K__` (K = the class's position in the course, 1–26 for ADA1O), which the installer turns into real dates: every other weekday at 07:00, anchored at September 8 of the current school year (installing any time from August through the following July lands on the same school year's September). ADA1O's semester runs September 8 → mid-November, all weekdays, strictly ascending — so the listing sorts, and the dates read like a real course's. | The sentinel replacement lives in the shared Python installer — Windows needs nothing beyond shipping the same payloads. Payload authors: number `__CREATED_CLASS_K__` in chronological order; the installer does the rest. |

| 96 | 2026-08-11 | Every page a class links to (a convention, a discussion, a warm-up…) carried the install-time date, so category pages (Conventions, Discussions, …) could not list their pages in the order the course actually met them — an order that has meaning for students. If Choral Speaking first happens on Unit 3, Day 5, its page should share Unit 3, Day 5's date. | ✅ Implemented — automatically, with no annotation burden on payload authors. At install time the installer reads the payload's class pages in chronological order, collects every page each class links to, and gives each shared page the date of the FIRST class that references it (`first_use_dates` in `setup_course.py`). Pages no class references — the site-reference pages in Style, for instance — keep the install-time date, which sorts them harmlessly below the semester. Verified in a built course: the Conventions listing now runs in teaching order, newest first. | The derivation lives in the shared Python installer — Windows needs nothing. Payload authors need to know only one thing: class pages' links ARE the schedule; linking a page from a class page is what dates it. |

## Windows status

The Windows counterpart lives in [`windows-app/`](windows-app/PROGRESS.md)
(WinUI 3, first take built 2026-08-11). Statuses below are per entry
number in the table: ✅ implemented (behaviour present in the
Windows app), 🟠 partial, ⬜ not yet, — no Windows work needed.

**Entries 1–106 were assessed as they landed; 107–255 were assessed in one
pass on 2026-08-17** (see the dated block at the end of this section). An
entry missing from either list is unassessed, not "not yet".

- ✅ 1 (course-name auto-fill + suggestion buttons) · ✅ 4–6 (caption/
  header vocabulary) · ✅ 7, 9 (list editors: + always enabled, ".md"
  hidden, Media reserved, kind-specific wording) · ✅ 8 (every input
  visibly editable — WinUI's stock bordered controls)
- ✅ 2, 3 (font/scheme previews render in the bundled typefaces via
  ms-appx references inside recessed sample boxes, in the wizard and in
  Course Settings — a bare path#family silently fell back to Segoe UI)
- ✅ 10, 11 (WebView2 Back/Forward/Reload, open-in-browser via
  127.0.0.1, server probe gated on the run's own launch line) · ⬜ menu
  accelerators for Back/Forward/Reload (toolbar-only so far)
- ✅ 12, 77 ("Show in File Explorer" reveals selected; "Open in
  Terminal" via wt -d) · ✅ 13–15, 17, 85 (picker states, silent
  unrecognized-folder, empty-folder set-up offer with breadcrumb,
  machinery-free copy) · ✅ 16, 18, 21, 22, 26, 27, 51 (milestone bar,
  "Step k of n", upload counts, bounded clipped console, incremental
  progress — markers re-derived from the .ps1 output) · ✅ 19, 24
  (stable toolbar; one Preview/Stop button; console layer stays mounted
  under the web view) · ✅ 20 (affirmative action bottom-right —
  ContentDialog's platform placement)
- ✅ 23, 52, 53, 58 (footer +/− with min hit targets, filter, no-matches
  state; text-scale-relative sizing still to verify on hardware)
- ✅ 25 (deploy freshness check; silent build-first as one task) ·
  ✅ 30–35 ("Input required" dialog, lifted bracketed defaults, hidden
  keystroke asides, cancel via the script's own token) · ✅ 36 (failure
  explanations; recognise-or-stay-quiet) · ✅ 37 (panel follows the
  current task) · ✅ 38 (live link from labelled lines; https
  promotion) · ✅ 39, 40 (Stopped ≠ failed; titles re-read when done)
- ✅ 41, 43 (example-course panel driving `--install-example`; code
  read back from EXAMPLE_COURSE_CODE=) · — 42, 49, 50 (shared content)
- ✅ 44 (No Courses Yet + Add a Course…) · ✅ 45, 46 (prompts shown
  verbatim; no context guessing) · ✅ 47 (settings store isolated from
  tests; stored folder validated at launch) · ✅ 48, 84 (per-window
  folders; new window inherits the key window's folder, else picker)
- ✅ 54, 55 (Archived group, filename rule excluding wizard backups,
  restore with refusals and section-number bookkeeping) · ✅ 56, 57
  (working-folder breadcrumb bar; crumbs reveal in Explorer) · ✅ 59–65
  (the app-owned {folder, frame} list IS restoration on Windows;
  recorded while windows exist, replayed at launch, app preference
  defaulting to restore) · ✅ 66, 67 (port leases with both refusals;
  per-folder containers named by physical-path hash; last-window-closed
  stops the container) · ✅ 68, 71 (launcher refresh + .toolchain
  mirror from the app's bundled recipe; extraneous files removed) ·
  ✅ 69 (quit stops containers, then `wsl --terminate` only when Docker
  is idle) · ✅ 70 (section-number validator, verbatim wording)
- 🟠 72 (zero-prerequisite: the launchers install/start the engine in
  WSL and the app rides that; a silent no-distro bootstrap — install
  Ubuntu without a console question — still needs its own pass)
- ✅ 73 (product name Plantoir; assembly Plantoir; settings in
  %LOCALAPPDATA%\Plantoir) · ✅ 74 (Add Section dialog: digits-only,
  stepper, reserved warning line, stay-open-on-failure, sibling-
  mirroring scaffold) · ✅ 75 (Open Working Folder… Ctrl+O) · ✅ 76
  (menus on crumbs only) · ✅ 78 (emoji presets + one-emoji field;
  Win+. hint) · ✅ 79 (tooltips on all section toolbar buttons)
- ✅ 80 (Obsidian: registry at %APPDATA%\obsidian\obsidian.json, quit-
  then-register dance, defaults seeding, autoReveal patch, index.md
  targeting — implemented; needs a live click-through) · ⬜ 81 (UIA
  press-and-look regression tests) · 🟠 82 (About dialog with the
  companion copy AND the real icon — derived from the mac Icon Composer
  art via Assets/make-icon.ps1, on the exe, title bar, and About panel;
  accessibility scaling still pending) · — 83,
  88 (toolchain-side) · ✅ 86 (drafts via SectionAdder fallback +
  sibling mirroring) · ✅ 87 (custom domain field, normalization,
  orange note, link swap) · ✅ 89 (literal per-section grade toggle +
  warning in settings and wizard) · ✅ 90 (Revert, not Cancel)
- ✅ 97 (the still-working timer originated on Windows) · ✅ 98 (no
  picker flash on Windows: the folder is decided synchronously before
  first paint — no claim polling exists to race) · ✅ 99 (per-window
  sidebar memory: open courses, Archived/Backups disclosure, and the
  selection restore exactly; deliberate divergence per the user — legacy
  entries restore all-EXPANDED, not the mac's all-collapsed; en route,
  fixed chevron collapses being eaten: WinUI raises Collapsed BEFORE the
  chevron's pointer event reaches the tree, so the glitch guard now
  defers its user-vs-glitch decision one dispatcher pass) · ✅ 100
  (computed-title font samples — implemented Windows-first, both
  rounds) · ✅ 101, 102 (folder publishing: shared Publishing control in
  settings AND wizard, live plain-words validation gating Save/Create,
  --to-folder deploys under never-Netlify milestones, PUBLISHED_FOLDER
  completion with Show in File Explorer and the render note; verified
  end to end including the incremental second publish) · ✅ 103
  (Publish, never Deploy; the empty-state invitation follows
  deploy_target) · ✅ 104 (Add Section declines while its course
  previews or publishes anywhere; CourseActivity registry beside the
  port leases; menu state read at open time per the staleness lesson) ·
  ✅ 105 (stop preview reclaims container processes — plus two Windows
  launcher fixes: the WSL docker wrapper swallowed pipeline stdin, and
  stop mode bootstrapped the engine) · ✅ 106 (whole-course backups +
  deletable archives, all confirmations in the mac's wording; two
  Windows-only fixes for container-made links that sank the zip walk
  and the restore's delete)
- ✅ 91 (Preview/Deploy wear text labels beside their icons, "Stop
  Preview" while running; Obsidian/Browser icon-only with tooltips) ·
  ✅ 92, 93 (Starting Content section: pre-populate + curriculum
  toggles when a bundled payload exists for the typed code, quiet note
  otherwise; both keys written, forced false without a payload;
  pre-populating hides the Structure editor behind the caption —
  verified end-to-end: a real MTH1W create installed the manifest's
  structure, 26 dated class pages, and the Curriculum folder) · ✅ 94
  (school-neutral factory defaults mirrored from DEFAULT_*/LCS_* with
  the "Use LCS-specific terminology" switch swapping the factory
  portion and writing use_lcs_terminology; hidden list covers both
  vocabularies) · — 95, 96 (shared Python installer + payloads; ships
  via the bundled support folder, staggered dates verified on Windows)

### Entries 107–255, fully synced 2026-08-17

**Full sync completed on Windows on the `windows-sync` branch.** All 457 tests
run clean via `dotnet test Plantoir.Tests/Plantoir.Tests.csproj` (0 failures).

- — **111–121, 123–129, 131–138** (mermaid fonts, labels and pie charts;
  KaTeX + mhchem; the "When did we do this?" / table-of-contents split; the
  ~1,900 skeletons; the whole Curriculum Coverage map, its palettes and its
  contrast work; the half-credit pairing; the linter's dead-link holes).
  Shared toolchain — Python, patches and payloads.
- ✅ **107–109** (About-panel credits, the deploy-after-preview live-reload
  bug, up-front duplicate-code validation) — originated on Windows and mirrored to mac.
- ✅ **110** (Timer correctly timing from step launch)
- ✅ **122** (`createdSection<N>` / `publishSection<N>` in `PageFrontmatter.cs`)
- ✅ **130** (Coverage explainers toggle)
- ✅ **139** (Cloudflare Pages as third destination — `PublishingChoiceView.cs` + `deploy.ps1`)
- ✅ **140–141** (`draft:` → `publish:` rename — originated on Windows)
- ✅ **143** (Deploy for site, publish for a page; `SectionDetailView.xaml`)
- ✅ **144, 147, 180, 196** (Local assistant with model tier choices and hardware budget; thinking flags `--reasoning off` and `--reasoning-budget 0` passed in `LocalModel.cs`)
- ✅ **148–185** (Assistant window, bubble styling, prompt history, one assistant at a time, `deploy_section` delegation, plan-then-apply)
- ✅ **186–204** (Shared contracts: all contract files (`assist-cases.json`, `app-rules.json`, `assist-wording.json`, `course-management.json`, `file-formats.json`, `schedule-rules.json`, `class-planning.json`, `shared-rules.json`) fully wired into `Plantoir.Tests` via `ContractLoader.cs`, `AssistCasesContractTests.cs`, `ClassPlanningContractTests.cs`, `ScheduleRulesContractTests.cs`, `ContractTests.cs`)
- ✅ **205–211** (Course renaming from sidebar with Obsidian vault dance, 12-char limit, uppercase rule, and rollback)
- ✅ **212–218** (Problem report zip gathering, `LogRedactor`, and activity trail in `%LOCALAPPDATA%\Plantoir\Logs`)
- ✅ **219** (Assistant Settings dialog with confirmation toggle, model choice, and model housekeeping / removal)
- ✅ **220–243** (Plain-spoken plans, `add_next_class`, `plan_add_next_class`, `remember_timetable`, Unit/Day ordering, unpublishing whole units, adding days, duplicating classes, starting new units, visible-referrer rules, confirmation setting, and discoverability after 15 plans)
- ✅ **246–248** ("May I ask you for your class dates?", revising date lists, follow-up answers)
- ✅ **249–251** (Re-dating onto timetable with overflow fallback on final date; `add_next_class` falling back to final date when dates run out)
- ✅ **252** (New course default name uses Short name catalog)
- ✅ **253–254** (Rich credential dialogs for Netlify & Cloudflare tokens, Cloudflare Account ID "Where do I find this?" link button, no unwanted browser tabs opened)
- — **255** (Shared plantoir.app marketing site)

| 97 | 2026-08-11 | During first preview or deploy, the per-section dependency install (npm) can run quietly for a long time on slower machines, and the milestone label looks frozen. Show a live seconds counter — "still working… (Ns)" — while a step is quiet. | ✅ Implemented on macOS, ported FROM the Windows app (the feature's origin — commit "Fix empty glyphs, layout, wizard gating and slow-step reassurance"). Identical semantics: a step that reports its own count (uploads, build steps) shows the count — the count is the movement; otherwise, once the script has been quiet for 4 seconds, the milestone label appends "still working… (Ns)" with N counting up live each second. On macOS this rides the existing one-second TimelineView tick and the runner's `lastOutputAt`; three unit tests pin the quiet threshold, the own-count precedence, and that a finished task never shows the timer. | Already present on Windows — this row records the macOS port for parity. |

| 98 | 2026-08-11 | Restored windows briefly flash "Choose Your Working Folder" for a few seconds at launch before each loads the folder it had — the claim machinery polls for the restored frame to settle while the picker is already on screen. | ✅ Fixed two ways. (1) The AppKit layer now does the timing: `NSApplication.didFinishRestoringWindowsNotification` fires when state restoration completes and every frame is final, so each window tries its frame match at that exact moment instead of discovering it by polling (the polling stays as the fallback, and the notification path is success-only so a mismatched window can never give up early and order-claim a sibling's folder). (2) While a claim may still arrive — claims open AND unclaimed entries remain — a folder-less window shows a quiet small spinner instead of the picker (`isResolvingRestoredFolder`); the flag clears the moment the claim resolves either way, so a genuine no-folder window still gets its picker, a mid-session window immediately, and a first launch instantly (no remembered entries). Three tests pin `aClaimMayStillArrive`: true with open claims and entries, false after claims close, false with nothing to claim. | The Windows app restores windows through its own mechanism; if it shows a picker flash on restore, apply the same two ideas: resolve folder assignment on the platform's restoration-complete signal, and hold a neutral loading state (never the picker) while an assignment is still possible. |

| 99 | 2026-08-11 | A restored window comes back to the right working folder, but every course's disclosure triangle (and the Archived group) is folded up — the sidebar forgets which courses were being worked on. Track disclosure state per window and restore it. | ✅ Implemented — the state rides in the same per-window memory that restores folders. Course triangles and the Archived group now bind to the window's model (`expandedCourseCodes`, `isShowingArchived`) instead of view-internal state; every change re-records the window list; each remembered entry stores the open course codes (comma-joined — codes never contain commas) and the Archived flag beside the path and frame; and claiming an entry restores the folder AND the sidebar exactly as it was left. Old entries without the new keys load all-collapsed, and three tests pin the storage round-trip, the legacy-entry fallback, and the per-window recording. Round two adds the SELECTION: the chosen course, section, or archived item persists per window too, stored in a plain string form (`course\|CODE`, `section\|CODE\|N`, `archived\|ID`) with round-trip tests for every case; unrecognized or empty stored selections restore none. Verified live on-machine: two windows quit and relaunched with their expansion and selections intact, confirmed both in the claim log and in the re-recorded store. Live in-app probes also pin that a freshly created sidebar honours pre-set expansion — the restoration order. | Windows already restores per-window folders through its own memory; extend its entries the same way (expanded course list + archived flag + selection string) and apply them when a window claims its folder. |

| 100 | 2026-08-11 | The header-font sample previews a stand-in headline ("Grade 11 Computer Science") even when the course has a name. Once a course name exists, the sample should render THAT — the teacher previewing their own site's headline. | ✅ Implemented — the font editor takes a `sampleHeadline`; the per-section settings pass the saved course name, and the new-course wizard passes the name field LIVE, so typing "Drama" re-renders the header sample as "Drama" immediately. Whitespace-only or empty names keep the stand-in, pinned by two tests. Body and code samples keep their explanatory sentences — they demonstrate running text and code, which a course name cannot. Round two: the sample shows the landing title as the build actually computes it — grade prefix and \", Section N\" suffix included, exactly per the section's grade and marker switches (`CourseConfiguration.landingTitle`, mirroring `computed_landing_title` in `build_site.py`); in the wizard it follows the name, code, both switches, and the first section number live. | Same rule on Windows: feed the COMPUTED landing title (grade + name + marker, per the switches) into the header-font sample, with the stand-in as the empty-name fallback; mirror `computed_landing_title` from `build_site.py`. |

| 101 | 2026-08-11 | Some teachers publish on their own hosting rather than Netlify. Offer a per-course "publish to a local folder" option: Plantoir copies the built section sites into a chosen folder on this computer (remembered per course), and the teacher uploads them however they prefer (e.g. SFTP). Be smart about copying — only changed files should move. | ✅ Implemented. A course-level "Publishing" setting: Netlify (the default, unchanged) or "A folder on this Mac" with a folder chooser, stored as `deploy_target` + `deploy_folder_path` in `course_config.json`. Deploys then run `deploy.sh --to-folder <path>`: since the working folder is bind-mounted, the built site is already on the host, and the launcher does a host-side incremental sync (`rsync -a --delete` — only changed files copy, removals propagate; verified: second run copies 0 files). Each section publishes into its own `sectionN` subfolder so sections can never overwrite each other — the caption says so. Netlify credentials are never touched in folder mode. On completion the app shows "published to a folder" with a Show in Finder button (the launcher prints a `PUBLISHED_FOLDER=` marker) instead of a live-site link. `deploy.ps1` carries the same flag using `robocopy /MIR`. | The Windows settings UI needs the same Publishing picker + folder chooser writing the same two config keys, and its deploy invocation passes `--to-folder`; the launcher work is already done (`deploy.ps1`, robocopy exit codes below 8 are success). Completion shows the folder in Explorer via the same `PUBLISHED_FOLDER=` marker. |

| 102 | 2026-08-11 | Round two of folder publishing, from live testing: (a) the Publishing choice only existed in Course Settings, not the new-course wizard; (b) a course could be SAVED set to publish to a folder with an empty path — and the deploy then quietly went to Netlify; (c) nothing checked the chosen folder was writable; (d) the progress milestones still said "Connecting to Netlify…" during folder deploys; (e) after a folder publish, teachers may double-click the copied pages and wonder why the site looks broken. | ✅ Implemented. (a) A shared `PublishingChoiceView` (picker + folder field + Choose… + captions) now serves BOTH Course Settings and the wizard's new Publishing section, and the wizard writes `deploy_target`/`deploy_folder_path` into the config it hands the setup wizard — plus `setup_course.py` now carries over any config key it does not own when re-saving, so app-written keys survive the setup run (verified end-to-end in the container: deploy keys and a synthetic future key both survived). (b+c) `CourseConfiguration.deployFolderProblem(forPath:)` validates live — empty, nonexistent, a file, or unwritable each gets its own plain-words message shown in orange under the field; Course Settings disables Save while a problem stands, and the wizard blocks Create the same way. Choosing a folder through the panel runs the same check instantly. (d) Two new milestone lists (`deployToFolder`, `buildAndDeployToFolder`) replace the Netlify wording with "Copying your files…" keyed to the folder-mode output; a test pins that folder publishing never mentions Netlify. (e) The folder-deploy completion adds a quiet note: the pages won't look right opened straight from the folder — the site only displays properly once it's on the web host. | Windows needs all five: the Publishing group in BOTH its settings and wizard (share the control); block save/create on an empty, missing, or unwritable folder with the same inline messages; validate on selection; folder-mode progress labels keyed to `deploy.ps1`'s robocopy output (never "Netlify"); and the same render-note sentence after folder publishes. The `setup_course.py` preservation fix is shared Python — nothing to port. |

| 103 | 2026-08-11 | "Deploy" is web-developer jargon — and the empty-state message under it ("…or Deploy to publish it to Netlify") was wrong twice over for a course set to publish to a folder. Drop "Deploy" from the GUI entirely. | ✅ Implemented. The toolbar button is now "Publish" (same paper-plane icon), and the No Preview Running message follows the course's actual publishing choice: "…or Publish to put it online." for Netlify, "…or Publish to copy it to your publishing folder." for folder publishing. Those were the only two user-facing uses — the tooltip and progress title already said "Publish"; everything else matching "deploy" is internal (script names, config keys, output markers) and unchanged. The `deployButton` accessibility identifier is kept, so UI tests are untouched. | Same sweep on Windows: the visible button says "Publish", the empty-state message follows `deploy_target`, and internal names stay as they are. The launchers keep their file names (`deploy.ps1`) — teachers never see them. |

| 104 | 2026-08-11 | Adding a section re-runs the course setup, which rewrites the course's folders and files — while a preview or publish of that same course may be mid-copy of those very files. Clicking quickly could make the scripts stomp on each other. | ✅ Implemented. A small cross-window registry (`CourseActivity`) knows which courses are busy: previews via the existing `PreviewLeases` (held for the preview's whole life), publishes via begin/end records around `startDeploy` (ended on every exit path, including a failed build). The sidebar's "Add Section…" item is disabled while its course is previewing or publishing anywhere, with a short menu line naming what's in the way: "Available once preview completed" / "Available once publish completed" (or both). Round two, from live testing: the menu showed STALE state — context-menu content is built when the row renders, not when the menu opens, and the registries were plain static arrays, so starting a preview never re-rendered the row (a normal click did, which is why the gate seemed to work only after selecting the course first). Both registries now keep their arrays in `@Observable` stores, and the row reads the busy state during its own render, so rows — menus included — redraw the moment activity changes. Other courses in the folder are unaffected, and the same code in a different working folder is a different course — five tests pin the registry, including two publishes of one course ending independently. Adding a NEW course stays allowed during previews: its file tree is disjoint, builds are isolated per section, and the container patches are idempotent (analysed in this session; the rare container-recreation edge is in TODO.md). | Windows: same rule — its Add Section entry point declines while any of the course's sections is previewing or publishing in any window, with the same explanatory line. Track publishes centrally beside the port-lease registry the port-block behaviour (entry above) already requires — and heed the staleness lesson: make sure the menu item's enabled state is read when the menu OPENS (or the registry change re-renders whatever hosts the menu), not captured at some earlier render. |

| 105 | 2026-08-11 | Stopping a preview (the button, clicking away from the section, closing the window) terminated only the HOST-side launcher; inside the container the whole serve chain (python3 → npm exec quartz → node → esbuild) kept running — proven by SIGTERMing a live preview and getting HTTP 200 from inside the container afterwards. An orphaned server idles but holds node's RAM; an orphaned mid-flight BUILD burns real CPU until it completes. | ✅ Implemented. `preview.sh CODE N --stop` kills the section's container-side processes and exits. Two design points: (1) processes are found by WORKING DIRECTORY (`/proc/*/cwd` under the section's `.merged_output` folder), not by port — so mid-build processes are caught too, and another section's processes can never be touched; SIGTERM first, SIGKILL survivors after a second. (2) Stop mode never starts anything — no engine bootstrap, no image build, no container creation; engine down or container absent is "nothing to stop", exit 0. The kill script is piped to the container's python3 over stdin, so it works against ANY image, no rebuild needed. The app runs it fire-and-forget from `stopPreview` (`PreviewStopper`, output discarded, process held until exit) whenever a preview was actually running. Verified end-to-end: server up → `--stop` → "Stopped 4 process(es)", only the container's `tail` keepalive left, server unreachable — and the host-side launcher exits by itself once its docker exec loses the remote process. `preview.ps1` carries the same `--stop` (same stdin-piped kill script). | Windows app: call `.\preview.ps1 CODE N --stop` fire-and-forget wherever a preview stops (stop button, navigate away, window close). The launcher work is done; note it must run the WORKING FOLDER's launcher, quietly, and never surface its output. |

| 106 | 2026-08-11 | Teachers will be encouraged to use an LLM for bulk edits in Obsidian (re-dating a raft of files, say) — and an LLM can make a mess that is hard to undo by hand. Give the app whole-course backups: made on purpose before risky editing, listed in the sidebar, restorable, and deletable for good. | ✅ Implemented. "Back Up Now" in a course's context menu zips the whole course through the SAME exclusion list section-removal already uses (`.merged_output`, `node_modules`, caches — the rebuildable bulk stays out), named `<CODE>_backup_<timestamp>.zip` in the shared `courses/_backups/<CODE>/` folder. The NAME is the discriminator: backups, archives (`<CODE>_<timestamp>.zip`), and the wizard's automatic zips (`<timestamp>.zip`) each have a parser that accepts only its own form, so the three kinds never appear in each other's lists (pinned by tests). A "Backups" sidebar group sits above Archived (hidden when empty; opens itself when a backup is made; its disclosure and selection persist per window like everything else). Each backup row: Restore…, Show in Finder, Delete Backup…. RESTORE archives the current course first (WITHOUT removing it) — even a restore has an undo — then unpacks and verifies the zip and replaces the course folder's CONTENTS in place, never the folder itself: the folder is Obsidian's vault and its file watcher is anchored to the folder's identity, so a swapped folder left an open vault showing stale files until reopened (a test pins the folder's file-system identity across a restore); the zip STAYS after restoring (only deleting removes it); declined with a plain-words message while the course is previewing or publishing (`CourseActivity`). DELETE is the app's one true deletion, and both its confirmation and the restore confirmation say exactly what will happen (including that anything added after the backup was made isn't in it). Backing up stays available mid-preview — it only reads. Selecting a backup shows a detail pane with Restore/Delete, mirroring Archived. Round two: archives are deletable too — Delete Archive… in the archived row's context menu and detail pane, with a confirmation warning that if the course is no longer in Courses & Clubs, the archive is its only remaining copy. The removal-time promise ("nothing is deleted") stays true: deletion is a second, separate, deliberate act. | Windows: same three name forms in the same `_backups` folder (the zip/unzip helpers differ; robocopy-era code already handles paths); a Backups group above Archived with the same context menu, confirmations, and busy-decline; restore = archive current without removing, then replace the course folder's CONTENTS in place (never the folder itself — Obsidian's watcher is anchored to it), keep zip. |

| 107 | 2026-08-12 | (Origin: Windows — mirrored to macOS from MAC-HANDOFF.) The About panel's credits match plantoir.app's footer: a rounded-rect callout making the case for sponsoring Jacky Zhao, then three plain acknowledgement lines (Phosphor Icons, Russell Gordon, Made with Claude). No separate "Built on Quartz" line — the callout already says whose work this stands on. The Support row is removed (help is coming into the app itself), leaving Email as the only contact row. | ✅ Mirrored on macOS: `AboutView` gains `sponsorCallout` (quaternary fill, separator stroke, markdown links to quartz.jzhao.xyz and github.com/sponsors/jackyzha0), the credits array carries the three new lines with their links, and the Support grid row is gone. All sizes remain `@ScaledMetric`. | Already implemented on Windows (`AboutDialog.cs`) — this row records the macOS mirror. |

| 108 | 2026-08-12 | (Origin: Windows `94e25f8` — mirrored to macOS.) Deploying right after previewing published the preview's build, whose pages carry Quartz's live-reload client (`ws://localhost:…`) — the LIVE site then knocked on every visitor's localhost and Chromium-family browsers prompted about accessing "other apps and services on this device". The freshness check compared only dates, so a newer-than-content preview build looked deploy-fresh. | ✅ Mirrored on macOS: `BuildFreshness.builtForPreview` reads the built `public/index.html` — `ws://localhost:` present (or the file unreadable) means never deploy-fresh, so the app's ordinary, visible build-first step runs instead of relying on the shared `deploy.py` safety net (which detects the client in-container and re-emits a production build silently). Pinned by `testAPreviewBuildIsNeverDeployFresh`, mirroring the Windows theory test. | Already implemented on Windows (`BuildFreshness.cs`, `BuiltForPreview`); the shared `scripts/deploy.py` backstop protects both platforms regardless. |

| 109 | 2026-08-12 | (Origin: Windows `2d10e4c` — mirrored to macOS.) A filled-in New Course form with a duplicate course code explained nothing until Create was clicked — the sections field explained its problems inline while the code field stayed silent. | ✅ Mirrored on macOS: the code field now explains itself live, in the same orange as every other inline warning — "A course code cannot contain spaces." / "A course named ICS4U already exists — choose a different code." (clash found case-insensitively, whitespace trimmed; an empty code is not-ready-yet, not an error). Single-sourced: `NewCourseWizardView.courseCodeProblem` feeds both the live caption and the Create-time validation, so the explanation and the gate can never disagree. Five cases pinned by test. | Already implemented on Windows (`CourseCodeProblem()` in `NewCourseDialog.cs`) — this row records the macOS mirror. |

| 110 | 2026-08-12 | The "still working… (Ns)" reassurance timer did not start at zero. Creating a course showed the first step — "Getting this Mac ready…" — opening at 35 seconds, which reads as though the app had been stalled since before the teacher pressed the button, undermining confidence in the whole progress bar. | ✅ Fixed at the source. `lastOutputAt` (the quiet clock) was initialized when the ScriptRunner was CONSTRUCTED, and a runner is built when its sheet or window appears — so the wizard's runner had been ticking for the whole time the teacher spent filling in the form, and the section view's preview/deploy runners tick from the moment a section is opened. Starting a run now resets the clock, so it measures silence from THIS task's start. Every runner benefited, not just the wizard's. A test pins it: a runner whose clock is 35 seconds stale, then started on a stand-in script, must show no timer and a fresh clock. | Same bug shape on Windows if its runner equivalent initializes the quiet clock at construction: reset it when a task starts, not when the view appears. |
| 111 | 2026-08-13 | Mermaid diagrams render differently in the Plantoir preview than in Chrome. | ✅ Fixed, and it was not the rendering engine. Quartz hyphenates body text, and that hyphenation was leaking into mermaid diagram labels — WebKit acts on it and Chromium ignores it, so a flowchart node read "A. STEM Skills, Ca-reers, and Connec-tions" in the preview and correctly in Chrome. A diagram label is not prose, so `append_mermaid_styles()` in `scripts/build_site.py` now turns hyphenation off inside `.mermaid`. Established by rendering the same page through both engines: the rule makes WebKit's output identical to Chromium's, moving only the node text. Verified through a real container build, and through a fresh build of an example-content course. It carries its own marker rather than joining the transclusion block, so course folders built before today pick it up on their next build instead of being skipped. | Shared Python, so Windows gets this for free — no Windows-side work. (A Chromium preview engine was also prototyped while diagnosing this; it works, but the CSS fix made it unnecessary, so it stays unmerged on the `chromium-preview` branch with its findings in `CHROMIUM-PREVIEW.md`.) |
| 112 | 2026-08-13 | Still wrong in Safari/the preview: mermaid label text is cut off mid-word ("An error ap", "Did any output p"), and Arc renders the same page correctly. | ⚠️ Fix shipped for the mechanism that explains it, but NOT reproduced locally — stated plainly rather than claimed. Mermaid sizes each box by measuring its label in the course's code font, which Quartz serves from Google Fonts with `display=swap`: the browser paints with a narrower fallback and swaps the real font in later. If mermaid measures before the swap, every box is sized for the fallback and the real text overflows and clips. `patch_mermaid_font_wait()` in `scripts/build_site.py` now awaits `document.fonts.ready` before `mermaid.run()`. On this machine the font was always cached before mermaid ran (`document.fonts.check` true at nav), so the clipping could not be reproduced here — the fix addresses the race rather than a reproduction. | Also corrects entry 111: the mermaid patches were inside the `full_rebuild` branch, so existing course folders would NOT have healed on a normal build as claimed. Both mermaid patches now run in the ALWAYS section, verified against a course folder that already existed. Shared Python, so Windows gets both for free. |
| 113 | 2026-08-13 | The pie chart in "What This Site Can Do" renders with its title cut off, in both WebKit and Arc — the showcase was demonstrating that the site renders pie charts badly. | ✅ Fixed, and it is mermaid's bug rather than ours — reproduced with mermaid alone, no Quartz involved. Mermaid centres a pie title on the PIE, which the legend pushes leftward, and never widens the chart to fit the title, so the overflow falls outside the viewBox and `overflow: hidden` eats it. Measured budget is a constant **450px** (~28 characters); legend width does not change it. 27 of the payloads' 33 pie titles were over it — "Where a working programmer's hours actually go" (711px) lost its first nine characters. All 33 titles shortened and re-measured through the real renderer: 0 clipped, tightest now 433px of 450px. `lint_payload.py` fails a pie title over 28 characters, and the rule is in the example-content skill. | Content and lint only — nothing for Windows to port, but the same limit applies to any pie chart a teacher writes, so it is worth knowing when supporting them. |
| 114 | 2026-08-13 | The pie-title fix should also work for pie charts teachers write themselves, not just the bundled example content. | ✅ `patch_mermaid_pie_title_fit()` in `scripts/build_site.py` now re-fits every pie chart's viewBox to what mermaid actually drew, immediately after `mermaid.run()`. CSS cannot do this — the viewBox is an attribute — and `getBBox()` covers the title even when it lies outside the box, so a long title widens the chart instead of losing its first words, and a short one loses the wasted margin. Only pie charts are touched (`svg[aria-roledescription="pie"]`); re-fitting every diagram would disturb layouts that are already correct. Proved end to end on a real build: a deliberately planted 68-character title clipped 300px before and renders complete with 6px clearance on BOTH sides after. Tested to 127 characters. Because this removes the constraint, the linter's hard 28-character failure was removed — it would now block legitimate titles — and the skill keeps short titles as guidance rather than a rule. | Shared Python, so Windows gets it for free. |
| 115 | 2026-08-13 | The first pie slice is drawn in the page background colour, so it disappears — and its legend swatch with it. | ✅ Not that one chart: EVERY pie chart in every course lost its first slice. Mermaid's base theme takes `pie1` from `primaryColor`, and Quartz sets `primaryColor` to `--light`, the page background — correct for a flowchart node, wrong for a pie slice. Measured: slice 1 `#faf8f8`, page `#faf8f8`, identical. `patch_mermaid_pie_colours()` now builds `pie1`–`pie12` from the site's own accents (`--secondary`, `--tertiary`, `--darkgray`) blended toward `--light`. Blending toward `--light` is what makes it work in BOTH themes, since `--light` is always the page and `--dark` always the text. Blend fractions were chosen by search, not by eye: each of the first six slices keeps ≥4.5:1 with the label text and ≥1.45:1 with the page, as distinct from each other as a two-accent scheme allows. Also sets `pieOpacity: 1` — mermaid draws slices at 0.7, blending them 30% into the page, which pushed dark mode below the contrast the palette was chosen for. Verified on a real build in both themes. | Shared Python; Windows gets it free. Any course colour scheme is handled, because the palette is derived at render time from that course's own variables. |
| 116 | 2026-08-13 | Does the pie palette hold up with more than three slices, and across the colour schemes teachers can choose? | ✅ It did not, and both questions were right to ask. The fractions in entry 115 were tuned against one scheme; swept across all 43 schemes × light and dark = 86 combinations, **74 failed on the first six slices**, some with slices barely distinguishable from the label text. The palette is now SOLVED at render time instead: build a pool of tints of that scheme's own colours, keep only those clearing 4.5:1 against the label and 1.45:1 against the page, then walk the pool taking the colour farthest from everything chosen so far — anchored on the course's main accent so the chart still looks like that course. Across all 86 combinations: **0 contrast failures**, worst separation 14 (was 0 — identical slices). Verified on real builds with a teacher-written page of 3, 6, 9 and 12 slice charts: contrast never below 4.55/1.45, no slice ever the page colour, separation 56/24/19/15 on the default scheme and 47/15/12/10 on a black-and-grey monochrome one. The patch also removes the earlier fixed-fraction block from a scaffold that already has it, so no duplicate declarations. | Shared Python; Windows gets it free. A monochrome scheme can only yield tints of one hue, so its slices differ by lightness alone — inherent, not a defect. |
| 117 | 2026-08-13 | On a page linked from many others, "When did we do this?" stretches up and crowds out the table of contents. Make them share the column, roughly half each, each with its own scrollbar — but keep the backlinks tucked under a short table of contents. | ✅ `append_sidebar_sharing_styles()` in `scripts/build_site.py`. The table of contents is now unshrinkable and capped at 50% of the column; the backlinks take whatever is left; the two LISTS carry the scrollbars, because their parents are row flex containers so the overflow happens one level in. Measured on the reported page (ICS4U ▸ The Software Project): before, the contents got 98px and were clipped; after, they get their full 291px with all ten headings and no scrollbar, while the backlinks take 417px and scroll. With both long the split is 386/354 and both scroll. The cap is skipped via `:not(:only-child)` so a page nothing links to still uses the whole column, and specificity is kept below Quartz's `.toc:has(button.toc-header.collapsed)` so collapsing still works. | Shared Python, so Windows gets it free. Known trade-off, stated rather than hidden: a LONG table of contents with only a few backlinks leaves the contents capped at half with slack below, because CSS cannot make one panel's cap depend on whether its sibling needs the space. |
| 118 | 2026-08-13 | "Getting this Mac ready" sits for nearly half a minute before a preview, and expanding the details shows only "Starting…". What is it doing, can it be faster, and can the pane say more? | ✅ It was computing the image tag — a SHA-256 of every file in the build recipe — by spawning one `shasum` PROCESS PER FILE. With eighteen example-content payloads now inside the recipe that is **3,536 files**. Measured on the reported Mac: the hash took **36.4s** of a 36.75s startup; the same run with `--image` (which skips hashing) finished in **0.33s**. `toolchain_hash` now pipes `find -print0 | sort -z | xargs -0 shasum`, batching them into a handful of invocations: **35s → 0.16s**, and the hash is byte-identical (`afef79f8` both ways), so no teacher gets a surprise image rebuild. Fixed in all three launchers, whose function bodies are verified identical. The launchers also now print "Checking whether your website builder is up to date…" before the step, so the details pane is never blank — worded to avoid colliding with any milestone marker. | Windows had the same bug, worse: `$combined += (Get-FileHash …)` inside a loop is quadratic because PowerShell strings are immutable. All three `.ps1` launchers now collect into `$hashes` and `-join` once — same characters, same order, same tag. **Not execute-tested: no PowerShell on this Mac.** |
| 119 | 2026-08-13 | Mermaid diagrams still mangled until a reload, on a fresh build where the font fix was demonstrably present. | ✅ Root cause found from a console screenshot, not from my measurements: the Google Fonts request was returning **HTTP 400**. Quartz builds ONE stylesheet request from all three typography choices, and Plantoir offers system stacks — this course used `Helvetica, Arial` for header and body. Google rejects the entire request if any family is unknown to it, so **no fonts downloaded at all**, including the IBM Plex Mono that mermaid measures diagram labels in. It also explains why `static/fonts/` was always empty: the emitter fetches the same URL to self-host and got the same 400. `patch_google_font_href()` now filters system stacks and known system families out of the request (a stack needs no download — it is already on the machine) and drops the request entirely when nothing is left to ask for; `Head.tsx` skips the empty link. Confirmed against Google directly: old URL 400, filtered URL 200. In the browser the page now reports `IBM Plex Mono 400 loaded` and `600 loaded` for the first time. | Shared Python — Windows gets it free. Worth knowing generally: `document.fonts.check()` returns true for a family that never downloaded, because the name resolves through the fallback. It misled four of my reproduction attempts. |
| 120 | 2026-08-13 | A chemistry page wants `\ce{}` — mhchem — which teachers coming from other KaTeX setups expect, and which three payloads warned was unavailable. | ✅ Available now, and it cost one import. Quartz renders KaTeX at BUILD time through `rehype-katex`, and mhchem is a KaTeX extension loaded by importing it — the `katex` package Quartz already installs ships `katex/contrib/mhchem`, so nothing new is downloaded. `patch_katex_mhchem()` in `scripts/build_site.py` adds `import "katex/contrib/mhchem"` to `quartz/plugins/transformers/latex.ts`, in the ALWAYS section so course folders built earlier pick it up on their next build. Verified on a real build of the SCH3U example course: `$$\ce{CH4(g) + 2O2(g) -> CO2(g) + 2H2O(g)}$$` renders with upright symbols, true subscripts and a drawn reaction arrow, `\ce{CaCO3(s) <=> CaO(s) + CO2(g)}` gives a real equilibrium arrow, and the whole built site has zero `katex-error` spans. The three chemistry payloads (SNC2D, SCH3U, SCH4U) carried a teacher warning saying mhchem was NOT here and failed silently — the opposite of the truth once the import landed. They were then CONVERTED outright, because these pages teach by example and a teacher meeting `\ce{}` for the first time should meet it in use: **1,533 maths spans across 110 files** now write chemistry as `\ce{}` and nothing carries the `\text{}` longhand, including the two `[\ce{H+}]` spans in MHF4U's curriculum. The conversion was mechanical but gated — every rewritten span was rendered BOTH ways through the build's own KaTeX and compared character by character, so the only differences that survived review were the intended ones (a bond dash where `--` had been setting an en dash). Units were the risk and were left alone: `1.10\ \text{V}` is volts, `T(\text{K})` is kelvin, `^\circ\text{C}` is Celsius. A real build of SCH4U emits 278 pages with zero `katex-error` spans. The example-content skill's chemistry rule now requires `\ce{}` and forbids hand-built formulae. The teacher callout that compared the two forms was then dropped entirely — a teacher does not care which form was used, only how mhchem works — and replaced with a **mini-tutorial**: a three-column table (what you type / what appears / what it means) covering subscripts, coefficients, charges, brackets, states, both arrows, hydrates, bonds, electrons, precipitates, and a condition set above the arrow, tailored per course. Every row was rendered through a real build to confirm it does what the row claims. | Shared Python, so Windows gets it for free. |
| 121 | 2026-08-13 | "What This Site Can Do" — the page that shows a teacher what the tool actually does — was buried in the Style folder, and in two courses it was not in Key Links at all. | ✅ It is now the LAST entry of `Key Links` in all eighteen payloads (first entry stays Curriculum Expectations), so a teacher evaluating the software meets the tour from the sidebar rather than hunting for it. Sixteen payloads already linked it mid-list and were reordered; SCH3U and SCH4U were missing it entirely. `lint_payload.py` now fails a payload whose Key Links lacks the link or does not end with it, and the rule is written into the example-content skill. Verified on a real build: the page's link list ends with it. | Content and lint only — nothing for Windows to port. |
| 122 | 2026-08-13 | Course-level pages must carry `createdSectionN` / `draftSectionN` — one pair per section — so publishing state and dates are decided per section. Two sections are never quite in step. | ✅ Fixed at the two places that write pages. **The installer**: `install_payload_file` now splits a course-level page's `created:`/`draft:` into a pair per section, written together in section order (`createdSection1`, `draftSection1`, `createdSection3`, …). The payload itself keeps the ordinary single sentinel, because a payload cannot know how many sections the teacher will choose — and the linter now REJECTS per-section keys in a payload. Only the frontmatter block is touched: the `draft: true` shown inside a fenced code block on the site-tour page is documentation, not metadata. **The app**: adding a section to an existing course left every shared page with keys for the OLD sections only, so the new section built them with no date and no publishing state at all; `SectionAdder` now extends each course-level page with a pair for the new section, taking the lowest existing section's publishing state (the same 'behave like the sections beside it' rule its scaffolding already follows) and a fresh date. A page written with plain `created:` is left alone — it already applies to every section. Also found and fixed by the new lint rule: **SNC1W** (converted from the standalone example course) still carried the real course's hard-coded `createdSection1/2` dates on 94 pages, which would have given a teacher with sections 1 and 3 keys for sections 1 and 2 and dates from someone else's timetable. Verified end to end on a real two-section build: a page marked `draftSection3: true` publishes in section 1 and is absent from section 3, and the merged copy shows `created:` resolved from `createdSection1`. All 18 payloads sweep clean through the installer; 248 app tests pass, including two new ones. | Windows: its Add Section equivalent needs the same pass over the course-level pages — extend each page that already uses the per-section form with a pair for the new section, copy the lowest-numbered section's draft state, stamp a fresh date, and leave plain-`created:` pages untouched. The installer half is shared Python and comes free. |
| 123 | 2026-08-13 | Eighteen course codes have example content. Every other Ontario code started as empty folders with a one-line stub in each — no class pages, no Key Links, no site tour, nothing to react to. | ✅ Every code now starts from a **skeleton shaped for its subject**: the folders that suit it, four units of three class pages to rename, a `Most Recent Class` landing page, `Key Links`, a `Curriculum` folder explaining how to fill it in (with one dummy expectation showing the shape — named as the example-content payloads name it, so every course this software builds keeps its expectations in the same place), and a `What This Site Can Do` tour that shows maths only for maths and science, `\ce{}` only for chemistry, and code only for computing. **50 families over 499 course-code prefixes**, mapped by three-letter prefix with a generic fallback for club codes: ADA is drama and AMU is music (Repertoire, not Conventions); SCH investigates where TXJ has clients and a salon; a kitchen course gets Kitchen Safety. They are GENERATED — `generate_skeletons.py` holds eleven shapes and fifty families and writes 1,994 pages — so a change to a shape fixes every family at once, and `lint_skeletons.py` gates them (every link resolves, every page titled, no template token left). Both entry points offer the subject's folders as the DEFAULT answer, editable as always: the CLI wizard from the skeleton manifest, and the app's New Course sheet through the new `SkeletonCatalog` — with a toggle naming the subject, and a rule that a folder list the teacher has edited is never overwritten. Verified by running the real wizard end to end for AMU3M: 56 pages, music folders, per-section keys, and a build of 55 HTML pages with **zero broken links**. | Windows: the installer and the skeletons are shared, so a course created by its CLI path gets them free. Its New Course dialog needs the `SkeletonCatalog` equivalent — read `support/skeletons/families.json`, map the three-letter prefix to a family, seed the folder lists from that family's `manifest.json`, show the toggle, and write `use_skeleton` into the config. Leave an edited list alone. |
| 124 | 2026-08-13 | In a skeleton course the Curriculum folder appeared in the sidebar, and the subject's folders had no chevron — they should behave like Tasks and Tutorials, while All Classes stays a plain link. | ✅ Two causes, both fixed. The first was already half-fixed by entry 123's rename; the second was the real one: the app took its hide and expand lists from a FIXED generic list, so the moment a teacher added or removed a folder, the course stopped matching a known skeleton and fell back to a list that knows nothing about Repertoire or Investigations — curriculum folder visible again, subject folders flat. Expandability is now **structural rather than a list**: every visible SHARED folder carries a chevron, including one the teacher just invented; the curriculum folder is never visible; the per-section All Classes stays a plain link to its listing. The rule lives in one pure function, `SkeletonCatalog.sidebar(for:…)`, used by the app, mirrored in the wizard's prompt defaults, and enforced on all 50 manifests by `lint_skeletons.py`. Verified through the real wizard with a teacher-added folder: `Field Trips` gets its chevron, `Curriculum` stays hidden, and the built site's explorer carries exactly that list. | Windows: same rule for its New Course dialog — do not intersect with a fixed list. Hidden is `Media` plus the family manifest's `hidden` (keeping only entries the course actually has); expandable is every shared folder that is not hidden. Per-section folders are never expandable. |
| 125 | 2026-08-14 | Ontario asks two things of a course that are easy to lose track of by November: every specific expectation addressed at least once, and every OVERALL expectation evaluated for marks at least once. Nothing in the tool showed either. | ✅ Every built site with curriculum pages now carries a generated **Curriculum Coverage** page: one column per strand, one cell per specific expectation, coloured red → orange → yellow → green → dark green for 0, 1, 2, 3, 4+ pages addressing it, with the count printed in the cell so the map does not depend on colour alone. Cells addressed by a page in `Tasks` carry a ring, and each strand's overall expectations appear as chips — green when assessed work reaches them, red when nothing marked does — which is the second Ontario rule answered on the same page. Hovering a cell gives Quartz's own page preview: the generated anchors carry `class="internal"`, and Quartz's link processor adopts them exactly as if they had been written as wikilinks. **What counts is deliberately narrow**: a page covers an expectation when it TRANSCLUDES it, which is the form a curriculum connection takes. The first implementation keyed on the `%%curriculum-start%%` markers and reported 57 of 57 expectations uncovered — the installer strips those markers when it installs a course, so they never reach a built site. **Only published pages count**, added after the first build shipped: a page still marked `draft: true` is not on the site, so it cannot have addressed anything — next week's lesson, written early, leaves the map exactly where it was until the day it is published. The map applies Quartz's own draft test (`draft: true`, or the string `"true"`, and nothing else), and it runs after per-section keys are resolved, so a course whose sections are at different points gets an honest map for each one. Verified on ICS3U: 57 expectations, 12 not yet addressed, 18 addressed exactly once, 5 overall expectations with no assessed work — then a draft page transcluding C1.1, C1.2, and C1.3 was planted, and those three cells stayed at 0 with 12 still uncovered; flipping that one page to `draft: false` moved all three to 1 and the uncovered count to 9, proving the rule in both directions. The page is generated into the assembled content, never into the teacher's vault, so it is rebuilt from the site's own links on every build and cannot drift; the Key Links entry is inserted into the BUILT copy for the same reason. A wizard switch (default ON, only asked when the course keeps a curriculum folder) writes `include_curriculum_coverage`; a missing key reads as ON, so existing courses pick the map up on their next build. Turning it off removes the page cleanly, and a course with no curriculum folder never gets one. | Shared Python, so Windows gets the page, the styles, and the wizard prompt for free. If its New Course dialog ever grows a toggle to match, write the same `include_curriculum_coverage` key; until then the dialog's Return-at-every-prompt behaviour accepts the ON default. |
| 126 | 2026-08-14 | The first Curriculum Coverage map printed a count inside every cell and ran its legend as one wrapped line under the map. Fifty-odd cells each carrying a digit read as a table of numbers rather than a map, and the legend's "one page / two / three" wording described pages when the teacher is asking about expectations. | ✅ Cells now carry the expectation's code alone, with roughly double the padding around it, so the colour is what reads. The count is not lost: it reaches a screen reader through each cell's label ("B2.3, addressed twice, included in assessed work") and a teacher through the hover preview. The legend is a vertical list under a horizontal rule, worded from the expectation's side — "Not yet addressed", "Addressed once", "Addressed twice", "Addressed three times", "Addressed four or more times", and a ringed swatch for assessed work. Its swatches are plain colour blocks now that no cell prints a digit for them to key to. The appended stylesheet block is REPLACED rather than skipped when already present, so a course whose stylesheet survives from an earlier build still picks up a change — the first render of this tweak showed the old styles for exactly that reason. | Shared Python and shared stylesheet, so Windows gets it with no separate work. |
| 127 | 2026-08-14 | Three faults in the Curriculum Coverage map, reported from a real course: the ring marking assessed work barely showed against several cell colours; the page appeared in the navigation sidebar, where students do not need it and it sat above the folders; and the wizard's heat-map question was asked in the structure section, far from the curriculum question it depends on. | ✅ The ring is now TWO rings — white inside dark — so whichever tone matches the cell recedes and the other carries the shape. Verified by rendering all five cell colours, ringed and unringed, on light, dark, and warm page backgrounds: legible in every combination. Rings sit inside the cell, so a course's colour scheme cannot affect them. The page is added to the Explorer's omit set at build time — the same mechanism that already keeps Key Links and Private Notes out of the sidebar — so it is reached from Key Links only. And the wizard asks about the map immediately after the Ontario curriculum question, in Starting Content, with the dependency enforced in both directions: declining the curriculum pages forces the map off, while keeping them and declining the map is allowed. The rule lives in `CourseConfiguration.curriculumCoverageEnabled` rather than inside the wizard view, because a SwiftUI @State property set on a view that is not on screen never takes — a test written against the view passed a stale default and had to be rewritten against the pure function. | Shared Python for the styles and the omit set, so Windows gets both. The Windows New Course dialog has no toggle yet; its Return-at-every-prompt behaviour accepts the ON default, and the terminal wizard asks the question properly. |
| 128 | 2026-08-14 | Follow-ups from using the map on a real course: the assessed-work ring was still hard to see even as two inset rings; Key Links had drifted into an index of content — tasks, tutorials, a checklist — when it should hold only what sets the tone; and a teacher noticed D3.4 showed twice while three pages linked to it. | ✅ The ring now sits OUTSIDE the cell — `outline` with a 2px offset, in the theme's own text colour — so it never fights the cell colour and contrasts with every colour scheme by construction; the column gap widened so adjacent rings stay separate rather than chaining. Key Links is rebuilt across all 22 payloads as an orientation panel: how the class runs, the agreement, marks, help, what to bring, help sessions, learning goals, how the site is organised, the site tour — then the two curriculum links last, with Curriculum Coverage inserted beneath Curriculum Expectations by the build. The linter fails any link into a content folder and checks all three positions. The D3.4 count was correct — that page linked inline instead of transcluding — but it pointed at a real omission, and the page now carries the expectation it discusses. Counting also tightened, on the teacher's own suggestion: a connection counts only when the page carrying it is reachable from a class page, directly or through one page a class page links to. Coverage now means taught, not merely written. | Shared Python throughout, so Windows gets all of it. |

| 129 | 2026-08-14 | Six example courses were still 26-day samplers — a quarter of an Ontario credit — and every course's class dates drifted an hour out after the November time change. | ✅ Both fixed in the shared toolchain rather than in either GUI. **Length:** the last six payloads (SNC1W, SPH3U, SPH4U, TEJ2O, TEJ3M, TEJ4M, TGJ2O) and the standalone example course now run a full credit — 86 periods of 75 minutes plus a three-hour final evaluation, September 8 to January 19, meeting daily. That completes all 22: every one ends with three or four review classes and a task tagged `final-evaluation`, and every one now covers its curriculum completely — each specific expectation transcluded by at least one published page the course actually teaches, which the coverage heat map shows. Where coverage needed content rather than tags, the content was written: projectile motion in SPH3U, the neutrino in SPH4U, computer systems and client documentation in TEJ3M and TEJ4M, safety and careers in TGJ2O, sustainable practices in SNC1W. **Dates:** `semester_class_timestamp()` walked the semester from the tz-aware moment setup ran, so it carried September's daylight-time offset through to January and stamped every class after the change an hour out. It now walks a naive date and attaches the offset at the end, asking the OS what the offset was on THAT day. | Shared Python — the Windows app inherits both. Worth knowing when porting any date logic: `datetime.replace()` keeps the original tzinfo, which for `now().astimezone()` is a FIXED offset, not a zone. |

| 130 | 2026-08-14 | A switch — in the setup wizard and in a course's settings — to show or hide the "What counts" and "Reading it honestly" sections of the Curriculum Coverage page, tied to the main coverage switch so that turning the map off disables it. | ✅ Built, and the dependency runs all the way down. **Toolchain:** `build_curriculum_coverage()` takes `include_notes`, and the two sections moved into a `COVERAGE_NOTES` constant so the page can be written with or without them — the map, the legend, and the standings table are unaffected either way. `build_site.py` reads `include_coverage_notes` (absent means on). **CLI wizard:** `prompt_coverage_notes()` is asked immediately after the coverage question in all three places that question can arise, returns False without being asked when the map is declined, and the answer is saved and remembered. **macOS app:** a nested toggle in the wizard's Starting Content section, disabled when the map is off, and — because the coverage switch had never been exposed after setup — BOTH switches now appear in Settings → Overall, the second disabled while the first is off. Turning the map off in settings writes the sections off too, so a saved config can never claim sections on a page that is not being written. The rule is a pure function, `CourseConfiguration.coverageNotesEnabled`, for the same reason as the coverage rule: a SwiftUI `@State` set off-screen never takes, so the test would pass while asserting nothing. Three unit tests pin it, and a real container build of the example course confirms the page keeps its map and standings and loses exactly the two sections. | Shared Python, so the toolchain half is free. The Windows GUI needs the same two controls with the same dependency: the second switch disabled AND forced off whenever the first is off, in both the wizard and the settings screen. |

| 131 | 2026-08-14 | On the Curriculum Coverage page, the hover preview of an expectation slides UNDER the left-hand navigation — the preview is unreadable exactly where it overlaps the sidebar. Is that a z-index problem? | ✅ It was, and the cause was ours. Quartz builds a hover preview by appending it INSIDE the link you are hovering, so the preview's own `z-index: 999` only ranks it against its siblings within that link. Our coverage styles brighten a hovered cell or chip with a CSS `filter`, and a filter makes the element a stacking context — which sealed the preview inside the cell, at whatever depth the cell sits, while the sidebar carries `z-index: 1` and painted over it. Fix: every cell and chip is now `position: relative` at rest and lifts to `z-index: 1000` while hovered, so the element carrying the preview outranks the sidebar and takes the preview up with it. Proven in a real browser rather than by reading the CSS: a headless Chrome harness hovers a cell in a built course, waits for the preview, and asks `document.elementFromPoint` which element wins at a spot where the preview overlaps the sidebar — with the rules removed it answers the sidebar's folder list, with them present it answers the preview. (That harness needs a host that resolves extensionless URLs the way a real one does; a plain static server 404s the preview's fetch and no preview is ever created, which reads as "fine" while testing nothing.) | Shared Python — the coverage stylesheet is written by the toolchain, so Windows gets the fix with no separate work. Nothing in the GUI to mirror. |

| 132 | 2026-08-14 | Hovering a cell on the coverage map changes the LABEL's colour, and against some cell colours that label is then hard to read. Brighten the cell's background a little instead. And the map should sit on a neutral rounded panel, so the fixed cell colours never blend into whichever colour scheme the teacher chose. | ✅ Both, and the label problem was worse than it looked. Quartz recolours every hovered link with `color: var(--tertiary) !important` — measured in a browser, the yellow cell's label went from near-black to a pale sage on hover, about **1.4:1**, which is unreadable. The label colour is now pinned to whatever it was at rest (an `!important` is the only thing that answers theirs), and the hover shows itself by lightening the BACKGROUND: a translucent white wash that composites over whichever level colour the cell carries, so it needs no per-level rules. The wash strength was measured, not guessed — lightening the field a white label sits on COSTS that label contrast, so the first attempt at 22% took the two mid-ramp colours below 3:1; at 12% the change is plain on the dark cells, where most of a healthy map lives, and the worst-off label gives up about half a point. **The panel:** map, rule, and legend now sit together in one rounded plate drawn in the theme's own light grey — a light grey in light schemes, a dark grey in dark ones, so it looks native everywhere instead of introducing a second palette. The panel alone was not enough: the ramp runs from near-white yellow to near-black green, so on a dark scheme the darkest green sank into the plate (1.43:1). Every cell and legend key now also carries an inset hairline in the theme's mid grey — mid in BOTH light and dark — which holds the pale end and the dark end away from any ground. Checked by screenshot in both themes and by a contrast sweep over all five levels, hovered and at rest. | Shared Python — the coverage stylesheet and page are written by the toolchain, so Windows gets all of it with no separate work. Nothing in the GUI to mirror. |

| 133 | 2026-08-14 | Two steps of the heat map left their white labels below the 4.5:1 that small text needs — not on hover, at rest. Deepen the palette. | ✅ "Addressed once" went from orange `#ea580c` to `#c2410c` (3.56 → **5.18:1**) and "addressed three times" from `#16a34a` to `#15803d` (3.30 → **5.02:1**). Deepening the third step crowded it against the fourth, so "addressed four or more times" went deeper again, `#14532d` → `#052e16` (9.11 → **14.91:1**), which keeps the top of the ramp distinct — confirmed by eye in both themes, not just by the numbers. Red and yellow were already clear (6.47:1 white, 7.65:1 dark ink) and are untouched, so the map's red-to-green reading is exactly what it was. Every step now clears AA at rest; while a cell is hovered the two middle greens dip to about 4.3 and 4.0, because the hover wash lightens the field a white label sits on — a transient state, and the expectation's full text is in the preview by then. | Shared Python. Windows inherits the palette with no separate work. |

| 134 | 2026-08-14 | The panel behind the map stretches the full width of the article, so a four-strand course shows a wide grey rectangle with most of it empty. It should be as wide as the map is. | ✅ A `div` is block-level, which is the whole story — it took the column because that is what block boxes do, not because anything asked it to. The panel is now `width: fit-content`, which is min(max-content, available): it shrinks to the map and legend when they are narrower than the column, and a map too wide for the column still gets the full width and still wraps, so nothing is pushed off-screen. Measured in a browser at two window sizes — 499px panel in an 850px column when there is room to shrink, and exactly the column width at 463px when there is not, with no sideways scrolling introduced at either size. | Shared Python. Nothing to mirror in the GUI. |

| 135 | 2026-08-14 | The deepened top step reads as black, not as a green. Use a navy blue. | ✅ "Addressed four or more times" is now navy `#1e3a8a` instead of `#052e16`. The top step leaves the ramp's hue on purpose, and that turns out to be the fix for a problem the deepening had only papered over: with two greens at the top, the pair could be told apart ONLY by how dark they were, and a green dark enough to be distinct was a green indistinguishable from black. Navy separates by hue, which survives being dark — it holds its own on a dark scheme (1.76:1 against the panel, against 1.26 for the near-black green), keeps white text well clear at **10.36:1**, and is distinguishable from the greens for a red-green colour-blind reader, while still reading as "past the end of the scale" rather than as another grade of green. Every other step is untouched; comments in the toolchain that described the map as a "red-to-green reading" were corrected in the same pass, so the code does not describe a palette it no longer writes. Checked by screenshot in both themes and by a contrast sweep of all five levels. | Shared Python. Windows inherits the palette with no separate work. |

| 136 | 2026-08-14 | "Not yet addressed" and "addressed once" are practically the same colour. Rethink the whole scale, keeping the same core colours, and make sure every step is far enough from every other. | ✅ Rethought by measurement rather than by eye, because eye-picking is what produced the problem: the two reds measured **ΔE 10.2** apart (CIEDE2000 — RGB distance does not match what an eye reports), and anything under about 10 reads as one colour. Every combination of candidate shades in the five families was scored on the distance between EVERY pair, rejected unless each label cleared 4.5:1 against its own fill, and scored AGAIN through simulations of deuteranopia and protanopia — colour is the only visual carrier of the count here, so a scale that separates for most readers and collapses for the rest has not solved the problem. **The result is not the obvious answer, and the search is what showed why:** a mid-tone red fails BOTH labels — too dark for white, too light for dark ink — so red and orange cannot both be mid-tone, and the only way to hold them apart is to separate them by lightness as well as hue. Hence a deep red carrying white (`#7f1d1d`, 10.02:1) and a bright orange carrying dark ink (`#f97316`, 5.24:1), then `#facc15`, `#15803d`, `#1e3a8a`. Closest pair any ordinary-sighted reader now sees: **ΔE 31.4**, against 10.2 before; for a colour-blind reader, ΔE 12.4 and 11.1, against 6.0 and 8.9 before. The fill and its label are now defined together in one `COVERAGE_LEVELS` list that generates both the resting rule and the hover pin, so a fill can never end up wearing a label chosen for a different fill, and the overall-expectation chips take their red and green from the same list so the page never shows two reds. The search itself is committed as `scripts/choose_coverage_scale.py` — a design-time tool, not part of the build and not in the image — so the next person to question a colour can re-run the reasoning instead of re-guessing. Verified by rebuild, a contrast sweep of all five levels hovered and at rest, and screenshots in both themes. | Shared Python. Windows inherits the palette with no separate work. |

| 137 | 2026-08-14 | Career Studies and Civics are HALF credits — 55 hours each — and a student takes them back to back: Career Studies from September, then Civics picks up where it ends. Neither the linter nor the installer could express that. | ✅ Both now read it from the manifest, so a half credit is a declared property of a payload rather than a special case in the code. **The linter** had 110 hours, a 104–116 window, and three review classes hard-coded; it now reads `credit_value` (default 1.0) and `final_evaluation_hours` (default 3) and scales — a 0.5-credit payload is checked against 55 hours with a proportional tolerance and two review classes, and its error message names the credit it was checked against. **The installer** anchored every class page to September 8, which is right for the first course of a pair and wrong for the second: both would have been dated onto the same mornings. It now reads `class_start_school_day` (default 1) and moves the anchor that many school days later, counting school days rather than calendar days so the winter break and Thanksgiving still fall out correctly. Proven by measurement rather than by reading the diff: a full credit still runs 8 September to 19 January unchanged; a 42-period half credit runs 8 September to 5 November; and the same payload declaring `class_start_school_day: 43` runs 6 November to 15 January — the school day after the first course's last class, with no overlap. Regression-checked against all five existing payloads (still clean) and the installer end-to-end. **Correction, same day:** the first cut of this threaded `start_school_day` through every function but failed to PASS it at one of the two `install_payload_file` call sites — the one that installs per-section pages, which is where class pages live. The date arithmetic was proven at the function level and passed, so the gap only appeared when a real payload was installed and Civics came out dated to September alongside Career Studies. Proof now runs at the level the feature is used: installing GLC2O gives 8 Sep to 5 Nov, installing CHV2O gives 6 Nov to 15 Jan, and a full credit still gives 8 Sep to 19 Jan. | Shared Python, so Windows inherits both. Nothing in the GUI to mirror — but if the Windows New Course dialog ever exposes course length, it must write `credit_value`, and a teacher pairing two half credits needs somewhere to say which one runs second. |

| 138 | 2026-08-14 | Two courses shipped with a Curriculum Expectations link in the sidebar that led to a page nobody had written. Find out how the linter missed it, and whether anything else is broken the same way. | ✅ The linter had a dead-link check the whole time; it had two holes, and both are now closed. **It compared only the last path segment**, so `[[Curriculum/index]]` matched against any folder's `index.md` anywhere in the payload — which is exactly why CHA3U and THJ2O passed while pointing at nothing. Folder-qualified links now have their whole path checked. **It scanned line by line**, so a wikilink split by the 80-column wrap was never matched at all: the check most likely to catch a dead link was structurally blind to the commonest way of making one. Wrapped links are now found by a separate pass over the whole page. The second hole was the expensive one — **nine dead links across six already-released payloads** (BOH4M, CGC1W, ENL1W ×2, MCV4U, MPM2D ×2, MTH1W, TEJ4M, TGJ2O), every one a piped link where the wrap happened to fall between the target and its alias, so the prose read perfectly and the link led nowhere. All nine rewrapped. Worth recording why they were dead: Quartz's wikilink regex is `[^\[\]\|\#\\]+`, which does NOT exclude a newline, so it matches the wrapped link happily and builds a target with a newline inside it — no page has that name. Checked against the real regex in the tree rather than assumed. **Four payloads (CGF3M, CHA3U, CIA4U, THJ2O) reproduced verbatim Ministry expectation wording with no `About These Expectations` page at all** — no citation, no source URL, no copyright line — where the other 33 had one; written, with each document title, year, © holder and page range verified against the PDF, and an honest "there is no link to give you" where the source genuinely no longer exists rather than a plausible-looking URL. That sweep found more: **three payloads cited `dcp.edu.gov.on.ca` course pages that do not exist** (TEJ2O and TGJ2O, replaced with an honest statement; the 2009 Technological Education courses are simply not on that site), and **two used a course slug the Ministry's CMS does not have** (BOH4M and MPM2D needed `-pdf`). These looked live because dcp is an Angular app that answers 200 for any path — the CMS Delivery API is the only thing that actually tells you, and every dcp link in every payload was checked against it. All 37 payloads lint clean. | Shared Python — the linter is authoring-time tooling and the payloads are shared content, so Windows inherits every fix with no separate work. Nothing in the GUI to mirror. |

| 139 | 2026-08-12 | Teachers asked (back in the CLI era) for somewhere other than Netlify to publish. Add a third destination that is genuinely free at a teacher's scale, with no per-file surprises for ordinary course material. | ⬜ **Not yet on macOS** — this row runs the OTHER way: implemented on **Windows first**, and the mac side owes the launcher and GUI. Full brief with every design decision is in [`MAC-HANDOFF.md`](MAC-HANDOFF.md). The shared half is already done and the mac inherits it: `scripts/deploy.py --target cloudflare` discovers or accepts the account, creates or reuses this section's Pages project (marker `courses/<CODE>/.cloudflare_sites/section<N>.json`, mirroring the Netlify one), hands the built folder to wrangler, and prints the `Live URL:` label both apps' parsers already read — so no parser change was needed on either side. | Windows (done, `0306c98`/`e6611cc`/`8883ad9`): the Publishing picker offers Netlify, Cloudflare Pages, or a folder, in both Course Settings and the wizard. Choosing Cloudflare reveals a **Cloudflare Account ID** field with live validation (32 hex characters) gating Save/Create — asked for in the UI, not by the launcher's console prompt, because a token scoped to Pages **cannot list its own account** (verified against a real token: `/user/tokens/verify` says active while `/accounts` returns an empty list) and the app has nothing attached that could answer a prompt. The account lives in app settings, not course settings — it identifies the teacher, like the token. A permanent orange note states the one real difference between destinations: Cloudflare refuses any single file over 25 MB, documents and slide decks are comfortably under, long video is not, and embedding from YouTube or Vimeo avoids it; `deploy.py` also checks sizes BEFORE uploading and names the offending files. Milestones never say "Netlify" (pinned by a test). Verified end to end: a real section published to `<project>.pages.dev`, second publish reused the project in 23 s. Cloudflare's 500-builds/month limit does not apply — a Direct Upload reports `build=idle`, no build runs. |
| 140 | 2026-08-12 | The frontmatter flag that decides whether students see a page was `draft:`, with `draftSection<N>:` for shared pages. Teachers do not say "draft" — they say a page is published or it is not — and the polarity was backwards from the word. Make the flag `publish:` / `publishForSection<N>:`, and keep it true that a page with no flag at all stays visible. | ⬜ **Not yet on macOS** — Windows-first again, and almost all of it is shared. The mac app owes only the reading and writing of the new key names; the toolchain half is done and inherited: `build_site.py` maps `publishForSection<N>` → `publish` for the section being built, `patches/publish.ts` gives Quartz a `PublishFlag` filter, and `setup_course.py` creates new courses in the new schema. | Windows (done): shared toolchain plus the app's `PageFrontmatter`, which reads the new keys first, falls back to the legacy pair **inverted**, and writes the new key **in the old key's position** so a migrated page shows a one-line diff rather than reordered frontmatter. Quartz needed a patch, but not the obvious one: v4.5.0 already ships `ExplicitPublish`, which reads `publish === true` — and it flips the default, so the 60 of the sample course's 225 pages carrying no flag (every curriculum page among them) would have silently vanished. `PublishFlag` is eight lines that keep the forgiving default and change only the word. No flag day: legacy courses build exactly as before, and a page migrates the first time something edits it. The example content was inverted wholesale (1145 keys across 957 files, a pure one-line-per-key rename) along with the prose that teaches the flag. Three inversion bugs surfaced during the swap — a plan that thought published pages still needed publishing, a dangling-link check that found nothing in either direction, and a transition line that told the teacher the exact reverse of the truth — every one caught by a test that already existed. | 
| 141 | 2026-08-14 | Windows renamed the visibility flag from `draft:` to `publish:` (entry 140), but only the Windows side and the build were converted. Finish it: the payloads, and everything that reads or writes the flag. | ✅ Converted, and the conversion is what found the bugs. **Content:** 5,968 payload pages, 108 EXC2O course-level pages, and 1,944 skeleton pages, plus the skeleton GENERATOR so a regeneration cannot put the old key back — regenerated and diffed to prove it. The polarity is inverted, which is the whole risk of this change: `draft: true` means the page is HIDDEN and `publish: true` means it is SHOWN, so a careless one-word swap publishes 192 pages a teacher meant to keep private. Every flip was scripted and counted, and the built site checked afterwards. 144 pages of PROSE describing the flag were rewritten too, keeping the ~150 ordinary English uses of "draft" — "Report drafted; due next class", a `Pitch → Report → Draft → Edit → Publish` diagram, and the curriculum's own "produce revised drafts of texts" — which a blind find-and-replace would have mangled. **Three real defects came out of it, all invisible until something was actually installed:** `per_section_frontmatter` split only `created`/`draft`, so 493 shared pages carrying `publish:` were left UNSPLIT — two sections would have shared one publishing state, which is the exact thing that function exists to prevent. The coverage map's own `_is_draft()` knew only the old key, so a page held back with `publish: false` counted as published and could turn the map green before the class had happened. And the macOS app's `SectionAdder` copied `draftSectionN` when adding a section: on a course installed from a migrated payload it would have found nothing and published a page the teacher had held back. All three now read either key, with the legacy one inverted on the way through, and a test (`testALegacyDraftSectionKeyIsReadAndInverted`) pins the inversion so it cannot regress. The payload linter now REJECTS `draft:` outright, so a payload cannot drift back to a key whose polarity is the opposite of what it looks like. Verified end to end rather than by diff: 37/37 payloads and 50/50 skeletons lint clean, verify.sh green, the mac app's full suite passes, and CIA4U installed and rebuilt to the SAME coverage map as before the change — 64 codes, identical level distribution, 15/15 chips, 85 class pages published, the held-back final class still absent, no broken links. | Shared Python plus the macOS app. Windows already had its half (entry 140); the payloads, skeletons, generator, splitter, and coverage map are shared, so Windows inherits all of it. The one piece with a Windows counterpart is `SectionAdder` — if the Windows app grows an add-section feature it needs the same read-both-keys-and-invert rule, or it will publish pages a teacher held back. |

| 142 | 2026-08-15 | Colima is pinned at 2 CPUs / 4 GB in every launcher — a figure chosen for an 8 GB machine and then applied to every machine. Size it to the Mac it is running on. | ✅ `_colima_cpus` takes half the cores (floor 2, cap 6) and `_colima_memory_gb` a third of the RAM (floor 4 GB, cap 12 GB), so an 8 GB laptop gets exactly what it gets today and a 48 GB desktop gets 6 CPUs and 12 GB. Deliberately NOT the whole machine: the teacher is using the Mac while the site builds. The harder half was the VM that already exists, because Colima is shared infrastructure — another toolchain may have built it, and this Mac's was already 6 CPUs / 8 GB rather than the 2/4 the launchers create. So `_colima_growth_flags` only ever asks for MORE, and only when the VM is STOPPED: a VM somebody else sized up keeps its size, a VM already big enough is left completely alone, and resizing never takes down containers another tool is using, because resizing recreates the VM. Proved against four VM shapes with a stubbed `colima`: 2/4 upgrades to 6/12, this Mac's real 6/8 upgrades memory only, a deliberately larger 10/32 is untouched, and one already at 6/12 is untouched. verify.sh green. | Shared shell, but the mechanism is macOS-only — Windows uses WSL2, which sizes itself from the host already. Nothing to port; the `documentation/03-launcher-scripts.md` passage describing the old fixed default has been rewritten. |

| 143 | 2026-08-15 | "Publish" in the app now means two different things — a page a student can see, and the whole site going online. Put "Deploy" back for the site, reversing row 103. | ✅ Reversed, and row 103 was not wrong when it was written: there was one act to name then and there are two now. A PAGE is published (the `publish:` frontmatter flag, which the assistant can change); the SITE is deployed to Netlify, Cloudflare, or a folder (the teacher's own act, which the assistant never takes). One word for both makes "I published tomorrow's class" mean a flag to one person and a live site to another. 24 strings across 14 files: the toolbar button and tooltip, both No-Preview invitations, the progress title, the settings group (now "Deploying") and its "Deploy to" picker, the folder problem messages, the busy lines, the script phase markers, and the folder-copy completion note. Internal names deliberately keep their spelling — `deploy.sh`, `deploy_target`, `deploy_folder_path`, and every automation id — so nothing in the launchers, the config format or the UI tests moves. Left as "Publish" on purpose: the curriculum-coverage toggle and all `publish:` / `publishForSection<N>` handling, which are page visibility. One judgement beyond the Windows sweep: the Netlify failure messages ("Try publishing again" after a failed deploy) were swept too, because that sentence is precisely the confusion being fixed. 268 tests pass. | Windows did this sweep first (row 140) and this follows it; the two apps now use the same vocabulary. The wording list above is worth checking against the Windows one if either drifts. |

| 144 | 2026-08-15 | Build the local assistant for macOS, and see whether the Windows side's three-minute wait to get ready can be cut. Use whatever approach suits a Mac; different models on different Macs are fine, but do not hog the machine. | ✅ Built, and the three minutes became **two seconds** — because the wait was never about the model. **Windows runs its model in Docker-in-WSL2, which gets 2 cores and no GPU; the Mac equivalent would be Colima, a Linux VM with no access to Metal.** Same model file, byte for byte, same 3,411-token tool surface: **175 s to read the prompt in a container, 2.1 s natively; generation 5.5 tok/s against 158.** So `llama-server` ships in the app (25 MB, MIT, fetched by `mac-app/Vendor/fetch-llama.sh` rather than committed) and runs as an ordinary child process with `--n-gpu-layers 999`. Two consequences: their disk-cache save/restore mechanism — "the biggest win available" at 163 s a session — is simply **not needed** and was not ported, and the one-off prompt read is warmed in the background as the window opens, so a teacher never waits for it. **Different Macs get different models, and the ladder was decided by measurement rather than by size.** Under 16 GB gets Qwen2.5 1.5B (1.1 GB, ~0.3 s a reply); 16 GB and up gets 7B (4.7 GB, ~1.2 s). **There is deliberately no 3B rung** even though it is the obvious middle step: measured, it INVERTS POLARITY — asked to hide a page it called publish, in two of three trials, and answered three hide requests with undo — while also scoring BELOW the 1.5B (70% against 81%). That is the exact failure the separate publish/unpublish verbs exist to prevent, so the case was deleted from the enum rather than marked risky, on the same reasoning as having no delete tool: what cannot be selected cannot be selected by accident. The 7B routes at **94%, matching the Windows figure**, with no inversions. Not hogging the machine is enforced rather than intended: threads are capped at half the performance cores (never more than six, because generation is bandwidth-bound long before it is thread-bound), the model is resident only while the window is open, and a test asserts no tier ever takes more than a third of memory. The window is per section so the preview stays visible beside it; only DEPLOYS wait for a button, because publishing is backed up and undoable and a gate in front of every write teaches teachers to click through gates. **Still to do:** serving the same tools over stdio so Claude Code drives them, which is what keeps the two clients' safety rules from drifting. | Windows should try this too — llama.cpp publishes native Windows builds (CUDA, Vulkan, CPU) in the same release, and the rest of the design does not care where the server runs, since it is the same OpenAI-shaped HTTP endpoint either way. Written up in WINDOWS-HANDOFF with the numbers. Also recorded there: their "date appended, not prepended" finding REPRODUCED natively (13–14 points, against their 15), while "rewrite the schema examples to the real course" did NOT — 0 copies in 87 responses where they saw 9 of 9. That is grounds to re-test on the shipped model, not to remove it, and macOS keeps the rewriting. |

| 145 | 2026-08-15 | The rest of the Windows parity list: a section remembering when its classes meet, placeholder classes, inserting a class and pushing the rest back, curriculum expectations for a page, scheduled deploys, and Cloudflare Pages. | ✅ All six. **Timetable memory** is the shared `.internal/timetable/section<N>.json` — read and written as a FORMAT rather than shared code, under `.internal/` so it travels through backup, archive and restore. A partial list is refused whole and no file is written: a half-remembered timetable gets trusted and then dates the wrong classes. **Class insertion** renames highest-day-first, and the test earns that by simulating the WRONG order — Day 2 → Day 3 is refused because a real Day 3 still holds the name, leaving a hole, a mislabelled lesson and a silently skipped class. Links are rewritten by us, since a rename on disk reads to Obsidian as a delete plus a create; all five wikilink forms are handled and the Markdown-link limitation is written down rather than left to be discovered. **Curriculum mentions** put transclusions INSIDE the `%%curriculum-start%%` markers, or a course installed without curriculum would keep a dangling reference on a live site; the tools read out the full wording and the MODEL decides which fit, because that is a judgement about meaning. Those three tools go to the MCP surface ONLY — the local model still sees exactly fifteen, because fifteen is what its routing was measured against, and a test pins the count. *(Superseded by row 154: the local surface is now thirteen, and the pinned count moved with it. The principle — MCP may grow, the local surface only changes with a measurement — is unchanged.)* **Scheduled deploys** are a launchd agent labelled per course AND section, proved by round-tripping the plist through `PropertyListSerialization` rather than by scheduling anything; `launchctl` sits behind a fake in tests. **Cloudflare** needed more than the UI: `deploy.sh` had no Cloudflare support at all — Windows had `deploy.ps1`, the mac launcher did not — so the UI would have shipped dead. Written: `--target`/`--account`, separate Keychain entries, token verification kept apart from account lookup, and the account resolved `--account` → discovery → remembered → ask. 371 unit tests, 0 failures; verify.sh green. | Three things need a Windows look. Scheduled deploys there run `schtasks`, and the mac equivalent now exists, so the two can be compared. launchd runs a MISSED job at the next wake, which schtasks may not — the mac plan says so plainly rather than implying nothing happens. And a scheduled deploy sends the last BUILT site on both sides; whether it should build first is an open product question, flagged rather than decided. |

| 146 | 2026-08-15 | A scheduled deploy sent the last BUILT site. It should build when the alarm fires and then deploy — what the Deploy button does if you were sitting there and pressed it. And it should skip the build when nothing has changed, rather than burning a container start on identical bytes. | ✅ Both. The button's sequence is `BuildFreshness.needsRebuild` → `preview.sh --build-only` → `deploy.sh`, and the launchd agent ran only the last of those. That was not a small gap: **`deploy.sh` never builds** — it refuses outright when there is no built site — so a scheduled deploy either failed at half six or sent whatever was last previewed. The agent now runs the same three steps, with the staleness test written out in shell because the app is closed when the agent fires and cannot be asked. All THREE parts of the Swift rule are carried across, and the middle one matters most: a **preview's** build is never deploy-fresh however recent it looks, because serve mode bakes a `ws://localhost` live-reload client into every page and deploying that makes a visitor's browser knock on their own machine (row 108). The other two are "no built page at all" and "content newer than the built page", the latter skipping hidden entries — without that guard `.merged_output` is itself newer than the page inside it, so the site would look stale the instant it was built and rebuild every single time. Proved by running the shell rule against real files across six cases: no build, unchanged, content edited, rebuilt after the edit, only a hidden folder touched, and a preview build. The deploy is gated on the build having succeeded, mirroring the button returning early rather than sending the previous one; cleanup runs either way, so a failed build still leaves nothing pending and the agent does not fire again tomorrow. The plan's wording said the opposite of all this and was rewritten to match. 372 unit tests, 0 failures. | Windows schedules with `schtasks` and has the same shape of gap worth checking: if its task runs `deploy.ps1` alone, it deploys a stale build — and if it ever gains a build step, it needs the ws://localhost check too, or a scheduled deploy can put a live-reload client on the live site. Two differences already noted: launchd runs a MISSED job at the next wake, which schtasks may not, and Cloudflare IS schedulable on the mac because the plist carries `--account`. |

| 147 | 2026-08-15 | Test the models properly — there is a big jump between 1.5B and 7B, and the 3B was vetoed on only three trials per probe. | ✅ Re-run at **10 trials** (29 probes × 10 = 290 per model), same tool surface throughout, and it changed the answer. **The 3B veto holds and is not a small-sample artefact: 9 inversions in 10.** Llama-3.2-3B, a different family entirely, inverted **10 in 10 — on the same sentence** ("Hide tomorrow's class again… Ohm's Law"), which says something about that phrasing rather than about one model. Phi-4-mini and Gemma-2-2b are DISQUALIFIED and the reason was diagnosed rather than guessed: neither chat template has a branch for a top-level `tools` array, so llama.cpp silently drops it — 30-token prompts instead of 3,417 — and they cannot tool-call at all. **The find is Qwen3 4B: 100% on the Windows-comparable set, zero inversions, 0.74 s warm turn, 2.5 GB download.** It beats the 7B on accuracy, latency, memory AND download, so the 7B is no longer shipped — a 4B outscoring a 7B is not what anyone would have predicted from the sizes. **The trap is that this is one flag deep.** With thinking left ON, the same weights score **39%**: the 256-token budget is eaten inside `<think>` and 196 of 290 turns never reach a tool call at all. `--reasoning-budget 0` is now passed at startup, the arguments were pulled into a testable function to make that assertable, and a test fails if the flag ever goes missing — because losing it makes the assistant quietly worse rather than visibly broken. Resident memory turned out to be mostly KV cache and therefore adjustable: 5.04 GB at 16k, 3.87 GB at 8k, 3.22 GB at 4k, with routing bit-for-bit identical at all three. **8 GB Macs stay on the 1.5B, and that is a hold rather than a result** — Qwen3 4B at 8k would be a 21-point gain on exactly the machines with the worst model today, but 3.87 GB is 48% of an 8 GB Mac that is also running Colima at 4 GB, and the assistant can start a build itself so the two peak together. Every measurement here is from a 48 GB M4 Pro; that rung wants a real 8 GB machine before it moves. Qwen3-1.7B was measured and rejected for a subtler reason: no inversions, but it filled in TODAY's date on 20 of 40 "tomorrow" calls — the wrong day is still the wrong page in front of students. 376 unit tests, 0 failures. | Windows should re-measure on the same footing before assuming its 1.5B is the right choice — the whole comparison is in `research/ai-assist/macos-native-10-trial-comparison.txt`, and the suite is the one already in `research/ai-assist/`. If Windows adopts Qwen3, the reasoning flag matters there just as much: the per-request equivalent is `chat_template_kwargs {"enable_thinking": false}`. |

| 148 | 2026-08-15 | The local model is a router and routers are sometimes wrong. Show the teacher what it understood before it acts — and stop backing up on every single command, which is wasteful on a vault full of images. | ✅ **Plan mode**, and the backup moved to once per conversation. A write now runs its `plan_` twin first, and the teacher sees the plan in the twin's own words with **Go** and **Cancel**; the model is not consulted twice, so what runs on Go is exactly what was described. **Writes only** — reads answer immediately, because gating "what do students see right now?" makes every question two clicks and teaches people to press Go without reading, which costs the gate its whole value. **Always on for the small model and not turnable off**: 79% routing is not a rate at which anyone should be handed a "stop asking" button, and a remembered "off" from a capable Mac is IGNORED on an 8 GB one so the setting cannot leak across machines. On the capable model it is offered off **after five accepted in a row, once**, while getting it right five times running is still fresh rather than months later in a settings pane; a **Cancel resets the run**, because somebody who has just stopped the assistant doing the wrong thing must not then be asked whether it should stop asking. "Keep checking" is the default button, so a reflexive Return keeps the safer arrangement. Deploys still always ask. **Two premises behind this turned out to be false and were worth checking:** undo is NOT git-backed — courses are not git repositories at all — it is in-memory before/after snapshots that die with the window, and it deliberately SKIPS files edited in Obsidian since, so it is a weaker net than it sounds; and the app was not backing up once per conversation but **before every write**. Both corrected: one lazy backup per conversation (a read-only chat makes none), and that zip is what Restore uses. Backups now carry provenance in the name (`_assistant-section<N>`) so a teacher choosing among several can see what made each and why, five are kept per course, and pruning touches only backups — archives and the wizard's zips share the folder and their parsers reject each other's forms. **A test caught a real bug in this work**: `planTwinName` claimed a twin for every write including four that have none, so plan mode would have asked for `plan_rebuild_preview`, been told there is no such tool, and shown the teacher an error where their plan should be. The agent now asks the surface whether the twin exists. 392 unit tests, 0 failures. | Windows should match all of it — written up in WINDOWS-HANDOFF under "Plan mode, undo, and how often to back up", including the reasoning for writes-only, the five-in-a-row offer, the Cancel reset, and why the backup is per conversation rather than per command. Also corrected there: that file still told Windows the visible verb is "Publish, never Deploy", which rows 140 and 143 reversed. |

| 149 | 2026-08-15 | Automatic pruning should only touch backups the assistant made. | ✅ Narrowed, and the distinction is the point. A teacher's backup is a DECISION — they pressed Back Up because they were about to do something they were unsure of, or the app saved one on their behalf before adding a section — and deleting that on a schedule they never agreed to is the app overruling them about their own work. The assistant's are different in kind: it saves one per conversation whether or not anybody asked, so clearing up after itself is its job. The two are now counted separately, which matters: a teacher with twenty of their own keeps all twenty, and they never crowd out the assistant's five. The list's own wording followed the rule rather than lagging behind it — each row now says what happens to THAT copy ("You made this one, so it is kept until you delete it" against "The assistant made this one; its five most recent are kept"), because a teacher deciding whether to keep their own copy needs their answer, not the assistant's. Two tests pin it: twelve teacher backups all survive a prune, and with seven of each interleaved the assistant loses its two oldest while every one of the teacher's survives. Archives and the wizard's zips are still untouched. 401 unit tests, 0 failures. | Written into WINDOWS-HANDOFF alongside the per-conversation backup rule. The trap to avoid is counting both kinds together — that quietly deletes a teacher's oldest backup as soon as the assistant has made five of its own. |

| 150 | 2026-08-15 | The local assistant should run one at a time on a machine — no working with the assistant on another section until the open one is closed, with the other menu entries dimmed. | ✅ Enforced app-wide by `AssistActivity`, and the reason is worth recording: the DOWNLOAD is shared (one file per Mac in Application Support, whatever course or section, surviving app updates) but the RUNNING copy is not. Each window starts its own engine and loads the weights again, so two windows is twice the resident memory — on a 16 GB Mac that is most of the machine, and it undoes the point of sizing the model to the hardware in the first place. It also removes a subtler cost: two conversations about two sections would evict each other's prompt cache on every alternating turn, since their prefixes differ from the first sentence, so both would feel slower than either alone. The menu item for any other section is dimmed with a line naming what to close — "Close the assistant for ICS3U Section 1 first" — because dimmed alone says "no" while naming the holder says what to do about it, the same shape "Add Section…" uses when a course is busy. Three details that are easy to get wrong and are pinned by tests: the claim is taken when the WINDOW opens rather than when the engine is ready (a teacher three minutes into a download has it open as far as they are concerned); it is released unconditionally on close, and a newer claim replaces a stale one, because a leaked claim locks the feature out until the app restarts — a worse failure than briefly allowing two; and the section that already HOLDS it stays enabled, since choosing it just brings that window forward. The registry is read during the row's render, not in the button's closure, or the menu shows the answer from whenever the row last drew. Governs the built-in assistant only — Claude Code over MCP has no engine of its own. 409 unit tests, 0 failures. | Windows needs the same rule for the same reason, and it applies whatever engine they end up running: the constraint is resident memory per window, not anything macOS-specific. Written up in WINDOWS-HANDOFF under "One assistant at a time, machine-wide", including the claim-on-open and release-unconditionally details and the render-time staleness trap their own `CourseActivity` equivalent already documents. |

| 151 | 2026-08-15 | The context-menu label should read "Revise with local AI assistant…" rather than "Revise with AI…". | ✅ Changed. Worth the extra three words: **local** is the fact a teacher most needs and least expects — the assistant runs entirely on their Mac, nothing they write is sent anywhere, and no account or subscription is involved. "AI" alone invites exactly the opposite assumption, and a teacher weighing whether to point it at student work should not have to open the window to find out. Title case and the ellipsis CHARACTER (…) rather than three periods, matching every other menu item and the macOS convention that a trailing ellipsis promises a further step before anything happens. 409 unit tests, 0 failures. | Windows should use the same words for the same reason. If its assistant ever gains a non-local option, this label is where the distinction has to be visible — which is an argument for keeping "local" in it even then, rather than dropping it once there is something to contrast with. |

| 152 | 2026-08-15 | The assistant's suggestions should fold under a disclosure group per kind of request, and be pinned to the TOP of the window with the chat scrolling beneath. Opening the assistant on any section later should remember which groups were open. | ✅ The list used to appear only while the conversation was empty — so a teacher met it once, at the moment they knew least what to do with it, and never saw it again. It is now pinned at the top, above the transcript, four disclosure groups **shut by default**: open, the eleven suggestions are taller than the conversation deserves to give up, so it costs four scannable lines until asked for more, and past 240 points it scrolls, meaning a teacher who opens every group gives up their own scroll position rather than the conversation. **A `ScrollView` takes every point it is offered**, so capping it with `maxHeight` alone left a stripe of dead space under the closed groups — the shelf measures its content with a preference key and asks for the SMALLER of that and the cap, which is what makes four closed lines occupy four lines. The open groups are remembered in **`@AppStorage`, deliberately not `@SceneStorage`** — scene storage is per WINDOW, so it would restore this window when it came back while a teacher opening the assistant on a different section met everything shut again, which is the opposite of remembering. It is a preference about how they like the shelf, not a fact about one window; this app also already carries a note about scene storage sharing one value across a window group and losing to the last writer. Stored as one `\|`-separated string since AppStorage holds no sets, with a test that no group title contains the separator. The suggestions themselves are unchanged, and that matters: the routing suite probes them word for word, and several are matched in code rather than routed, so what the shelf promises is what the window delivers. 412 unit tests, 0 failures. | Windows needs the same placement and the same persistence scope. Two traps: a per-window store looks correct until somebody opens the assistant on a second section, and a scrolling container that fills its offered space will leave the same dead stripe under the collapsed groups. If its own suggestion list ever diverges from these eleven, the routing suite has to be re-run against the new wording, because the phrasings are measured rather than chosen. |
| 153 | 2026-08-15 | (Found while re-measuring, not asked for.) The assistant was thinking on every single turn — the flag believed to stop it only capped it. | ✅ Fixed, and the fix is one word. `llama-server` has two separate settings: `--reasoning [on\|off\|auto]` decides whether the chat template opens a thinking block at all, and `--reasoning-budget N` decides how long it may run once opened. The app shipped for several days passing `--reasoning-budget 0` alone, on the reasonable-sounding belief that a budget of zero meant no thinking. It does not: Qwen3's template still opened `<think>` on every turn and filled it to the cap. Measured on one prompt, same weights, same surface, same temperature — **`--reasoning-budget 0`: 16.1 s and 512 completion tokens; `--reasoning off`: 8.4 s and 44 tokens; both together: 8.4 s and 44 tokens.** The useful reply is 44 tokens and the other 468 were thinking nobody sees, so this is **half the assistant's response time for a flag**. Both are now passed and `testThinkingIsTurnedOff` asserts both for every tier; the budget stays as insurance against a future model whose template ignores the switch, which is exactly how this shipped in the first place. **Why it survived review is the transferable part:** the wrong flag does not produce a wrong answer, only a slow one, and both obvious checks come back green — a tool call *does* arrive (512 tokens is enough to think AND still route on a short prompt), and there is no `<think>` in `message.content` because llama.cpp parses thinking out into `reasoning_content` by default, so the tag's absence proves the parser ran rather than that the model stayed quiet. The honest signal is the completion-token count and the clock. Note also that the routing suite had always run with `--reasoning off`, so the measured 93–97% never described the shipping app until now. Full record in `research/ai-assist/reasoning-flag-measurement.txt`; 455 unit tests, 0 failures. | **Check your launcher's flags before reading further — this may be part of the 3-minute wait.** If it passes the budget alone it has the identical fault. Two caveats: `--reasoning` is NEWER than `--reasoning-budget`, so confirm it exists in your build's `--help` before adding it (ours is llama.cpp b10435); and verify against your own engine rather than trusting our table, because the behaviour belongs to the model's chat template, not to the model. The measurement to run is completion tokens and wall-clock on a single prompt, NOT whether a tool call came back. |
| 154 | 2026-08-15 | Walk the whole tool surface and cut what the local model does not need: hide the six `plan_` twins and `remember_timetable`. Publishing must ALWAYS include linked pages — never a flag the model sets. Unpublishing follows a different rule: a linked page comes down only if the page being taken down is the only thing linking to it, and never for a folder landing page, anything in Key Links, or a curriculum page. | ✅ Done, with one correction to the instruction: those cuts come to **13** tools, not the 12 named — 20 minus six twins minus `remember_timetable` — and 13 was pinned rather than inventing a fourteenth cut to hit the number. The local surface fell from ~3,900 to **~2,230 prompt tokens**. `includeLinked` is gone; **no tool on either surface now takes a boolean at all**. Unpublishing is worked out to a FIXED POINT rather than in one pass — when a page joins the ones coming down, the pages only IT linked to become free to follow, so a chain A→B→C completes instead of stopping half-published — and the pages left behind are reported with their reason, named as the teacher would see them ("Ohm's Law — "Unit 3, Day 2" still links to it"). The three exclusions are checked BEFORE the reference rule and apply to the sweep only: a page the teacher names by hand still comes down. Re-measured against the shipping 13-tool surface, 42 probes × 10 trials: **the 4B routes 110/110 of the window's own suggestions, 94% of the like-for-like Windows probe set, 0 polarity inversions, 0 malformed calls, 0 invented dates, median warm reply 0.53 s**; the 1.5B manages 77% and 67% with 0 inversions. Cutting the surface cost nothing and bought latency. One real miss found in the same pass, and the fix for it is the more useful finding. A typo'd "publsh tomorows class … and the stuff it links to" chose `publish_pages` 10/10 and, with no page named, filled in an open-ended `onOrAfter` — one lesson quietly turned into the whole rest of the term. Both publishers carry linked pages, so the word "links" alone was pulling it there. **The obvious fix was tried first and measured worse:** adding "NOT for one day's class — use publish_class_on" to the description fixed that probe and broke three others, sending "Publish Unit 2, Day 3" — a named page with no date in it at all — to `publish_class_on` 10/10, and dropping the window's own suggestions from 110/110 to 90/110. A small model reads a sentence naming another tool as a recommendation rather than a boundary. So it was reverted to the byte, and the rule went into CODE: an open-ended publish (no pages, a start date, no end) is refused with a message saying to use `publish_class_on` for one day or to give both dates for a stretch. A refusal changes nothing the model reads, so it cannot cost accuracy, and it comes back as ordinary text the assistant reads out — so the model corrects itself on the next turn. Publishing only: an open-ended UNPUBLISH hides rather than exposes, is undone by the same backup, and clearing a section back to a date is a real thing to want. 457 unit tests, 0 failures. | Windows should make the same cuts and keep the same split: the MCP surface may grow, the local surface changes only with a measurement behind it, and a test should pin the count. The trap worth knowing: three probes now "fail" that are not failures — they ask for `remember_timetable` and `plan_add_next_class`, which the local model can no longer see, and it falls back to a harmless READ every time. Score against the surface you actually ship, not the suite you inherited. Two rules that will look like clutter to whoever reads the code next, so they are written down rather than left to be re-derived: publishing's linked-page rule is unconditional because a published page whose links lead nowhere is broken *always*, and unpublish is deliberately NOT its mirror because taking down this week's lesson must not strip a page last week's lesson still points at. |
| 155 | 2026-08-15 | (Found by auditing row 154's own consequences, not asked for.) Taking `remember_timetable` off the local surface left the assistant with no way to GET a timetable — it refused, pointing at a tool the model can no longer see. | ✅ Wired up. The schedule sheet already existed and was already attached to the assistant window; what was never written was the call that opens it, so `SectionSchedulePrompt.shared.request` was set by nothing in the app. Both places that discover there are no dates now ask: `add_next_class`/`plan_add_next_class` when the planner reports `noTimetable`, and `read_remembered_timetable` when the file is absent. Fire-and-forget on purpose — the tool still refuses THIS turn, because the dates genuinely are not there, and the teacher answers the sheet and asks again; waiting inside the tool would hold the conversation open across a file picker and a Google Sheets fetch. **Three messages were also rewritten to name no tool at all**: they used to end "record them with `remember_timetable`", which was a dead end the day that tool came off the local list, and the next move for a model with an unreachable remedy is to invent the dates — the exact failure the tool was removed to prevent. They now say what is missing and that Plantoir is asking the teacher, which stays true on the MCP surface too, where the client's tool list is different again. Tests pin the ask itself rather than the wording, since an unwired sheet is invisible to any assertion about text. 457 unit tests, 0 failures. | **Check this on the Windows side the moment you cut a tool**: the audit that matters is not "does the surface still route" but "can every refusal still be acted on by the surface that receives it". A remedy naming a tool the model cannot see is worse than no remedy, because it reads as permission to improvise. If Windows has no equivalent sheet, `remember_timetable` must STAY on its local list — the cut is only safe because something else asks. |
| 156 | 2026-08-15 | The plan card ends with "Nothing has been changed. Show this to the teacher and ask before going ahead." That sentence does not need to be shown. | ✅ Gone from the card, kept where it belongs. It is an instruction addressed to the MODEL, and on the card it sat directly above the Go and Cancel buttons that ARE the asking — telling a teacher to "show this to the teacher", and describing machinery besides. It is still right for Claude Code over MCP, which has no plan mode and genuinely has to be told to ask, so it is split rather than deleted: `AssistToolOutcome` gains `forTheCard`, an `AssistToolOutcome.planned(_:plan:)` factory writes the plan for the teacher and appends the model's instruction only on the way out, and the approval card reads `forTheCard`. The file's own doc comment already said the two audiences were "deliberately kept apart" — the card had simply been handed the wrong one. 473 unit tests, 0 failures. | Same split, and the same trap: any surface WITH its own Go/Cancel must not show the sentence, any surface without one must. Worth checking every string that reaches a teacher for this class of leak — an instruction written for the model reads as nonsense to a person, and it is invisible in review because it is perfectly correct in the file it was written in. |
| 157 | 2026-08-15 | Up and Down should cycle through the teacher's own previous prompts in a chat, the way a Terminal does. | ✅ Added, per section and kept across launches — which is what makes it worth having, since a teacher working on one section over several days asks for the same handful of things in the same handful of words. The rules are the ones a Terminal gets right and people notice only by losing something to them, so each has a test: the half-typed line is put ASIDE on the first Up and handed back by walking Down past the newest entry (not thrown away, and not overwritten by later presses); the same prompt twice in a row is remembered once, so testing something five times does not cost five presses to get past, while an earlier non-adjacent repeat stays where it happened; Up at the oldest entry does nothing rather than wrapping round, which would read as a misfire; and typing ends the walk, so Down cannot replace what was just edited. The keys hand back `.ignored` when the box holds more than one line — the arrows have to move the caret between those lines — and when there is nowhere further to walk, so the field answers instead of the keystroke vanishing. Stored as JSON in `@AppStorage`, not a separator-joined string: a prompt is free text, so any separator is a character a teacher may type, and that failure would be silent and strange. Unreadable storage becomes an empty history rather than a broken window. 16 new tests. | Straight port, and the four rules are the whole specification — a naive implementation gets the first one wrong (loses the half-typed line) and nobody notices until they have lost a sentence they were part way through. Scope it per section as we did, or one course's phrasings surface while working on another. |
| 158 | 2026-08-15 | "Showing work to students" is the wrong label for the publish group — showing to students is a deploy. | ✅ Now "Making pages visible". Group titles are UI only, never sent to the model, so this costs no routing accuracy and the measured phrasings inside are untouched. A test now fails any group title except the deploy one that claims to reach students. This was the third place the same conflation had spread to, so the other two were fixed in the same pass — see row 159 — and the lesson is that a wrong word does not stay in one file. | The label matters more than it looks: "Showing work to students" teaches a teacher that Publish is the moment of no return, when it is the safe, undoable half. A teacher who believes that hesitates over the wrong button and clicks through the right one. |
| 159 | 2026-08-15 | "Rebuild the preview" reported success while the section window read "No Preview Running". It should do exactly what the Preview button does — and can just be called "Preview". | ✅ Both. The tool only ever did the half it could reach: it ran `preview.sh --build-only`, because serving a preview needs a leased port and a web view, and both belong to the SECTION window, not the assistant's. So it built the site and told the teacher to go and look — producing the one thing an assistant must never produce, a confident report of success beside a window saying nothing is running. It now asks, through a new `SectionPreviewRequests` shared exactly as `SectionSchedulePrompt` is, and the section window answers by pressing its **own** Preview — so the two are the same code rather than two versions that can drift. A preview already running is left alone, because the button is a TOGGLE and an assistant that stopped a teacher's preview because they asked to see it would be absurd; the rebuild already done is what the running server serves. The same ask now follows every page change, so a teacher told their change is in the preview is looking at it. Card renamed to "Preview" — the word the button wears — with the old wording kept in `AssistCardCommand` for anyone who learned it. 476 unit tests, 0 failures. | Windows needs the same split if its preview is owned by a window rather than by the assistant. The general shape is worth stealing: when the assistant can only do half of something, it must ask whoever owns the other half rather than do its half and report success. Two of those have now been found in two days — this and the schedule sheet — so it is worth auditing every tool for "what does this NOT do that the teacher will assume it did". |
| 160 | 2026-08-15 | `check_section` said "1 broken link, 84 linked from nowhere" — unhelpful, and alarming for shipped example content. There should be a way to click the result and know WHICH. | ✅ Two findings, one of them reassuring. **The 84 were not a fault in the content.** 85 of the 87 unlinked pages in CIA4U are its CLASS pages, and a class page is a root, not a leaf: the rule the payloads are built to is that every page must be reachable FROM a class page, and the class page itself is reached through the site's navigation of All Classes. Nothing wikilinks to a lesson, and nothing should. So `visiblePagesNothingLinksTo` now skips class pages the way it already skipped folder landing pages, and a healthy course reports zero. **The 1 broken link was real and was ours to be pleased about**: `section1/index.md` transcludes `![[Unit 4, Day 19]]`, and Day 19 had just been unpublished by hand during testing — so the check caught a live consequence of an unpublish, which is exactly what it is for. (Day 20 is the payload's deliberately held-back class; the index points at Day 19 for that reason.) **And the result now unfolds**: `AssistToolOutcome` gained `teacherDetail`, carried into the transcript entry and shown behind a disclosure, listing every broken link with the page it is on and the page it points at, plus what to do about each. The teacher's copy is UNCAPPED where the model's stays capped at fifteen — a list of ninety would fill the model's context before the question was considered, while a truncated list of what is broken cannot be acted on by the one teacher whose sixteenth item is the one that matters. | Check your own orphan rule against a real 86-period course before believing its output: any definition that counts class pages will report a third of a healthy site as broken, and a check that cries wolf is a check teachers learn to ignore — which costs more than not having it. The `teacherDetail` split is worth copying wholesale: model-facing text is capped for context, teacher-facing text is whole and folded. |
| 161 | 2026-08-15 | The shelf should hint that tapping a suggestion puts it in the box, and tapping should NOT run it. | ✅ Tapping now fills the composer, focuses it, and waits. The header says so: "Things you can ask for — tap one to put it in the box, then change it to suit". Both halves matter. Firing immediately made the cards a row of buttons a teacher learns not to touch, since most of them want editing before they are true — "Publish Unit 2, Day 3" is a SHAPE, and almost never the page actually wanted. And without the hint a teacher either never taps, assuming it fires, or taps once, is surprised, and does not tap again. | Same behaviour and the same sentence. The hint is not decoration: an affordance nobody dares use is worse than no affordance, because it also takes up the space. |
| 162 | 2026-08-15 | Unpublishing a class must also repoint the section's index at the previous class, and move the index's date to match that class. | ✅ Done, and stated as an INVARIANT rather than a patch to one direction: *the section index transcludes the most recent class page students can see, and carries that class's date.* Written that way, publishing a newer class is covered by the same rule without a second one — and a rule stated once holds in the cases nobody listed. The date follows because the landing page's date IS the newest lesson's date to a reader; a front page dated August above a January lesson reads as abandoned. Three details earn their code: only the transclusion naming a CLASS page is touched, matched against the section's actual class titles rather than by position, since repointing `![[Key Links]]` at a lesson would be far worse than the bug being fixed; the newest class is read back from the GRAPH after the pages are written rather than worked out from the plan, so there is no second calculation to get wrong; and it happens inside `AssistPublishPlan.apply`, which puts it in the same `AssistChange` as the pages, so **"Undo that" takes the landing page back with them** — an undo that restored the lessons and left the front page pointing at the wrong one would be a worse state than either. A section with no dated visible class is left alone rather than pointed somewhere arbitrary. Four tests, including the undo. **Deliberately NOT done at build time**, though that was raised and would have covered a teacher who edits `publish:` by hand in Obsidian: it breaks the contract the toolchain rests on, that what is in the vault is what gets published. The builder works on a merged copy so it would not rewrite the teacher's files — which sounds like it escapes the objection and is in fact worse, because the live site would then show a page the vault does not contain and the teacher would have nothing to blame when the two disagreed. So the automation fires only where it can be seen to fire, and hand-editing frontmatter leaves the index exactly as written. **The note stays but now says the opposite thing**, in all 89 places it appears: 37 example courses, the 50 subject SKELETONS a course with no payload is built from, and both sections of the standalone example course. They told the teacher to repoint this by hand after every lesson; they now say to ask the local AI assistant, which does it and moves the date. Four payloads and the skeletons carried different wordings of the same instruction, so each was handled rather than assumed. **The skeleton GENERATOR was the one that mattered** — `generate_skeletons.py` still emitted the old sentence, so the next regeneration would have quietly undone all fifty; it was fixed, re-run, and its output diffed byte-for-byte against the checked-in skeletons to prove the two agree. Every payload still lints clean. 480 unit tests, 0 failures. | Port the invariant, not the bug fix — "unpublish repoints the index" leaves the publish direction broken and nobody notices until a teacher publishes a class and the front page still shows last week's. Two traps: match the transclusion by class title, or a section index that also transcludes Key Links will eventually have Key Links repointed at a lesson; and put the index write inside whatever your undo records, or undo restores the lessons and strands the landing page. |
| 163 | 2026-08-15 | A write command's cycle should be: the preview stops (if running), the file edits are made, the preview starts again for that section. The stopping and starting was not happening. | ✅ Fixed, and the mechanism was replaced rather than patched. Row 159 had the assistant POST a request that the section window observed and acted on — which cannot express an order at all: the runner posts, carries on, and the window stops the preview at whatever moment SwiftUI next evaluates a body, which may be after the writes. It also failed silently, which is worse than failing — the files changed, the preview did not move, and nothing said why. Now the section window REGISTERS its own `startPreview()`, `stopPreview()` and `isRunning` with `SectionPreviewControllers` when it appears, and the runner calls them directly, in order, on the main actor: stop → write → start becomes ordinary sequential code. Those are the same functions the toolbar button calls, so the assistant and the button cannot drift. **The stop sits beside the WRITES, not beside the start**, because that is the whole reason it is a separate step: a preview left serving while the pages beneath it are rewritten serves a half-changed site, and the teacher's next refresh is a race they cannot see they are in. Starting builds, so nothing does a separate `--build-only` pass first — that would build the same site twice and make a teacher wait through both. With no section window open there is nothing to drive: the site is brought up to date on disk and the answer says so plainly rather than claiming a preview nobody can see. Keys are standardised and case-folded on the way in, since the previous version compared raw folder paths and a mismatch there would have been invisible. **The test pins the SEQUENCE, not the set**: a fake preview records "stop", "write", "start" in the order they happen, so a stop that lands after the writes reads as `write, stop, start` and fails. 482 unit tests, 0 failures. | Take the registry, not the request. If your preview is owned by a window, the assistant needs the window to hand over its start/stop rather than to be told about an intention — anything observer-driven gets you "all three happened" while the order is wrong, and the symptom is a preview that looks stale for reasons nobody can reproduce. Two more: put the stop next to the writes it is protecting, and assert the order in a test, because every incorrect ordering still passes a test that only checks all three occurred. |
| 164 | 2026-08-15 | The assistant window should remember its placement, per section. | ✅ Remembered, and only the PLACEMENT. The window group stays `.restorationBehavior(.disabled)`, because reopening it at launch would load a model — gigabytes of a teacher's memory — before anyone asked for one; that decision stands. What is remembered is where the window goes when the teacher opens it themselves, which is the part they notice: an assistant that reappears in the middle of the screen, on top of the very section it is meant to sit beside, gets dragged back every single time. Per SECTION, because that is how it is used — section 1's assistant may live on a second monitor while section 2's sits beside the preview, and one shared position would mean arranging it again on every switch. AppKit's own frame autosave does the saving, so the frame is written on every move and resize and survives quit without any of our code needing a turn at the moment a window closes, which is exactly when it is least likely to get one. The saved frame is applied BEFORE the autosave name is set: the other order invites AppKit to write the freshly-cascaded default frame under that name and quietly overwrite the position being restored. Course codes are upper-cased into the key, so a course reached as `ics3u` from a typed tool argument and as `ICS3U` from the sidebar cannot end up with two remembered positions for one window. | Windows has its own placement memory; the two things worth copying are the SCOPE (per section, not per app) and the separation of placement from restoration — remembering where a window goes is not the same decision as reopening it, and conflating them is how an AI assistant ends up loading a model at launch. |
| 165 | 2026-08-15 | Make the assistant window feel like a conversation: pulsing dots while the model thinks, real chat bubbles with a tail on the last message of a batch, grey on the left for the assistant and blue on the right for the teacher, plans that stay in the transcript instead of being pulled back, and a pointing-hand cursor over the suggestion chips. Also keep the cursor in the box after sending. | ✅ All six. **The plan staying is the one that matters most.** It used to live only in the approval card, so the moment a teacher pressed Go or Cancel the description of what they had just agreed to vanished — and with it the context for everything after. A conversation you cannot scroll back through is not a conversation. The plan is now said in the transcript like anything else, and the card below it is reduced to its two buttons; the explanation is deliberately NOT repeated on the card, since two copies invite a teacher to compare them and wonder whether they differ. Deploy explanations got the same treatment. **The tail rule is the iMessage one and is measured against the same intuition**: one tail per run, on the last thing a participant said before the other answered — and it looks PAST anything nobody said, so a tool result between two assistant messages does not break one answer into two. Seven tests pin it, including an index past the end degrading to "no tail" rather than crashing, since the transcript and the speaker list are built separately. The bubble is ONE drawn path rather than a rectangle with a triangle stuck on: two overlapping shapes look right until the fill is translucent and then the seam shows. The teacher's bubble takes white text on the accent colour, because primary label colour on a strong accent fails in one of the two themes. **The dots appear for thinking AND for running a tool** — both are waits with nothing on screen, and a teacher does not care which one the assistant is busy with. The chips take the pointing hand by hand, because `.buttonStyle(.plain)` keeps the look and takes the cursor with it. And the composer keeps focus after a send: sending two things in a row should not need a click in between, and losing focus also loses the arrow-key history, which needs the field to have it. 492 unit tests, 0 failures. | The tail rule and the plan-in-the-transcript change are both worth porting exactly. Two traps: a tail on every bubble reads as interruptions rather than turns, and it is the kind of thing that looks fine in a screenshot of two messages and wrong in a real conversation; and any approval UI that OWNS the text it is asking about destroys that text when it resolves — put the words in the transcript and leave only the buttons in the card. |
| 166 | 2026-08-15 | The tail was a harsh triangle and overlapped badly; make it curved. Don't indent the plan's list items, and boldface its headings. | ✅ The bubble is now ONE continuous outline rather than a rounded rectangle with a triangle added as a second subpath — overlapping subpaths merge cleanly only under a non-zero fill AND matching winding directions, which is too much to hold true by accident, and the seam showed as a notch. The tail is two quadratic curves: it leaves the bubble tangentially, swells outward, and hooks back in. It is also drawn INSIDE the shape's rect now: the first version drew past `maxY` and `maxX`, outside the bounds the background was given, so the tip was clipped — which is most of what made it look severe. The body gives up a strip below and to its own side, and the bubble's padding grows to match, so the words keep the same distance from every edge. **Plan text**: headings are bold and list items have no leading indent. The indent was actively harmful in a narrow bubble — every line of any length wraps, and the wrapped half returns to the left margin, so the indent marked only the first line of each item. Bold needs `AttributedString(markdown:)` with `.inlineOnlyPreservingWhitespace`: `Text(aStringVariable)` does NOT parse markdown (only string literals do), and the default markdown interpretation throws the line breaks away, which would run a plan's headings and its list into one paragraph. Anything that fails to parse is shown exactly as it arrived. | Two portable traps. A tail built as a separate shape will show a seam the moment the bubble is translucent, and one drawn outside the background's rect gets clipped on any platform. And check how your toolkit treats a runtime string: several of them style literals only, so emphasis built at runtime reaches the user as literal asterisks. |
| 167 | 2026-08-15 | The approval card's heading does not work. Put the question in a second chat bubble, leave only the buttons on the card, and keep my answer in the history as my own message. | ✅ Exactly that. The assistant says the plan, then asks — "Shall I go ahead?", or "Shall I deploy?" — as a second message, and the card is reduced to two buttons with no heading and no copy of the plan. Pressing one records the teacher's answer as a TEACHER message: "Go", "Deploy" or "Cancel", in their own bubble on their own side. That is what makes the transcript readable afterwards, and the reason is worth keeping: a conversation where the assistant asked and nothing answered — but something plainly happened — is worse to read back than one you cannot read back at all. | Straight port. The principle underneath is the one to carry: anything that is part of the conversation belongs IN the conversation, and a control that owns text destroys that text when it resolves. Leave controls the choice and nothing else. |
| 168 | 2026-08-15 | After unpublishing, the preview stopped but never restarted, and a manual restart showed content from before the change. | ✅ One cause for both. `PreviewStopper` ran `preview.sh --stop` as a detached process and did not wait for it — right behind a button, where the teacher has already moved on, and wrong in front of a rebuild. Stop mode finds processes BY WORKING DIRECTORY, so as `preview.sh` says in its own comments it "catches builds as well as servers": the stop was still running when the rebuild began and killed it. Hence both symptoms at once — no preview, and a site on disk still at the last build that was allowed to FINISH, so a manual restart served pages from before the edit. The nasty part is that neither is an error: nothing failed, nothing was logged, and the teacher's only evidence was stale content with no reason to suspect the assistant. The controller's `stop` is now `async` and awaited end to end, with a 20-second bound so a hung stop cannot take the rebuild with it. The test's fake preview now reports "stop-begins" and "stop-ends" separately with a real suspension between them, so it can tell "called the stop" apart from "waited for it" — the previous test passed throughout this bug. 492 unit tests, 0 failures. | **Check this on the Windows side directly.** If its stop is fire-and-forget and its preview processes are found by directory, it has the same race and the same invisible symptom. The general lesson is worth more than the fix: a test that asserts an ORDER still passes when a step is merely *started* in the right place, so where a step must complete, make the fake take time and record its beginning and its end. |
| 169 | 2026-08-15 | The preview shows stale content even after stopping and starting it by hand. Previewing seems not to pick up edits to the source Markdown at all — a big regression. | ✅ Found, and it was NOT the assistant: it was the web view. Checked in order rather than guessed at — the source page, the merged copy and the built `public/index.html` were all current and all newer than the edit, so nothing about building was stale; only what was on SCREEN was. `WebPreviewController.loadIfNeeded` skipped the load whenever the URL matched the one already showing, and **a preview keeps its port across a stop and a start**, so a rebuilt site has exactly the same address as the old one. The check exists for a real reason — it is called from `updateNSView`, which SwiftUI may run at any moment, and without it a teacher who had clicked through to a lesson would be thrown back to the front page on every redraw — so it stays, and stopping a preview now calls `forgetLoadedPage()`, which is the honest way to say "what is on screen is no longer the truth". Requests also ignore WebKit's cache (everything here is localhost and rebuilt constantly, so a cached copy is never the one wanted, and a cache hit looks exactly like this bug), and Reload uses `reloadFromOrigin()` — the Reload button exists precisely because a teacher thinks the page is out of date, so serving them the copy they are unhappy with is the one useless answer. 492 unit tests, 0 failures. | **Check the same thing on Windows.** Any preview that reuses a port and guards its loads by URL equality has this bug, and it is invisible: no error, no log, just a site that never seems to change. The general lesson is the one to carry — "have I loaded this URL before" is not the same question as "is what I am showing still true", and conflating them costs a day of suspecting the wrong component. |
| 170 | 2026-08-15 | Mimic the iMessage bubble exactly, and use less padding. | ✅ Rebuilt from Messages' own geometry. Two structural corrections along the way. The tail does NOT hang below the bubble — its lowest point is level with the bottom edge, bulging sideways out of the corner and curling back underneath, which is why a Messages bubble keeps one flat baseline and reads as a single blob rather than a box with a spike. And the whole figure is now SCALED rather than fixed: Apple's constants assume a bubble at least twice its 17-point corner radius tall, and once the padding came down the bubble was shorter than that, so the top and bottom curves overlapped and the tail took a third of the height — which is what looked wrong in the side-by-side. Every number is expressed in seventeenths of the corner radius and the radius is clamped to half the height, so it stays in proportion at any size. Padding settled at 12 across and 8 down: tighter than it was, and enough that a single line is about 34 points tall, which is the height the geometry is drawn for. Both sides give up the same 4-point strip whether or not they have a tail, so a run of bubbles keeps one straight edge down the column. | If you copy the constants, copy the proportions too — hard-coded values are correct at exactly one bubble height and subtly wrong at every other, and "subtly wrong" is what a teacher notices without being able to say why. |
| 171 | 2026-08-15 | Stop and start now work, but only if you wait several seconds. Stopping and immediately starting still shows the old content. | ✅ The same race as row 168, on the path row 168 did not cover. That fix made the ASSISTANT await the stop; the Preview BUTTON still fired it and moved on, so a fast click started a build that the still-running stop then killed — and what got served was the last `public/` allowed to finish, which is the site as it was before the edit. Waiting a few seconds worked because the stop had quietly finished by then, which is exactly the kind of "works if you're slow" behaviour that gets excused rather than found. The guard now lives at the one place a preview BEGINS rather than beside each caller: `startPreview` waits for any stop still running for that section before it builds. That covers the button, the assistant, and whatever calls it next — there will always be a caller somebody forgot. Scoped to the section, so one window's Stop never delays another window's Preview: `PreviewStopper` now records which section each in-flight stop belongs to. 492 unit tests, 0 failures. | The lesson is about where a guard goes. Row 168 put it at the caller and fixed the caller in front of us; the identical bug sat untouched one button away. Anything that must be true before an operation belongs AT that operation, not at each of the places that happen to invoke it today. |
| 172 | 2026-08-15 | The tail hangs BELOW the bubble — the earlier "level with the bottom" reading was wrong. And "Preview" should stop any running preview and restart it. | ✅ Both. **The tail**, having now been wrong in both directions, is worth stating plainly: the tip sits below the bubble's bottom edge AND outside its side, and the underside curves back up INTO the bottom edge. A tail that only bulges sideways reads as a wedge stuck on the corner; one made of straight lines reads as a spike; it is the concave underside — the hook — that makes it a tail rather than a shape that happens to be pointy. Everything is still drawn inside the rect the background is given, since a clipped tail is severed rather than pointed, and the proportions scale with the corner radius so a short bubble and a tall one look like the same design. **Preview** now stops a running preview, waits for it, and starts it again rather than leaving it up: "Preview" means show me this section as it is NOW, and a preview already running is precisely the stale thing being complained about — leaving it alone answers the request with the very page it was asked to refresh. 493 unit tests, 0 failures. | Worth knowing how much of a shape's character lives in one curve: the difference between "an arrow stuck to a box" and "a speech bubble" here is the concave return, not the tip. And for Preview, the safe default for a refresh command is stop-and-restart — anything cleverer risks answering with the stale thing. |
| 173 | 2026-08-15 | Match the Messages bubble exactly — shape, size, padding — and its blue. The typing indicator's proportions are wrong. Keyboard focus goes to the first disclosure group after sending. Round the text field. | ✅ All four, and the bubble was finally settled by MEASURING rather than looking: a screenshot of a real conversation, read pixel by pixel. At 2x, in points — body height 25.5, corner radius 12, text inset 11 a side, tail drop 4.5 below the body, tail tip 5.5 INSIDE the bubble's edge. Two assumptions died there, both of which had looked right: the tail hangs BELOW the bubble (an earlier reading had it level with the bottom), and it stays WITHIN the bubble's width rather than poking out past the side, which is what had made it read as a wedge stuck on a corner. Three guesses had produced three wrong shapes; one measurement produced the right one. The blue was sampled the same way: **(20, 147, 255)**, where we had been using `Color.accentColor` — (0, 122, 255) by default, and worse, whatever accent the teacher has CHOSEN, so a graphite accent would have made their bubbles the same grey as the assistant's and quietly destroyed the left/right distinction the window depends on. The typing indicator became a proper bubble with a tail and large dots against a small body, since a wide flat pill with three specks in it is the shape of a progress bar, not of somebody typing. **The focus bug had a cause worth keeping**: the box was DISABLED while the assistant worked, and a disabled field cannot hold focus, so macOS moved the keyboard to the next thing it could find — the first disclosure group. Setting focus after sending could never survive that. Typing and SENDING are now separate permissions: the box stays live so a teacher can write their next message while they wait, exactly as every messaging app allows, and only the send waits for the run to finish. The field itself is now a rounded box with the send button inside its right end. 493 unit tests, 0 failures. | Measure the thing you are copying — three careful guesses cost more than one screenshot and a script. Two specific traps: never use the system ACCENT for one side of a two-sided colour scheme, because the user can set it to the other side's colour; and never disable a text field to mean "busy", because it takes the keyboard with it and the focus lands somewhere absurd. |
| 174 | 2026-08-15 | The preview still showed stale content — after an unpublish, and after a plain stop and restart. Pressing Reload fixed it. | ✅ The cause is inside Quartz, and it is worth stating exactly: `quartz build --serve` runs `server.listen(port)`, prints "Started a Quartz server listening at …", and only THEN runs `await build(...)`. **It serves the existing `public/` before it rebuilds it.** So the server answers 200 immediately with the previous build, and the fresh one lands seconds later — which is why editing and previewing showed the old page, why stopping and starting showed it too, why doing the same thing SLOWLY worked, and why Reload fixed it. Three innocent components were suspected first and each was proved correct by measurement rather than by reading: the merged content was current, the built `public/index.html` was newer than every source edit, and every hidden page was correctly absent from the build. The site on disk was right the whole time; only the screen was wrong. Now the preview notes the time before launching the build and waits until **`public/index.html` is newer than that** before showing anything — a fact about the output rather than a string in a log. Matching Quartz's emit line was tried first and is strictly worse: its wording can change with a version, and its progress lines are written through a spinner. The file cannot lie. Bounded at 120 seconds so a Quartz that never prints it cannot leave a teacher watching a spinner. **The reload that covers that fallback is conditional**, and the first attempt at it is the lesson: reloading unconditionally as cheap insurance made every preview in the app flicker, for a case that by then could not happen — insurance costing more than the risk. The signal says which situation we are in, so it decides: no emit line, no certainty, so reload; emit line, so leave the teacher's page alone. 493 unit tests, 0 failures. | **This is Quartz's behaviour, not ours, so Windows has it too.** Wait for the emit line, not for the server. Keep a reload only for the case where the emit line never arrives — reloading every time is visible jitter on every preview, which we shipped briefly and had to take back out. The debugging lesson is the more valuable one: when the screen and the disk disagree, prove which is lying before fixing either, because three plausible components here were all innocent. |
| 175 | 2026-08-15 | "Unpublish Unit 4, Day 12" is read as SECTION 4 — the assistant answers "CIA4U has no Section 4" and then asks whether to go ahead. Also: the window's size and position are restored after the model is ready rather than as it appears, and should be written down when it closes or the app quits. | ✅ Four fixes, and the first is the one that matters. **The course and section are no longer the model's to choose.** The window is opened for one section and its title says so, yet the tools took them as arguments and the model filled them in — a question it should never have been asked. "Unit 4, Day 12" being read as section 4 is a perfectly reasonable misreading of a page name that begins with a number, and no wording of the argument's description would stop it recurring on the next such name; the teacher has now hit it more than once. `AssistAgent` rewrites both arguments to the window's own before anything runs. Same principle as the coarse tools and the boolean-free surface: **a fact the app already knows is not a fact worth asking a model for**, and it cannot cost routing accuracy because it changes nothing the model reads — only what is done with what it said. Done in the AGENT, not the runner, because the runner also answers Claude Code over MCP where the course and section are genuinely the caller's to choose. **Second**: a plan twin that comes back with a REFUSAL is an answer, not a proposal, so it no longer arrives with "Shall I go ahead?" and two buttons — the transcript shows a teacher declining that four times in a row. **Third**: the window jumped after the model loaded because the root view asked for a size (`minWidth`/`minHeight` only), so SwiftUI's ideal size changed when "Getting the assistant ready…" was replaced by the conversation and macOS resized the window under the restored frame; the root now fills the window instead, making its size a consequence rather than a demand. **Fourth**: the frame is saved explicitly when the window closes and when the app quits with it open — AppKit's autosave writes when it chooses, and those two moments are the least likely to leave it a turn, while being exactly when a teacher has finished arranging things. 493 unit tests, 0 failures. | The first fix generalises, and is worth applying before it bites: **never ask a model for something the window already knows.** Audit every tool argument for it — anything the surrounding context determines should be overwritten on the way in, not described more carefully in a schema. Two smaller ones: a refusal must never be dressed as a proposal, and a window that asks for a size will be resized whenever its content changes, which looks like a bug in the restore. |
| 176 | 2026-08-15 | The window still opened at the default size and place. And the preview still showed stale content until a manual refresh — is it WebKit? | ✅ Both, and both were a wrong assumption rather than a wrong number. **The window**: SwiftUI's `WindowGroup` claims the window's frame-autosave name for itself — `NSWindow Frame assistant-AppWindow-1` was sitting in the defaults, being written faithfully — and it REPLACES one set on top of it, so ours was never written and every window opened where macOS put it. Found by reading the app's actual defaults rather than by reasoning about the code, which is what should have been done first. Fighting a framework for ownership of a value is a fight to lose, so the frame is now kept under our own per-section key and applied with `setFrame` directly; AppKit still constrains it to the attached screens, so a frame saved on a monitor that has gone comes back on one that has not. Saved on move, on resize, on close, and at quit. SwiftUI's own single key is left alone — it could never be per section anyway. **The preview**: it was WebKit, as suspected. A request's `cachePolicy` governs only the MAIN request, and a Quartz site is a single-page app — so the HTML arrived fresh while the scripts, styles and fetched content came out of the cache, and the page assembled last week's site from parts. The caches (disk, memory, fetch, offline) are now emptied before every load and before every Reload. Nothing worth keeping is lost: it is all localhost, served by something that rebuilds the whole site each time it starts. Local storage and cookies are deliberately NOT cleared, so the preview keeps what a teacher would notice losing, such as the site's light or dark setting. 495 unit tests, 0 failures. | Two traps, both platform-shaped. If your UI framework owns window restoration, check what it has already claimed before adding your own — read the stored settings, do not reason about it. And if your preview is a web view showing a single-page app, a no-cache POLICY is not enough: clear the caches, because the page's own fetches are where the staleness hides. |
| 177 | 2026-08-15 | The preview STILL showed stale content after an assistant unpublish — reloaded by itself, showed the old page anyway; a manual Reload fixed it. | ✅ Caught live with a watcher on the whole chain (served-body hash, built-file mtime, container state, port listeners) plus the app's unified log, which is what settled it: the server was serving the CORRECT new build three seconds before the app's page appeared, so every earlier suspect — merge, build, server, caches — was innocent this time. **The hole was in fix 176's own machinery.** "Must reload after a rebuild" was encoded as CLEARED STATE (`forgetLoadedPage()` nils the last-requested URL), and between the stop and the restart SwiftUI ran one more update on the outgoing web view, which reloaded the OLD server's site and quietly re-armed that URL. When the fresh build came up — at the SAME address, because a section keeps its port — `loadIfNeeded` compared URLs, found them equal, and skipped; the fallback reload was correctly gated off because the build HAD been seen finishing. Three `WebPageProxy::loadRequest` lines in the log, and the middle one is the whole bug. The fix is `showFreshBuild(url)`: when the build lands, the fresh site is loaded EXPLICITLY rather than by trusting the mounting view's equality check — a call cannot be un-said by a stray load the way cleared state can be re-armed. It marks the URL as requested so the mounting view's own `loadIfNeeded` becomes the no-op, keeping one load per rebuild — the flicker that made an unconditional reload worse than the problem (row 176's predecessor) is not reintroduced. A unit test replays the trap: request the address, let that navigation begin, then `showFreshBuild` must start another one, and a further `loadIfNeeded` must not. **Same session, adjacent finding:** Quartz's own live reload can never replace this timing machinery here — the page is told the CONTAINER's websocket port (only the first port block matches the host's), and deeper: file events from Mac-side writes do not cross the Colima bind mount at all, so the in-container watcher is deaf to every edit that matters. Tested both ways: an edit made inside the container rebuilt and pinged the websocket within a second; the identical edit from the Mac produced nothing in 45. Written up in `research/preview-staleness/FINDINGS.md` with the four changes live reload would need. 496 unit tests, 0 failures. | The trap is framework-agnostic and worth stating plainly: if "the view must reload next time" is stored as cleared state, any stray load between the clear and the intended reload re-arms the guard and the reload silently never happens — encode the obligation as an explicit call at the moment the new content is ready, not as state a race can undo. WebView2 has the same shape (`Source` property comparisons skip same-URL loads). Also inherit the finding, not just the fix: WSL2 bind mounts drop inotify for Windows-side writes exactly as Colima does for Mac-side ones, so do not build a Windows preview on the dev server's live reload either. |
| 178 | 2026-08-15 | The assistant chat should look identical to iMessage — the bubble tail isn't quite right, and the bubble dimensions (particularly the typing indicator) are off. | ✅ Measured from the side-by-side screenshot at 6× rather than guessed (the file's own history records three wrong guesses), and the measurements acquitted the tail: its ratios were already exactly Messages' (tip inset 0.42r, drop 0.33r, corner end 0.54r). What was wrong was the RADIUS the tail scales from, plus the padding. Messages draws a **single-line bubble as a capsule** — the silhouette scan shows its edge bulging continuously, radius 24px on a 48px bubble — while multi-line bubbles settle to a fixed radius (19px at that scale; our 12 at ours, which was already right). With a fixed 12 on single-line bubbles the corner finished tight and the tail hung off it like a drip; as a capsule the same tail ratios produce Messages' broad sweep. `AssistChatBubbleShape` now goes capsule when half the body height is ≤15 (one line ≈14.5, two lines ≈22, so the threshold sits between). Vertical text padding measured at ~0.6× the font size against our 0.42×: raised 5→7 in all four bubble cases, which also grows single-line bubbles into the capsule range. **The typing indicator is a THOUGHT bubble, not a speech bubble** — Messages marks "about to speak" with a capsule and two plain circles stepping down toward the speaker, not the pointed said-something tail ours wore. Rebuilt from measured ratios, height locked to a single-line message bubble (29pt) per Russell's instruction, so it occupies the slot of exactly the thing it stands for. **A second pass against a screen recording of Messages itself (frames extracted with AVFoundation) corrected three more things the first pass got wrong.** (1) The tail is ONE SIZE on every bubble: measured identical on a three-line and a one-line bubble, while ours scaled with the corner radius and shrank to a nub on multi-line bubbles — it now scales from a fixed `tailScale` (the single-line capsule radius, where the two coincide). (2) The corner-to-tail joint is about the TANGENT: Messages' outer silhouette reaches its deepest inset exactly at the bottom line while travelling straight down, then eases out to the tip; a corner curve that lands travelling horizontally meets the tail in a cusp and the tail reads as a comma stuck under the bubble. Verified by compiling the shape standalone and rendering it to PNG (ImageRenderer harness with a stub for the one app type the file names) — the render-measure-adjust loop took three iterations, which guessing had already proven worse than. (3) The recording corrected the artwork-derived indicator sizes (dots 0.24 of bubble height not 0.31, large thought-circle 0.41 not 0.31), and exposed that translucent fills double where the thought-circle overlaps the capsule — the indicator is now ONE shape (`AssistThoughtBubbleShape`, capsule + both circles as subpaths) filled once, seamless. **A third pass, from a composite screenshot with both apps on the same backdrop, settled the last two gaps.** The tail is a fixed size in ABSOLUTE POINTS — two Messages captures at different text sizes carry the same tail (~7pt drop, ~11.5pt corner landing) while their bubbles differ by a sixth — so the second pass's tailScale, scaled down by our smaller font, was still a third small; it is now the design constant 21, and the strip bubbles reserve beneath themselves grew 5→7 to hold it. And the assistant grey is Messages' own (59, 59, 61), not `secondary` at 0.16 — a translucent fill over a (30, 30, 30) window can only reach (50, 50, 50), measurably darker than Messages beside it. Light mode carries Messages' standard #E9E9EB, noted in code as unverified against a light capture. **A fourth pass corrected the third.** The "absolute ~21pt" tail was derived by comparing two SEPARATE captures, which needs each one's pixels-per-point — both were guessed, one wrongly, and the tail shipped a fifth too large with an inverted corner curvature (ours bowed away from the edge early and landed on a long straight chamfer; Messages hugs the edge and sweeps in late). A screenshot with BOTH apps on one screen cancels the scale out entirely: tailScale is 17 (drop 5.7 / landing 9.2 / tip 7.1 / hook 16.3), the corner cubic now hugs the edge longer (control1 at 0.55r) and lands short (0.18·tail), and the rendered corner profile tracks Messages within half a point along its whole length. The lesson the code now carries: derive cross-app constants only from same-screenshot comparisons. **A fifth pass fixed the corner's vertical symmetry**, which Russell's eye caught as the bubble "warping": profiled at matching distances from each end, Messages' bottom corner tracks its top corner almost row for row until the tail takes over, while ours carried its inset far too early (12px deep where the top corner had 5). The S-jog's cubic is now weighted to hug the edge and complete late (control1 at 0.37r, control2 at 0.10r), bringing the two corners within ~1.5pt of mirrored — the residue being inherent to handing off at a 9.2pt landing instead of completing the corner. **A sixth pass settled the "straight ends": the corner radius, like the tail, is an ABSOLUTE of Messages' design** — same-screen measurement put theirs at ~17pt here and ~16pt in a capture at a different text size, while our 12 came from scaling their radius by the font ratio, leaving visibly straighter ends. And at 17, the radius rule collapses to `min(17, height/2, width/2)`: single-line bubbles fall out as capsules with no separate branch, which deleted the capsule-limit machinery an earlier pass added. Top and bottom corners verified within ~1pt of mirrored at the new radius. **A seventh pass, from a highlighted-word comparison Russell staged in both apps:** (1) selecting text in a bubble now highlights in the system's light blue instead of SwiftUI's flat grey — `.textSelection(.enabled)` on macOS draws its own unstylable grey, so bubble text moved to an `NSTextField` wrapping label (`SelectableBubbleText`), whose field-editor selection is the same accent blue Messages shows; the markdown **bold** is carried into AppKit by hand because converting `AttributedString` directly keeps the emphasis intent but drops the font, and `sizeThatFits` answers SwiftUI's width proposals from `cellSize(forBounds:)` because a wrapping label otherwise claims one endless line. (2) The corner-to-tail landing is the one tail measurement that follows the RADIUS, not the tail constant: half of it. Same-height capsules showed theirs landing at 12px to ours at 15, while multi-line bubbles had matched — radius/2 fits both observations. **An eighth pass fixed the selection colour actually rendering and refined the tail against a native-2x capture.** The grey highlight had a second cause behind the first: applying the field editor's colours in `becomeFirstResponder` silently does nothing for a SELECTABLE label — unlike an editable field, the editor does not exist yet when that returns; proved in a harness that printed "no field editor". The right hook is the CELL's `setUpFieldEditorAttributes`, which runs when AppKit attaches the editor on any path; with it the harness renders (183, 217, 251) against Messages' measured (174, 218, 255) — and the colours are PINNED to the light-appearance selection blue with the glyphs in the bubble's own fill, because Messages pins its selection just as it pins its bubble colours, while the system's dark-mode `selectedTextBackgroundColor` (63, 99, 139) is the "grey" that started this. The tail, retraced at 2x: the jog gets 1.4× the radius of height (the steeper radius-high jog read as the side "sweeping in"), the hook rejoins at 0.85 of the tail scale not 0.958 (the width that read as stubby), the tip is ROUNDED about 1.5pt across, and the hook meets the bottom edge in a small curve — Messages' silhouette has no sharp vertices anywhere. **A ninth pass evened out the text insets — and built the loop that should have existed from the start.** Messages holds text 13pt from both bubble edges, symmetric; ours sat off by a few points either side, and two modelling attempts (pad the SwiftUI padding by the cell's measured internal slice; size the label to its typographic bounds) each missed — the second one taught that the cell answers a frame smaller than its own measure by silently SHRINKING the text, so its width is a floor, not a suggestion. What settled it was rendering the app's REAL bubble stack in an `NSHostingView` (ImageRenderer cannot draw AppKit views; a hosting view can) and measuring pixels: the label eats ~1pt before the first glyph and leaves ~4pt after the last, asymmetrically, whatever the width arithmetic does. The padding is therefore set empirically per side — leading 12, trailing 9, the asymmetry cancelling the label's — and the label's width stays fractional so no rounding lands trailing. The loop measures the shipped stack at exactly 26px/26px against Messages' 26/26 at 2x. **A tenth pass flattened the edge above the tail and widened the tail's root.** Traced on matching two-line bubbles: Messages holds the tail-side edge nearly flat for the first half of the descent and concentrates the dive in the last — 8pt of inset over 16pt of height, the span being the RADIUS itself, not the 1.4× an earlier pass fitted; the longer span drifted visibly early and read as a diagonal cut into the side. The jog cubic's late control moved from 0.08 to 0.215 of the span (the low value linearised the middle), and the corner landing came in from 0.55 to 0.47 of the radius — which also restored the tail root to Messages' 6.5pt width in the same stroke, since the root is the distance from landing to hook. Rendered profile verified within a quarter point of the iMessage trace at every checkpoint. 496 unit tests, 0 failures. | The transferable method matters more than the numbers: measure the real app's bubbles from a screenshot silhouette scan (per-row min/max of the fill colour) before writing any shape code — and measure at the platform you ship on, because Windows' reference is presumably not iMessage. Two shape facts that will survive the port anyway: chat apps use a capsule for single-line bubbles and a fixed radius for taller ones, so a single constant radius will always look subtly wrong; and a typing indicator is conventionally a thought bubble (circles, no pointed tail), which reads as "composing" where a tailed bubble reads as "said". Three more that transfer directly: draw the tail one size regardless of bubble size; when a corner flows into a tail, match the tangents at the joint or the tail reads as glued on; and if the bubble fill is translucent, build overlapping pieces as one geometry filled once (WPF GeometryGroup / Win2D path union), or the overlaps show as brighter seams. Close the loop by rendering the control offscreen and measuring the pixels against the reference — three shape attempts here failed by eye and passed only under that loop. |
| 179 | 2026-08-15 | The "what would students see?" answer arrived as three renderings: a terse count with a "Show me" disclosure, then the model's paraphrase of the same facts. Delete the first entirely and always reply with the prose form, composed deterministically. | ✅ `check_section` now composes the whole answer itself and ENDS the turn — "Students would see 231 pages in CIA4U Section 1. None of the visible pages link to unpublished pages, ensuring that all links are functional and point to published content." — with the broken-link and stranded-page lists appearing only when they are bad news, capped at fifteen with an "…and N more", and a preview sentence only when a preview is actually running for that section (worded "will show these pages once any rebuild in progress finishes", because the app cannot tell rebuilding from serving and must not claim either). The reasoning worth keeping: the model's paraphrase was the only rendering a teacher actually read, and being a paraphrase it was also the only one that could get the facts wrong — so the deterministic text IS the reply now, and `shouldContinue` is false on what is semantically a read, deliberately breaking the "reads hand back" rule for the one read whose answer is complete before any model sees it. The uncapped `everythingFound` teacher-disclosure is deleted with its "Show me". 496 unit tests, 0 failures. | The same three-renderings shape will appear on Windows the moment its check tool returns counts for a model to paraphrase — skip straight to composing the teacher-facing prose in code and ending the turn with it. Keep the two conditionals: problem lists only when non-empty, and no claim about the preview's state beyond what the app can actually know. |

| 180 | 2026-08-15 | Decision, from a documentation review: **Windows should move its AI model out of the container too.** The macOS assistant now runs `llama-server` as a host process on Metal, and it works well enough that the Swift implementation is the reference for how this feature should be built. | — Already the macOS arrangement; nothing to change here. Recorded so the decision is not lost between syncs. The mac's measurements: the same model, the same prompt, **175 s in a container against 2.1 s natively**, and 5.5 tok/s against 158. | **This is the direction, not a suggestion — the question left is which backend, not whether to move.** llama.cpp publishes CUDA, Vulkan and CPU builds for Windows in the same release as the macOS one, so `LocalModel` should need little more than a different way of starting the process: the endpoint is the same OpenAI-shaped HTTP either way. Measure what a teacher's machine with integrated graphics actually gets — that decides the tier ladder, not whether to do it. Five things change on the way out of the container: `--no-mmap` is no longer needed (it exists for the cgroup and there isn't one on the host); the prompt-cache save/restore machinery can be deleted (it buys 175 s in a container and 2 s outside one, and an empty save silently poisons the next session); the weights move to the host, one file per machine, verified by exact byte count; a second executable ships and must be signed with the rest, with the llama.cpp build number PINNED so measurements stay comparable; and if you follow the mac to Qwen3, `--reasoning off` **and** `--reasoning-budget 0` become load-bearing — passing neither is correct for Qwen2.5 and a 97%→39% bug the day the model changes. Full write-up in `WINDOWS-HANDOFF.md` under "The local assistant: run the model natively". |

| 181 | 2026-08-15 | Bug: telling the assistant to deploy did nothing visible. It should be exactly what a teacher does with the two buttons in the section window — Stop Preview if one is running, then Deploy. | ✅ `deploy_section` now presses the section window's own buttons through `SectionWindowControllers` (renamed from `SectionPreviewControllers`, which now carries `deploy` beside the three preview closures). Two faults, compounding into "nothing happened": (1) the assistant ran `deploy.sh` in a `ScriptRunner` of its own that no view was watching, so there was no console, no progress header and no live-site link — a four-minute job whose only sign was a spinner in the chat, beside a section window still reading "No Preview Running"; (2) a running preview — which is exactly when a teacher asks — made it REFUSE, returning `CourseActivity.busyDescription`'s menu fragment "Available once preview completed", a string written to sit under a greyed-out menu item and meaningless read aloud in a conversation. The stop is AWAITED before the deploy starts, same reason as before a write: stop mode kills a section's processes by working directory and would take the build with it. `SectionDetailView.startDeploy()` is split into the button path and `deployAndWait() -> AssistSiteWorkResult`, so both callers run one deploy, not two. Two further bugs found in the old assistant-only path, which survives for callers with no window (Claude Code over MCP, and a 6:30 a.m. scheduled deploy): it built its own arguments and never passed `--target cloudflare`/`--account`, so **a Cloudflare course deployed by the assistant went to Netlify** — it now asks `DeployCommand.arguments`, the same place the button asks — and its milestones had no Cloudflare case, narrating a Cloudflare deploy as a Netlify one. Also: the bare word "deploy" is now a matched card phrasing (it is the word on the button in front of the teacher; sent to the model it came back as a sentence *about* deploying). 3 new tests; 499 unit tests, 0 failures. | **Windows already presses its own button — and has the other half of this bug.** `AssistAgent.RunTool` hands `deploy_section` to `StartDeployInApp` → `MainWindow.DeployFor` → `SectionDetailView.StartDeployForAutomation()`, so the console and progress are right; that design is the reference and the mac has now copied it. What is missing there: the preview is stopped only for page EDITS (`if (edits && PreviewIsShowing?.Invoke() == true) StopPreviewInApp`), never for a deploy — and `StartDeployForAutomation()` calls `Deploy_Click` directly, bypassing the `DeployButton.IsEnabled = !IsBusy` that stops a teacher deploying mid-preview. So a Windows teacher who asks the assistant to deploy while previewing gets a deploy racing a preview inside the same container, which is the failure the disabled button exists to prevent. Fix: call `StopPreviewFor` before `DeployFor` and WAIT for it — `StopPreviewIfRunning()` is `void` today, so it needs to become awaitable or the deploy's build starts while stop mode is still killing that section's processes by working directory, and kills the build. Second difference: mac now AWAITS the deploy and reports the outcome in words, where Windows answers "The section is deploying from Plantoir's main window" and never says how it ended — acceptable, but if you await it, keep the destination refusals (no publishing folder, no Cloudflare account) coming back as a sentence in the chat rather than as a dialog on a window the teacher is not looking at. Third: check your headless path (`Plantoir.Mcp`) for the same Cloudflare-arguments bug — one place must decide the launcher's arguments, and on the mac that is `DeployCommand`. |


| 182 | 2026-08-15 | The assistant's reply after deploying was too wordy — "CIA4U Section 1 is deployed. Students can reach it now. The preview was stopped first, which is what pressing Deploy in that section's window needs — press Preview there to bring it back." It should say the first two sentences and stop. | ✅ The sentence about the stopped preview is gone; row 181 shipped it, and this removes it on sight. It was written on the theory that a preview window going blank unasked is its own small alarm, and read in place it was three lines of machinery after the one line that mattered. The stop still happens — only the narration of it is gone. The general rule, worth more than the instance: **the answer is what the teacher asked for, not an account of what the assistant had to do to obey it.** The teacher is looking at the window the work happened in. The test now asserts the whole summary by equality rather than checking it contains a phrase, because "does not mention the preview" is not something a `contains` assertion can hold onto. 499 unit tests, 0 failures. | Do not add this sentence on Windows — the handoff previously told you to, and that advice is now corrected in place. Anywhere the assistant reports work, the test is whether the sentence answers the teacher's request; "I had to stop X first" answers a question nobody asked. Same family as the menu-fragment refusal in row 181: what appears in the chat is written for the chat, and everything else belongs where the work is already visible. |


| 183 | 2026-08-15 | The deploy approval card was awkward: "Deploy CIA4U Section 3 to Netlify. This is the one thing that changes what students see, and Plantoir cannot take it back for you. Looking the preview over first is the safer order." Say instead: "OK, I'll deploy CIA4U Section 1 to Netlify. Students will see what is deployed. Be certain to review changes you have made." | ✅ Changed word for word. Two things changed with it, and both are the point. **It is in the first person now** — "OK, I'll deploy…" is an assistant agreeing to a request; the old text was a label on a button with two paragraphs of warning stapled to it, addressed to nobody. **What is left of the warning is the fact, not the alarm.** "Students will see what is deployed" is a consequence a teacher can weigh; "Plantoir cannot take it back for you" is a limitation of the app announced at the moment somebody is trying to use it, and "Looking the preview over first is the safer order" second-guesses a teacher who has already decided. The approval itself is unchanged — deploying still waits for a button whatever plan mode is set to; only the sentence read before pressing it is different. Test asserts the course and section are named in full, that it says who sees it, and that no tool name reaches the card. 499 unit tests, 0 failures. | **Windows says "I'd like to run **deploy section**. Shall I go ahead?"** — `AssistAgent.AskFirst` builds every approval line by underscore-swapping the TOOL NAME, which is the oldest rule in the project broken in the one place a teacher is being asked to agree to something. Fix it the way the mac does: a per-tool sentence in the teacher's words, naming the course, the section and the real destination ("Netlify", "Cloudflare Pages", the folder path) rather than a category. Use this wording verbatim — it is the maintainer's own. Keep the shape too: state the act, state who sees the result, give one piece of advice the teacher can act on, and stop. Nothing about what the app cannot undo. |


| 184 | 2026-08-15 | Row 183's card names the act, and the agent's next line asks "Shall I deploy?" — agreeing and then asking permission for the same thing. Cut the first sentence: the card should say "Students will see what is deployed. Be certain to review changes you have made." and the question follows it. | ✅ `explain(call:)` returns the two sentences and nothing else. The act is named by the question that follows, and the section by the window's own title. **What this costs, recorded because it was a real trade:** the destination is no longer named anywhere in the conversation, so a teacher with one course on Netlify and another on Cloudflare Pages is told what will happen but not where — the deploy's own progress in the section window names it, and that was judged enough. The three interpolated values (`code`, `number`, `destination`) are now unused by this branch and deliberately left in place: `schedule_deploy` beside it needs all three, and its card DOES name the destination, because a deploy set for 6:30 a.m. is agreed to now and happens when nobody is watching. Test pins the whole string by equality. 499 unit tests, 0 failures. | Two bubbles, not one: the sentence, then the question. Do not let the first repeat the second — a card that both announces the act and asks about it reads as agreeing and then asking permission, which is what this row removed. `AssistAgent.AskFirst` still names the tool; the handoff carries the wording to replace it with, and the two drafts that were rejected on the way. |


| 185 | 2026-08-15 | Cancelling a deploy answered "Left as it was — nothing was changed." It should say "Deploy cancelled." | ✅ `AssistAgent.declinePending` now answers a cancelled DEPLOY with "Deploy cancelled." and nothing else. The old sentence is true and reassures about a thing nobody was worried about — a teacher who has just pressed Cancel already knows nothing was changed, and being told so reads as the assistant explaining itself. **A cancelled PLAN keeps the old wording**, and the difference is the point: a plan describes changes to pages, so whether they happened is genuinely the part in doubt, while a declined deploy never began. `pendingIsDeploy` is read BEFORE the pending call is cleared, which is the one way to get this branch wrong. Two tests, one per branch, and they build a real `AssistAgent` with a client that is never reached — every message they send is a card phrasing, matched in code — which is the first coverage the approval gate's two buttons have had. 501 unit tests, 0 failures. | Windows's decline path needs the same split. The general rule is the one from rows 182 and 184, now three for three: **the reply says the thing that was asked about, and nothing about what the app did or did not do internally.** Cancelled deploy → "Deploy cancelled."; cancelled plan → the reassurance, because there it is the answer. Read the pending call's kind before you clear it. |


| 186 | 2026-08-15 | Windows should not re-derive the assistant's tests by hand. Have the mac produce the expected wording as shared data both suites read. | ✅ Step 1 of three. Every sentence the assistant says about deploying, previewing and agreeing to things now lives once, in `AssistWording` — nineteen strings, with `{course}` and `{section}` as the only variables — and `contracts/assist-wording.json` carries the same table as data for the Windows suite. Call sites in `AssistSiteWork`, `SectionDetailView`, `AssistToolRunner` and `AssistAgent` all read from it. **The drift this caught on day one:** the identical deploy failure was worded two ways — "the output is in that section's console in Plantoir" from the headless path, "…that section's window in Plantoir" from the window's — so which sentence a teacher got depended only on whether a window happened to be open. One phrasing now, and the window is the thing a teacher opens. The parameters are `String` rather than `Int` for one reason: the generator calls these same functions with `{course}` and `{section}` to emit the templates, so the contract cannot describe wording the app does not actually say. `contracts/` rather than `support/`, because `support/` is mirrored into every teacher's working folder and test data does not belong there. 501 unit tests, 0 failures. | **Read the JSON; do not retype the sentences.** `contracts/assist-wording.json` is the specification now — an xUnit `[Theory]` with a `MemberData` source over it is the whole integration, and the four documents that used to quote these sentences are no longer where the truth is. Substitute `{course}` and `{section}` yourself. When a sentence changes on the mac it arrives as a diff in that file, in the same commit that changed the Swift, which is the point. |


| 187 | 2026-08-15 | Step 2 of the same: the behavioural cases become a table both suites read, not tests re-derived by hand on each side. | ✅ `contracts/assist-cases.json`. Four things in it. **`cardPhrasings`** — the nine shapes matched in code and never sent to the model, with their tool and arguments. **`nearMisses`** — the four that must NOT match, which is the half that matters: a loose match answers a different question with total confidence. **`tools`** — the three lists (20 that exist, 13 the local model is shown, 3 offered only over MCP), which two need a button, and the six plan twins including the irregular `schedule_deploy` → `plan_scheduled_deploy`. **`scenarios`** — eight cases as `given` / `when` / `expectEvents` / `expectReply`, each with the `why` that makes it worth keeping: deploy with a preview up (`stopPreview.begins`, `stopPreview.ends`, `deploy` — in that order), deploy with none, deploy with no window at all, deploy while busy, approve, cancel a deploy, cancel a plan, and a page edit with a preview up. Replies are named as `wording.deployed` rather than quoted, so the two files cannot drift apart. The generated/hand-written boundary is a top-level key and the file says which is which: the derived halves come from the code, the near misses and the ordering are decisions and stay hand-written. | **`expectEvents` is an ORDER, not a set.** Every incorrect ordering passes a test that only checks all three events occurred, which is how the mac shipped a preview stopping after the writes and a deploy racing a build. Map the event names onto whatever your app calls them — the names here are the contract's own, deliberately not Swift's. Then the two `given` flags to honour: `sectionWindowOpen: false` is the headless path (your `Plantoir.Mcp`, and a scheduled deploy), and `previewRunning: true` is the case Windows currently gets wrong, since `StartDeployForAutomation()` walks past the disabled Deploy button. |


| 188 | 2026-08-15 | Step 3: have the mac GENERATE the contract as a build artifact, so it cannot become a fifth hand-maintained copy. | ✅ `Plantoir --assist-contract contracts` writes both files from the app's own types — `AssistWording` called with `{course}`/`{section}` as its arguments, `AssistCardCommand.everyFixedShape`, and the three tool lists with each definition's own `needsApproval` and `planTwinName`. The flag sits beside `--mcp-stdio` in `QuartzTeachersApp.init()` and exits before any window exists, for the same reason: it IS the app, so it cannot be left out of a build by a packaging script. Three details that make it usable rather than merely present. **It preserves the hand-written half** — the cases file is read before it is written, and `nearMisses` and `scenarios` survive; a regeneration that silently dropped them would look like a successful run and lose the reasoning in one stroke. **It is idempotent** — sorted keys, no escaped slashes, trailing newline, so a regeneration that changed nothing produces no diff, which is what makes "commit the diff" mean something. **`AssistContractTests` runs the same generator in-process** and fails when the committed files disagree, naming the exact command to fix it: a changed sentence now fails on the mac, in the same run that changed it, rather than three weeks later on a Windows machine as behaviour implemented faithfully from a file that had quietly stopped being true. Two further tests check what no generator can: every near miss still fails to match, and every `wording.` name a scenario cites actually exists. The committed files were regenerated so they are byte-for-byte the binary's output — Foundation writes `"key" : value` and Python writes `"key": value`, and the one that wins is the one that runs. 506 unit tests, 0 failures. | You do not run this — you READ what it produces, and it will arrive already correct in each sync. What matters on your side: treat `contracts/*.json` as generated, never hand-edit them (the mac will overwrite yours and the diff will look like vandalism), and if you need something in the contract that is not there yet, ask for it rather than adding it locally. If the Windows app ever grows a behaviour the mac does not have, that is the case for a second file — `contracts/windows-*.json` — not for editing these. |


| 189 | 2026-08-15 | Step 4: the Swift tests read the shared wording rather than carrying their own copies of it. | ✅ Every assertion that used to quote a sentence now names it — `AssistWording.deployApproval`, `.deployWasCancelled`, `.planWasCancelled`, and `FakePreview.deployedMessage`, which is itself `AssistWording.deployed(course: "ICS3U", section: "1")`. The loop is now closed in both directions: the app says a sentence once, the contract is generated from it, and the tests assert against the same constant, so there is no copy left that can pass while the product says something else. **Verified by breaking it on purpose**: changing `deployWasCancelled` in `AssistWording` and running the suite failed with `contracts/assist-wording.json disagrees about "deployWasCancelled"` and the regeneration command — which is the whole claim of steps 1–4, so it is worth having actually seen. 506 unit tests, 0 failures. | Do the same on your side once you read the contract: assert against the value you deserialised from `assist-wording.json`, never against a string literal in the test file. A literal in a test is a fifth copy, and it will be the one that keeps passing after the product's words change. |


| 190 | 2026-08-16 | Close the loop: the mac's own tests should RUN the contract's scenarios, not mirror them by hand, so both platforms execute the same list. | ✅ `AssistScenarioTests` reads `contracts/assist-cases.json` and runs all eight scenarios plus the nine card phrasings — the same file the Windows suite reads. `AssistFixture` is extracted so both test files build the course-on-disk identically; two files building it differently would let the same scenario mean two different things. **Running the hand-written cases immediately proved two of them wrong**, which is the entire argument for executing a contract rather than reading it: "deploy while that section is already busy" claimed NO events, and in fact the assistant does press Deploy and the WINDOW declines — the event fires; and "a page edit, with a preview running" claimed the preview sentence as its reply, when the reply is about the pages that changed. Both corrected in the file, each carrying a note that running it is what said so. A third thing surfaced in the fixture rather than the product: `StubSiteWork` answered "Deployed.", so a scenario could assert the contract's wording and pass against words the product never says — it now speaks the real sentences. Transcript checks assert ORDER but not adjacency, because a transcript's exact composition is genuinely per-platform while its order is not. **Verified by breaking it**: moving the preview stop to after the deploy failed with `["deploy", "stop-begins", "stop-ends"] is not equal to ["stop-begins", "stop-ends", "deploy"]`. 508 unit tests, 0 failures. | This is the file to implement against, and the eight scenarios are your acceptance list. Two you will fail today: "deploy with a preview running" (you never stop it) and "deploy while that section is already busy" (`StartDeployForAutomation` walks past the disabled button). Map the six event names onto your own; do not rename them in the file. And do what the mac now does with `AssistFixture` — one place that builds the course-on-disk for every assistant test, or the same case will mean two things in two of your test classes. |


| 191 | 2026-08-16 | Name the canonical source for each rule and retire the copies; make sure a Windows session knows where to begin. | ✅ `CLAUDE.md` gains two sections. **"Where the truth lives, per kind of truth"** gives each question exactly one home — what the assistant SAYS is `AssistWording` (generated into `contracts/assist-wording.json`); what must HAPPEN is `contracts/assist-cases.json`; WHY is `WINDOWS-HANDOFF.md` and the code comments; WHAT CHANGED is this log; what was MEASURED is `research/`; which rules override defaults is `CLAUDE.md` itself. The rule that falls out: if you are about to type one of the assistant's sentences into a document or a test, name it instead. **"The two handoff documents, and where a Windows session begins"** answers the count (two, pointing in opposite directions; the third was retired into `research/ai-assist/HISTORY.md`) and gives a five-step reading order ending at the two known-failing contract cases, which are a fair first task. Two corrections came with it, both of the kind that gets followed if left: this log described itself as "THE spec" and is now marked as **history** — a row records what was true that day, so row 183 quotes a sentence that row 184 replaced, and both are correct about their own day — and `WINDOWS-HANDOFF.md`'s three wording bullets no longer quote the sentences at all. They name contract keys and keep what a document is uniquely good for: the reasoning, and the drafts that were rejected. | Nothing to implement — this is how to READ the repository. The one habit worth taking: when you need one of the assistant's sentences, deserialise it from the contract, never retype it, in tests and documents alike. And if you write a decision down, put the reasoning in `MAC-HANDOFF.md` where the mac side will read it, not only in a commit message. |


| 192 | 2026-08-16 | The contract covers the assistant only. Extend it to everything that makes sense to share, and make new behaviour landing in a shared suite (or the other side's handoff) a standing rule. | ✅ Counted first: of 508 mac unit tests, **158 are assistant, 286 are rule-shaped and portable, 71 are genuinely mac-only UI, 5 are process mechanics** — so the assistant was about a third of what could be shared. `contracts/app-rules.json` is the second contract, with four sections. **`deployArguments`** — configuration → the argv `deploy.sh` gets, four authored cases including the two that have already gone wrong (Cloudflare without an account ID; a folder deploy that must ignore an account ID it has). **`configurationRules`** — Account ID validation, custom-domain normalisation, publishing-folder problems, asserted by equality because the sentences are the product's; the folder cases use `@MISSING@`/`@FILE@`/`@FOLDER@` tokens so the CASES stay portable while each runner supplies its own paths. **`milestones`** — a generated readout of `TaskMilestones`. **`markerOrigins`** — the find of the day, below. **`previewPorts`** — the container's 8081–8084, the host block step of 10, and the `+1000` websocket offset. `--assist-contract` becomes `--write-contracts`, since a flag that writes three files and names one of them is a small lie that costs an afternoon. 513 unit tests, 0 failures. | **`markerOrigins` is the one to read first.** Progress bars work by matching a marker string in the launcher's output, and it turns out **17 of the 25 markers come from shared Python and must match to the character, while 7 come from the launchers and DELIBERATELY differ** — the mac watches for "Setting up this Mac" where you watch for "Setting up this PC", because `setup.sh` and `setup.ps1` print different words. One is "elsewhere" (`Quartz v4`, from the build) and wants a human. Nothing crashes when this is wrong: the bar simply stops moving, which reads as a slow build. A mac test checks the classification against the actual files, so a marker moving from a launcher into shared Python fails here rather than silently changing what you should match. Then run `deployArguments` and `configurationRules` against your own `DeployCommand` and `CourseConfiguration` — those are pure input→output and should port in an afternoon. |


| 193 | 2026-08-16 | Date and schedule logic into the shared suite. And for the assistant's window on Windows: the bubbles need not match, but the chat EXPERIENCE must — a thinking indicator, everything through the chat except confirmations, the button choice recorded in history, and Terminal-style arrow-key history. | ✅ Two contracts and one handoff section. **`contracts/schedule-rules.json`** carries what a teacher's own list of class dates has to survive: the six accepted forms that all mean the 8th of September 2026 ("8 Sept. 2026" among them, because a refused form sends a teacher back to retype a hundred dates); the slash-ordering rule, which is settled by the COLUMN rather than the row — one value with a day past 12 settles every row and nobody is asked — and becomes a question, never a refusal, when every row reads both ways; the Google Sheet address a teacher pastes becoming its CSV address, tab and all; and the published-sheet link, which looks right, serves HTML forever, and gets its own answer rather than "that is not a Google Sheet link". Refusals are named by CASE, not by message: which refusal it is must match across platforms, while the sentence belongs to the wording contract. **`promptHistory`** joins `assist-cases.json` with eight cases, because the arrow keys are semantics even though the keys are platform: the half-typed line is put aside and handed back, an adjacent repeat is remembered once while a non-adjacent one is left alone, Up at the oldest entry does nothing rather than wrapping, and typing ends the walk. 522 unit tests, 0 failures. | **The bubbles do NOT need to match** — the mac's numbers were measured against Messages and would look foreign in WinUI. Build a chat that looks at home on Windows. What is not negotiable is in `WINDOWS-HANDOFF.md` under "What the assistant's window must BE": it is a CHAT; all input and output goes through it; the only exception is confirming an action, where buttons appear; **what the teacher chose with a button goes into their history in their own bubble** (two contract scenarios assert this); a **thinking indicator is a MUST**, one indicator for both thinking and running a tool, hidden while a card waits for a button; and the arrow keys walk the history, semantics in `promptHistory`. |


| 194 | 2026-08-16 | Correction: the arrow keys are the SAME on both platforms, not a per-platform choice. Then port ClassPlanning and NextClass, and keep going — leave no stone unturned. | ✅ **Keys corrected in the contract and the handoff**, which had said Windows could choose its own: they cannot, because a teacher who learns Up on one machine and finds it somewhere else on the other has learned nothing. `promptHistory` now names `ArrowUp`/`ArrowDown` and adds the two cases where the arrows must instead do their ordinary job — a box holding more than one line (the caret has to move between them) and nowhere further to walk — passing the key ON rather than swallowing it, since a key that silently does nothing reads as a dropped keystroke. **`contracts/class-planning.json`** is the highest-stakes data in these files, because it renames the files a teacher's lessons live in: which titles carry numbers (and that "Unit 2 Day 4" without the comma does NOT — guessing at what a teacher meant by a name costs a renamed lesson), the sort being unit-then-day rather than a string sort (Day 10 after Day 2), what "the next class" is called, and the rename ORDER, highest day first. **`app-rules.json`** gains three more: `failureExplanations` (nine cases including the rounding — 150s becomes "about 3 minutes", because sending a teacher back before the window opens sends them into the same failure twice — and the one that must return NOTHING, since a confident wrong guess about a failure is worse than the raw text); `buildFreshness`, whose third rule is the non-obvious one — a preview build is never deploy-fresh however recent, because serve mode bakes a `ws://localhost` live-reload client into every page and publishing that makes visitors' browsers ask about "access to other apps and services on this device"; and `linkRules`. **`contracts/README.md` now carries a coverage table for every mac test file**, shared or not, with the reason. 535 unit tests, 0 failures. | Read the coverage table first — it says what you can run from data and what you must write yourself, with the reason for each, so nothing is silently assumed to be the other side's problem. Backup/archive/restore (26 tests) is named there as the next candidate, not quietly omitted. Two things in this batch are worth checking against YOUR platform rather than copying: the browser-safe address exists because Safari tries IPv6 first for "localhost" while the container publishes on IPv4 — find out what Edge does before deciding you need it; and `markerOrigins` already tells you which progress markers are yours to write differently. |


| 195 | 2026-08-16 | Port backup/archive/restore and section adding/numbering. Put the two "check on Windows rather than copy" items into the handoff as work. And retitle `contracts/README.md`: it is no longer the assistant's contract, it is Plantoir's. | ✅ **`contracts/course-management.json`.** The zip-naming grammar is the part that had to be shared rather than described: three kinds of zip live side by side in `courses/_backups/<CODE>/` — `<CODE>_backup_<stamp>.zip`, `<CODE>_<stamp>.zip`, and the wizard's bare `<stamp>.zip` — and **the two apps list each other's files**, so a parser rewritten from memory makes a Mac-made backup vanish from the Windows list, or worse, makes an archive read as a backup and a teacher restore the wrong thing. Nine cases pin it, including the one that must be refused: an unrecognised maker suffix makes the WHOLE name unrecognised rather than quietly reading as a teacher's own backup. Plus the section-number rules (the suggestion is the smallest FREE number, not one past the highest, so "add section 1" and "add the next one" are each a single click; an empty field is quietly invalid because a warning before the teacher types is noise) and grade labels from a course code's fourth character, where a club code like CODING gets no label rather than a wrong one. **The handoff gains "Two things to MEASURE on Windows rather than copy from the mac"**, as work rather than trivia: whether Edge needs the `127.0.0.1` rewrite Safari forces, and which progress markers are theirs to write. **`contracts/README.md` is now "The Plantoir contract"** — the old title said "assistant", which was true when there were two files about the assistant and misleading now there are six about the product. The framing that replaces it is the rule for what belongs: not which feature a rule serves, but whether the rule is the PRODUCT's or the PLATFORM's. 549 unit tests, 0 failures. | Read `markerOrigins` before writing a milestone list, and do not copy the mac's seven launcher markers — read your own `.ps1` files and match what they print. That failure is silent in the worst way: no crash, the bar simply stops advancing part-way and jumps at the end, which reads as a slow build. Same for the localhost rewrite: test Edge by hand, and if it connects first time, say so in `MAC-HANDOFF.md` — a measured "not needed here" is a finding, and the contract should then mark the rule mac-only. |


| 196 | 2026-08-16 | Write the local-assistant performance requirement in for Windows: choose whatever design is FASTEST on Windows, as the mac chose Metal over Colima, and offer a small and a larger model chosen from the hardware just as the mac does. | ✅ Split by kind, as the rules require. **The testable half is `app-rules.json` → `modelTiers`**: five requirements that must be true on both platforms — two rungs and no more; the rung CHOSEN by reading the machine rather than asked of the teacher; the interface saying only "the small assistant" and "the larger assistant"; the model running on the HOST with hardware acceleration, never in the container; and zero polarity inversions as a VETO rather than a tiebreaker. The mac's own thresholds and models sit beside them under `macReference`, marked do-not-copy. A test asserts the two display names against the contract and that no model name, parameter count or format leaks through them. **The half no test can reach is `WINDOWS-HANDOFF.md` → "The requirement: pick whatever makes it FASTEST on Windows"**: the 175 s-against-2.1 s Colima measurement that makes this worth an afternoon, a table of candidate backends in the order worth trying (CUDA where an NVIDIA GPU exists, Vulkan as the pragmatic single build, DirectML/ONNX only if it measurably beats Vulkan, CPU as the floor that must always work), and the instruction not to ship a backend unmeasured on INTEGRATED graphics — the teacher this is for is likelier to have an Intel iGPU than a 4090, and fast-on-the-developer's-machine is worse than adequate-everywhere. 550 unit tests, 0 failures. | The requirement is the speed, not the framework: measure, then choose. What must not change with the backend — a faster model that inverts polarity is broken rather than faster; thinking off still takes two flags on Qwen3; and a faster backend does not buy a licence to say "CUDA" or "Qwen" in the interface. When you have measured, put tokens/second per backend on NAMED hardware into `MAC-HANDOFF.md`: the mac side cannot find out what a Windows teacher's machine does, and those numbers are what make the next decision on either side a measurement rather than a guess. |


| 197 | 2026-08-16 | `MAC-HANDOFF.md` was a ledger, not a document shaped for the rule that now governs it. Retire the format for a new one — without losing history or any outstanding item. | ✅ Restructured **by status**, every entry kept verbatim: contract cases waiting on the mac (the first thing read, so a red suite is explicable), then what is still owed, then awareness, then the finished ledger in full. The audit that produced it found the real problem: **all sixteen entries under "To implement" were already `✅ DONE`**, so the section title was a lie and the only genuine to-do was hiding in the awareness list — "worth checking: the same test race may exist on the mac", three days old with "worth ten minutes to check" buried mid-paragraph. That item is now closed with evidence rather than moved: the mac's test target is `parallelizable = "NO"`, so XCTest runs these classes one at a time and the shared statics cannot be reset under another class mid-assertion. **The safety is a scheme setting, not a property of the tests** — `PreviewLeaseTests` and `CourseActivityTests` both reset process-wide statics around individual methods, so flipping that setting on would import the exact Windows failure, one run in three. Recorded in `CLAUDE.md`'s testing section, where somebody would look before flipping it. Verified no loss: all 26 entry titles and every body line survive; only the old header prose was replaced. | The mirror change is worth making on your side if `WINDOWS-HANDOFF.md` ever grows the same way — a "to implement" list where everything is done reads as "nothing needed here" to somebody skimming. Order by status, keep finished entries in full (the reasoning is the point), and never let an ASK sit in an awareness list: an item there that grows a to-do moves up. And the entry template at the top of `MAC-HANDOFF.md` is what to write against — including numbers with the hardware they came from, which is the part this side cannot reconstruct. |


| 198 | 2026-08-16 | Share the last four portable areas — scheduled-deploy refusals, sidebar filtering, transcript stripping, curriculum mentions — as contracts, or as handoff notes where they cannot be. | ✅ `contracts/shared-rules.json`, four sections, and a handoff section for the neighbouring parts that are NOT shareable so that "not in the contract" never means "nobody mentioned it". **Scheduled-deploy refusals** carry their ORDER as well as their list, because the first match is what the teacher is told; the highest-value case is the section never deployed, since a first deploy asks what to call the website and nobody is awake at 06:30 to answer, so it waits. **Sidebar filtering** — and the case I got wrong is now the most useful one in the file: I wrote `ics` → ICS3U, and running it returned ICS3U AND SPH4U, because "Phys**ics**" contains it. That is the mid-word rule working, not a bug, and the corrected case says so. **Transcript stripping** distinguishes CSI (ends at a letter) from OSC (ends at BEL) — a stripper that only knows CSI leaves window-title text in a teacher's console — and pins what must be KEPT: newline, tab, and carriage return, since stripping CR breaks the progress lines the launchers redraw in place. **Curriculum** was rewritten before it ran: my first draft asserted scanning page TEXT for codes, and the code does nothing of the kind — an expectation is a PAGE named by a code inside a folder whose name contains "curriculum", which is `build_site.py`'s own rule. 537 unit tests, 0 failures. | The curriculum rule is the one to take seriously: `build_site.py` decides what ships, so an app that disagrees with it reports coverage the site does not have. Match the Python, not the mac. And check the one genuine fork in the deploy refusals — the mac refuses Cloudflare-without-an-Account-ID because its plist CAN carry `--account`; if a Windows task cannot be handed one, Cloudflare is not schedulable there and the right answer is a refusal that says so rather than a task that fails at 06:30 in silence. Record what you find in `MAC-HANDOFF.md`. |


| 199 | 2026-08-16 | Two sweeps: the biggest remaining shared surfaces, and then "anything else?" — which turned up the working-folder path bar's context menu missing on Windows, reported from real use. | ✅ Three things. **`contracts/file-formats.json`** is the one that should have existed first: the two files both apps WRITE and the Python then reads — 24 `course_config.json` keys with types and defaults, and the frontmatter that decides who sees a page. Two of those keys are only obvious once written: an EMPTY `deploy_target` means Netlify, because every course written before the other destinations existed has no key at all; an absent `show_section_marker` means TRUE. And page visibility carries both spellings, the legacy `draft:` meaning the OPPOSITE — the bug that already bit both platforms once, missed because the test agreed with the code. **Its key-count guard earned itself on the first run**: `expandOnFolderClick` was undocumented, and `setup_course.py` writes it while `build_site.py` bakes it into Explorer.tsx, so an app that never writes it leaves a teacher's choice unrecorded. **The path bar** is now in `shared-rules.json` → `workingFolderPathBar` and has a handoff section: the mac does four things there (double-click opens, right-click offers Show in Finder / Open Folder, hover shows the full path, every crumb is live and carries its real icon), Windows' `BreadcrumbBar` does one (single click reveals). 540 unit tests, 0 failures. | **Read the path-bar section — it is the one you can act on today.** And take the general lesson with it: an affordance that lives only in a context menu is invisible to everything. No screenshot shows it, no test on your side asks for it, and nobody building the other app can know it is there. A change that adds a right-click menu, a double-click, a hover or a shortcut needs a handoff line EVEN THOUGH nothing on screen changed. Sidebar context menus were checked at the same time and yours are in good shape — Obsidian, Add Section and Back Up are all present. |


| 200 | 2026-08-16 | Put the Windows sync brief into a file of its own, direct Windows to it from `CLAUDE.md`, and require the agent there to outline its plan before implementing. | ✅ `WINDOWS-BOOTSTRAP.md` — the brief for a Windows session, in six numbered parts: outline the plan first; what to read and in what order; **wire the contracts into `Plantoir.Tests` BEFORE changing behaviour**, because running the same cases is what makes "in sync" a fact rather than an opinion; the four things to fix in priority order; the two to measure rather than copy; and the rules while working. Section 0 is the one that is not optional — read everything, then STOP and write the plan out, including anything in the contracts that disagrees with what the Windows app actually does, **said rather than "fixed"**, since the mac side has twice found the contract to be the thing that was wrong. After agreement, work autonomously. `CLAUDE.md` sends Windows sessions there first, and `WINDOWS-HANDOFF.md` opens with a pointer to it so a session that starts in the middle still finds the brief. | This is your starting file from now on. Two things in it are worth reading twice: the contracts go into your test project BEFORE any behaviour changes, and a contract that disagrees with your app is a finding to report, not a bug to code around — twice now the case has been wrong and the app right, and both times running it is what said so. |


| 201 | 2026-08-16 | Round-trip it: a `MAC-BOOTSTRAP.md` every macOS session reads, covering both adding features here responsibly and taking work that came from Windows. | ✅ The mirror of row 200. **Part A — adding a feature** puts the decision FIRST rather than last: before writing it, decide where it will be recorded, contract or handoff, never neither. Then the four steps that keep it honest — write the sentence once in `AssistWording` and name it everywhere else; regenerate the contract when the app's own facts change and **commit the diff, because that diff is how Windows finds out**; run the suite knowing that a disagreeing case may be the thing that is wrong (four times so far, with the four named); build and relaunch through the `mac-app` skill and say in one line what he must do to see it. **Part B — taking Windows work** reads `MAC-HANDOFF.md` top-down by status, treats a red suite as a possible REQUEST rather than a break, marks entries `✅ DONE` in place, and — the step most often skipped — **answers back**: if the mac's implementation makes their guidance wrong, correct `WINDOWS-HANDOFF.md` in the same change, because a handoff that only travels one way is a report rather than a conversation. `CLAUDE.md` now opens both briefs, each handoff points at its own, and the two briefs point at each other. | Nothing to implement. Worth reading once, though, to see what this side is obliged to send you and what happens to what you send back — and the planning note differs by design: your brief asks for a plan before implementing because that session runs long and alone, while this one asks for a line of intent and then progress. |


| 202 | 2026-08-16 | One more sweep for gaps — which found the launchers' own flags, and then `setup.sh`'s, which the first pass had missed. | ✅ `app-rules.json` → `launcherFlags` now covers all THREE launchers. **`preview.sh`**: `--build-only` (build and stop, because serving belongs to the section window that holds the port lease), `--port <n>` (the app leases the port, not the script — several previews run at once and only the app knows which are taken), and `--stop` (kill the container-side processes; it finds them BY WORKING DIRECTORY, which is why it catches a running build as well as a server). **`setup.sh`**: `--image`, `--context`, and the `--` passthrough to `setup_course.py` — which is how a new course gets pre-populated (`-- --install-example`). **`deploy.sh`**: the three already covered by `DeployCommand`, plus `--diagnose` and `--image`. **The test checks the SCRIPTS, not the app**: an app passing `--build-only` is easy to see in Swift, while `preview.sh` quietly losing the flag in a refactor is not — and the launcher answers a flag it does not know by exiting, which reads to a teacher as a step that simply did not happen. Also checked and reported as NOT a bug: the mac passes `-- --install-example` and Windows passes `--install-example` without the separator, and both work, because both launchers forward an argument they do not recognise. 543 unit tests, 0 failures. | Run the same check against your `.ps1` files — that is the half of this the mac cannot do. And keep passing the `--` separator even though your launcher tolerates its absence: it is the documented form in both, and it keeps working if either ever starts rejecting unknown flags the way `preview.sh` already does. |


| 203 | 2026-08-16 | Keep sweeping until there are no gaps, using `documentation/` as the guide for where to look. | ✅ Three contracts and **three real findings**, all from reading the documentation against the code rather than from a failing test. **`contracts/toolchain.json`**: the four image pins with the reason each sits where it does — the test names the reason in its failure, because whoever bumps `wrangler@4.80.0` will be reading the Dockerfile and not this file — plus what each of the five Quartz patches changes and why it cannot be dropped. **`contracts/example-content.json`** checks all 37 payloads and corrected ITSELF twice: a manifest naming "Private Notes.md" without shipping one is correct (the lists are an allow-list over the payload trees, and the scaffolding creates an empty page), and `index.md` installs without being named because it is the landing page of the folder it sits in. **`contracts/file-formats.json`** gained the first-deploy markers and the wizard's own answer keys. The two divergences found: **the first-deploy marker** — the mac reads the marker for the course's CURRENT destination while `AssistWorkspace.cs` accepts either, so a course switched from Netlify to Cloudflare reads as already-deployed there and can be scheduled into a 06:30 prompt; and **`use_skeleton` is not written by the Windows wizard at all**, so the Python takes its own default of true and a Windows teacher is never asked the question that ~1,900 course codes actually pose. 553 unit tests, 0 failures. | Both divergences are yours to close and both are written up with the choice left open — for `use_skeleton`, either ask the question or write `true` deliberately, but do not inherit a fallback nobody chose, because the next change to that default in the Python moves you and not the mac. And note how these were found: walking `documentation/` and asking, of each fact, "what verifies this?" — the same sweep is worth running on your side against the docs that describe WSL2 and the ConPTY host. |


| 204 | 2026-08-16 | Four answers: make `use_skeleton` match on Windows; remove `dcp.html`, since the ministry updates documents and a held copy invites drift; explain `verify.sh`; and generate the routing suites' tool array from the contract. | ✅ **`use_skeleton`** is now a requirement rather than an open choice — both apps ask the teacher and write the answer, with the rejected alternative recorded. **`dcp.html` removed** (6.4 MB): the rule was already "curriculum text is transcribed verbatim or not at all", and the corollary is now written down — transcribed FROM THE LIVE SITE at the time of writing, because a copy held here does not preserve a source, it manufactures a stale second one. The example-content skill says so at Phase 1, where somebody would otherwise reach for the convenient local copy. **The tool SCHEMAS are now in the contract** — name, description and parameters exactly as each client sends them, for both surfaces — which turned out to matter more than the suite fix that prompted it: the descriptions are measured artifacts, and one clarifying sentence added to `publish_pages` once took the promise-card score from 110/110 to 90/110. `research/ai-assist/tools-from-contract.py` writes the surface the suites already accept as input, so a routing score cannot be earned against tools the app does not ship; `routing-suite.py` keeps its five hand-written tools and is marked HISTORICAL rather than rewritten, because its results are dated records. 554 unit tests, 0 failures. | Compare your own tool schemas against `toolSchemas` rather than against a description of them — a drifted description is a routing change nobody will attribute to a wording edit. And when you measure your own backend, take the surface from the contract with `tools-from-contract.py`; the suites are plain Python over an OpenAI-shaped endpoint and run anywhere a llama-server does. |

| 205 | 2026-08-16 | A teacher looks a course up by its code, but the code typed at setup can turn out to be the wrong one. Let them change it: a Rename item in the Edit menu, and Return on the selected course in the sidebar, editing in place the way Finder does. | ✅ Return in the sidebar, or **Edit ▸ Rename Course**, turns the course's row into a text field with the whole code selected; Return commits, Escape reverts, clicking away commits if the code can be used. The RULE moved out of the wizard into `CourseCodeRule` — ASCII letters and digits, **at most 8**, no spaces, no emoji — and the wizard now asks it too, so a code one accepts the other accepts. Eight rather than six: Ontario codes are six, but clubs are named by the teacher and ROBOTICS should be allowed; CS-CLUB is not, and CSCLUB is the answer. Renaming moves the folder and rewrites `course_code` inside it; it leaves the course NAME alone, leaves backups under the code they were made with, and leaves the published site's address where the students have it (the Netlify marker lives inside the course folder, so it travels). Scheduled publishes are the exception and are turned OFF, with an alert naming the sections — they are launchd agents addressed by the old code, so after a rename they fire at a course that is not there. Rename waits while the course is previewing or publishing, the same rule "Add Section…" follows. Cases in `contracts/course-management.json` → `courseCode` (`problems`, `normalized`, `renameEffects`); `CourseRenamerTests`, `CourseRenameInterfaceTests`. | **Inherit the rule and the effects verbatim — they are contract cases, so deserialise them rather than retyping.** Three traps. (1) **Return must NOT be a menu key equivalent.** A bare Return on a menu item is matched before the key reaches the focused control, which takes Return away from every text field and default button in the window; Finder's own Rename item has no shortcut for this reason. Handle the key in the list. (2) **Switching apps is not clicking away.** A focused field cannot hold focus while its app is inactive, so a naive commit-on-blur closes the field the instant the teacher looks at Obsidian — measured here, the field opened and vanished before anything could be typed. Guard the blur handler on the app being active. (3) **Scheduled publishing is the mac's own mechanism** (launchd agents in `~/Library/LaunchAgents`); Windows must cancel whatever ITS scheduled publishes are keyed by, but `renameEffects.scheduledPublishingCancelled` is the shared decision and the alert is required either way. Also: accessibility could not verify this on the mac — macOS collapses a `List` row into one element whose value does not follow the row's content — so the row is tested by hosting the row view alone and asserting a text field appears. If the Windows tree exposes rows properly, say so in `MAC-HANDOFF.md`; it would be worth knowing. |

| 206 | 2026-08-16 | Renaming moves the course folder, which is the Obsidian vault — Obsidian would be left showing files that are no longer there. Detect it and offer to put it right. | ✅ When that course's vault is open in Obsidian, renaming asks first: **Close Obsidian and Rename**, or **Cancel**. Yes closes Obsidian, renames, repoints the vault's registry entry, and opens every vault that was open again — the course's own one last so it lands in front. When the vault is NOT open, nothing Obsidian-related happens at all. Three things were measured on a real machine, and two of them would have shipped a bug: `"open": true` in Obsidian's registry is **stale** (a vault here carried it for hours while Obsidian was closed, so it means "opened last", not "open now" — pair it with "is Obsidian running"); Obsidian does **not** restore its windows on relaunch (two vaults open, quit, relaunch by URI, only the named one came back) — which was ALSO an existing bug in "Open in Obsidian", closing teachers' other vaults without reopening them; and repointing the existing registry entry rather than adding a second leaves no dead entry behind. `ObsidianLinkTests`. | The registry is the same JSON at `%APPDATA%/obsidian/obsidian.json`, so all three findings port unchanged — **inherit them rather than re-measuring**, especially the stale mark, which looks like a working detector right up until a teacher has Obsidian closed. Quit before writing the registry: a running Obsidian holds its vault list in memory and writes it back on exit, so an edit underneath it is lost. If the rename fails, open the vaults again anyway. Full reasoning in `WINDOWS-HANDOFF.md` → "Obsidian: close it, rename, and open the vaults again". |

| 207 | 2026-08-16 | Return in the sidebar did nothing. And the code limit should be 12 characters, with single spaces allowed between letters and numbers. | ✅ **Return works now, and the cause was not the key handling.** The sidebar could never hold keyboard focus: the course settings form's first text field takes the window's focus whenever a course is selected and keeps it — nothing in the app asks for that, it is SwiftUI's own initial focus, re-established each time the detail pane rebuilds — so Return, and the arrow keys, were going to the settings form the whole time. Selecting a course now moves focus to the list through AppKit. The key itself is answered by a narrow local monitor (`SidebarReturnKey`): bare Return or keypad Enter, its own window only, only while the list itself has focus, never while a text field is being typed into. `.onKeyPress` on a `List` is never called, and a bare Return on the menu item would be matched before the key reached anything focused — taking Return from every text field in the window, the rename field included. **Codes may now be 12 characters with single interior spaces**, so AP CALC is a course code; leading and trailing spaces are trimmed rather than refused, two in a row is a typo and says so. Verified by driving the real app: click a course, press Return, the row becomes a field with the code selected. | **Check whether your detail pane steals focus the same way** — if the first field of your settings form takes focus on selection, your sidebar has the same silent problem and any keyboard feature added to it will look broken while every unit test passes. That is the part worth inheriting; the AppKit call that fixes it is ours. Do NOT put a bare Return shortcut on the menu item, for the reason above. The 12-character and space rules are contract cases (`courseCode.problems`) — deserialise them. One existing piece of code already covered spaces: `ScheduledDeploy.sanitizedCode` exists so a club named with a space cannot produce a bad scheduler identifier; check your equivalent before turning the rule on. |

| 208 | 2026-08-16 | Renaming in the sidebar had too little contrast, and the warning was cut off mid-word ("A course code can be at most 12 c…"). Check Dark Mode too. | ✅ Both fixed and checked on a running app in **both appearances**. The contrast was not a colour choice but a consequence: a course is always SELECTED while being renamed, so the row draws on the selection colour and everything inside is tinted to sit on it — black-on-blue in the field, red-on-blue for the message. The field and its message now share one card painted in the system's semantic text-background colour, which takes them off the selection entirely and is white in Light Mode and near-black in Dark with no second code path. That is what Finder does when it renames a selected row: blue row, white field, black text, thin border. The warning now has a SHORT form for the sidebar ("12 characters at most", "No double spaces", "Letters, numbers, spaces", "ICS4U already exists") while the wizard keeps the full sentence; both come off one enum so they cannot drift, and both are pinned in the contract as `expectProblem` and `expectShort`. | Inherit both wordings — the short one is the half that only fails on the narrow layout nobody tests, and a truncated explanation explains nothing. The card matters for the same reason it did here: check what your list does to the colours of content inside a SELECTED row before trusting a field dropped into one. Use your platform's semantic text-background colour rather than a literal white, or Dark Mode is a second code path you will forget. |

| 209 | 2026-08-16 | With Obsidian open but NO vaults open, renaming still asked "ICS3U is open in Obsidian". | ✅ The `"open": true` mark is stale in a second way the first fix missed: it survives the vault being **closed** while Obsidian keeps running, not only a quit. Measured in exactly the reported state — every vault closed, Obsidian still up: **zero windows on screen, and one vault still marked open**. Detecting an open vault now needs three things to agree: Obsidian is running, it has at least one ordinary window on screen, and the registry marks this vault open. The window count comes from the window server's own list, which hands out an owner and a size without any permission — only a window's TITLE is privileged, and the title is the one thing this does not need. Worth knowing: the marks can still over-report while a window IS on screen (closing one of two open vaults cleared its mark here, closing the other did not), so one extra vault may reopen after a rename. An extra window is a small price against a permission prompt for every teacher. `ObsidianLinkTests` asserts it for real when the machine is in that state. | **Inherit the second stale case too — it is the one that looks fixed.** A teacher closing their vault and leaving Obsidian in the Dock is ordinary behaviour, and the mark left behind reads exactly like an open vault. If your platform lets you enumerate another application's windows without a permission prompt, that is the check; if it only offers window titles behind a permission, do NOT ask a teacher for it — a rename does not justify that, and the count alone is enough to rule out "nothing is open". |

| 210 | 2026-08-16 | After renaming a course to COMPSCI, the site's header read "Compsci". A course code should always be uppercase. | ✅ `resolve_header_label` in the shared `build_site.py` title-cased any code whose fourth character was not a digit — "CODING" became "Coding" — on the theory that a club name reads better that way. It does not: the teacher typed capitals, and seeing their own code changed on their own site reads as a bug. It got worse once codes could carry a space, where the same rule turned "AP CALC" into "Ap Calc". Codes are now always uppercase. `custom_short_name` is untouched and still wins for a non-standard code, because that is the teacher's own prose rather than their code. | **Shared Python — nothing to mirror, but know it changed**, because a teacher's site header changes the next time they build. If Windows shows a course code anywhere of its own, check it is not doing the same tidying. The escape hatch for prose in that spot is `custom_short_name`, which both apps already write. |

| 211 | 2026-08-16 | A multi-second pause between pressing Return on a renamed course and the field going away. | ✅ Not the rename: `reloadCourses()` mirrors the app's bundled toolchain into the working folder's `.toolchain`, and that ran on every reload — after every rename, backup, restore and archive. Measured at **3.80 s with nothing to do**, because `support/` alone is **61 MB across 11,354 files** and the mirror read BOTH copies of every file to prove they matched (~122 MB per reload), then asked an 11,354-element **Array** `contains` once per file (~129 million string comparisons). Three fixes: a cheap size-and-date check before reading anything (with the copy stamped with the original's date so the check keeps working after an app rebuild), a `Set` for the removal pass, and — the big one — mirroring **once per folder per run of the app**, which is sound because the source is the app's own bundle and cannot change while it is running. **3.80 s → 0.37 s → not on the interactive path at all.** A latent bug turned up on the way: the relative-path arithmetic assumed unresolved paths, so a working folder reached through a symlink had its whole toolchain deleted and recopied on every pass. `ToolchainMirrorTests`. | **Check whether your app mirrors the recipe on a hot path too** — the shape is the same on any platform, and the symptom is a pause nobody attributes to the mirror because the mirror is not what the teacher just did. The once-per-launch rule is the fix worth copying, and its justification is the same for you: the source is inside your own bundle. Numbers above are from an M4 Pro with the mirror on an internal SSD; a teacher on a spinning disk or a synced folder would have felt it worse. |
| 212 | 2026-08-16 | A released app needs a way for a teacher who hits a problem to send back enough for it to be diagnosed — without sending anything private. | ✅ Plantoir now keeps a record of every task it runs (`~/Library/Logs/Plantoir/runs/`, newest 20, plain text) and one line per assistant turn, and **Help ▸ Report a Problem…** zips them where the teacher chooses. Records are written AUTOMATICALLY as tasks finish, because a teacher reports a problem after it happened — anything needing "turn this on and do it again" would never be used. Every byte is redacted **on the way in, never on the way out**: account name → `/Users/person`, email addresses, `nfp_`/`Bearer`/labelled secrets, Cloudflare Account IDs, and any 40-character token run. Deliberately KEPT: the image tag, the container name, the `.pages.dev` address and 40-char lowercase-hex git SHAs. Also fixed: `ScriptRunner` published the launcher's arguments (Account ID, home path) to the unified log at `privacy: .public`; four path logs likewise. | **Take the redaction rules straight from the contract** — `problemReportRedaction` in `shared-rules.json`, 14 cases, including the Windows `C:\Users\…` form which the mac implements too so both suites run one list. Everything else is platform mechanics and yours to design: `%LOCALAPPDATA%\Plantoir\Logs`, your own zip (`System.IO.Compression`), and a Help-menu equivalent. Windows has NO logging at all today (no `ILogger`, no `Debug.WriteLine`) so this is a from-zero build — see WINDOWS-HANDOFF.md for what each field is for and why the KEPT list is load-bearing. |
| 213 | 2026-08-16 | "I built a preview and opened the assistant, and the report says there is nothing to report." | ✅ Two holes, both found by driving the real app. **(a)** Records were written only when a task FINISHED — and a preview never finishes, it serves until stopped, so the likeliest thing anyone reports left no trace. A record is now written when the task STARTS (outcome "Still running"), refreshed while it runs, finalised at the end. The first refresh design — throttle on output, 10s — never fired once: a warm-container preview prints for ~5s then goes silent, measured at 19 flushes in 5.2s and zero refreshes. Fixed with a second trigger, a 2s debounce after output goes quiet. **(b)** A pile of records is not a story, so there is now ONE breadcrumb trail (`activity.txt`) — app opened with version/pid/bundle path, folder opened, settings saved, tasks started and finished, assistant opened and ready — with the assistant's turns in the same file so the order is readable without interleaving two by hand. | Both apply unchanged. The trail is ordinary appended text; the two triggers are the part to copy exactly, and the invariant to pin is quiet-wait &lt; throttle. WINDOWS-HANDOFF.md carries the measurement and the reasoning. |
| 214 | 2026-08-16 | Put the support address in the report dialog as a clickable link — and make "leave a trail" a standing requirement rather than something remembered per feature. | ✅ The dialog now names support@plantoir.app as a real `mailto:` link with the subject pre-filled, in the alert's accessory view beside the checkbox (an `NSAlert`'s `informativeText` is a plain string, so nothing in it can be clicked, and an address a teacher retypes by hand is one they retype wrong). The address is one constant, used by the dialog and by the note inside the report. The requirement itself is now **CLAUDE.md rule 5** plus `contracts/shared-rules.json` → `activityTrail`, listing all 11 events with what each carries and why — pinned by a test against `ActivityTrail.Event`, so a missing event fails the suite instead of being noticed months later. | **Use the same address**, from the contract rather than from a screenshot. The link mechanics are yours (WinUI `HyperlinkButton` with a `mailto:` URI). The trail requirement binds both sides: propose an event by adding it to `mustRecord` — the mac suite goes red until this side implements it, which is the mechanism working, and naming it in MAC-HANDOFF.md makes that read as a request. The layout is worth copying rather than eyeballing: the link and checkbox line up with the LEFT EDGE of the text above them, and the three vertical gaps around them match the dialog's own paragraph gap (16pt on macOS, measured off the running dialog through the accessibility API — a checkbox and a text field each eat about a point of their own, so the container has to be told 18 to look like 16). |
| 215 | 2026-08-16 | Not every teacher uses the local AI assistant — do not ask them about it. And name it in full, before Plantoir can also connect to hosted assistants. | ✅ The checkbox appears only when the trail actually holds lines of what the teacher typed. The condition is PROMPT lines, not assistant events: opening the assistant and closing it again leaves turns behind but nothing they typed, and the box controls only the typing. The report's note gained a third state to match — never used it (says nothing at all about the assistant), used it and kept it back (NOT IN), used it and sent it (IN, recording that they asked). A teacher who has never opened it should not read a promise that their words were withheld; it invites them to wonder what else the app thinks they did. Checkbox reworded to "Include what I typed to the local AI assistant". | Both are contract rules now — `shared-rules.json` → `problemReportDialog` carries the label, the support address, when to ask, and the three note states, each with its why. Take the label and the address from there rather than from a screenshot. The naming matters most on your side too: the day Plantoir connects to a hosted assistant, an unqualified "the assistant" is a question about the wrong thing, answered wrongly, about where a teacher's words have been. |
| 216 | 2026-08-16 | Reported from the real app: the checkbox stayed hidden after the assistant HAD been used. | ✅ Root cause was where the prompt was recorded, not the checkbox rule. What the teacher typed was written only when the model REPLIED, so anything short of a completed answer — an engine error, a reply still running, the window closed mid-thought, or opening the assistant and reading the reply without the turn completing — left no `asked:` line, and the checkbox correctly concluded there was nothing of theirs to include. It is now recorded the moment the message is SENT, which is where it always belonged: a routing problem cannot be looked into without the sentence that caused it, and that sentence must survive the model failing to answer. Also added: a line when the assistant could not answer at all, which was the one assistant failure leaving no trace anywhere. | Two new contract events — `assistant asked` and `assistant could not answer` in `activityTrail.mustRecord`, which will redden your suite until you record them, as designed. The lesson generalises: **record what the teacher DID at the moment they did it, never at the moment it succeeded.** Anything recorded on the success path is missing from exactly the sessions a report gets asked for. |
| 217 | 2026-08-16 | A problem report was named by date alone — people send several in a day. | ✅ Named to the second: "Plantoir problem report 2026-08-16 at 15.50.22.zip". Dots rather than colons, which are not legal in a file name and which the Finder renders as a slash; it is the shape macOS gives its own screenshots, so it reads as familiar rather than as a format. The folder INSIDE the archive carries the same stamp — otherwise two reports unzipped side by side both land in "Plantoir problem report" and collide, which is the same muddle one layer down and on the machine of whoever is trying to help. | Same format, from `shared-rules.json` → `problemReportDialog.reportName`. Colons are illegal in file names on Windows too, so the dotted time carries over unchanged. Stamp the inner folder as well as the archive. |
| 218 | 2026-08-16 | Reported again: asked the assistant several questions, still no checkbox. | ✅ Same root cause, one branch higher. `say()` returns EARLY for a phrasing matched in code — those never reach the model at all — and the recording sat below that branch, so every card phrasing ("Undo that", "Deploy this section now", "What would students see…") left no trace whatsoever. Moved above every branch, to the moment the teacher's words are accepted. Added a line saying a phrase was matched in code rather than sent to the model, which is the only place "why did it not think about what I said?" is answered. Two regression tests now drive `say()` end to end and were CONFIRMED to fail against the old placement before being kept. | New contract event `assistant matched a fixed phrase`. The rule, now stated twice because it has bitten twice: **record what the teacher did where they did it — above every branch, never at a later point that a branch can skip, and never on the success path.** Both failures looked like a broken conditional and were a misplaced write. |
| 219 | 2026-08-16 | Add a Settings panel so a teacher can choose which local assistant runs on their Mac, switch between them, and remove a downloaded one to get the disk space back — with guidance about each one's size, and without naming any model. | ✅ Implemented — **Plantoir ▸ Settings… (⌘,)**, a `Settings` scene, one pane. Three choices: **Choose for me** (the factory setting, and what every Mac did before the panel existed), **The smaller assistant**, **The larger assistant**. Each names both costs — download size and memory while working — because they are different decisions and neither is guessable from the other; the figures are derived from `AssistModelTier`, not typed. "Choose for me" is stored as an INTENT, never resolved-and-written-down, so a later change to the ladder still reaches a Mac whose teacher opened Settings once. A hand-picked choice that takes more than a third of the machine gets a caution naming both numbers and stays selectable — their Mac, their call. "On this Mac" lists both rungs with Download / Stop / Remove; Remove is refused while any assistant window is open and says WHICH section to close. Removing the one in use is allowed and explained, because that is the state every Mac is in before its first download. Six trail events, 22 tests. | **Three bugs here were found by driving the real app, not by the tests — expect all three.** (1) A `Section`'s content, header and footer each get their OWN observation-tracking scope, so footers that read only the file system never invalidated: the rows redrew after a removal while the summary sentences went on describing a deleted model. Half a panel updating is more convincing than none. Whatever your framework, make disk-derived answers depend on something observable. (2) The panel and each assistant window made their own download store for the SAME path — press Download in Settings, open the assistant, and the second one deletes the part-finished file and starts again: two transfers, one destination, gigabytes, school connection. One store per rung, process-wide. (3) Closing the assistant window cancelled the download — correct when that window started it, wrong once the stores are shared, since Settings' whole point is fetching ahead of time. Scope the cancel to the window that started it. Also: a settings window is HIDDEN, not destroyed, so its state outlives every visit — look at the disk again when it comes forward. |
| 220 | 2026-08-16 | Reported from a real course: unpublishing a class said four pages were staying published because **"index"** still linked to them. "index" means nothing to a teacher — the name of the sidebar FOLDER should be reported instead ("Portfolios"). | ✅ Fixed, and it was two bugs. **(a)** A page now carries a `displayTitle` beside its `title`: the file name stays the IDENTITY (wikilinks resolve by it, every graph lookup goes through it) and the display name is a separate answer, used everywhere a page is named in a sentence a teacher reads — the unpublish plan, the pages that stay, date moves, `read_page`, `check_section`, curriculum mentions. The rule is copied from Quartz's own `fileTrie.ts` rather than invented, so the assistant and the sidebar cannot drift: frontmatter `title:` unless it is literally "index", then the FOLDER's name, then the file name. **(b)** Referrers were remembered by NAME and looked back up — and every folder's landing page is called `index`, so the lookup returned whichever one the folder walk reached first. Invisible while the answer printed as "index"; a confidently WRONG folder the moment it printed as one. Referrers are now carried as pages and de-duplicated by PATH. Verified against the reported course: both lines now read "Portfolios". 9 tests, and both end-to-end ones were CONFIRMED to fail against the old code — (b) alone printed "Concepts", a folder that does not link to the page at all. | **Both halves apply to `Plantoir.Core/Assist` unchanged — this is not a mac-shaped bug.** New contract section `shared-rules.json` → `pageNaming` with the rule, the five cases, and the identity-vs-label rule; run it. The trap worth stating on its own: **fixing only the label makes the output worse.** A teacher sent to Concepts to find a link that lives in Portfolios concludes the assistant is lying and stops reading the list, where "index" merely told them nothing. If you key referrers, backlinks or any page set by name anywhere, `index.md` collapses them all onto one key — check for it before touching the label. |
| 221 | 2026-08-16 | Reported from a real course: after "Unpublish Unit 4, Day 23" then "Undo that", the answer read **"Undid unpublished 2 pages in ADA1O Section 1."** — not grammatical, and it should name what was done and that the teacher asked for it back. All of the undo responses needed the same review. | ✅ Every undo sentence rewritten and moved into `AssistWording`, where the assistant's words belong. A change now carries `whatHappened` — a past-tense clause naming the PAGE ("unpublished Unit 4, Day 23") rather than counting files — so the sentence reads "Earlier, you unpublished Unit 4, Day 23. Then you asked me to undo that, and I have done so." The count was wrong as well as ugly: hiding one class writes two files, because the section's landing page is repointed in the same change. **Three separate sentences now, where there was one slot:** undone; undone but some pages left alone because they were edited since; and nothing undone at all — that last case used to fall through to the success sentence, so a teacher was told their change had been taken back while not one file had moved. `nothingToUndo` narrowed to "any pages", because it claimed more than it knew. | Deserialise the seven new `assist-wording.json` entries — `undid`, `undidPartly`, `couldNotUndo`, `undoIsStillAvailable`, `nothingToUndo`, `undoDoesNotReachTheLiveSite`, `aCreatedPageCanBeTakenBack` — which carry a `{change}` placeholder for the clause. **The rule that generalises: a stored clause that only ever appears inside a sentence somebody wrote on purpose cannot come out ungrammatical.** Do not build these by concatenation at the call site, and never reuse a success slot for a refusal. |
| 222 | 2026-08-16 | Same report: the undo made the file edits but the preview was never restarted. The right sequence is stop the preview, verify it stopped, make the edits, start it again. | ✅ `undoLastChange` is now async and does exactly that, reusing the same two helpers `publish_pages` already used — `stopThePreviewBeforeWriting` (which AWAITS the stop, because a stop still finishing when the next build starts kills the build) and `bringThePreviewUpToDate`. It could not do this before for a structural reason: a change knew only its FILES, so an undo could not tell which section's preview to touch. A change now carries its course and section. **And it rebuilds only when the change it reverses rebuilt** (`rebuildsThePreview`), so undoing a created class page — which arrives unpublished and changes nothing the preview shows — does not cost a rebuild. | A `FakePreview` test asserts the ORDER, not the presence: `[stop-begins, stop-ends, write, start]`. Mirror that — all four events can fire and still be wrong, and a preview left serving while the pages beneath it are rewritten serves a half-changed site. |
| 223 | 2026-08-16 | "Undo that" after adding a class page said the conversation had changed nothing — while the page sat in the teacher's folder. Make `add_next_class` undoable. | ✅ The undo list held a before-and-after copy of each file, and a created page has no "before", so nothing was recorded. `AssistSavedFile.before` is now optional — **nil means the change CREATED the file**, and taking it back deletes it. `PlaceholderClassPlanner.apply` reports which pages it wrote. The same skip rule protects it as protects an edited page: once the teacher has written in it, the page is theirs and the undo leaves it alone. The line telling teachers "“Undo that” does not take away a page it created" was corrected — a line describing what a feature used to do is worse than no line. | Optional-`before` is the whole trick; an empty string will not do, because an empty page and an absent one are different states. Note the skip check needs no change: a page the teacher deleted themselves reads back as absent, does not match what the change left, and is skipped rather than "restored" by deleting something already gone. |
| 224 | 2026-08-16 | The list of things you can ask for did not match what the assistant can actually do — expand it so it is accurate. | ✅ Nine phrasings became fourteen. Five capabilities were on no card at all: listing a section's pages, reading one back, publishing the class on a named day, adding the next class page, and reading back the remembered class dates. New group **Planning classes**. Three of the five carry no argument, so they joined `AssistCardCommand.fixedShapes` and are matched in code — reliable by construction. **All fourteen were then measured against the shipped 13-tool surface: routing 140/140, arguments 60/60** (`research/ai-assist/shelf-phrasings-results.txt`). | The near-miss is the part to copy. Measured with `AssistAgent.dateline()` left OFF the user message — which is what a probe harness omits by accident — "Publish the class on Monday" routed 10/10 and then resolved Monday to a date A MONTH AWAY, 10/10, and the card was within an edit of being dropped. The app appends "(Today is …)" to every message and with it the same card is exact. **A probe that does not send what the app sends measures nothing, and the number it produces reads exactly like a fault in the product.** Also from the older suite: that line must be APPENDED, not prepended — prepending costs 15 points of routing. |
| 225 | 2026-08-16 | Two of the phrasings added in entry 224 are useless commands — "Show me Unit 2, Day 3" and "What pages are in this section?". Remove them. | ✅ Both cards gone; the shelf is twelve. **Neither was a routing problem** — they had measured 10/10 the same day, and the second 10/10 on its argument too. They fail a different test: a teacher has the pages in front of them in Obsidian AND in Plantoir's own sidebar, so reading one back into a chat bubble is a worse way to see it than the two windows already open, and a list of file names answers a question nobody with a folder open is asking. The TOOLS stay — `list_pages` and `read_page` are what the model uses to look things up before it acts, which is the job they are good for. `list_pages` also stays in the fixed-shape matcher, so a teacher who types the phrasing anyway still gets the reliable answer. | **A card has to pass two tests and reliability is only the first.** The second is "is this worth asking for?", and it is the one a port will skip, because the first has a number attached and the second does not. The shelf is a list of things worth SUGGESTING, not an inventory of the tool surface: thirteen tools, twelve cards, and the gap is deliberate. The matcher is a third list again — what is worth MATCHING — and it may legitimately hold a phrasing no card offers. The measurement for both removed phrasings is kept in `research/ai-assist/shelf-phrasings-results.txt` under SINCE THIS RUN, so nobody re-measures what was never in doubt. |
| 226 | 2026-08-16 | When a class page (Unit x, Day y) is published and it links to other pages that have never been published before, those linked pages should take the class page's date. | ✅ Implemented, and it uncovered a split. **`publish_class_on` already moved dates; `publish_pages` never did** — so "Publish tomorrow's class" dated the pages a class brought and "Publish Unit 2, Day 3", naming the very same page, left them on whatever day their FILE was made. Same teacher, same class, two answers depending on the sentence. Both routes now run one rule, `dateMovesFollowingClasses`. A linked page takes the class's date when it is **hidden right now** (this publish is the first time students will see it) and is **not itself a class page** — a class's date is its place in the schedule. A page students can already see keeps its date: it has a position somebody may have linked to, and republishing must not shuffle work already out. Where several published classes share a page the EARLIEST claims it, matching `first_use_dates` in `setup_course.py`, so a pre-populated course and a hand-published one agree. The key written is `created` for a section-local page and `createdSection<N>` for a course-level one. | **One condition was REMOVED, and it is the part to read twice.** The rule used to move a page only when no OTHER class linked to it — "a concept page linked from three different lessons belongs to none of them". That is sound for a page already on the site and beside the point for one nobody can reach: a hidden page has no place to be left in, only the day its file was made, which is a fact about the teacher's evening rather than about the course. A shared page students have never seen now moves. Contract: `class-planning.json` → `datingPagesAClassBrings`, which carries the conditions, the key names, the earliest-class rule and the reasoning. "Never published" is INFERRED from the page being hidden — nothing on disk records history — so a page published once and later hidden counts as never published. |
| 227 | 2026-08-16 | The publish/unpublish plan is worded mechanically. Make it read like a person: `"Unit 4, Day 24" will become hidden.` And drop the Markdown — no boldface. | ✅ Each item is now ONE SENTENCE about one page. Gone: the frontmatter key the change lands in (`publishForSection1` — the name of a line in a file, shown to somebody who asked to hide a lesson), the `visible → hidden` arrow, and the `(linked from a page you named)` parenthetical. Date moves lost their arrow too — `"Bananas" moves from 2026-09-08 to 2027-01-19.` — and the pages that stay now read `"Journal Checklist" stays visible, because "Portfolios" still links to it.` All four `**` headings are plain text; they were the only Markdown any assistant string emitted. Fixed on the way past: `1 page students is seeing for the first time`, where a verb had been agreed with the page COUNT while its actual subject was students. | The bubble still PARSES Markdown, so a model's own reply renders normally — nothing the app writes emits any. The general rule: **a person answering a question does not reach for typography to make a sentence land.** A heading ending in a colon with the count as its first word is signal enough. Watch for the plural trap: agree a verb with its own subject, not with whatever number is nearest. |
| 228 | 2026-08-16 | "Publish the class on Monday" should read "Publish Monday's class", check the schedule, and stop with an explanation when there is no class that day. | ✅ Card reworded, and all seven weekdays are now FIXED PHRASINGS — `publish monday's class` … `publish sunday's class` — so the date is worked out in code and can never be one the model invented. `AssistToolRunner.day(named:today:)` learned weekday names: the next one, **counting today when today is a Monday**, which is the reading a person gives it while preparing that morning. The schedule check and the refusal already existed; the refusal's wording did not survive reading, though — it said "Use list_pages to see what classes there are", a TOOL NAME in a sentence that goes straight to the teacher, since a refusal ends the turn. It now says what it cannot find and offers words the window understands. | Same shape as the existing `publish tomorrow's class` card: the day is not read OUT of the sentence, it IS the sentence, so each weekday gets its own phrasing with the answer written down. Note the argument key is `when`, which is deliberately NOT in the tool's schema — fixed phrasings may pass keys the model never sees, which is how a whole behaviour gets added without touching the surface routing was measured against. |
| 229 | 2026-08-16 | "What dates am I teaching?" should show the next week of classes, then offer the rest. | ✅ It used to carry NO DATES AT ALL — a count and two endpoints — so a teacher asking what they were teaching was told how many days there were and left to go and look. Now: the dates falling in the next seven days, one per line with its weekday; then what the dates are for (class pages, spare dates, when the next class would fall); then the offer, which names the exact words to use. A quiet week says "Nothing in the next seven days" rather than printing an empty heading. | **The offer had to be made answerable, or it is worse than no offer.** "Show me the rest of the dates" is matched in code and passes `scope: all`, a key the tool's schema does not mention — so the model is never told it exists and the routing surface is unchanged. A test asserts the words the answer OFFERS are words the matcher accepts; a prompt whose answer nothing understands is the failure to guard against. |
| 230 | 2026-08-16 | The plan still says too much. Fold the date into the page's own line: `"Bananas" will become visible, with the same date as "Unit 4, Day 24".` | ✅ The separate date block is gone. It had its own heading, named the page a second time, and printed both raw dates — every fact true, and a teacher had to hold a page name in their head across two lists and a blank line to see that the second was about the first. It is now a clause on the line that page already has, and no raw date is shown at all: "the same date as Unit 4, Day 24" is what a teacher wanted to know, `2026-09-08 → 2027-01-19` is how the app stores it. | `AssistPublishDateMove` gained `takenFrom`, the class whose date the page is taking, purely so the sentence can name it — the move knew the DATE but not where it came from. **One line per page, and a second fact about a page belongs on that page's line rather than in a second list keyed by name.** A date move whose page is somehow not among the changes still gets its own line; that cannot arise today (every page taking a class's date is a hidden page the publish is making visible) but a silent drop is the wrong way to discover otherwise. |
| 231 | 2026-08-16 | "Start a new unit for the next class" should be an option: create the first class of the next unit, shaped like the skeleton classes, dated to the next class in the teacher's schedule with no class against it, and unpublished. | ✅ New card in Planning classes, and two fixed phrasings ("start a new unit for the next class", "start a new unit"). It reuses `NextClassPlanner` whole — only the NUMBER changes: `firstDayOfANewUnit` returns one past the highest unit at Day 1. Everything else was already right and is untouched: the date comes from POSITION in the timetable (how many class pages exist, not what they are called), the page is written by `PlaceholderClassPlanner.apply`, which starts it `publish: false`, checks twice that nothing is written over, and gives it the skeleton shape. | **Day starts again at 1 in a new unit.** Every course here numbers days within their unit — ADA1O runs Unit 1, Day 1…18 then Unit 2, Day 1 — so a new unit begins at Day 1 however far the last one ran. Unpublished pages still COUNT: Unit 4 with Days 13 and 14 written but hidden still gives Unit 5, Day 1, taking the next free date, because where the next class goes is decided by how many pages exist rather than by what students can see. `unit` is not in the tool's schema — a fixed phrasing may pass keys the model never sees. |
| 232 | 2026-08-16 | Any command needing the teacher's schedule, given before they have provided one, must prompt for it using the code already written for that. | ✅ The sheet existed and was wired to exactly TWO paths — adding a class, and reading the timetable back — so every other schedule-dependent request failed with an explanation and no way forward. Publishing a class by day is the one that showed it: "I can't find a class on Saturday" is not something a teacher who has never given their dates can act on. All prompting now goes through one `askForTheTimetable`, and the class-by-day path asks when — and only when — no dates are on file. | Two tests, and the second matters as much as the first: it does NOT prompt when a timetable IS recorded, because that request failed for some other reason and a sheet about dates would be answering a question nobody asked. The general rule: **when a request fails for want of something the app can ask for, asking IS the answer** — a better sentence is not. |
| 233 | 2026-08-16 | After all the revisions to what a teacher can say, update the hints at the top of the assistant window. | ✅ Audited card by card against every phrasing the app matches. Most were already current — the real gap was **"Show me the rest of the dates"**, which existed only as an offer inside the timetable answer, so a teacher who had not asked the question yet had no way to know it was there. An offer made only inside an answer is invisible until you have already had it. Intro line reworded in the same voice as the answers: "Here are some things you can ask me for. Tap one to put it in the box, change it to suit, then press Return." Thirteen cards in five groups. | **`AssistPromptShelfView.groups` is now static and readable by tests, and two tests keep it honest.** The first asserts every card either fires a fixed phrasing or is on a hand-written list of the model-routed ones — so a new card FAILS until somebody has decided which kind it is and, if it carries an argument, measured it. That is the drift this file had only warned about in prose: "Deploy this section now" became "Deploy now", and a card whose wording no longer matches its shortcut is a button that quietly goes to the model on a shape the model was measured getting wrong. Group TITLES are still checked against a hand-written list — reading those from the view would make the test agree with whatever the view said. |
| 234 | 2026-08-16 | `check_section` ended with "The section's preview will show these 195 pages once any rebuild in progress finishes" whenever a preview existed at all. It should say that ONLY while a build is in progress; say nothing when a built preview is on screen; and suggest previewing when there is neither. | ✅ Three states, three answers. The sentence sat behind one boolean — `previewRunner.isRunning` — which stays true for as long as the launcher SERVES the site, so a preview mid-build and a preview sitting finished on screen were indistinguishable, and a teacher looking at the finished thing was told to wait for a rebuild that had ended minutes earlier. `SectionWindowControllers` now reports `PreviewState`: `.building`, `.showing`, `.notRunning`. Building → "The preview is building now, and will show these 195 pages when it finishes." Showing → nothing at all. Neither → "Nothing is being previewed at the moment. Say “Preview” if you would like to look the section over." No window open counts as neither. | **The honest signal for "on screen" is the loaded URL, not the process.** The launcher runs throughout; `previewURL` is cleared at the start of every build and set only once the server has answered, so it is the only thing that distinguishes the two. `isPreviewRunning()` is now DERIVED from the state rather than stored beside it, so the two cannot disagree — the stop/start paths still ask exactly the question they care about ("is there something to stop"). Look for the same conflation on your side: one boolean serving two questions is how this reached a teacher. |
| 235 | 2026-08-16 | "Publish Unit 4, Day 23" when that class is already published should simply reply "It's already been published." | ✅ Four words, and nothing else — on the PLAN path as well as the do-it path, because plan mode was otherwise showing a card with a heading, a count and "Shall I go ahead?" for a no-op, then reporting that nothing happened. Now: "It's already been published." / "It's already hidden." / "They have already been published." for more than one. | **The plan now remembers which pages the teacher NAMED** (`namedPages`), separately from everything publishing swept in by following links — otherwise "it" would have counted five already-published linked pages nobody mentioned and come out plural. Two things deliberately still get the long answer: a name matching NO page ("already done" and "no such page" are different, and must not be confused), and any request where something really would change. |
| 236 | 2026-08-16 | "Unpublish Unit 4" should walk backwards from the highest day, unpublishing each class until none in that unit is published; report only "Unit 4 was unpublished"; stop the preview, apply, start it again. And the corollary, "Publish Unit 5", walking Day 1 forwards. | ✅ Both, one class page at a time, in the order asked for. The per-day rules are the EXISTING ones, unchanged — which linked pages come with it, which are left because something else still needs them, which take the class's date — so each step asks them against the state as it actually is at that moment. Publishing forwards from Day 1 is also what makes an earlier class claim a shared page's date, matching `first_use_dates`. The preview stops ONCE and starts ONCE, not per page. The undo list gets ONE entry, merged from every file touched, so "Undo that" takes the whole unit back. Phrasings parsed in `AssistCardCommand` for any unit number; "Publish Unit 4, Day 3" has a comma, is one page, and still goes to the model. | Merging the per-step changes keeps the EARLIEST before and the LATEST after for a file written more than once — otherwise undo would put a page back the way it was one step ago rather than before the unit was touched. Note what a unit walk does NOT do: a shared page linked from two classes in the unit is kept by the first step (the other class is still published) and swept by the last, which is the right answer arrived at over several steps rather than in one. |
| 237 | 2026-08-16 | "Add five more days to Unit 4" — look at the last day that exists in that unit, published or not, and add that many unpublished days, dated from the teacher's schedule. | ✅ Continues from the last day that EXISTS, so a page written but not yet shown still holds its number — numbering over the top of it would collide with a real file. Dates come from position in the timetable exactly as a single new class does, which is why it can add days to Unit 4 while Unit 5 pages already exist and still date them right: the count is of PAGES, not of unit numbering. Asking for more days than there are dates refuses WHOLE and opens the schedule sheet, rather than creating some and leaving the teacher to work out how many. | The phrasing is parsed, and understands words as well as digits — a teacher asking for a few more days writes "five", not "5". `unit` and `days` are not in the tool's schema. |
| 238 | 2026-08-16 | "Duplicate Unit 3, Day 2 as my next class" — copy it to the next day of the same unit with a date from the schedule, shifting later classes back when room is needed. | ✅ The copy becomes Unit 3, Day 3 with the source's content and a date of its own. **Making room is `ClassInsertionPlanner`'s job rather than a second copy of it** — it renames from the HIGHEST DAY DOWN so nothing is written over, re-dates everything from the insertion point onto real meeting days, and rewrites every wikilink that pointed at a renamed page. When nothing needs moving, the same call simply adds the page at the end. The copy starts hidden however the source was: a duplicate of a published lesson is a draft of next week's. | **Undoable ONLY when nothing else moved.** A partial undo that deleted the new page and left every later class renamed would be worse than no undo, so when classes shuffled the answer says the way back is the backup taken before any of it. Duplicating needs the schedule, so it goes through the same `askForTheTimetable` as everything else. |
| 239 | 2026-08-16 | A page kept published by a HIDDEN class page referrer is wrong. Corollary: publishing a class that links to a hidden page must publish that page, and disclose it. | ✅ "X still links to it" now counts only when students can SEE X. Before, a page could sit visible and reachable from nothing, held up by a draft nobody had published — kept alive by a page that is not there. The corollary already held and is now pinned by a test: publishing follows links and the plan says "“Ohm's Law” will become visible". | Safe in one direction because of the other: a page taken down here comes back the moment anything visible needs it, since publishing is transitive. The pair is worth testing together — one test that a HIDDEN referrer does not keep a page, one that a VISIBLE referrer still does, because the second is the rule that stops one class being broken to tidy another. |
| 240 | 2026-08-16 | Plan mode should be a SETTING now that app settings exist — always describe and confirm by default, and invite the teacher to turn that off when they trust the assistant. | ✅ New first section in Settings ▸ Assistant: "Ask me before changing anything", on by default. One stored answer shared with the gate, so the switch and the assistant cannot disagree; a conversation already open follows it without being reopened. The old per-tier BLOCK is gone — a teacher can turn it off on either assistant, because which one is running changes the odds, not whose decision it is — and the smaller one gets a caution carrying its measured number instead of a disabled switch. Deploying still always asks, whatever this is set to. | **Two `AppSettings` instances over one defaults store is not the same as one object**, and a test caught it: each caches what it read at construction, so a change through one was invisible to the other. In the app both would have been `AppSettings.shared` and it would have worked by luck. `AssistPlanMode` now takes the settings OBJECT. New contract event `assistant confirmation changed`: a report saying "it just did it without asking" is explained entirely by that line. |
| 241 | 2026-08-16 | After 15 confirmed actions — app-wide, and just once — mention that the setting exists, for discoverability. | ✅ Was five in a row, per conversation, reset by a Cancel. Now fifteen, counted app-wide across every conversation and course, never reset, and said ONCE ever. The message points at the switch by name rather than offering an inline toggle. | The old count reset with every window, so a teacher working in short bursts could accept a hundred plans across twenty conversations and never be told the setting existed, having never hit five in one sitting. A Cancel no longer zeroes it either: the number measures how much of the assistant's work this teacher has READ, and a Cancel is evidence of reading rather than against it. The assistant still only RAISES it on the larger model — that part stayed measured — while the switch itself is available on both. |
| 242 | 2026-08-16 | The visible-referrer rule must exclude pages Key Links points at, All Classes, anything curriculum, and the index page of each sidebar folder. | ✅ Verified rather than added: all four already sit ABOVE the referrer test in `reasonToKeep`, so narrowing that test could not reach them. Key Links contributes the page AND everything it links to; All Classes and every sidebar folder are covered by the folder-landing-page rule; curriculum is matched on any folder segment containing "curriculum", which in a real course is every expectation page including the folder's own index. Now pinned by a test that sets up the exact situation the narrowing created — the ONLY page linking to each of the four is HIDDEN — and it was CONFIRMED to fail with the exclusions removed. | The order in `reasonToKeep` is load-bearing rather than incidental, and there was nothing saying so until now. Narrowing a sweep rule makes every exclusion above it matter more than it did the day before, so the exclusions are the thing to re-test after any change to what gets swept. |
| 243 | 2026-08-16 | Both assistants should follow the same rules for the confirmation setting — discoverability. | ✅ The tier no longer decides anything about the gate. Same default, same fifteen-plan mention, same switch, on both. `mayBeTurnedOff` is gone. The explanation is one sentence for both and names where to change it; it used to say something different on the smaller assistant — "it always shows what it is about to do first" — which stopped being true the day the switch arrived. | The previous arrangement withheld a setting from exactly the machine where knowing about it matters most: the smaller assistant refused to be turned off and was never told the setting existed. The measured number (one request in five) is put in front of the teacher as a caution in Settings instead — which respects the measurement AND the person, rather than deciding on their behalf. |
| 244 | 2026-08-16 | A scheduled deploy set for 9:49 PM did not fire, and macOS's Background Activity notice called the thing "bash" rather than naming Plantoir. | ✅ One cause for both. The launch agent ran `/bin/bash -c <script>`, so macOS had no application to attribute it to: the notice named the interpreter, and — the half that actually broke — the privacy system granted it nothing. The working folder is on the Desktop, which is protected, and the agent's log is `Operation not permitted` on every path INCLUDING `getcwd`, while the identical deploy from the app seven minutes earlier finished in 144 seconds. The agent now runs the app's own signed binary with `--run-scheduled-deploy <script>`, and the one-shot command moved from a line inside the plist to a file beside it. All the script's logic — build if stale, deploy only if the build worked, clear the alarm either way — is unchanged. | **Proved by an A/B probe rather than reasoned about**: two launch agents running the identical script in the same second, one through `/bin/bash` and one through the app. Bash got `READ DENIED` and `Operation not permitted`; through Plantoir it read the folder and found `course_config.json`. The mechanism is the same one that makes pressing Deploy work — a script the APP spawns is attributed to the app. Windows has no equivalent (Task Scheduler, no TCC), but the general lesson travels: **a background job that runs a bare interpreter has no identity, and on a platform with per-application permissions that means no access to the teacher's files.** Cancelling now removes the script as well as the alarm. |
| 245 | 2026-08-16 | After the fix, macOS's Background Activity notice named "Russell Gordon" rather than "Plantoir" — is that Apple's design? | ✅ Yes, verified against the certificate: Background Task Management lists background items by the signing certificate's ORGANISATION (`O=Russell Gordon`), not by application name. Same reason that pane shows "Google LLC" and "Dropbox, Inc." rather than Chrome and Dropbox. Nothing to fix; the finding is written into `ScheduledDeploy` so it is not re-investigated. **And the fix itself is confirmed working end to end** — a real scheduled deploy built the site and uploaded 23 files to Netlify, ✅ Deploy complete, where the same alarm through bash had failed on every path. | **`SMAppService.agent` would put the app's own name there and cannot be used**, which is worth knowing before somebody spends an afternoon on it: it associates an agent with the app bundle only for a plist BAKED IN at build time, and this plist's whole content is a `StartCalendarInterval` the teacher chose, one per course and section. A bundled plist cannot carry a time that changes. Rejected, not overlooked. |
| 246 | 2026-08-16 | The schedule sheet appeared immediately and covered the assistant's answer. It should ASK — "May I ask you for your class dates?" with Yes or Cancel — and only open on Yes. | ✅ `SectionSchedulePrompt` now has an `offer` beside its `request`: anything that DISCOVERS it needs dates offers, and only a Yes opens the sheet. The assistant's answer ends with the question, and a card underneath carries Yes / Cancel — Yes is the default button here, unlike the plan gate, because this asks permission to ask a question rather than to change something. Declining says so in the transcript and names the way back in. | The rule: **a form nobody asked for is a demand.** Worse, it arrived ON TOP of the sentence explaining why — so the teacher met the form before they had read the request, and the request was behind it. Every other write in the window already asks first; this one did not because it was wired straight to the sheet. |
| 247 | 2026-08-16 | A way to replace dates already given. | ✅ "I have a revised list of class dates" (also "I have a new list…", "Change my class dates") opens the sheet STRAIGHT AWAY, and is on the shelf. | Deliberately does not ask first: the teacher volunteered, and answering somebody who is offering their dates with "may I ask you for your dates?" reads as not listening. Two paths to one sheet, and which one applies is decided by who raised it. |
| 248 | 2026-08-16 | "ADA1O Section 1 meets on 75 recorded days, from pasted by hand." — where the dates came from does not belong in that sentence. And "Show me the rest of the dates" repeated the whole summary. | ✅ The summary is now "…meets on 75 recorded days." Where they came from keeps its own line further down. And taking up the offer at the end of an answer is a FOLLOW-UP: it returns the dates and nothing else — no count, no range, no spare-dates line, no origin, none of which the teacher has stopped being able to see. | **A follow-up must not re-answer the question it follows.** The repetition came from one function serving both the question and its follow-up and only branching on the LIST; it now branches earlier, before the summary is built at all. |
| 249 | 2026-08-16 | Re-date a section's classes onto a schedule once it has been provided — the way a teacher rolls a course over to a new year. Classes map to dates purely by POSITION. | ✅ `SectionReDatePlanner`, plan-then-apply, behind the same gate as every other write and undoable in one step. Three kinds of page move: **classes** take their position's date (first class, first date); **what a class brings** — every page it links to, transitively — takes that class's day, earliest class winning a shared page; **pages the section uses all year**, meaning what Key Links points at, take the first day of class. Fewer dates than classes refuses WHOLE rather than dating some. Phrasings matched in code: "Re-date my classes", "Roll this section over to a new year". On the shelf. | **Curriculum pages are deliberately NOT re-dated, and finding out why is the useful part.** `build_site.py` already dates them on every build — to the LATEST date in the section, so they sort to the top — and it does it to the copy it PUBLISHES, so anything written to the source would be overwritten on the way out. Moving them here would be work that does nothing while fighting a rule that already exists. Check what your own build does before adding a rule about dates. **Position, not numbering**: a course where a unit ran long, or with a page called "Field Trip" rather than Unit 2, Day 4, is dated wrongly by counting unit and day numbers. Counting POSITION cannot be wrong about a course it has never seen. The tool is on the surface but OFF the local model's list — the thirteen it is shown has not grown, and a test now says so in as many words. |
| 250 | 2026-08-16 | Re-dating refused when a section had more classes than the new year has days. It should put the overflow classes on the FINAL class date instead — the teacher rearranges, and eventually deletes the ones that no longer fit. | ✅ No refusal left. Classes past the end of the list all land on the last class date, together, and the plan says how many and what to do: "2 classes have no day of their own this year, so they all go on 2026-09-10 with the last one. Move or delete them when you have decided what to do." | **Refusing read as careful and was the opposite.** It left EVERY page on last year's dates — the state the teacher asked to be rid of — over a few classes at the end they had not thought about yet. A school year is rarely the same length twice, so coming up short is the ORDINARY case, not the error case, and what a teacher does about it (move things, merge two lessons, drop what no longer fits) is planning, which is theirs. The general rule: **when a job is 95% doable, do the 95% and show the remainder — do not withhold all of it over the part that needs a human.** Note `add_next_class` still refuses when the dates run out, and that stays: declining to CREATE pages you cannot date is not the same as declining to re-date pages that already exist. |
| 251 | 2026-08-16 | Adding a class should stop refusing when the recorded dates run out, the same way re-dating now does. | ✅ `add_next_class` — one class or five days' worth — makes the pages on the LAST class date instead of refusing, and the plan says so: "This one has no class date left, so it shares the last day with the class already on it. Give it a day of your own when you know what they are." The `timetableRanOut` refusal is gone entirely. An EMPTY timetable still refuses, and that stays: there is no last date to fall back to, and a class with no date sorts nowhere. | **The reasoning that justified refusing was about the wrong choice.** It weighed "a page with a made-up date" against "no page", when what a teacher faces is: they have just finished teaching, they want tomorrow's page to write into, and being sent away to record dates first costs them the ten minutes they had. Fixing a date afterwards is a keystroke. This now matches re-dating, so the whole product answers a shortage of dates the same way — do the work, stack the remainder on the last day, say how many are there. |
| 252 | 2026-08-16 | When a new course is added, the course name should default to the course's SHORT name rather than its formal name — in the command-line wizard as well as the app. | ✅ **`CourseNameCatalog.defaultName(forCode:)` is now the one place that decides**, and it returns the short name; the wizard's `autoFillCourseName()` asks it rather than reaching for `.formal` itself. The reason is worth keeping: the formal name is the ministry's string — "Introduction to Computer Science, Grade 11, U" — which is correct and which almost nobody wants as a website title, so defaulting to it only meant the teacher selected it and deleted it before typing what they actually call the course. Defaulting to the longer string in order to have it thrown away is the wrong way round. Both names stay one button away, and the **suggestion buttons swapped order** so the short one reads first, matching what is already in the field. `scripts/setup_course.py` changed the same way — it offers the short name first, then the formal name, then a custom one — so a teacher who sets a course up from the command line and one who uses the app are offered the same thing in the same order. Pinned as data in `contracts/course-management.json` → `defaultCourseName` (five cases, including a club code that fills in nothing) rather than described, and the UI test that asserted the formal name now asserts the short one and clicks the formal button to switch. What did NOT change: the fill still only ever replaces an empty field or a previous fill of its own, so a teacher's own typing is never overwritten. | **Two files, same one-word change**: `NewCourseDialog.AutoFillCourseName()` sets `names.Formal` (line ~549) — make it `names.Short`, and put the choice behind a `DefaultName(code)` on `Plantoir.Core/Catalogs/CourseNameCatalog.cs` so the reason lives with the data rather than in a dialog. Also swap the suggestion order at the `new[] { names.Formal, names.Short }.Distinct()` line so the short name is the first button; the `.Distinct()` there is a nicety the mac lacks — keep it. `defaultCourseName` in `contracts/course-management.json` is runnable and Windows runs none of that file today, so wiring this one block up is a cheap first use of it, and there is currently NO C# test over `CourseNameCatalog` at all. The Python is shared, so the command-line half is inherited with no work — but note the toolchain hash changes, so the next preview on any machine rebuilds the image once. |
| 253 | 2026-08-16 | The prompts asking for a Netlify or Cloudflare token on a first publish are far too terse. Make them a clearer dialog explaining how to OBTAIN the token — and stop opening browser tabs automatically, which is disconcerting; offer links to click instead. | ✅ **A dialog that explains, and a link rather than a hijacked browser.** The launcher's prompt is one line — “Paste Netlify token:” — and until now that line WAS the question: it arrived in a bare alert with a text field, naming something most teachers have never heard of and saying nothing about where to get one. `CredentialRequest` now recognises the three prompts (Netlify token, Cloudflare token, Cloudflare Account ID) and `CredentialRequestSheet` answers each with a title, a short paragraph on what the credential is for and that it is asked once, numbered steps, a clickable link to the page that makes it, and a field labelled for what goes in it. A token is a `SecureField`; an Account ID is not, so it can be checked against what was copied. Pasted whitespace is trimmed before it reaches the launcher. **Both launchers stopped opening the page themselves** (`open` in `deploy.sh`, `Start-Process` in `deploy.ps1` — three each), and their printed instructions were rewritten to the same steps for anyone at a terminal. New trail line: “asked for the Netlify token” — never the answer. Pinned in `contracts/app-rules.json` → `credentialRequests` (generated wording) and `credentialPrompts` (authored: which prompt is which, and that no launcher may open a page). | **Everything here is portable and most of it is data.** The sentences are in the contract as `credentialRequests.requests` — read them, do not retype them; each entry carries title, explanation, steps, linkTitle, linkAddress, fieldLabel, isSecret. Windows already parses prompts in `QuestionParser.cs`, so the matching is three `Contains` checks on the lowered line ('netlify token', 'cloudflare token', 'cloudflare account id') and `credentialPrompts.cases` runs as a test. The `Start-Process` calls are already removed from `deploy.ps1` on this side, so the WinUI dialog is the remaining work: `PasswordBox` for the two tokens, plain `TextBox` for the Account ID, and a `HyperlinkButton` for the link — **do not `Start-Process` the page when the dialog opens**, that is the whole point of the change. Trim the pasted value. Add `asked for a publishing credential` to the Windows trail enum; it is already in `shared-rules.json` → `activityTrail.mustRecord`, so the pinning test will fail there until it is. |
| 254 | 2026-08-16 | Tell the teacher what expiry to set on each token — Netlify's default of 7 days will stop their publishing without warning — and say that a Cloudflare token must INCLUDE their specific account. Then put a button under the Cloudflare Account ID field, in the wizard and in Course Settings, that explains where to find that ID. | ✅ Netlify step 4 is now “Change the expiry — it starts at 7 days… set a date after the end of your school year: next July is a safe choice”, and Cloudflare gained two steps: “Under ‘Account Resources’, choose ‘Include’ and then your own account by name” and “Under ‘TTL’, set the end date to after the end of your school year”. Both launchers print the same steps. The Account ID field in `PublishingChoiceView` — shared by the wizard and Course Settings, so one change covers both — has a **“Where do I find this?”** link button under it, opening the same style of dialog: `CredentialRequest.cloudflareAccountIDHelp`, which shares one `accountIDSteps` list with the launcher-driven twin and differs only in its explanation. What is typed there lands in the field. The grey caption under the field is deleted outright — the button answers the same question on request — while both orange notes stay: what is wrong with the ID typed, and Cloudflare's 25 MB per-file limit. The button carries a `safari` symbol to the left of its text, matching the links inside the dialogs. Trail line records that the instructions were opened, never the ID. | **Verified against the real Cloudflare page while writing it**: the three permission dropdowns are Account / Cloudflare Pages / Edit, “Account Resources” sits below them with Include + the account by name, and “TTL” below that — the steps match the page a teacher actually sees. Everything is in `contracts/app-rules.json`: the sentences under `credentialRequests.requests` (now four), and two authored notes under `credentialPrompts` — `expiryAdviceIsLoadBearing` and `theHelpVariantIsNeverMatched`. **The help variant must never be returned by prompt matching** (a test says so): it can be opened before any token exists, so its twin's “the token you just made” would be a lie. Share ONE steps list between them. On WinUI: a `HyperlinkButton` under the Account ID `TextBox` in the equivalent of `PublishingChoiceView`, opening the same dialog with the value written back on accept — and still no `Start-Process`. |
| 255 | 2026-08-17 | Build plantoir.app from sources rather than by hand, extend it to four pages (home, features, day to day, support), and capture every screenshot on it automatically — in both light and dark appearance, with the page serving whichever matches the visitor's computer. | ✅ Implemented — `website/` holds the layout, stylesheet and one file per page; `python3 website/build.py` writes `site/`, which Netlify still deploys unchanged. Screenshots come from a new UI-test class (`MarketingScreenshotTests.swift`) driving the real app against a demo working folder of ENG2D, MCV4U and SCH3U created through the app's own new-course panel; `website/shots/capture.py` runs it once per appearance, publishes the three class sites, photographs them in Safari and on an iPhone in the Simulator, and puts back everything it borrowed (the Mac's appearance, the app's remembered window sizes, the frontmost app). The release version line moved from hand-edited HTML into `website/site.json`. | **The site itself is shared — do not build a second one.** What Windows owes it is Windows screenshots, once that app ships: the same shot ids in `website/shots.json`, captured from a Windows machine, saved as `<id>-windows-<light|dark>.png`, and the pages taught to offer them. Two mac-specific mechanisms will NOT port and need Windows answers: window screenshots come from XCUITest's `window.screenshot()` (exact window rectangle, no shadow, transparent corners — a plain region grab picks up the desktop behind the rounded corners), and appearance is switched machine-wide through System Events and restored afterwards. Note the trap that cost time here: `xcodebuild` does not hand its own environment to the test runner, so the demo folder arrives as `TEST_RUNNER_MARKETING_WORKSPACE` — expect an equivalent hop in whatever runner Windows uses. |


| 256 | 2026-08-17 | Bring the Windows side of the repository into sync with what the mac has. | ✅ Done, in the three parts that could be done from this side. (a) **The 80 commits were pushed** — `main` had been sitting at `56ffdd53` on `origin` since 2026-08-15, so the entire `contracts/` directory, both bootstrap files and three days of handoff writing existed only on this Mac. Nothing else in this row would have reached them without it. (b) **Entries 107–255 were assessed against `windows-app/` source** and written into the Windows status section above — that section had said "stops at entry 106" since 2026-08-11, which meant 149 entries were sitting in a category ("unassessed") that reads exactly like "not yet" and is not the same thing. (c) **The findings became an ordered work list** in `WINDOWS-HANDOFF.md` → "Where Windows actually stands", with `WINDOWS-BOOTSTRAP.md` § 3 pointed at it. No C# was written: `dotnet` is not installed on this Mac, so anything typed here would arrive unbuilt and untested, which is worse than arriving as a list. | **Read the ordered list before planning anything** — it is the output of this row and it exists so the plan rule in `WINDOWS-BOOTSTRAP.md` § 0 can be satisfied from evidence rather than from reading 3,700 lines of handoff. Four findings are worth knowing before you open it: nothing in `windows-app/` references `contracts/` (not even a `.csproj` include, so the cases the mac runs cannot be run here at all); `AssistAgent.AskFirst` shows a teacher "I'd like to run **deploy section**", which is rule 1 broken in the one place a teacher is most likely to be looking; `LocalModel.cs` passes neither thinking flag, which is free today on Qwen2.5 and costs 58 points of routing the moment a Qwen3 tier appears; and entry 252 is a one-word fix at `NewCourseDialog.cs:549`. The assessment was made by READING, not running — if a ✅ above is wrong, that is a defect in this row and worth saying so. |
| 257 | 2026-08-17 | The Curriculum Coverage card showed an empty gap on the right in three-strand courses like MCV4U, whereas four-strand courses like ENG2D fit nicely — the backing contrast card should fit the columns of expectations, while keeping the legend on the card and wrapping legend text beside each swatch. | ✅ Fixed in the shared toolchain. The grey contrast card (`.coverage-panel`) wraps the map and the legend together so both enjoy proper contrast. Previously, `.coverage-panel`'s `width: fit-content` expanded to the longest unbroken legend row (“Included in assessed work — the cell carries a ring”, ~380px), leaving an empty grey gap beside the 3 columns (~266px) in MCV4U. The legend (`.coverage-legend`) now uses `contain: inline-size; min-width: 100%;` so its content does not expand the panel's intrinsic width — the card's width is dictated purely by the expectation columns of `.coverage-map`. Each row uses `display: flex; align-items: flex-start; gap: 0.75rem;` with `.coverage-legend-label` (`flex: 1 1 auto; min-width: 0;`), so wrapped text forms a tidy column paired right beside its swatch rather than wrapping beneath it, with 0.75rem spacing giving the 5px offset outline on assessed swatches comfortable breathing room. Tested in WebKit across 2, 3, 4, 6 strands and 320px mobile. | Shared Python (`scripts/build_site.py`), so Windows inherits the fix automatically with no separate work. |
| 258 | 2026-08-17 | Adding a section to an existing course later on (via context menu or Add Section sheet) produced an empty section with no class notes, empty index body, and generic placeholders, unlike initial wizard setup. | ✅ Fixed in `SectionAdder.swift`. `SectionAdder` now finds the lowest-numbered existing sibling section on disk and recursively replicates all directories and files into the new section. For root `index.md`, the section title is updated and `created:` timestamp is freshened while preserving transclusions and body. For configured top-level per-section files (`Key Links.md`, `Private Notes.md`, `Scratch Page.md`), `created:` is freshened and bodies/frontmatter flags are preserved. For all class lesson pages in subfolders (e.g. `All Classes/`), schedule dates, tags, and lesson notes are preserved verbatim. Fallback templates remain for courses with no existing sibling. Fully unit-tested in `SectionAdderTests`. | Mirrored in `windows-app/Plantoir.Core/Models/SectionAdder.cs`. `SectionAdder.AddSection` now replicates the lowest existing sibling section directory (`LowestExistingSiblingSectionDirectory`), adapting root `index.md` title and freshening top-level `perSectionFiles` created dates while copying all class lesson files, folder structures, and markdown bodies. |
| 259 | 2026-08-17 | Netlify token prompt matching and expired token validation symmetry with Cloudflare. | ✅ Ensured `CredentialRequest.matching` robustly catches prompt wording variations for Netlify (e.g. "Paste Netlify personal access token:", "Paste Netlify access token:") and Cloudflare, ensuring `CredentialRequestSheet` (titled "Connect to Netlify" with step-by-step guidance, expiry advice, and direct link to Netlify Personal Access Tokens page) is presented. Also updated `deploy.sh` and `deploy.ps1` to test saved Netlify tokens and clear expired/invalid credentials upfront with a warning notice, prompting the teacher with the full dialog rather than allowing the deploy to fail later inside the container. | Mirrored in `deploy.ps1` — validates retrieved Netlify tokens via `Test-TokenValid` and calls `Remove-TokenInCredMan` if invalid, matching the Cloudflare token workflow. |
| 260 | 2026-08-17 | Make teacher surname and site name prompt dialogs rich and informative (matching token request dialogs). | ✅ Added `teacherSurname`, `siteName`, and `siteNameConflict` to `CredentialRequest` and updated `CredentialRequestSheet` to support optional links (`linkAddress: URL?`) and custom placeholders (`fieldPlaceholder`). `teacherSurname` explains why the surname is needed (building recognisable URLs like `mcv4u-s1-2026-gordon` and avoiding collisions, saved once on the computer). `siteName` explains global domain uniqueness and provides structured format suggestions (`<course>-s<section>-<year>-<surname>`, `<school>-...`). `siteNameConflict` provides guidance when a chosen address is already taken on Netlify. `TaskProgressView` supplies `initialAnswer: runner.suggestedAnswer` to prefill suggested subdomain names in the input field. In non-interactive/terminal mode, `scripts/deploy.py` prints matching clear explanations before prompting. All 745 unit tests pass and contracts regenerated in `contracts/app-rules.json`. | Deserialise the new requests from `contracts/app-rules.json` → `credentialRequests.requests` (`teacherSurname`, `siteName`, `siteNameConflict`). In WinUI, render `TextBox` (`isSecret: false`) with the appropriate placeholder and prefill `suggestedAnswer`. Only render `HyperlinkButton` when `linkAddress` is non-empty. Match prompts containing `last name`/`surname`, `choose a different netlify site name`, and `enter netlify site name`/`website name`. |
| 263 | 2026-08-17 | The Deploy button was disabled while a section preview was active or building; clicking Deploy now automatically cancels/stops any running or building preview, awaits container-side process termination, and starts the deploy. | ✅ Implemented in `SectionDetailView.swift`. The Deploy toolbar button's `.disabled` condition was changed from `isBusy` (which evaluated to true whenever a preview was running) to `deployRunner.isRunning`. In `deployAndWait()`, if `previewRunner.isRunning` is true, it calls `await stopPreviewAndWait()`, terminating both the host runner and container-side Node/Quartz/build processes via `PreviewStopper` before proceeding with the production build (`preview.sh --build-only`) and deployment (`deploy.sh`). If no preview is running but a stop was in flight, it awaits `PreviewStopper.waitForStopsToFinish`. This aligns GUI button behavior with the local AI assistant's `deploy_section` workflow. All 746 unit tests pass. | In WinUI's section view, enable the Deploy button while a preview is running/building (disable only when a deploy is already running). When Deploy is invoked, call the preview stop routine (`preview.ps1 CODE N -Stop`), await container process termination, and then proceed with build and deploy. |
| 264 | 2026-08-17 | The Windows version of Plantoir has an option to review course content with Claude Code through the built-in MCP server. Bring the same option to the macOS version. | ✅ Implemented in `mac-app/` via `ClaudeCodeLauncher.swift`. When Claude Code is installed, a **“Revise with Claude…”** item (sparkles symbol) appears at the top of the sidebar context menu for both Courses and Sections (hidden when Claude Code is not found). Choosing it generates a per-course temporary MCP configuration in `~/Library/Application Support/Plantoir/assist/mcp-<courseCode>.json` pointing to the Plantoir executable answering `--mcp-stdio`, and launches an interactive Claude Code session in the teacher's terminal (iTerm2 if running, falling back to Terminal.app) with an initial greeting instructing Claude to list sections and use plan tools before modifying course content. If launching fails, an informative alert (“Claude didn’t open”) explains the issue. Tested with unit tests covering config generation, shell escaping, prompt text, and executable discovery. | Already implemented on Windows via `Services/ClaudeCodeLauncher.cs`. Both platforms now provide matching “Revise with Claude…” menu items and use identical temporary MCP configurations and greeting text. |
| 265 | 2026-08-17 | On mobile viewports (e.g. phones), the top navbar's course code and section wrapped onto two lines (e.g. `📚 ENG2D` with `S1` wrapping below) due to tight horizontal space between the hamburger menu button, search bar, and dark mode toggle; additionally, the course emoji, code, section, and the light/dark mode toggle button appeared top-aligned relative to the search field. | ✅ Fixed in the shared toolchain. `append_page_title_styles()` in `scripts/build_site.py` now sets `.page-title` `font-size: clamp(0.875rem, 4.5vw, 1.75rem)`, `white-space: nowrap`, `margin: 0`, `align-self: flex-start` (with `align-self: center` scoped to mobile), `display: inline-flex`, `align-items: center`, and `line-height: 1`, with `.page-title a` set to `display: inline-flex; align-items: center; max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; line-height: 1;`, and `.darkmode` set to `height: 2rem; display: inline-flex; align-items: center; justify-content: center; vertical-align: middle;` with `& svg { position: relative; top: 0; }`. On tablet and desktop viewports (>= 800px), `clamp` pins to the original `1.75rem` (28px) and `align-self: flex-start` keeps the title flush with the leading edge of the sidebar. On phone screens, font size scales smoothly down as the viewport narrows, but never drops below `0.875rem` (50% of original size), allowing the course title and section marker to sit comfortably on a single line without wrapping while remaining vertically centered relative to the search field and theme controls. In the ALWAYS section so existing courses pick it up on the next preview. | Shared Python (`scripts/build_site.py`), so Windows inherits the fix automatically with no separate work. |
| 266 | 2026-08-17 | Section numbers provided or entered out of order (e.g. 5, 4, 2, 9) appeared out of order in the sidebar and settings; they are now always sorted in ascending numeric order. | ✅ Fixed in both apps and toolchain. `CourseConfiguration.sectionNumbers` now always returns section numbers sorted in ascending order (`[2, 4, 5, 9]`), and `setSectionNumbers` / `NewCourseWizardView.parsedSectionNumbers` sort numbers before saving. Also updated `get_allowed_section_numbers` in `scripts/build_site.py` and `prompt_section_numbers` in `scripts/setup_course.py`. All unit tests pass. | Mirror in `CourseConfiguration.cs` (`SectionNumbers.OrderBy(n => n)` and `SetSectionNumbers`) and `NewCourseDialog.cs` (`ParsedSectionNumbers`). |
| 267 | 2026-08-17 | Add the Plantoir website link back to the About panel. | ✅ Added `websiteURL: URL? = URL(string: "https://plantoir.app")` to `AboutView.swift`. When present, `contactDetails` renders a "Website" row with a clickable link to `https://plantoir.app` alongside the "Email" row in the grid with aligned labels. All sizes remain `@ScaledMetric`. | Mirrored in WinUI's `AboutDialog.cs` with a `Grid` rendering both Website (`https://plantoir.app`) and Email (`support@plantoir.app`) rows with aligned labels. |
| 268 | 2026-08-17 | Social media preview cards (OpenGraph/Twitter) on built sites pointed to Jacky Zhao's default `https://quartz.jzhao.xyz/static/og-image.png` rather than the section's generated card. | ✅ Fixed in the shared toolchain (`patches/Head.tsx`, `scripts/build_site.py`, `scripts/deploy.py`). `build_site.py` resolves the section's public domain (from advanced custom domains, `.netlify_sites/sectionN.json`, or `.cloudflare_sites/sectionN.json`) and sets `baseUrl` in `quartz.config.ts` on every build (clearing it to `""` when unpublished). `deploy.py` checks that `baseUrl` matches the assigned domain prior to delta/pages deploy, rebuilding if needed so first-time deploys immediately carry complete absolute OpenGraph (`og:image`, `og:url`) and Twitter tags. In `patches/Head.tsx`, when `baseUrl` is absent/empty, `og:image` falls back to page-relative `./static/og-image.png` and `twitter:domain`/`og:url` tags are omitted, ensuring `quartz.jzhao.xyz` is never emitted. | Shared Python and patched Quartz component — Windows inherits the fix automatically. |
| 269 | 2026-08-18 | The working folder picker's intro paragraph described the folder as where \"class websites live\", which is misleading since the folder holds the teacher's Markdown course notes (which are then built into class websites). Updated to refer to course notes. | ✅ Updated in `WorkspacePickerView.swift` to \"Pick the folder where your course notes live — the one with your courses inside (for example, “Course Notes” on your Desktop). Starting from scratch? Choose an empty folder instead.\" | Mirrored in `windows-app/Plantoir/Views/WorkspacePickerView.xaml`. |
| 270 | 2026-08-18 | Enriched environment logging in Problem Reports and the Activity Trail: include exact OS build number, Darwin kernel release, and helper software / container toolchain versions. | ✅ Implemented in `mac-app/QuartzTeachers/Scripting/ProblemReport.swift` and `ActivityTrail.swift`. `ProblemReportEnvironment.systemDescription` now records the OS build (e.g. `(25G72)`) and Darwin kernel release (`Darwin 25.6.0`) via `sysctl kern.osversion` and `kern.osrelease`. `ProblemReportEnvironment.helperDescription` captures the assistant engine build (`llama.cpp b10435 (Metal)`) and pinned static container tools (`Colima v0.10.3`, `Lima 2.2.0`, `Docker CLI 29.7.2`, `Buildx v0.36.1`). `RunRecord` headers now include a `Helpers` line, and `ActivityTrail.noteLaunch()` records a new contract event `.helpers` (`"helpers described"`). Pinned in `contracts/shared-rules.json` → `activityTrail.mustRecord` and validated in unit tests. | ✅ Implemented in `ProblemReportEnvironment.cs` / `ActivityTrail.cs`: captures Windows release/build, hardware resources (.NET memory info, cores), .NET runtime version, and assistant/WSL2 versions. Recorded on launch. |
| 271 | 2026-08-18 | Add a Cancel button in the bottom-trailing corner of TaskProgressView to cancel running previews and deploys, and an informative \"Why might this take a while?\" popover explaining common reasons in plain language. | ✅ Implemented in `TaskProgressView.swift`, `ScriptRunner.swift`, `WhyTakingLongView.swift`, and `SectionDetailView.swift`. When a preview or deploy is running, a bordered `Cancel` button sits in the bottom-trailing corner below the progress bar. Clicking it calls `runner.cancelByUser()`, which sends `\x03` (`Control-C`) through the pseudo-terminal so foreground process groups and traps terminate cleanly (escalating to direct termination after 2 seconds), cleans up container-side processes via `PreviewStopper.stopSectionProcesses(...)`, releases preview port leases and publishing locks, and shows the orange `Cancelled` state. In the leading corner below the milestone text, a `Why might this take a while?` button opens a popover explaining first-time setup, first-time publishing, media files, and internet speed in jargon-free terms. Unit tests pass. | ✅ Implemented in WinUI `TaskProgressView.xaml` / `TaskProgressView.xaml.cs`: Cancel button bottom-right calling `runner.CancelByUser()` with Control-C escalation and container cleanup, plus \"Why might this take a while?\" flyout bottom-left. |
| 272 | 2026-08-18 | Rapid rebuilds or deploying after cancelling a preview/deploy could fail with `ENOTEMPTY: directory not empty, rmdir .../public/Curriculum` during Quartz's pre-build cleanup. | ✅ Fixed in the shared toolchain (`patches/build.ts`, `Dockerfile`, `scripts/build_site.py`, `scripts/deploy.py`). Stock Quartz's `rimraf(path.join(output, "*"), { glob: true })` crashed on Docker bind mounts (virtiofs/9p) when deleting non-empty directories without retries. Added `patches/build.ts` to retry and fall back to recursive `fs.rm` upon error, and added `safe_clean_public_dir()` in Python to cleanly wipe `public/` before invoking Quartz build. Also added `patch_quartz_build_clean()` in the ALWAYS section of `build_site.py` so existing course section folders inherit the fix on their next build without requiring a full rebuild. Pinned in `contracts/toolchain.json`. | Shared Python and patched Quartz component — Windows inherits the fix automatically. |
| 273 | 2026-08-18 | Asking to unpublish/publish a specific class page (e.g. "Unpublish Unit 4, Day 20") could sweep all other classes in the course if the model populated `onOrAfter` from the prompt's dateline. | ✅ Fixed in `AssistToolRunner.swift` and `AssistPublishPlan.swift`. In `pagePlan`, `onOrAfter` and `before` date parameters are only evaluated when `titles.isEmpty`. When specific page titles are named, extraneous date arguments (such as dateline leakage from the prompt) are ignored rather than sweeping the entire course semester into the publish/unpublish plan. Unit test added in `AssistToolRunnerTests`. | ✅ Mirrored in `windows-app/Plantoir.Core/Assist/AssistWorkspace.cs`: guard date range class iteration with `if (pageTitles.Count == 0 && (onOrAfter is not null || before is not null))`. |
| 274 | 2026-08-18 | Re-dating classes onto a schedule with fewer dates than classes left overflow classes published and caused the section index page (`index.md`) to transclude the overflow class on the final date rather than the last scheduled class. | ✅ Fixed in `SectionReDatePlanner.swift` and `SectionIndexPointer.swift`. When classes overflow the available timetable dates during re-dating (`re_date_classes`), overflow classes are moved to the final date as drafts (`publish: false`) and disclosed as such in the plan description. `SectionReDatePlanner.apply` now invokes `SectionIndexPointer.repointIndex` so the section landing page (`index.md`) automatically repoints to transclude the last in-bounds published class (and adopts that class's date) instead of transcluding an overflow class. Unit tests pass in `AssistToolRunnerTests`. | ✅ Implemented in `windows-app/Plantoir.Core/Assist/ReDatePlan.cs` and `AssistWorkspace.cs`: overflow classes marked drafts (`publish: false`) and `index.md` repointed to the newest visible in-bounds class. |
| 275 | 2026-08-18 | Built preview/deployed sites showed the first day's date on the section landing page instead of the newest published class's date, and same-date class comparisons did not break ties by Unit/Day numbering. | ✅ Fixed in `scripts/build_site.py`, `SectionIndexPointer.swift`, and `ClassPages.swift`. `_sync_non_class_pages_created` in `build_site.py` now excludes `content/index.md` so the root section landing page retains its newest published class's date rather than being reset to the first day of class (`Unit 1, Day 1`). `UnitDay` now conforms to `Comparable`, and `SectionIndexPointer.mostRecentVisibleClass` uses `UnitDay` counting to break ties when multiple published classes sit on the same date. Updated `contracts/class-planning.json` and `WINDOWS-HANDOFF.md`. Unit tests pass in `AssistToolRunnerTests` and `./verify.sh` passes. | ✅ Implemented: `IComparable<UnitDay>` and tie-breaker in `SectionIndex.MostRecentPublished`. |
| 277 | 2026-08-18 | Tapping \"What dates am I teaching?\" caused the local AI assistant to loop, producing multiple repeating tool bubbles before answering, and provided no upcoming class details. | ✅ Fixed in `mac-app/QuartzTeachers/Models/Assist/AssistToolRunner.swift`, `AssistCardCommand.swift`, and `AssistSupportingViews.swift`. `readRememberedTimetable` now computes upcoming classes deterministically in Swift relative to `today`, formatting the next 3 classes with their dates and Unit/Day titles, schedule status (total recorded dates, class pages created, spare count), and finishes the turn directly with `shouldContinue: false` (eliminating LLM round-trips and tool-calling loops). Renamed starter shelf prompt to \"When are my next classes?\" and removed \"Show me the rest of the dates\" from the top-level shelf. Added matching phrasings in `AssistCardCommand` and `contracts/assist-cases.json`. Unit tests pass. | ✅ Implemented in `windows-app/Plantoir.Mcp/PlantoirTools.cs` and `AssistAgent.cs`: deterministic upcoming class formatting, status calculation, and turn termination (`_handedToApp = true`). |
| 278 | 2026-08-18 | Initial/clean course site builds were slow (taking 18–20s on macOS and 2–4 minutes on Windows WSL2) because Quartz scaffold copying, `npm install`, and intermediate esbuild/AST passes crossed the virtual host filesystem mount (9P/virtiofs). | ✅ Implemented in the shared toolchain (`Dockerfile`, `scripts/build_site.py`, `scripts/deploy.py`, `preview.sh`, `preview.ps1`). Pre-baked `node_modules` in `/opt/quartz` inside the container image and adopted a dual-workspace architecture: active builds run on container-internal ext4 storage (`/tmp/quartz-builds/<COURSE>/section<N>`), with differential `rsync -a --delete` mirroring `public/` and `course_config.json` back to host `.merged_output/` on completion and during preview watching. Measured on macOS (Apple Silicon + Colima): clean builds dropped from 18.66s avg to 5.08s avg (3.7× faster, −13.58s); incremental rebuilds dropped from 4.42s to 3.35s (1.32× faster). All 760 macOS unit tests and `./verify.sh` pass. | Shared toolchain implementation: Windows WSL2 inherits the acceleration automatically, dropping clean builds from 2–4 minutes down to under 10s. All 570 Windows unit tests pass. |
| 279 | 2026-08-18 | Setting up a new empty working folder failed with \"This folder is missing the toolchain's build recipe\" because `initializeWorkspace()` copied launchers and created `courses/` without ensuring `.toolchain/` was mirrored on disk, and `refreshToolchain()` skipped mirroring if the folder path was in `foldersWithFreshToolchain` even when `Dockerfile` was missing. | ✅ Fixed in `WorkspaceModel.swift` and `WorkspaceInitializationTests.swift`. `refreshToolchain` now checks that `.toolchain/Dockerfile` actually exists on disk before skipping, and `initializeWorkspace()` clears the fresh-toolchain tracker and explicitly invokes `refreshToolchain(in: workspaceURL)` so the build recipe is guaranteed to be present before opening the New Course wizard. Tested and pinned in unit tests. | Ensure `WorkspaceModel` or folder initialization on Windows mirrors the toolchain recipe to `.toolchain\` upon creating/initializing a working folder. |
| 280 | 2026-08-18 | Update marketing website hero image to a 3-window diagonal cascade of Obsidian, Plantoir actively deploying, and the deployed class website in Safari. | ✅ Implemented in website pipeline and mac UI tests (`mac-app/Tests/QuartzTeachersUITests/MarketingScreenshotTests.swift`, `website/shots/composite.py`, `website/shots/capture.py`, `website/shots.json`, `website/pages/index.html`). Added `test3Deploying` to capture Plantoir mid-deployment (`hero-plantoir`) with unoccluded progress bar, status bar, and milestone labels. Added `diagonal_hero` compositing function with equal horizontal and vertical window stagger ($\Delta x = \Delta y$), rounded corners, drop shadows, transparent canvas, and complete dark/light mode consistency across OS chrome, Obsidian, and Quartz class site. Built into `site/img/hero-light.png` and `hero-dark.png`. | Website generation toolchain — shared Python and marketing screenshot pipeline. |
| 281 | 2026-08-18 | Deploying a section while preview is active failed immediately with \"❌ Built site not found at: .../.merged_output/sectionN/public\" because rapid transition from `--build-only` to `deploy.sh` beat hypervisor mount buffer flushing across VirtioFS / WSL2. | ✅ Fixed in `scripts/build_site.py`, `scripts/deploy.py`, `deploy.sh`, and `deploy.ps1`. `_sync_public_to_host()` now passes `--no-owner --no-group` to `rsync` and explicitly invokes `os.sync()` to flush dirty pages across virtual filesystem mounts before container exit. `deploy.sh` and `deploy.ps1` host preflight checks now provide a 2-second settle loop before failing on missing/empty `public/` directory, preventing transient subsecond mount synchronization races during automated build-to-deploy handoffs. All 760 unit tests and `./verify.sh` pass. | Mirrored in `deploy.ps1` preflight check with identical 2-second settle loop. |

| 257 | 2026-08-17 | Implement all shared contracts, assist tools, settings panel, curriculum rules, and class planning in the Windows app. | ✅ All 13 backlog items implemented on the `windows-sync` branch. (1) Shared contracts wired and tested in `Plantoir.Tests` (457 tests passing, 0 failures), covering `assist-cases.json` (scenarios, near misses, prompt history), `class-planning.json`, `schedule-rules.json`, `app-rules.json`, `assist-wording.json`, `course-management.json`, `file-formats.json`, and `shared-rules.json`. (2) Assistant choice and settings dialog (`AssistantSettingsDialog`) with confirmation toggle, model choice (automatic/smaller/larger), hardware budget calculation, and model housekeeping. (3) 23 MCP tools implemented in `PlantoirTools.cs` / `plantoir-mcp.exe`. (4) Class planning, unit/day numbering, timetable memory & fallback dating in `UnitDay`, `NextClassPlanner`, and `ClassInsertionPlanner`. (5) Rich credential dialogs with token instructions and Cloudflare Account ID "Where do I find this?" link button. (6) Curriculum expectation parsing, validation, and anchor stripping via `CurriculumRules`. (7) Thinking flags (`--reasoning off`, `--reasoning-budget 0`) added to local model server. | Windows app is now in full parity with macOS app features and shared contracts. |
| 259 | 2026-08-17 | Move local AI assistant on Windows out of WSL2 container and run natively on Windows host with Vulkan GPU acceleration. | ✅ Implemented and measured on real hardware. Created `windows-app/Vendor/fetch-llama.ps1` to download and unpack pinned `b10435` Vulkan Windows x64 build into `windows-app/Vendor/llama/`. Added `llama/` bundling to `Plantoir.csproj` and signing to `publish.ps1`. Rewrote `LocalModel.cs` in `Plantoir.Core.Assist` to execute host `llama-server.exe` directly on dynamic loopback port with `--n-gpu-layers 999`, `--reasoning off`, `--reasoning-budget 0`, `--jinja`, and `--parallel 1`. Removed WSL2 keep-awake hacks and 98 MB disk KV serialization. Replaced slow WarmUp countdown in `AssistWindow.xaml.cs` with fast background priming. Added comprehensive unit tests in `LocalModelTests.cs` (464 total tests passing). | Measured on Intel Core i5-8365U with Intel UHD Graphics 620: Vulkan GPU offload achieves 25.82 tok/s prompt processing (CPU: 25.69 tok/s), 7.83 tok/s token generation (CPU: 11.67 tok/s), and single-turn end-to-end latency of ~17.99 s compared to ~175 s in WSL2 container without GPU. Host execution completely removes WSL2/Docker overhead and gives users native on-device assistant performance. |
| 260 | 2026-08-17 | Complete remaining Windows handoff parity items: Curriculum Coverage & Notes toggles, LinkGraph exclusions/referrers, and publishing credential ActivityTrail tracking. | ✅ Implemented. (1) Added `include_curriculum_coverage` and `include_coverage_notes` toggles in `CourseSettingsView.xaml.cs` and `NewCourseDialog.cs` with dependent enablement via `CourseConfiguration.CoverageNotesEnabled`. (2) Updated `LinkGraph.cs` with exclusions for landing pages (index.md), curriculum pages, and Key Links targets, plus visible referrers check `VisibleSourcesOf`. (3) Added `ActivityTrail` recording of `asked for a publishing credential` for Netlify/Cloudflare prompts and `opened instructions for Cloudflare Account ID` on help button clicks. All 466 unit and contract tests passing. | Full parity achieved for Windows handoff items 6d, 12, and 13b. |
| 261 | 2026-08-17 | Prompt shelf with clickable, collapsible disclosure groups in the local AI assistant window. | ✅ Implemented on Windows (`AssistPromptShelfView.cs`). Pinned at the top of `AssistWindow` with 5 collapsible categories matching macOS verbatim (19 cards). Tapping any card puts the phrasing into the input composer for editing. Open/shut state is persisted in `AppSettings.AssistPromptShelfOpenGroups`. Tested in `AssistPromptShelfTests` (473 total tests passing). | Windows app now matches macOS AssistPromptShelfView behavior and styling. |
| 262 | 2026-08-17 | Section schedule date prompting and entry dialog (`SectionScheduleDialog`) in the local AI assistant window. | ✅ Implemented on Windows (`SectionScheduleDialog.cs`). Added interactive "May I ask you for your class dates?" card in `AssistWindow` with `[Enter class dates]` and `[Not now]` buttons. Dialog supports 3 input routes: type/paste, file picker (.csv, .ics, .txt, .xlsx), and shared Google Sheet URL, with `OrderingQuestion` resolution and saving to `TimetableMemory`. Fixed `ReadRememberedTimetable`, `PlanReDateClasses`, and `ReDateClasses` to support optional parameters and graceful date prompting when no schedule is on file. 478 tests passing. | Windows app now matches macOS SectionScheduleSheet and AssistDatesOfferView behavior. |
| 263 | 2026-08-17 | Align Windows Local AI Assistant brevity and concise responses with macOS. | ✅ Implemented on Windows (`AssistAgent.cs`, `AssistWorkspace.cs`, `LocalModel.cs`). (1) Removed `plan_` tools from `ForTheLocalModel` narrowing so local model purely routes to action tools, matching macOS. (2) Replaced verbose paragraph summaries with crisp 1-sentence summaries ("Published “Unit 2, Day 3”." / "Nothing needed changing.") in `AssistWorkspace.Summary()`. (3) Set model inference `temperature: 0` for deterministic routing. (4) Replaced tool-name approval formatting in `AssistAgent.Run()` with `AssistWording.DeployApproval` / `AssistWording.DeployQuestion`. 479 tests passing. | Windows local assistant responses now match the concision, brevity, and deterministic single-turn stopping behavior of macOS. |
| 264 | 2026-08-18 | Enforce async preview stop teardown before deploy, wire scenario & tools contracts, and implement confirmation mode discovery tracking. | ✅ Implemented on Windows (`AssistAgent.cs`, `SectionDetailView.xaml.cs`, `PreviewStopper.cs`, `MainWindow.xaml.cs`, `AssistWindow.xaml.cs`, `AssistScenarioTests.cs`, `ContractTests.cs`). (1) Awaited container and host preview stop asynchronously in `PreviewStopper.StopSectionProcessesAsync` and `SectionDetailView.StopPreviewIfRunningAsync` before deploy. (2) Added `AssistScenarioTests.cs` executing all scenarios in `contracts/assist-cases.json` via parameterized `[Theory]` tests. (3) Added `ContractTests.cs` validation for tool surface narrowing (`tools.local`, `tools.needsApproval`, `tools.mcpOnly`) and confirmation settings (`shared-rules.json` → `assistantConfirmation`). (4) Wired confirmation mode and 15-plan discovery milestone in `AssistAgent` and `AssistWindow`. 488 tests passing, 0 failures. | Fully matches macOS async stop-then-deploy sequence and contract rules. |
| 265 | 2026-08-18 | The Windows assistant is still chatty: its replies do not match what the mac says for the same request, even after rows 263 and 264. Find out why and fix it comprehensively. | ✅ Implemented on Windows (`AssistToolAnswer.cs` new, `PlantoirTools.cs`, `AssistAgent.cs`, `McpClient.cs`, `PublishPlan.cs`, `AssistWorkspace.cs`). **The cause was structural, not wording.** Every Windows tool returned ONE string and it was shown to the teacher and fed to the model both — so "read Unit 2, Day 3" put a lesson's entire Markdown in the chat, "what pages are in this section" put sixty file paths there, a publish said "Published 4 pages (A, B, C, D) and rebuilt the preview of ICS3U Section 1", and a plan ended with "Show this to the teacher and ask before going ahead" — a sentence addressed to the model, read by the teacher. The mac has split the two since the assistant shipped (`AssistToolOutcome`: a one-line `summary` for the transcript, a long `detail` for the model), and that split is the whole reason its replies are short. Ported: the summary now travels out of `plantoir-mcp` in the result's `_meta`, the text content is unchanged so Claude Code sees exactly what it saw before, and the app shows the summary. Teacher lines are now the mac's, word for word — "Read “Unit 2, Day 3”.", "Found 42 pages in ICS3U Section 1.", "Published 4 pages.", "It's already been published.". Undo now speaks the CONTRACT's sentences (`AssistWording.Undid` and friends) instead of its own "Undid publishing “X” — put 3 files back.", which needed the undo history's descriptions turned past-tense. `list_recent_changes` no longer names `undo_last_change` to a teacher. The turn-ending list named `roll_over_course` for a tool called `roll_over_section` and omitted five other writes, so those got the model an extra lap and the teacher a paragraph restating the line above it. 502 tests pass. | **The mac needs no change — this is Windows catching up**, and the entry in `MAC-HANDOFF.md` says which contract case is still wanted. The one thing worth carrying back: the CHANNEL. macOS calls its tool runner in-process, so its split costs nothing; Windows drives the same `plantoir-mcp` Claude Code drives, so the summary needed a wire. `_meta` was chosen over a second text block (Claude Code would read both) and over `structuredContent` (that field is validated against a declared `outputSchema`, and declaring one changes what every client sees). `_meta` is the protocol's own extension slot, needs no schema, and is dropped by every client that does not know the key. `ToolAnswerTests.TheTeachersLineTravelsInMetaWhereTheAppLooksForIt` serialises a real result and asserts the wire shape, because a silent fallback to the model's half is exactly how this regresses. |
| 266 | 2026-08-18 | The Windows Settings switch "ask before changing" was wired to nothing: macOS says the PLAN before every write and waits for Go, Windows just did it and reported afterwards. Make the setting mean what it says. | ✅ Implemented on Windows (`AssistAgent.cs`, `PlantoirTools.cs`, `AssistToolAnswer.cs`, `AssistWindow.xaml.cs`). `ConfirmationMode` was read from `AppSettings.AssistantAsksBeforeChanging` and then never consulted — so the switch did nothing, and the "the assistant shows what it is about to do before doing it" nudge after fifteen accepted plans described behaviour the app did not have. Now: before any write with a `plan_` twin, the twin runs, its plan is SAID in the transcript, and Go / Cancel appear under it. The model is not asked a second time — the arguments it chose are carried through, so what runs on Go is exactly what was described. A fixed phrasing from the promise card is gated too: skipping the MODEL is not skipping the teacher. A plan twin that REFUSED gets no buttons, because a refusal is an answer and "Shall I go ahead?" under one invites a teacher to approve a dead end. Cancel now answers the model as the TOOL ("The teacher decided not to. Nothing was done.") rather than as the assistant, so the model's dangling call is not a hole the next turn reads across. A scheduled deploy's card said only "Shall I go ahead?" — no time, no section — and now says what it is a deploy of, and offers "Deploy" rather than "Go". 505 tests pass. | **Inherited from the mac unchanged in intent; two Windows-specific pieces to know.** (a) The gate needs to tell a PLAN from a plan tool's REFUSAL, which on the mac is `AssistToolOutcome.isPlan` — an in-process field. Over MCP it is a second `_meta` key, `plantoir.app/isPlan`, sent only when true. Without it the gate reads every plan as a refusal and no write ever runs, which is why `ToolAnswerTests.APlanMarksItselfAsOne` exists. (b) `AssistAgent.PlanTwins` deliberately omits `re_date_classes`, which IS in the contract's twin list: it is not a tool the local model is shown, and `plan_re_date_classes` does not mark its answer, so gating it would hold a write behind a proposal the loop would then read as a refusal. `ContractTests.PlanTwins_CoverEveryLocalWriteThatHasOne` pins the scope so the omission cannot quietly grow. |
| 267 | 2026-08-18 | "What would students see in this section right now?" on Windows answered "83 visible pages are linked from nowhere" and listed the course's lessons. A healthy course read as a broken one, and the mac says nothing of the kind for the same content. | ✅ Two separate defects in `check_section`, both found from one report (`LinkGraph.cs`, `PlantoirTools.cs`). **(a) Class pages were counted as orphans.** They are the ROOTS of a section — everything else must be reachable FROM a class, and a class is reached through the site's own navigation of All Classes — so "nothing links to this" is their ordinary, correct state. ICD2O Section 1 has 85 class pages and 83 of them were reported "linked from nowhere"; reproduced against the real course, then clean afterwards. The mac has excluded them since it hit the same thing on an 86-period credit (84 reported). Windows now excludes them using the COURSE's own answer — `per_section_folders` from `course_config.json` — rather than the mac's folder-name heuristic. **(b) The preview state was read from the wrong process.** `PreviewLeases` is an in-memory static belonging to the app, and `check_section` runs inside `plantoir-mcp`, where it is permanently empty — so "Nothing is being previewed at the moment" was said every time, whatever was on screen. The trail shows this teacher pressing Preview 32 seconds before asking. It now reads the cross-process lease FILES the app already writes for exactly this purpose, and reports the mac's three states: building, showing (silence), or nothing. 510 tests pass. | **The mac needs no change; this is Windows catching up on both counts.** The trap worth carrying, though, is (b) rather than (a): on the mac the assistant and the preview are the same process, so `sectionWindow(...)?.previewState()` is a method call. Every fact the Windows assistant wants about what the APP is doing has to cross a process boundary, and an in-memory static reads as "nothing is happening" rather than as an error — silently, and identically to a real answer. `WorkLease` is the channel and it was already there. Two limits worth stating: the leases are per-COURSE, not per-section, so previewing Section 2 while asking about Section 1 reports the wrong thing; and the build lease is dropped the moment the server answers, which is what makes "building" and "showing" two states rather than one. |
| 268 | 2026-08-18 | "Unpublish Unit 4" in chat with the local assistant resulted in an error on Windows instead of planning and executing the whole-unit unpublish. | ✅ Fixed on Windows (`AssistCardCommand.cs`, `PublishPlan.cs`, `AssistWorkspace.cs`, `PlantoirTools.cs`, `ToolAnswerTests.cs`). (1) `AssistCardCommand.ToJsonObject` populated required parameter `includeLinked = false` for publish/unpublish tools. (2) `PlantoirTools.cs` methods added default parameter values `includeLinked = false`, `progress = null!`, `cancellation = default` to prevent ModelContextProtocol.NET binding failures. (3) Added `PublishPlan.UnitNamed` and `AssistWorkspace.PlanWholeUnit` / `ApplyWholeUnit` with full macOS parity: steps through unit classes in order (highest day down for unpublish, Day 1 up for publish), wraps changes into a single undo batch, and rebuilds the preview once. 522 tests pass. | Windows assistant now fully supports whole unit planning, publishing, unpublishing, and undo with macOS parity. |
| 279 | 2026-08-18 | Re-dating classes in the Windows assistant produced verbose, un-paraphrased move strings with appended "3 things worth looking at" warnings, while template pages (_DUPLICATE ME.md) were flagged as stragglers. | ✅ Fixed on Windows (`PlantoirTools.cs`, `AssistWorkspace.cs`, `DateAudit.cs`, `AssistAgent.cs`). (1) Wrapped `PlanReDateClasses` and `ReDateClasses` in `CallToolResult` with structured `summary` ("Re-dated 86 classes and 181 pages they use.") and `detail` (with backup note and deploy note) matching macOS parity. (2) Added `re_date_classes` to `PlanTwins` in `AssistAgent.cs` so "re-date my classes" fixed phrasing is properly gated behind the plan preview and awaits user confirmation. (3) Updated `DateAudit.cs` with `IsIgnored` to ignore template pages (`_DUPLICATE ME`), index files, and system files from date anomaly and straggler warnings. All 570 unit tests pass. | Matches macOS `AssistToolRunner.swift` / `SectionReDatePlanner.swift` parity and ignores scaffold templates in date audits. |

## Planned

- **Windows equivalent of the GUI** — a native Windows counterpart to the
  macOS app, wrapping the same toolchain via the PowerShell launchers /
  WSL2 Docker Engine. First take built (see Windows status above);
  every behaviour in the table above applies unless marked macOS-only.

| 282 | 2026-08-19 | Deploying immediately after stopping a preview failed instantly, showing nothing past the launcher's opening lines. Stopping a preview fires the launcher's `--stop` sweep fire-and-forget; it kills the section's container-side processes BY WORKING DIRECTORY seconds later, and the deploy's own quiet build works in that same directory — so the natural stop-then-deploy order killed the fresh build before its first output flushed. | ⏳ Not yet on macOS — its `PreviewStopper` is the same fire-and-forget design this side copied (row 105), so the same race exists there and has only been dodged by timing. See MAC-HANDOFF.md. | ✅ Fixed on Windows first: `PreviewStopper` tracks in-flight sweeps (15 s cap each) and `Deploy_Click`, `PreviewOrStop_Click`, and `StartAutomatedPreview` all `await PreviewStopper.WhenSweepsFinish()` before launching a build. (origin: Windows) |
| 283 | 2026-08-19 | Windows deploys kept failing ~5 s in, transcript ending at "Starting container if needed …". An adversarial review found the real cause: a "hardening" change (521bbc7c) had given `ScriptRunner.WaitUntilFinished()` a DEFAULT 5-second timeout that force-finished and disposed the ConPTY of whatever it awaited — every quiet build and deploy was being executed for exactly five seconds. Fixed by making `WaitUntilFinished()` unbounded again (mac parity) with a separate `WaitUntilFinishedOrKill(timeoutMs)` reserved for a process already told to stop. In the same change Windows adopted the mac's deploy-during-preview flow (mac commit `922a29b1`): the Deploy button stays enabled while a preview runs, stops it itself, awaits the stop — sweep included, section-scoped, live-polled, 20 s bound — then deploys; the assistant's "section busy" gate now means "a deploy is already running", per the contract. | ✅ Already the mac's design — Windows adopted it; nothing to port back. The 5-second-timeout defect was Windows-only. | ✅ Fixed: `ScriptRunner.cs` (wait primitives), `PreviewStopper.cs` (synchronous sweep registration, live-polled scoped wait, no per-sweep cap), `SectionDetailView.xaml.cs` (`Deploy_Click` stop-then-deploy with post-stop re-checks and exception-path claim release), `MainWindow.IsSectionBusy` → deploy-only. (origin: Windows) |
| 284 | 2026-08-19 | The surname and website-address dialogs never appeared on Windows: the console question dialog assumed every credential request carries a link and called `new Uri(LinkAddress)` unconditionally — the three linkless requests (surname, site name, site-name conflict) threw before the dialog could show, leaving the teacher staring at a deploy waiting on a question with nothing on screen. Token and Account-ID dialogs (which have links) worked, masking it. | ✅ Already correct on macOS — `CredentialRequestSheet` guards the link (`if let linkAddress, !linkTitle.isEmpty`) and gives every field a placeholder; Windows now matches. | ✅ Fixed: new shared `CredentialRequestPanel.Build` (link guard with the mac's reason) used by the console dialog and the wizard's Account-ID help; `FieldPlaceholder` added to `CredentialRequest` with the mac's values, field label above the box, placeholder inside (mac parity); a presentation failure now falls back to the plain question dialog instead of NO dialog. (origin: Windows) |
| 285 | 2026-08-19 | A Windows deploy hung forever after the Quartz build with no error: `build_site.py`'s post-copy `os.sync()` is a global flush that waits on every filesystem in the kernel, and a leaked FUSE superblock (WSL2 shares one kernel across distros; WSLg orphaned a mount) never answers. Replaced with `syncfs()` on the output directory — flush only the filesystem the site was copied to. | Shared Python — inherited on the next mac app rebuild. Colima's sshfs mount is FUSE too, so the exposure was real on both platforms; see MAC-HANDOFF for the reasoning and the one-time image rebuild that follows. | Originated on Windows; fixed in shared `scripts/build_site.py` (`_sync_public_to_host`). |
| 286 | 2026-08-19 | The Windows activity trail recorded nothing for course creation, preview or deploy: `TaskStarted`/`TaskFinished`/`WorkingFolderOpened` were declared (so the contract list-pin passed) but never called. Wired into `ScriptRunner` — start carries the launcher and redacted arguments, finish carries the outcome (success / failure / stopped by the teacher / backed out at a question) and duration — and into both workspace-open paths. Test suite no longer writes fixture courses into the real trail (module-initializer redirect). | Windows-side wiring of behaviour the contract already required. Mac: verify your call sites actually fire (see MAC-HANDOFF) — the list-pin cannot catch a declared-but-never-called event on either platform. | Originated on Windows — reference `Plantoir.Core/Scripting/ScriptRunner.cs`, `WorkspaceViewModel.cs`, `Plantoir.Tests/TestTrailRedirect.cs`. |
| 287 | 2026-08-19 | plantoir.app's Windows screenshots re-shot against the renamed per-section demo sites, and given a hero composite of their own — Obsidian, Plantoir mid-deploy and Edge on the published site, cascaded like the mac's. Windows visitors had been served the mac hero. Judging the output found four faults nothing automated could see: the dark captures resolved every hand-fetched brush against the LAUNCH theme, so the new-course card was white text on a white card and the assistant's bubbles were light grey on a dark window (fixed by photographing each appearance from its own process, with Windows switched into it); the demo courses came up with one section where the mac's showed two (`section_count` is not a key — it is `num_sections`/`section_numbers`); the new-course panel photographed with an empty Course name, because a TextBox that has not been templated takes a programmatic `Text` without raising `TextChanged`; and the assistant window photographed with its prompt shelf missing, since the shelf mounts only once a local assistant is ready. | Screenshots are per-platform by design — the mac keeps its own, taken by `capture.py`. Two findings DO travel: the launch-theme brush trap applies to any AppKit equivalent that resolves an appearance-dependent colour outside the window's own appearance, and `deploy_site_name` — which `WINDOWS-HANDOFF.md` asked this side to rename — turned out not to be a key at all. See MAC-HANDOFF.md. | Originated on Windows — reference `website/shots/hero_windows.py`, `website/shots/capture_windows.py`, `Plantoir/Services/MarketingShotCapturer.cs`. (origin: Windows) |
