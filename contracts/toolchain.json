{
  "note": "The image both platforms build from the same recipe, and the patched Quartz inside it. Neither app can change these — the launchers build the image from `Dockerfile`, `patches/` and `scripts/`, and tag it by a hash of that whole context. It is in the contract because both sides must know WHY each pin is where it is: a version raised for a good local reason can break every teacher's site build, and the reasons are not visible from the version numbers.",
  "patches": [
    {
      "cannotBeDropped": "Without it a course either publishes what a teacher hid, or hides most of itself.",
      "does": "Decides page visibility from `publish:`, defaulting to VISIBLE.",
      "file": "publish.ts",
      "replaces": "quartz/plugins/filters/publish.ts",
      "whyNotStock": "Quartz ships two filters and neither says what a teacher means. RemoveDrafts publishes everything except `draft: true` — the word reads as 'unfinished' rather than 'students cannot see it', and the polarity is backwards from how teachers talk. ExplicitPublish publishes only `publish: true` — the right word, but it flips the default: in the example course 60 of 225 pages carry no flag at all, including every curriculum page, and every one would silently vanish."
    },
    {
      "cannotBeDropped": "publish.ts is not reachable without it.",
      "does": "Exports the filter above in place of the stock pair.",
      "file": "filters-index.ts",
      "replaces": "quartz/plugins/filters/index.ts"
    },
    {
      "cannotBeDropped": "The teacher's own choices in the settings form stop reaching the site.",
      "does": "Reads `expandable` and `expandOnFolderClick` from course_config.json, sorts the tree the way a course reads, and passes both to the client script.",
      "file": "Explorer.tsx",
      "replaces": "quartz/components/Explorer.tsx"
    },
    {
      "cannotBeDropped": "The server-rendered attributes are read by nothing.",
      "does": "The client half of the same: which folders expand, and what a click on one does.",
      "file": "explorer.inline.ts",
      "replaces": "quartz/components/scripts/explorer.inline.ts"
    },
    {
      "cannotBeDropped": "Folder landing pages revert to Quartz's own listing.",
      "does": "What a folder's own page shows.",
      "file": "FolderContent.tsx",
      "replaces": "quartz/components/pages/FolderContent.tsx"
    },
    {
      "cannotBeDropped": "Two things go at once. Social preview tags point to Jacky Zhao's domain when baseUrl is empty or unconfigured, AND every page falls back to the stock <link rel=\"icon\" href=\"static/icon.png\">, so each of a teacher's class sites wears the Quartz logo in the browser tab.",
      "does": "Constructs OpenGraph and Twitter meta tags using the section's baseUrl when present, or page-relative paths when absent, rather than defaulting to quartz.jzhao.xyz. Also replaces the single stock favicon link with three page-relative ones — static/favicon.ico (sizes=32x32), static/icon.svg (type=image/svg+xml) and apple-touch-icon pointing at static/apple-touch-icon.png — in that ORDER, because a browser takes the last icon it understands, so the .ico serves older Safari and Windows shortcuts while the SVG wins wherever it is supported. The files themselves are installed by build_site.py from support/favicon.",
      "file": "Head.tsx",
      "replaces": "quartz/components/Head.tsx",
      "whyNotStock": "Stock Quartz unconditionally constructs og:image as https://${cfg.baseUrl}/static/og-image.png, which points to Jacky Zhao's site when baseUrl defaults to quartz.jzhao.xyz and generates invalid URLs when baseUrl is empty. The patch falls back to page-relative static/og-image.png when baseUrl is not configured. Stock Quartz also links its own quartz/static/icon.png as the favicon; the patch links Plantoir's icon set instead. If Head.tsx is ever re-derived against a newer Quartz, BOTH behaviours have to come with it — the favicon links are the half with no visible symptom during development, because a local preview looks identical either way."
    },
    {
      "cannotBeDropped": "Without it fast rebuilds and quick retries fail with ENOTEMPTY errors on bind-mounted host filesystems.",
      "does": "Cleans the output directory with retries and recursive fallback instead of crashing when rimraf encounters pending host directory unlinks.",
      "file": "build.ts",
      "replaces": "quartz/build.ts",
      "whyNotStock": "Stock Quartz calls rimraf with a wildcard glob that crashes on virtiofs/Docker bind mounts with ENOTEMPTY when removing subdirectories. The patch adds retry options and a recursive fs.rm fallback."
    }
  ],
  "pins": [
    {
      "pin": "baseImage",
      "value": "python:3.11-slim",
      "why": "Python for the build scripts, with Node added on top. The site build is Node; the pipeline around it is Python."
    },
    {
      "pin": "node",
      "value": "20",
      "why": "**What Quartz v4.5.0 is known-good against.** Raising it means revalidating every teacher's site build, which is why the wrangler pin below bends around this rather than the reverse."
    },
    {
      "mustStayBelow": "4.100",
      "pin": "wrangler",
      "value": "4.80.0",
      "why": "Cloudflare's own deploy CLI, used by `deploy.py` for the Pages destination. From 4.100 it requires Node 22. Staying on the newest Node-20 wrangler is a far smaller risk than raising Node. The exact pin also keeps the image reproducible and stops an upstream CLI change from breaking publishing for a teacher mid-semester."
    },
    {
      "pin": "quartz",
      "value": "v4.5.0",
      "why": "The site generator, cloned at a tag. The patches below are written against this version; a newer Quartz means re-deriving all seven."
    },
    {
      "pin": "pythonFrontmatter",
      "value": "1.3.0",
      "dockerfileContains": "python-frontmatter==1.3.0",
      "owedIn": "windows-app/Vendor/fetch-runtime.ps1",
      "whyOwed": "That file still does an UNPINNED `pip install python-frontmatter Pillow`, and it builds the runtime a Windows teacher's site is really produced by — so this pin does not hold there yet. Named as owed rather than as done: GitHub issue #140.", "why": "Reads and REWRITES every page's frontmatter in `build_site.process_frontmatter`, which is what decides whether a teacher's page reaches their students. Pinned 2026-09-18 with issue #140, at the version the image already had — the pin changes nothing about what is installed and everything about what can change underneath it."
    },
    {
      "pin": "pyyaml",
      "value": "6.0.3",
      "dockerfileContains": "PyYAML==6.0.3",
      "owedIn": "windows-app/Vendor/fetch-runtime.ps1",
      "whyOwed": "That file still does an UNPINNED `pip install python-frontmatter Pillow`, and it builds the runtime a Windows teacher's site is really produced by — so this pin does not hold there yet. Named as owed rather than as done: GitHub issue #140.", "why": "**The pin that decides what a teacher's `publish:` line MEANS.** python-frontmatter parses with PyYAML, which is YAML **1.1** — and that is the only reason `publish: no` and `publish: off` hide a page. PyYAML 7 is expected to move to YAML 1.2, where `no` is the string \"no\" and every such page would be PUBLISHED, silently, in courses already in front of students. `contracts/file-formats.json` -> `pageVisibility` states the 1.1 table as fact and both apps are written against it, so an unpinned upgrade would fail nothing anywhere. Named explicitly rather than left to python-frontmatter's own dependency range, because that range is not ours to control."
    },
    {
      "pin": "pillow",
      "value": "12.3.0",
      "dockerfileContains": "Pillow==12.3.0",
      "owedIn": "windows-app/Vendor/fetch-runtime.ps1",
      "whyOwed": "That file still does an UNPINNED `pip install python-frontmatter Pillow`, and it builds the runtime a Windows teacher's site is really produced by — so this pin does not hold there yet. Named as owed rather than as done: GitHub issue #140.", "why": "Draws each section's social sharing card. Pinned with the other two rather than for a known break: an unpinned image is not reproducible, and a card that renders differently is a thing teachers notice after the fact."
    }
  ],
  "rules": [
    {
      "detail": "Every file in `Dockerfile`, `patches/`, `scripts/`, `support/`, `contracts/` and the launchers. `contracts/` joined the context when the scripts themselves began reading the contract: the container's only bind mount is `courses`, so a shared rule it cannot see is no rule at all. The cost is accepted deliberately — a contract edit changes the hash and forces a rebuild.",
      "rule": "The image tag is a HASH of the whole build context",
      "why": "A changed recipe means a new tag, a rebuild and a recreated container — with no update checks anywhere, and no way for a teacher to be running a mismatched image."
    },
    {
      "rule": "Build with BuildKit (`docker buildx build --load`)",
      "why": "The legacy builder corrupts a layer. Do not remove that flag."
    },
    {
      "rule": "If you raise Node, revalidate Quartz BEFORE chasing a newer CLI",
      "why": "The order is the whole point of the wrangler pin."
    }
  ],
  "whyWranglerAtAll": "Cloudflare's upload protocol — BLAKE3 hashing, short-lived JWT, batched uploads — is undocumented enough that reimplementing it would break teachers' publishing whenever Cloudflare changed it. So the image carries their CLI instead.",
  "recipeFolders": {
    "folders": [
      "patches",
      "scripts",
      "support",
      "contracts"
    ],
    "note": "The recipe's FOLDERS, as data, because there were four hand-maintained copies of this list and they drifted the moment a fifth folder was added: the mac's WorkspaceModel.refreshToolchain, Windows' ToolchainMirror.RecipeFolders, the marketing screenshot harness (website/shots/capture.py), and the Dockerfile. Six carriers are pinned against this list in total, the other two being mac-app/project.yml and windows-app/Plantoir/Plantoir.csproj, which bundle and ship the folders; the Dockerfile is pinned in BOTH directions, since a COPY the contract does not list reproduces the original failure exactly. capture.py's own docstring said 'keep them in step' and it was the one that did not — which broke the demo workspace outright rather than subtly, because it copies the Dockerfile that expects the folder it did not copy.",
    "why": "A .toolchain/ holding the new Dockerfile without the new folder is not stale, it is UNBUILDABLE: `docker buildx build` fails on the missing COPY, and preview.sh's friendly 'missing the build recipe' message cannot fire because the Dockerfile is present."
  }
}
